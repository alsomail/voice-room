<!--
[AI 写入规约]
1. Plan Agent 创建。本 Task 横跨 adminServer + server，但主要改动在 adminServer，挂 adminServer 目录。
-->

# TDS: Redis Pub/Sub `admin:events` 双端契约对齐（adminServer 发布端 strict-typed + server 消费端 schema 校验）(Task ID: T-00105)

## 一、背景与目标

- **关联需求**：实证 — adminServer `AdminEvent { r#type: String, admin_id: String, payload: serde_json::Value }` 完全 schema-less，对端 server `AdminEvent` 是 strict enum，任何字符串拼写错误都会**静默丢事件**。`admin:events` 是 BAN/CLOSE_ROOM/BROADCAST 的关键管道，丢失即治理失效。
- **本 Task 的目标**：
  1. adminServer publisher 改用与 server consumer **共享同一份枚举**（通过 `app/shared` crate 提供 strict enum）；publisher 不再可能传错 `event_type` 字符串。
  2. 消费端 server 加运行时 JSON Schema 校验（基于 T-00100 schemas/pubsub/*.schema.json）；所有未通过 schema 的事件丢入 dead-letter 日志 + 上报埋点。
  3. 双端往返集成测试：adminServer 发布 → Redis → server 消费 → 4 类事件全部往返成功。

## 二、方案设计

### 涉及文件清单

| 文件 | 变更 | 说明 |
|------|------|------|
| `app/shared/src/admin_event.rs` | 新增 | 移动 server `events/admin_event.rs` 的 enum 到 shared crate（去重） |
| `app/server/src/events/admin_event.rs` | 修改 | `pub use voice_room_shared::admin_event::*` |
| `app/server/Cargo.toml` | 修改 | 加 `voice-room-shared` 依赖（如果还没） |
| `app/adminServer/src/modules/event/publisher.rs` | 修改 | `AdminEvent { r#type: String, ... }` → `pub use voice_room_shared::admin_event::AdminEvent`；改用 strict enum 序列化 |
| `app/adminServer/src/modules/user/service.rs` | 修改 | `AdminEvent { r#type: "ban_user".to_string(), payload: json!(...) }` → `AdminEvent::BanUser { payload: BanUserPayload { user_id }, admin_id, ts }` |
| `app/adminServer/src/modules/room/service.rs` | 修改 | 同上 CloseRoom |
| `app/adminServer/src/modules/notice/service.rs` | 修改 | 同上 BroadcastNotice |
| `app/server/src/events/handler.rs` | 修改 | 入站时调 schema_guard 校验（dev/test panic，prod ERROR） |
| `app/server/tests/admin_event_roundtrip_test.rs` | 新增 | 双端真往返集成测试 4 场景 |

### 🔌 协议路径绑定表

| # | 协议类型 | 入口 | 客户端（adminServer 发布方）| 服务端（app server 消费方）| 锚点 |
|---|---------|------|----------------------------|---------------------------|------|
| 1 | Redis Pub/Sub | `admin:events :: BanUser` ⭐ | `adminServer/user/service.rs::ban_user` | `server/events/handler.rs::handle_admin_event::BanUser` | [providers.md admin:events](../../protocol/providers.md) |
| 2 | Redis Pub/Sub | `admin:events :: UnbanUser` ⭐ | adminServer user service | server handler | 同上 |
| 3 | Redis Pub/Sub | `admin:events :: CloseRoom` ⭐ | `adminServer/room/service.rs::close_room` | server handler | 同上 |
| 4 | Redis Pub/Sub | `admin:events :: BroadcastNotice` ⭐ | `adminServer/notice/service.rs` | server handler | 同上 |

## 三、TDD 验收用例

- [ ] **PUBSUB-1**：adminServer publish `BanUser { user_id, admin_id, ts }` → Redis → server 消费成功，user 被加入封禁名单。
- [ ] **PUBSUB-2**：故意构造非法 type 字符串通过 raw publish (`r#"{"type":"BanUserrrr",...}"#`) → server schema_guard 拒绝并写 dead-letter 日志，无 panic。
- [ ] **PUBSUB-3**：4 类事件全部往返通过。
- [ ] **PUBSUB-4**：移除 `r#type: String` 后，编译期保证 adminServer 不可能再传错字符串（grep `r#type:` 在 adminServer 0 命中）。
- [ ] **REGRESSION**：原 T-00011B 集成测试 0 回归。

## 四、实现结果

## 五、Reviewer意见

### Round 1
- **🔴 Changelog 行**：

## 六、DoD 完成记录

- [ ] `doc/arch/adminServer/index.md` 协议入口索引补 4 个 Pub/Sub ⭐ 锚点
- [ ] `doc/arch/server/index.md` 协议入口索引补对应消费侧锚点
- [ ] `doc/protocol/providers.md` 加双端反向链接
- [ ] `doc/tasks/index.md` 标记 ✅ Done
