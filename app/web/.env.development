# 缺陷 3 修复（batch-e2e-foundation-01 第 1 轮）：
# URL 端点字段（VITE_API_BASE_URL / VITE_WS_URL / VITE_ADMIN_API_BASE_URL /
# VITE_ANALYTICS_ENDPOINT）已收口到根 `tests/scripts/env/.env.{profile}`，
# 由 vite.config.ts 的 define 注入。本文件仅保留 web 独有 VITE_* 字段（如有）。
