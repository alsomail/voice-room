<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建，TDD Agent 只读不写。
2. 创建后必须在 doc/Tasks.md 对应 Task 的行内补充链接：[TDS](./tds/infra/T-0000B.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: 共享 crate (shared/) (Task ID: T-0000B)

## 一、背景与目标
- **关联需求**：product.md - 工程基建
- **本 Task 的目标**：创建 Rust workspace + shared crate，使 App Server 和 Admin Server 共享数据模型、JWT 工具、错误码和加密工具，避免代码重复

## 二、方案设计

### Cargo Workspace 结构

```text
/Cargo.toml                   # Workspace 根（新增）
app/
├── shared/                   # 共享 crate（新增）
│   ├── Cargo.toml
│   └── src/
│       ├── lib.rs            # crate 入口，re-export 各模块
│       ├── models/
│       │   ├── mod.rs
│       │   └── user.rs       # UserModel
│       ├── jwt/
│       │   ├── mod.rs
│       │   └── token.rs      # encode / decode / Claims
│       ├── error/
│       │   ├── mod.rs
│       │   └── code.rs       # ErrorCode 枚举
│       ├── crypto/
│       │   ├── mod.rs
│       │   └── password.rs   # bcrypt hash / verify
│       └── types/
│           ├── mod.rs
│           └── ids.rs         # UserId, RoomId, AdminId 类型别名
├── server/Cargo.toml         # 修改：添加 shared 依赖
└── adminServer/Cargo.toml    # 修改：添加 shared 依赖
```

### Workspace 根 Cargo.toml

```toml
[workspace]
resolver = "2"
members = [
    "app/server",
    "app/adminServer",
    "app/shared",
]

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sqlx = { version = "0.8", features = ["runtime-tokio", "tls-rustls", "postgres", "uuid", "chrono"] }
jsonwebtoken = "9"
bcrypt = "0.16"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4", "serde"] }
thiserror = "2"
```

### app/shared/Cargo.toml

```toml
[package]
name = "voice-room-shared"
version = "0.1.0"
edition = "2021"

[dependencies]
serde.workspace = true
serde_json.workspace = true
sqlx.workspace = true
jsonwebtoken.workspace = true
bcrypt.workspace = true
chrono.workspace = true
uuid.workspace = true
thiserror.workspace = true
```

### 核心模块实现

#### models/user.rs
```rust
use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::FromRow;
use uuid::Uuid;

#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct UserModel {
    pub id: Uuid,
    pub phone: String,
    pub nickname: String,
    pub avatar: Option<String>,
    pub coin_balance: i64,
    pub vip_level: i16,
    pub is_banned: bool,
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
    pub deleted_at: Option<DateTime<Utc>>,
}
```

#### jwt/token.rs
```rust
use jsonwebtoken::{decode, encode, DecodingKey, EncodingKey, Header, Validation};
use serde::{Deserialize, Serialize};

#[derive(Debug, Serialize, Deserialize)]
pub struct AppClaims {
    pub sub: String,   // user_id
    pub iss: String,   // "voiceroom"
    pub exp: usize,
    pub iat: usize,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct AdminClaims {
    pub sub: String,   // admin_id
    pub role: String,  // "super_admin" | "operator" | "cs" | "finance"
    pub iss: String,   // "voiceroom-admin"
    pub exp: usize,
    pub iat: usize,
}

pub fn encode_token<T: Serialize>(claims: &T, secret: &[u8]) -> Result<String, jsonwebtoken::errors::Error> {
    encode(&Header::default(), claims, &EncodingKey::from_secret(secret))
}

pub fn decode_token<T: for<'de> Deserialize<'de>>(token: &str, secret: &[u8]) -> Result<T, jsonwebtoken::errors::Error> {
    let data = decode::<T>(token, &DecodingKey::from_secret(secret), &Validation::default())?;
    Ok(data.claims)
}
```

#### error/code.rs
```rust
use thiserror::Error;

#[derive(Debug, Error, Clone, Copy)]
pub enum ErrorCode {
    #[error("INVALID_PARAM")]
    InvalidParam = 40000,
    #[error("UNAUTHORIZED")]
    Unauthorized = 40100,
    #[error("TOKEN_EXPIRED")]
    TokenExpired = 40101,
    #[error("FORBIDDEN")]
    Forbidden = 40300,
    #[error("NOT_FOUND")]
    NotFound = 40400,
    #[error("RATE_LIMITED")]
    RateLimited = 42900,
    #[error("INTERNAL_ERROR")]
    InternalError = 50000,
}
```

#### crypto/password.rs
```rust
pub fn hash_password(password: &str) -> Result<String, bcrypt::BcryptError> {
    bcrypt::hash(password, bcrypt::DEFAULT_COST)
}

pub fn verify_password(password: &str, hash: &str) -> Result<bool, bcrypt::BcryptError> {
    bcrypt::verify(password, hash)
}
```

### 各端引用方式

```toml
# app/server/Cargo.toml
[dependencies]
voice-room-shared = { path = "../shared" }

# app/adminServer/Cargo.toml
[dependencies]
voice-room-shared = { path = "../shared" }
```

### 涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `/Cargo.toml` | 新增 | Workspace 根配置 |
| `app/shared/Cargo.toml` | 新增 | 共享 crate 依赖 |
| `app/shared/src/lib.rs` | 新增 | crate 入口 |
| `app/shared/src/models/user.rs` | 新增 | UserModel |
| `app/shared/src/jwt/token.rs` | 新增 | JWT encode/decode |
| `app/shared/src/error/code.rs` | 新增 | 公共错误码 |
| `app/shared/src/crypto/password.rs` | 新增 | bcrypt 工具 |
| `app/shared/src/types/ids.rs` | 新增 | 类型别名 |
| `app/server/Cargo.toml` | 修改 | 添加 shared 依赖 |
| `app/adminServer/Cargo.toml` | 修改 | 添加 shared 依赖 |

## 三、TDD 验收用例
- [ ] 正向：`cargo build -p voice-room-shared` 编译通过
- [ ] 正向：App Server `cargo build -p voice-room-server` 可引用 shared crate 编译通过
- [ ] 正向：Admin Server 同上
- [ ] 正向：`UserModel` 可通过 `sqlx::FromRow` 从数据库行映射
- [ ] 正向：`encode_token` + `decode_token` 往返一致
- [ ] 正向：`hash_password` + `verify_password` 往返一致
- [ ] 异常：`decode_token` 传入过期 token 返回错误
- [ ] 异常：`verify_password` 传入错误密码返回 `Ok(false)`

## 四、实现结果（由 TDD Agent 完成后填写）
- **实际修改的文件**：`Cargo.toml`（workspace 根，新增），`app/shared/`（新增完整 crate），`app/adminServer/Cargo.toml`（新增），`app/adminServer/src/main.rs`（新增），`app/server/Cargo.toml`（升级），`app/server/src/main.rs`（axum 0.8 API），`app/server/src/lib.rs`（axum 0.8 API），`app/server/src/infrastructure/logging.rs`（axum 0.8 API），`rust-toolchain.toml`（移至 workspace 根，升级为 stable）
- **测试覆盖率**：`cargo test -p voice-room-shared` 7/7 通过；`cargo test -p voice-room-server` 1/1 通过
- **完成时间**：2026-04-18

## 五、Reviewer意见
> 状态枚举：🔴 未通过
### Review理由：
**🔴 CRITICAL**
- `token.rs:39`：`decode_token` 使用 `Validation::default()`，不校验 `iss` 字段。App token 可被当 Admin token 使用（跨角色滥用）。需增加 `expected_iss: &str` 参数，调用 `validation.set_issuer(&[expected_iss])`。

**🟠 HIGH**
- `token.rs:8,9`：`exp`/`iat` 使用 `usize`（32 位系统 2038 年溢出），改为 `u64`。
- `app/server/Cargo.toml:5`：`rust-version = "1.85"` 与 `rust-toolchain.toml` stable(1.95) 不一致，删除或更新此行。