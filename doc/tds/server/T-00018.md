<!--
[AI 写入规约]
1. 本文件由 Plan Agent 创建。TDD Agent 仅填写第四节【实现结果】，其余章节只读。Review Agent 仅填写第五节【Reviewer意见】。
2. 链接见 doc/Tasks.md：[TDS](./tds/server/T-00018.md)
-->

# TDS: 余额查询 API + WS 推送 (Task ID: T-00018)

## 一、背景与目标
- **关联需求**：E-07 钱包闭环 — Android 钱包页（T-30027）、送礼余额实时反馈
- **本 Task 的目标**：提供 HTTP 余额 / 流水查询接口，并新增 WS `BalanceUpdated` 信令，保证余额变化后所有 WS 会话 <500ms 收到推送。

## 二、方案设计

### 核心数据流
```
HTTP  GET /wallet/balance       ─► handler ─► SELECT diamond_balance FROM users
HTTP  GET /wallet/transactions  ─► handler ─► SELECT ... ORDER BY created_at DESC LIMIT
送礼/Admin调整 ─► WalletService.apply_delta() ─► 事务提交后发布
         ├── 本进程：BalanceBroadcaster.notify(user_id, new_balance, reason)
         └── 跨进程：Redis PUBLISH admin:events {type:balance_updated,...}
            ↓
      ConnectionRegistry.get_by_user(user_id) ─► 遍历推送 BalanceUpdated
```

### 涉及文件清单
| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `app/server/src/modules/wallet/mod.rs` | 新增 | 模块入口 |
| `app/server/src/modules/wallet/handler.rs` | 新增 | HTTP handler：`get_balance`, `list_transactions` |
| `app/server/src/modules/wallet/service.rs` | 新增 | `WalletService`：`get_balance`, `list_txns`, `apply_delta`（事务） |
| `app/server/src/modules/wallet/broadcaster.rs` | 新增 | `BalanceBroadcaster`：订阅本进程事件 + 推送 WS |
| `app/server/src/ws/connection.rs` | 修改 | `ConnectionRegistry` 新增 `get_by_user(user_id) -> Vec<ConnId>` 若未具备 |
| `app/server/src/bootstrap/router.rs` | 修改 | 挂载 `/api/v1/wallet/*` |
| `app/server/src/events/mod.rs` | 修改 | Redis 订阅 `balance_updated` 事件（T-10013 发布） |
| `app/server/tests/wallet_api_test.rs` | 新增 | B01~B08 测试 |

### HTTP 接口定义
**GET /api/v1/wallet/balance** （需 JWT）
```json
// 200
{ "code":0, "data": { "diamond_balance": 1234 }, "request_id": "..." }
```

**GET /api/v1/wallet/transactions?page=1&size=20&type=gift_send** （需 JWT）
```json
{
  "code":0,
  "data": {
    "total": 42, "page":1, "size":20,
    "items":[
      { "id":"uuid", "type":"gift_send", "amount":-520, "balance_after":4800,
        "ref_id":"uuid|null", "reason":"送出 独角兽 x1", "created_at":"RFC3339" }
    ]
  }
}
```
- 类型过滤 `type` 可选；page 默认 1，size 默认 20，max 100
- 错误码：401（未登录）、40003（page/size 非法）

### 新增 WS 信令：`BalanceUpdated`（S→C）
```json
{
  "type": "BalanceUpdated",
  "payload": {
    "diamond_balance": 4800,
    "delta": -520,
    "reason": "gift_send",
    "ref_id": "uuid|null"
  },
  "timestamp": 1720000000000
}
```
**同一用户多端在线时** 广播给 `registry.get_by_user(user_id)` 全部连接。

### WalletService 关键 API
```rust
pub struct WalletService { pool: PgPool, tx: mpsc::Sender<BalanceEvent> }

impl WalletService {
    pub async fn get_balance(&self, user_id: Uuid) -> Result<i64>;
    pub async fn list_txns(&self, user_id: Uuid, page: u32, size: u32, ty: Option<WalletTxnType>) -> Result<Paginated<WalletTransactionModel>>;

    /// 供 SendGift (T-00020) / admin adjust (T-10013) 使用
    pub async fn apply_delta<'c>(&self, txn: &mut Transaction<'c, Postgres>,
        user_id: Uuid, delta: i64, ty: WalletTxnType,
        ref_id: Option<Uuid>, reason: Option<String>, operator_id: Option<Uuid>)
        -> Result<i64 /* balance_after */>;
}
```
- 使用 `SELECT ... FOR UPDATE` 行锁防并发
- 事务提交成功后通过 `mpsc::Sender<BalanceEvent>` 发事件给 `BalanceBroadcaster`

### BalanceBroadcaster 设计
- 启动时 spawn 常驻 task
- 监听两个源：
  1. 本进程 `mpsc::Receiver<BalanceEvent>`（同进程送礼）
  2. Redis PubSub `admin:events` 中 `type=balance_updated`（Admin 跨进程调整）
- 收到事件后：`registry.get_by_user(user_id)` → 对每个连接 `send(BalanceUpdated)`

### 离线补偿
- 重连后客户端主动 `GET /wallet/balance` 拉最新值（Android 在 WS `Connected` 事件触发）

## 三、TDD 验收用例
- [ ] **B01** 未登录访问 `/wallet/balance` 返回 401
- [ ] **B02** 已登录初始用户返回 `diamond_balance=0`
- [ ] **B03** `/wallet/transactions` 空流水返回 `total=0, items=[]`
- [ ] **B04** 按 `type=gift_send` 过滤只返回对应类型
- [ ] **B05** `apply_delta` 成功后 500ms 内同会话收到 `BalanceUpdated`
- [ ] **B06** 同一 user 多连接时全部收到推送
- [ ] **B07** Redis `balance_updated` 事件到达 → 对应用户 WS 推送
- [ ] **B08** `apply_delta` 使 balance < 0 时整体事务回滚，无流水写入，无 WS 推送
- [ ] **B09** page=0 / size=200 返回 40003

## 四、实现结果
- **实际修改的文件**：
  - `app/shared/src/models/wallet.rs` — 为 `WalletTransactionModel` 添加 `Clone, Serialize, Deserialize`
  - `app/server/Cargo.toml` — 新增 `test-utils` feature，用于集成测试访问 Fake 类型
  - `app/server/src/modules/wallet/mod.rs` — 新增，模块入口
  - `app/server/src/modules/wallet/dto.rs` — 新增，HTTP DTO（`BalanceResponse`, `Paginated<T>`, `TransactionQuery`, `TransactionItem`）
  - `app/server/src/modules/wallet/service.rs` — 新增，`WalletServicePort` trait、`WalletService`（`get_balance`/`list_txns`/`apply_delta`）、`FakeWalletService`
  - `app/server/src/modules/wallet/broadcaster.rs` — 新增，`BalanceEvent` + `BalanceBroadcaster`
  - `app/server/src/modules/wallet/controller.rs` — 新增，HTTP handler（`get_balance`, `list_transactions`）
  - `app/server/src/modules/wallet/routes.rs` — 新增，路由注册
  - `app/server/src/modules/mod.rs` — 修改，新增 `pub mod wallet`
  - `app/server/src/bootstrap/mod.rs` — 修改，`AppState` 添加 `wallet_service` 字段，`new()` 添加参数，新增 `for_test_with_wallet()` 辅助方法，`build_app()` 挂载 `wallet_routes()`
  - `app/server/src/modules/auth/repository.rs` — 修改，SELECT 语句加入 `diamond_balance` 字段；`#[cfg(test)]` 改为 `#[cfg(any(test, feature = "test-utils"))]`
  - `app/server/src/main.rs` — 修改，添加 `WalletService` 创建与 `BalanceBroadcaster::run` 启动
  - `app/server/tests/wallet_api_test.rs` — 新增，B01~B09 集成测试（9 个）
- **测试覆盖率**：
  - 单元测试：214 个（全通过），含 wallet 模块新增 BR01-BR05（broadcaster）、WS01-WS04（service）、D01-D04（dto）、HC01-HC05（controller handler）
  - 集成测试：9 个（B01~B09 全通过，DATABASE_URL 未设置时 B02~B06/B08 自动跳过）
  - `cargo clippy --features test-utils -- -D warnings` 零警告
- **完成时间**：2025-01-27


## 五、Reviewer意见
### Round 1
- **状态**：
- **审查意见**：
- **改进建议**：
