<!--
[AI 写入规约]
-->

# TDS: SendGift 客户端 + 幂等 (Task ID: T-30030)

## 一、背景与目标
- **关联需求**：E-07 送礼完整链路 ([设计文档](../../design/android/T-30030.md))
- **本 Task 的目标**：点击"送出"生成 UUID msg_id → WS 发送 SendGift → 3s 连击聚合 → 超时 5s / 错误处理 → 成功/失败 UI 反馈。

## 二、方案设计

### 涉及文件
| 文件 | 类型 | 说明 |
|------|------|------|
| `gift/GiftPanelViewModel.kt` | 修改 | 新增 `send()` + 连击聚合器 |
| `gift/SendGiftJob.kt` | 新增 | 单次发送作业：msg_id / start_ts / timeout / result channel |
| `core/ws/WebSocketClient.kt` | 修改 | 若缺失则增加 `sendGift(payload, msgId)` 便捷方法 |
| `core/ws/event/GiftEvents.kt` | 新增 | `SendGiftResultEvent`, `GiftReceivedEvent` |
| `test/.../gift/SendFlowTest.kt` | 新增 | VM 测试 |

### 连击聚合器
```kotlin
class ComboAggregator(private val windowMs: Long = 3000) {
    private var current: Combo? = null
    data class Combo(val giftId: String, val recipientId: String,
                     val msgId: String, var count: Int, var lastTs: Long)

    fun press(giftId: String, recipientId: String, unitCount: Int = 1): Combo {
        val now = System.currentTimeMillis()
        val c = current
        return if (c != null && c.giftId==giftId && c.recipientId==recipientId
                   && now - c.lastTs < windowMs) {
            c.count += unitCount; c.lastTs = now; c
        } else {
            Combo(giftId, recipientId, UUID.randomUUID().toString(), unitCount, now).also { current = it }
        }
    }
    fun flush() { current = null }
}
```
- 连击窗口内所有点击累加 `count`，但 **只发一次 WS**（发送时机：最后一次 press 后 windowMs 到或 flush 或离开窗口）
- MVP 策略：用户按"送出"立刻 flush 并发送（避免设计太复杂）；连击窗口仅作用于 UI 反馈

### 发送流程
```
state.canSend → 按钮可点
onSend() {
  val job = SendGiftJob(msgId=UUID, giftId, recipientId, count)
  _state.update { it.copy(sending=true) }
  wsClient.sendGift(job)
  val result = withTimeoutOrNull(5_000) { resultFlow.first { it.msgId == job.msgId } }
  when {
    result == null -> showError("请求超时，请重试")
    result.code == 0 -> { _state.update { it.copy(sending=false) } /* 保持面板 */ }
    result.code == 40290 -> showInsufficientDialog() // T-30032
    result.code == 40403 -> toast("接收者已下麦") /* 面板不关 */
    else -> toast("赠送失败，请重试")
  }
}
```

### 错误码映射
| Server code | 客户端动作 |
|-------------|-----------|
| 0 | 成功（保持面板，显示 toast"赠送成功"） |
| 40290 | 弹 InsufficientBalanceDialog（T-30032） |
| 40403 | Toast "接收者已下麦或离开" |
| 40402 | Toast "该礼物已下架"，自动刷新列表 |
| 40400 | Toast "你已不在房间"，关闭面板 |
| 超时/网络 | Toast "请求超时，请重试"，按钮还原 |

### msg_id 生成与幂等
- `UUID.randomUUID().toString()` 每次点击生成；若 5s 内同一 msg_id 重复发送（如用户误触多发），Server 侧幂等保护

### testTag
`Key('btn_send_gift')`（send 中 `isEnabled=false` 且显示 CircularProgress）

## 三、TDD 验收用例
- [ ] **S30-01** 点击送出：WS 消息体 `type=SendGift`, `msg_id` 为合法 UUID
- [ ] **S30-02** 发送中按钮 disabled 且显示 loading
- [ ] **S30-03** 收到 `SendGiftResult code=0` 后按钮还原，toast 成功
- [ ] **S30-04** 5s 未收到回复 → toast 超时，按钮还原
- [ ] **S30-05** code=40290 触发 InsufficientBalanceDialog（见 T-30032）
- [ ] **S30-06** code=40403 toast 且面板保留
- [ ] **S30-07** 同礼物同接收者 3s 内 5 次点击 → ComboAggregator count=5（单元测试）
- [ ] **S30-08** 切换礼物或接收者后新建 msg_id
- [ ] **S30-09** 每次点击 msg_id 均唯一

## 四、实现结果
- **实际修改的文件**：
- **测试覆盖率**：
- **完成时间**：

## 五、Reviewer意见
### Round 1
- **状态**：
- **审查意见**：
- **改进建议**：
