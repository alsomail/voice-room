<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建，TDD Agent 只读不写。
2. 创建后必须在 doc/tasks/index.md 对应 Task 的行内补充链接：[TDS](./tds/server/T-00001.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: 数据库表设计 (Task ID: T-00001)

## 一、背景与目标
- **关联需求**：product.md - 模块1用户认证
- **前置依赖**：T-0000B (共享 crate), T-0000C (数据库权限隔离)
- **本 Task 的目标**：设计 users 表，作为用户认证系统的基础数据模型（参考 doc/protocol.md §六）

## 二、方案设计
### 核心数据流
SQLx migration 文件 → PostgreSQL → users 表创建 → shared crate UserModel 映射

### 涉及文件清单
| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `app/server/migrations/001_create_users.sql` | 新增 | users 表 DDL |
| `app/shared/src/models/user.rs` | 新增 | UserModel struct |

### 接口 / 信令定义
参见 `doc/protocol.md` §六 6.1 users 表


### 🔌 协议路径绑定表

N/A — 本 Task 无跨端协议路径，仅服务端内部改造

## 三、TDD 验收用例
- [ ] 正向：migration 文件可成功执行，users 表创建完成
- [ ] 正向：phone 字段有唯一索引（soft delete 条件索引）
- [ ] 正向：包含 coin_balance (BIGINT DEFAULT 0), vip_level (SMALLINT DEFAULT 0) 字段
- [ ] 正向：支持软删除（deleted_at 字段）
- [ ] 异常：重复手机号插入应触发唯一索引冲突

## 四、实现结果（由 TDD Agent 完成后填写）
- **实际修改的文件**：
- **测试覆盖率**：
- **完成时间**：

## 五、Reviewer意见
> 状态枚举：🟢 通过 | 🔴 未通过
### Review理由：🟢 通过
- migration 文件结构正确，phone 条件唯一索引、软删除支持完备
- 🟡 建议后续补充 `updated_at` 自动更新触发器（不阻塞本次）