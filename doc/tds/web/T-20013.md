<!--
[AI 写入规约]
-->

# TDS: 用户详情页"行为流"Tab (Task ID: T-20013)

## 一、背景与目标
- **关联需求**：E-07.5 运营可视化用户行为
- **本 Task 的目标**：在 `UserDetailDrawer` 新增 `EventStreamTab`，支持时间筛选 + 事件多选 + CSV 导出。

## 二、方案设计

### 涉及文件
| 文件 | 类型 | 说明 |
|------|------|------|
| `app/web/src/features/user/EventStreamTab.tsx` | 新增 | Tab 主体 |
| `app/web/src/features/user/components/EventTimelineItem.tsx` | 新增 | 单条事件卡片 |
| `app/web/src/features/user/UserDetailDrawer.tsx` | 修改 | 新增 Tab 项 |
| `app/web/src/services/api/events.ts` | 新增 | `listUserEvents(userId, params)` |
| `app/web/src/lib/csv.ts` | 新增或复用 | CSV 导出工具 |
| `app/web/src/i18n/locales/{ar,en}.json` | 修改 | 新增 key |
| `app/web/src/features/user/__tests__/EventStreamTab.test.tsx` | 新增 |  |

### 组件功能
- 时间筛选：`Radio.Group`：[最近 1h, 24h, 7d, 30d, 自定义]
- 自定义时：`DatePicker.RangePicker`（限 30 天）
- 事件名过滤：`Select mode=multiple`，选项来自 `ANALYTICS_EVENTS` 常量（前端枚举，避免频繁查后端）
- 时间线列表：倒序，每项展示
  - event_name（Tag 高亮色按类别）
  - server_ts 格式化
  - 设备/app_version/network_type（小字）
  - properties 折叠 JSON（`JsonView` 组件，关键字 highlight）
- 分页：Ant Design Pagination，默认 20/页
- 导出 CSV 按钮：当前筛选下最多 1000 条；文件名 `user_{id}_events_{ts}.csv`

### 常量事件字典
`app/web/src/features/user/events.dict.ts`：
```ts
export const EVENT_CATEGORIES = {
  auth: ['login_request','login_success','login_fail'],
  gift: ['gift_panel_open','gift_select','gift_send_click','gift_send_success','gift_send_fail'],
  wallet: ['wallet_view','balance_update','insufficient_balance'],
  room: ['room_enter','room_leave','mic_take','mic_leave'],
  // ...
};
```

### API 调用
```ts
interface EventListParams { event_name?: string; from?: string; to?: string; page?: number; limit?: number; }
listUserEvents(userId, params)  // GET /admin/users/:id/events
```

### CSV 导出实现
前端遍历分页 API 最多 50 页（1000 条），合并后 `Papa.unparse` 生成 Blob 下载。若筛选结果超 1000 条，提示用户"结果过多，已导出前 1000 条"。

### testTag / data-testid
`event-stream-tab`, `event-time-range`, `event-name-select`, `btn-export-csv`, `event-item-{id}`

## 三、TDD 验收用例
- [ ] **E13-01** Drawer 新 Tab 可见
- [ ] **E13-02** 默认加载最近 24h 的事件
- [ ] **E13-03** 事件名多选过滤生效
- [ ] **E13-04** 时间窗自定义 >30 天时显示错误提示（不发请求）
- [ ] **E13-05** 空数据显示占位
- [ ] **E13-06** 导出 CSV 文件名包含 user_id 和时间戳
- [ ] **E13-07** properties JSON 可展开 / 折叠
- [ ] **E13-08** i18n：切换 ar 时文案正确

## 四、实现结果
- **实际修改的文件**：
- **测试覆盖率**：
- **完成时间**：

## 五、Reviewer意见
### Round 1
- **状态**：
- **审查意见**：
- **改进建议**：
