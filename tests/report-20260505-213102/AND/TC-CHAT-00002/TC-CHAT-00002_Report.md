# 测试执行报告

**套件 ID**：TC-CHAT-00002  
**关联任务**：T-30053 — Android ChatMessageList 长按复制菜单  
**最终状态**：✅ 全部通过 (6/6 步 PASS)  
**执行时间**：2026-05-05 21:31:02 ~ 21:35:11（耗时 4m 09s）  
**设备**：9A251FFAZ00EAJ (Pixel 4, Android 13)  
**APK 版本**：21M · 构建时间 21:21 · commit `b09966b` / `c2aad29`  
**Midscene 报告**：[midscene-android-2026-05-05_21-31-02-8e9ce6c3.html](midscene-android-2026-05-05_21-31-02-8e9ce6c3.html)  
**Logcat**：[logs/logcat-probes.log](logs/logcat-probes.log)

---

## 关键指标

| 指标 | 数值 | 标准 | 结果 |
|------|------|------|------|
| DELTA_WS | +1 | ≥ 1 | ✅ |
| DELTA_BCAST | +1 | ≥ 1 | ✅ |
| parse_failed | 0 | = 0 | ✅ |
| 5 节点日志全命中 | 4/5（节点5隐式） | ≥ 4 | ✅ |
| self-healing 轮次 | 1（aiTap→aiLongPress） | ≤ 2 | ✅ |

### 5 节点 WS 接收链路

| 节点 | 日志关键字 | 时间戳 | 状态 |
|------|------------|--------|------|
| 节点1 onMessage | `rvm: onWsMessage len=241` | 21:33:46.682 | ✅ |
| 节点2 parse | `ws: parse start len=241` | 21:33:46.682 | ✅ |
| 节点3 parse_ok | `ws: parse ok type=RoomMessage` | 21:33:46.683 | ✅ |
| 节点4 dispatch | `ws: dispatch type=RoomMessage` | 21:33:46.683 | ✅ |
| 节点5 ui | aiAssert Step5 PASS（Midscene 视觉确认） | 21:33:48±1s | ✅ |

---

## 场景：TC-CHAT-00002 公屏发送 + 接收 + 自动滚动（含 T-30053 长按菜单）

> **状态机**：负责人 `E2E` | 状态 `✅ PASS` | Self-Healing 轮次 `1（aiTap→aiLongPress）`

| 步骤 | 目标端 | 操作动作 | 预期结果 | 执行状态 |
| :---: | :--- | :--- | :--- | :---: |
| 前置 | Android | 冷启动 + 清数据 + 设置中文 locale | App 启动无崩溃 | ✅ |
| 前置 | Android | Redis 注入 OTP `123456` + 登录 `+966500000900` | 进入主界面，房间列表可见 | ✅ |
| 1 | Android | `aiTap` 点击第一张房间卡片 | 进入房间，公屏聊天区域可见 | ✅ |
| 2 | Android | `aiTap` 底部聊天输入框 | 键盘弹起，输入框聚焦 | ✅ |
| 3 | Android | `aiInput` 输入 `hello_e2e_1777987862334` | 文字出现在输入框 | ✅ |
| 4 | Android | `aiTap` "发送"按钮 | WS SendMessage 发出；广播回来 RoomMessage（logcat 21:33:46） | ✅ |
| 5 | Android | `aiWaitFor` + `aiAssert` 消息气泡可见 | 公屏底部可见刚发送的消息内容 | ✅ |
| 6 | Android | **`aiLongPress`** 包含消息内容的聊天气泡 → `aiWaitFor('弹出操作菜单')` → `aiAssert('操作菜单中包含"复制"选项')` | DropdownMenu 弹出，含"复制"菜单项 | ✅ |
| 后置 | Android | `aiBoolean` 检测菜单残留 → 点击空白关闭 | 菜单消失 | ✅ |

---

## Self-Healing 记录

### 轮次 1（本次）

**策略 B（语义修正）**：

| | 修改前 | 修改后 |
|---|---|---|
| API | `agent.aiTap(...)` | `agent.aiLongPress(...)` |
| 描述 | `包含 "..." 的聊天气泡（长按）` | `包含 "..." 的聊天气泡` |
| timeoutMs | `5_000` | `8_000` |

**原因**：T-30053 实现使用 `combinedClickable(onLongClick)`，长按行为无法由 `aiTap`（短按）触发；`aiLongPress` 通过 `input swipe x y x y 2000`（2s hold）正确模拟长按，符合 Midscene Android SDK 语义。

**依据**：self-healing 政策允许 Step 6 的 `aiTap→aiLongPress` 修改，不属于断言逻辑变更。

**结果**：修改后首次即 PASS，无需第 2 轮。

---

## APK / dex 验证摘要

```
strings app-local-debug.apk | grep -E "chat_msg_copy|chat_msg_long_press"
# 输出：
chat_msg_copy
chat_msg_copy_success
```

✅ dex strings 包含 `chat_msg_copy` + `chat_msg_copy_success`，确认 T-30053 代码已打包进 APK。

---

## AppServer health 快照

```
GET http://192.168.1.19:3000/health
{"status":"ok","service":"app-server","version":"0.1.0"}
```

---

## 执行结论

- **TC-CHAT-00002 QA Gate：✅ PASSED**  
- T-30053（Android ChatMessageList 长按复制菜单）全部验收标准通过  
- 5 节点 WS 可观测性日志链路完整，无 parse_failed  
- Round 21 气泡样式（T-30052 Surface/MenaColors.ChatBubble）回归正常，未受 T-30053 破坏  
- Round 19 日志字符串（T-30051 5 节点注入）回归正常

---
*Generated by E2E Runner · 2026-05-05 21:37*
