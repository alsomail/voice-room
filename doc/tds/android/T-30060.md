# TDS: 充值页 UI（SKU 列表）(Task ID: T-30060)

## §0 产品验收（Given-When-Then）

- **关联 Spec**：[`doc/specs/recharge_order.md`](../../specs/recharge_order.md)
- **关联状态机**：[`state_machines.md#order`](../../product/state_machines.md#order)
- **关联用户旅程**：[`user_journeys.md#j1-recharge-gift-noble`](../../product/user_journeys.md#j1-recharge-gift-noble)
- **关联业务约束**：[`business_constraints.md`](../../product/business_constraints.md)
- **必须满足的验收条款**（GWT 全文以 spec §5 为唯一事实源，**禁止本地改写**）：GWT-O1 / GWT-O5

---

## 一、背景与目标
- **关联需求**：[phase1_payment_billing.md §6.1 充值页](../../product/phase1_payment_billing.md) + [设计稿 T-30060](../../design/android/T-30060.md)
- **目标**：实现 `RechargeScreen`，顶部余额，2 列 SKU 卡片（钻石/价格/热门角标）；底部"充值历史"入口；网络失败重试；RTL 兼容。

## 二、方案设计
### 涉及文件
| 文件 | 变更 |
|------|------|
| `app/android/app/src/main/java/.../recharge/RechargeScreen.kt` | 新增 |
| `app/android/app/src/main/java/.../recharge/RechargeViewModel.kt` | 新增（loadSkus）|
| `app/android/app/src/main/java/.../recharge/RechargeRepository.kt` | 新增（getSkus 调用）|
| `app/android/app/src/main/java/.../recharge/data/SkuApi.kt` | 新增 Retrofit |
| `app/android/app/src/main/java/.../wallet/WalletScreen.kt` | 修改：替换占位 Toast → navigate("recharge") |
| `app/android/app/src/test/java/.../recharge/RechargeViewModelTest.kt` | 新增 |
| `app/android/app/src/androidTest/java/.../recharge/RechargeScreenTest.kt` | 新增 Compose UI test |

### 🔌 协议路径绑定表
| # | 协议类型 | 入口 | 客户端调用方 | 服务端处理函数 | protocol/ 锚点 |
|---|---------|------|------------|--------------|---------------|
| 1 | HTTP REST | `GET /api/v1/payments/skus` ⭐ | `SkuApi.kt::getSkus()` (Retrofit) | server `payment::controller::list_skus_handler`（待 T-00050 之后补；属于公开列表无需鉴权） | [payment_api.md §1](../../protocol/payment_api.md) |

> 注：T-00050 schema 落地时一并实现 `GET /payments/skus` handler；如未排期请视作 T-00050 的一部分。

## 三、TDD 验收用例
- [ ] 默认调 `GET /payments/skus` 拉 5 档
- [ ] Loading 显示 Skeleton（2 列网格 6 个占位）
- [ ] 选中卡片金色边框（border #F59E0B 2dp）
- [ ] 网络失败显示重试按钮，点击重新发起
- [ ] RTL 时网格从右向左排列
- [ ] testTag：`sku_card_${sku_id}` / `recharge_balance` / `history_entry`

### 🔴 协议路径绑定
- [ ] **PROTO-1**：grep `app/android` 包含字面量 `"/payments/skus"`；handler 入口被实测触发
- [ ] **PROTO-2**：响应字段 `data.skus[].sku_id / diamonds / display_price_usd / is_hot / sort_order / tag` 全字段映射 [payment_api.md §1](../../protocol/payment_api.md)

## 四、实现结果
## 五、Reviewer意见
### Round 1
- **🔴 Changelog 行**：
## 六、DoD
- [ ] `doc/arch/android/index.md` 模块清单 + 路由表追加 `recharge`
- [ ] payment_api.md §1 互链
- [ ] Changelog 行追加
