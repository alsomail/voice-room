# TDS: 进场广播 + 徽章字段下发 (Task ID: T-00069)

## 一、背景与目标
- **关联需求**：[phase1_nobility.md §3.5.2 进场特效 / §3.5.5 隐身](../../product/phase1_nobility.md)。
- **目标**：JoinRoom 时 `UserJoined.payload.noble` 扩展字段；新增 `NobleEntered`（房间进场特效）；高等级（duke/king）触发 `NobleEntranceGlobal` Pub/Sub 跨实例分发。

## 二、方案设计
### 核心数据流
```
JoinRoom handler (现有 T-00009)
  ↓ join 成功后
NobleEntryService::on_join(user_id, room_id):
  ├─ 查 user_nobles JOIN noble_tiers → tier 信息
  ├─ 若 tier=null → 退出（普通用户）
  ├─ 序列化 NobleSnapshot { tier_id, level, badge_color, frame_url, expire_at }
  ├─ 注入 UserJoined.payload.noble（已有信令字段补充）
  ├─ 按 privileges.entry_effect.scope 决策广播：
  │   - none → 仅 UserJoined（隐身贵族也广播 UserJoined 但 noble 字段被 filter）
  │   - own_room/half/fullscreen → 房间广播 NobleEntered
  ├─ 按 privileges.invisibility.scope 过滤接收者（隐身规则见 §10.4.5）
  ├─ 若 level >= 5 (duke/king) 且 today_count(noble_global_broadcast_log) < daily_limit:
  │     INSERT noble_global_broadcast_log + Redis PUBLISH "noble:global" NobleEntranceGlobal
   ↓
其他 server 实例 SUBSCRIBE "noble:global" → 推送给所有在线 WsConnection
```

### 涉及文件
| 文件 | 变更 |
|------|------|
| `app/server/src/modules/nobility/entry_service.rs` | 新增 |
| `app/server/src/modules/nobility/broadcast_filter.rs` | 新增（隐身规则） |
| `app/server/src/modules/room/handler/join.rs` | 修改：注入 NobleSnapshot |
| `app/server/src/ws/global_pubsub.rs` | 新增 noble:global 订阅器 |
| `app/server/src/ws/protocol/types.rs` | 修改：UserJoined payload 增 `noble: Option<NobleSnapshot>` |
| `doc/protocol/schemas/ws/NobleEntered.schema.json` | 新增 |
| `doc/protocol/schemas/ws/NobleEntranceGlobal.schema.json` | 新增 |
| `doc/protocol/schemas/ws/NobleChanged.schema.json` | 新增（共用） |
| `app/server/tests/nobility_entry_test.rs` | 新增 |

### 🔌 协议路径绑定表
| # | 协议类型 | 入口 | 调用方 | 处理函数 | 广播 | protocol/ 锚点 |
|---|---------|------|------|---------|------|---------------|
| 1 | WS S→Room 广播 | `UserJoined`（payload.noble 字段扩展）⭐ | 房间所有成员（隐身规则过滤） | `room::handler::join` 调用 `entry_service::build_user_joined` | - | [nobility_api.md §10.4.7](../../protocol/nobility_api.md) |
| 2 | WS S→Room 广播 | `NobleEntered` ⭐ | 房间成员（隐身过滤） | `entry_service::broadcast_room_entrance` | - | [§10.4.5](../../protocol/nobility_api.md) |
| 3 | Redis Pub/Sub + WS S→Global | `NobleEntranceGlobal` ⭐ | 全服在线用户 | `global_pubsub::on_noble_global` | channel `noble:global` | [§10.4.6](../../protocol/nobility_api.md) |

## 三、TDD 验收用例
- [ ] tier=null 用户 JoinRoom：UserJoined 不含 noble 字段
- [ ] 普通贵族 JoinRoom：UserJoined.payload.noble 含完整 5 字段
- [ ] earl 进场（mic_only invisibility）：UserJoined 正常广播；NobleEntered 半屏特效广播
- [ ] king 进场（all invisibility）：UserJoined 仍广播但 noble 字段对非 admin 接收者被剔除；NobleEntered 仅 admin 收到
- [ ] duke 第一次进场：触发 NobleEntranceGlobal Pub/Sub；多实例订阅器都收到
- [ ] duke 当日第二次进场：daily_limit 命中，不再广播全服
- [ ] privileges JSON 缺失字段 → 用 default 兜底（如 entry_effect.duration_ms 缺失 → 5000）

### 🔴 协议路径绑定
- [ ] **PROTO-1**：UserJoined.payload.noble 子对象字段严格 `tier_id/level/badge_color/frame_url/expire_at`
- [ ] **PROTO-2**：NobleEntered.payload 字段严格匹配 §10.4.5；scope 取值 `marquee|half|fullscreen`
- [ ] **PROTO-3**：NobleEntranceGlobal Pub/Sub channel 严格 `noble:global`，payload 严格匹配 §10.4.6

## 四、实现结果
## 五、Reviewer意见
### Round 1
- **🔴 Changelog 行**：
## 六、DoD
- [ ] `doc/arch/server/nobility.md` 进场广播流程图
- [ ] nobility_api.md §10.4 互链
- [ ] websocket_signals.md §6.14 互链
- [ ] Changelog 行追加
