# 测试套件：ROOM 房间大厅（Android）

> **需求模糊点 (Ambiguity Notes)**：
> - 无

覆盖 Task：T-30005~T-30009（HallScreen + Paging3 + 创建房间 + 进入房间）。

---

## TC-ROOM-00002：房间大厅网格渲染 + 分页下拉
**【元数据】**
- **归属模块**：`ROOM`
- **测试类型**：`Functional`
- **回归级别**：`P0`

**【前置条件】**
1. 后端存在 25 个活跃房间，member_count 分别为 1-25。
2. U1 已登录进入大厅。

**【执行步骤与断言】**
| 步骤序号 | 目标端 | 操作动作 (Action) | 预期结果 (Assertion) |
| :------: | :----- | :---------------- | :------------------- |
| 1 | `Android` | 进入大厅 | 顶部显示"语聊房"标题，右下角显示金色 `+` 圆形 FAB |
| 2 | `Android` | 观察房间网格 | 2 列网格，首屏显示约 10 张卡片，每张含封面、标题、在线人数、房主昵称 |
| 3 | `Android` | 首屏第 1 张卡片 | member_count 显示为 25（热度降序） |
| 4 | `Android` | 向下滑动 | 自动加载下一页，底部出现 Loading 指示器；共可滑到 25 张后底部显示"已加载全部" |
| 5 | `Android` | 顶部下拉 | 出现金色圆形进度圈，释放后刷新列表 |
| 6 | `AppServer` | 下拉请求 | 后端收到 `GET /api/v1/rooms?page=1&size=20` |

**【数据清理】**
- 无。

---

## TC-ROOM-00003：弱网 / 断网场景
**【元数据】**
- **归属模块**：`ROOM`
- **测试类型**：`Compatibility`
- **回归级别**：`P1`

**【前置条件】**
1. 已进入大厅。

**【执行步骤与断言】**
| 步骤序号 | 目标端 | 操作动作 (Action) | 预期结果 (Assertion) |
| :------: | :----- | :---------------- | :------------------- |
| 1 | `Android` | 开启飞行模式 | 网络断开 |
| 2 | `Android` | 下拉刷新 | 页面中部显示空状态插画 + 文字"网络不可用，请重试"，下方金色按钮"重试" |
| 3 | `Android` | 关闭飞行模式，点击"重试" | 列表恢复显示 |
| 4 | `Android` | 弱网（使用 ADB `tc` 模拟 500ms 延迟） | 下拉刷新不卡死，超时 ≤10s 后显示重试提示 |

**【数据清理】**
- 恢复网络。

---

## TC-ROOM-00004：创建房间 Bottom Sheet - E2E 成功
**【元数据】**
- **归属模块**：`ROOM`
- **测试类型**：`Integration`
- **回归级别**：`P0`

**【前置条件】**
1. U1 无活跃房间。

**【执行步骤与断言】**
| 步骤序号 | 目标端 | 操作动作 (Action) | 预期结果 (Assertion) |
| :------: | :----- | :---------------- | :------------------- |
| 1 | `Android` | 点击右下角金色 `+` FAB | 从屏幕底部滑入 Bottom Sheet，标题"创建房间" |
| 2 | `Android` | 观察输入框 | 含"房间标题"输入框、"房间类型"单选（公开/密码） |
| 3 | `Android` | 输入标题"测试间" | 底部"创建"按钮从灰色变为金色可点 |
| 4 | `Android` | 点击"创建" | 按钮显示圆形 Loading，禁用再次点击 |
| 5 | `AppServer` | 后端日志 | POST `/api/v1/rooms` 返回 201 |
| 6 | `Android` | 2 秒内 | Bottom Sheet 关闭，自动跳转 RoomScreen，顶部标题显示"测试间" |
| 7 | `DB` | rooms 表 | 存在 owner_id=U1 title="测试间" status=`active` |

**【数据清理】**
- 关闭房间。

---

## TC-ROOM-00005：创建房间 - 标题边界 UI 校验
**【元数据】**
- **归属模块**：`ROOM`
- **测试类型**：`Functional`
- **回归级别**：`P1`

**【前置条件】**
1. Bottom Sheet 已打开。

**【执行步骤与断言】**
| 步骤序号 | 目标端 | 操作动作 (Action) | 预期结果 (Assertion) |
| :------: | :----- | :---------------- | :------------------- |
| 1 | `Android` | 标题为空 | "创建"按钮置灰不可点 |
| 2 | `Android` | 输入 1 个字符 | "创建"按钮变金色可点 |
| 3 | `Android` | 输入 30 个字符 | 可点 |
| 4 | `Android` | 尝试输入第 31 个字符 | 输入框拒绝输入（maxLength）；或输入框底部显示红色文字"标题不超过 30 字" |

**【数据清理】**
- 无。

---

## TC-ROOM-00006：房间卡片点击 - 进入 RoomScreen
**【元数据】**
- **归属模块**：`ROOM`
- **测试类型**：`Functional`
- **回归级别**：`P0`

**【前置条件】**
1. 大厅首屏可见至少 1 张卡片 R1。

**【执行步骤与断言】**
| 步骤序号 | 目标端 | 操作动作 (Action) | 预期结果 (Assertion) |
| :------: | :----- | :---------------- | :------------------- |
| 1 | `Android` | 点击第 1 张卡片 | 卡片按压视觉反馈（水波纹） |
| 2 | `Android` | 导航切换 | 1 秒内进入 RoomScreen，顶部标题=R1 的房间名，麦位区显示主播麦 + 7 个副麦 |
| 3 | `AppServer` | 抓包 | 请求 `GET /api/v1/rooms/{R1}` + WS 连接建立 + `JoinRoom` 消息 |

**【数据清理】**
- 点击返回键离开。
