<!--
[AI 写入规约]
-->

# TDS: 被踢/被禁提示弹窗 (Task ID: T-30042)

## 一、背景与目标
- **关联需求**：E-10 终端反馈 ([设计文档](../../design/android/T-30042.md))
- **本 Task 的目标**：收到 `UserKicked`/`UserMuted` 后的全端反馈 UI + 本地 cooldown 计时。

## 二、方案设计

### 涉及文件
| 文件 | 类型 | 说明 |
|------|------|------|
| `feature/room/governance/UserKickedDialog.kt` | 新增 | 全屏弹窗 |
| `feature/room/governance/MuteStatusChip.kt` | 新增 | 倒计时 Chip |
| `feature/room/governance/MuteCountdownViewModel.kt` | 新增 |  |
| `feature/hall/HallViewModel.kt` | 修改 | 进房前检查本地 kickCooldown |
| `data/local/KickCooldownStore.kt` | 新增 | DataStore 存 kick 失效时间 |
| `feature/room/RoomViewModel.kt` | 修改 | 接 UserKicked/UserMuted 事件 |

### UserKicked 流程
```
WS UserKicked 到达
   ↓
RoomViewModel.onKicked(reason, cooldown_sec)
   ↓
显示全屏 UserKickedDialog
   ├ 文案 "你已被移出房间，原因：XXX，10 分钟后可再次进入"
   ├ [知道了] 按钮
   ↓ 点击后
KickCooldownStore.save(roomId, until = now + 600s)
离开 RoomScreen，返回 Hall
   ↓ Hall 点击同一房间卡片前
if (KickCooldownStore.get(roomId).isFuture) {
    Toast("你暂时不能进入此房间，还剩 N 秒")
    return
}
```

### UserMuted 流程
```kotlin
// RoomViewModel 收到 UserMuted 且 target_user_id == self
when (type) {
    "mic"  -> muteViewModel.startMicCountdown(expiresAt)
    "chat" -> muteViewModel.startChatCountdown(expiresAt)
}
// duration_sec=0 则解除
```

`MuteStatusChip` 在房间顶部显示 "你已被禁{麦|言} N:NN" 每秒刷新，到期自动消失。

### testTag
`Key('dialog_kicked')`, `Key('btn_kicked_ack')`, `Key('mute_countdown_mic')`, `Key('mute_countdown_chat')`


### 🔌 协议路径绑定表

N/A — 本 Task 无跨端协议路径，仅 Android 端内部改造

## 三、TDD 验收用例
- [x] **K42-01** UserKicked 到达后 Dialog 可见
- [x] **K42-02** 点"知道了"保存 cooldown 并回 Hall
- [x] **K42-03** 10min 内重进被 Toast 拦截
- [x] **K42-04** 10min 后可正常进房
- [x] **K42-05** UserMuted mic 显示倒计时 Chip
- [x] **K42-06** Chip 每秒减少
- [x] **K42-07** duration_sec=0（Unmute）Chip 消失
- [x] **K42-08** 连续两次 UserMuted 取最新 expires_at
- [x] **K42-09** mic / chat 独立倒计时共存
- [x] **K42-C1** acknowledgeKick() 使用注入的 Clock 计算 cooldown（非硬编码 System.currentTimeMillis）
- [x] **K42-C2** 共享 store — RoomViewModel.acknowledgeKick 写入后 HallViewModel.enterRoom 能检测到 cooldown

## 四、实现结果
- **实际修改的文件**：
  - `feature/room/governance/UserKickedDialog.kt`（新增→R2修改）— 全屏被踢提示弹窗，cooldownMin 向上取整
  - `feature/room/governance/MuteCountdownViewModel.kt`（新增）— 禁麦/禁言倒计时 ViewModel，依赖注入 Clock
  - `feature/room/governance/MuteStatusChip.kt`（新增）— 倒计时 Chip UI，每秒刷新
  - `feature/room/governance/Clock.kt`（新增）— 可注入时钟接口 + SystemClock 实现
  - `data/local/KickCooldownStore.kt`（新增）— KickCooldown interface + InMemoryKickCooldownStore
  - `feature/room/KickedState.kt`（新增）— 被踢状态数据类
  - `feature/room/RoomViewModel.kt`（修改）— 新增 clock: Clock 参数，替换 System.currentTimeMillis()
  - `feature/room/RoomEvent.kt`（修改）— 新增 UserMuted 事件
  - `feature/room/RoomScreen.kt`（修改 R2）— 接入 UserKickedDialog / MuteStatusChip / UserMuted 事件处理
  - `feature/room/HallViewModel.kt`（修改）— 新增 clock: Clock 参数，Factory 接受 kickCooldownStore
  - `common/AppContainer.kt`（修改 R2）— 新增 kickCooldownStore 单例字段（Application 级别）
  - `data/local/FakeKickCooldownStore.kt`（测试 Fake）
  - `utils/FakeClock.kt`（测试 Fake，新增）— 共享可注入测试时钟
- **测试覆盖率**：
  - K42-01~K42-09 + K42-C1 + K42-C2 共 16 个测试用例（含边界用例）全部 PASS
  - MuteCountdownViewModel：100% 行覆盖
  - HallViewModel：100% 行覆盖
  - RoomViewModel：95% 行覆盖
  - KickedState：100% 行覆盖
- **完成时间**：2026-04-23（R2 修复 2026-04-23）

## 五、Reviewer意见
### Round 1
- **状态**：未通过（BLOCKED）
- **审查意见**：
  1. **[CRITICAL] RoomScreen 未接入任何 T-30042 UI 组件**：`kickedState` 未被收集（`UserKickedDialog` 从不显示）；`events.collect{}` 仅处理 `ClearInput`，`RoomEvent.UserMuted` 分支缺失（`MuteCountdownViewModel` 永远不会被调用）；`MuteStatusChip` 未出现在 RoomScreen 中。整个功能从用户视角完全不可见，属于死代码集成。
  2. **[HIGH] KickCooldownStore 两 ViewModel 各持独立实例**：`RoomViewModel` 与 `HallViewModel` 均以默认参数各自 `new InMemoryKickCooldownStore()`，`HallViewModel.Factory` 也不传入 store。`acknowledgeKick()` 写入实例 A，`enterRoom()` 读取实例 B，cooldown 拦截在生产环境完全失效（K42-03 验收标准形同虚设）。
  3. **[HIGH] Clock 未注入 RoomViewModel / HallViewModel**：`acknowledgeKick()`（L526）与 `enterRoom()`（L187）均直接调用 `System.currentTimeMillis()`，违反 TDS 审查要点 #8 的可注入 Clock 设计要求，亦导致 K42-02 测试依赖真实系统时间而脆弱。
  4. **[LOW] `cooldownMin` 整数除法截断**：`UserKickedDialog` L86 `state.cooldownSec / 60` 遇非 60 倍数时显示偏低，建议向上取整。
- **改进建议**：
  - 在 `RoomScreen` 中收集 `kickedState`，条件渲染 `UserKickedDialog`；补全 `events.collect{}` 的 `UserMuted` 分支；显示 `MuteStatusChip`。
  - 在 Application 层提供 `KickCooldownStore` 单例（或通过 DI），`HallViewModel.Factory` 增加 `kickCooldownStore` 构造参数，确保两个 ViewModel 共享同一实例。
  - `RoomViewModel` 和 `HallViewModel` 各自增加 `clock: Clock = SystemClock()` 构造参数，替换所有 `System.currentTimeMillis()` 硬调用。

### Round 2
- **状态**：待审查
- **修复内容**：
  1. **[CRITICAL 已修复] RoomScreen 接入**：新增 `kickedState: KickedState?`、`onAcknowledgeKick`、`muteCountdownViewModel` 参数；`events.collect{}` 中新增 `UserMuted` 分支调用 `MuteCountdownViewModel`；Column 内条件渲染 `MuteStatusChip`（mic/chat 独立）；全屏渲染 `UserKickedDialog`。
  2. **[HIGH 已修复] KickCooldownStore 单例化**：`AppContainer` 新增 `kickCooldownStore: KickCooldownStore` 字段（默认 `InMemoryKickCooldownStore()`），由 Application 生命周期持有；`HallViewModel.Factory` 新增 `kickCooldownStore` 构造参数并透传；新增 K42-C2 集成测试验证共享语义。
  3. **[HIGH 已修复] Clock 注入**：`RoomViewModel` 和 `HallViewModel` 各自新增 `clock: Clock = SystemClock()` 构造参数；替换 3 处 `System.currentTimeMillis()` 调用；新增 K42-C1 精确时间测试验证注入正确性。
  4. **[LOW 已修复] cooldownMin 向上取整**：`UserKickedDialog` 改为 `(state.cooldownSec + 59) / 60`。

### Round 2
- **状态**：通过（APPROVED）
- **审查日期**：2026-04-23
- **审查意见**：

  **R1 修复核实（全部通过）：**
  1. ✅ **[CRITICAL → 已修复]** `RoomScreen.kt` L241-246：`kickedState?.let { ks -> UserKickedDialog(...) }` 正确渲染全屏弹窗；L209-222：`MuteStatusChip` mic/chat 独立条件渲染；L126-136：`UserMuted` 事件分支完整转发至 `MuteCountdownViewModel`。
  2. ✅ **[HIGH → 已修复]** `AppContainer.kt` L71 新增 `kickCooldownStore: KickCooldownStore` Application 级单例字段；`HallViewModel.Factory` 新增 `kickCooldownStore` 构造参数并透传，共享语义正确。
  3. ✅ **[HIGH → 已修复]** `RoomViewModel` L65 / `HallViewModel` L37 均注入 `clock: Clock = SystemClock()`；`acknowledgeKick()` L529 与 `enterRoom()` L190 分别使用注入时钟，3 处 `System.currentTimeMillis()` 全部替换。
  4. ✅ **[LOW → 已修复]** `UserKickedDialog.kt` L86：`(state.cooldownSec + 59) / 60` 向上取整正确；`cooldownSec=0` 时结果为 0，符合"立即可重进"语义。

  **K42-C1 / K42-C2 测试验证：**
  - ✅ `K42-C1`（`RoomViewModelKickedTest.kt` L130-153）：`fakeClock.currentTimeMs = 5_000_000L`，断言 `untilMs == 5_000_000 + 600_000`，精确验证 Clock 注入而非系统时钟，测试有意义。
  - ✅ `K42-C2`（`HallViewModelKickCooldownTest.kt` L158-245）：两 ViewModel 共享同一 `InMemoryKickCooldownStore` 实例；验证 `acknowledgeKick()` 写入 → `enterRoom()` 拦截；再推进 `hallClock +601s` → 正常进房。完整验证共享 Store 行为，测试有意义。

  **新发现问题（不阻塞本次合并）：**
  - **[MEDIUM]** `HallViewModel.Factory.create()` 仅透传 `roomRepository` 和 `kickCooldownStore`，未透传 `clock`。生产环境无影响（默认 `SystemClock()`），但若未来需通过 Factory 构造受控时钟的 ViewModel（集成测试场景），将无法注入。建议下迭代在 Factory 增加 `clock` 参数。
  - **[LOW]** `MuteStatusChip` 的 `remember { mutableLongStateOf(...) }` 仅在 Composable 首次进入时初始化；`expiresAtMs` 变更重启 `LaunchedEffect` 时 `remainingSec` 不立即重置，可能有最多 1 秒显示旧值的轻微视觉问题。建议在 `LaunchedEffect` 首行补充重置语句。
  - **[LOW]** `muteType` 使用裸字符串 `"mic"/"chat"` 散落多处，建议后续用 `enum class MuteType` 替换以保证类型安全。

- **结论**：所有 R1 问题均已正确修复，K42-C1 / K42-C2 测试存在且语义完整，**批准合并，T-30042 进入 DoD**。
