<!--
[AI 写入规约]
-->

# TDS: 公告栏 + 管理员徽章 + RoomInfoUpdated (Task ID: T-30043)

## 一、背景与目标
- **关联需求**：E-10 公告 + 角色可视化 ([设计文档](../../design/android/T-30043.md))
- **本 Task 的目标**：进房首次弹公告、持续入口、全局角色徽章 + WS 动态刷新。

## 二、方案设计

### 涉及文件
| 文件 | 类型 | 说明 |
|------|------|------|
| `feature/room/announcement/AnnouncementPopup.kt` | 新增 |  |
| `feature/room/announcement/AnnouncementIcon.kt` | 新增 | 顶部 📄 图标 |
| `feature/room/components/RoleBadge.kt` | 新增 | 👑/🛡️ |
| `feature/room/RoomViewModel.kt` | 修改 | 监听 AdminChanged/RoomInfoUpdated |
| `data/local/AnnouncementSeenStore.kt` | 新增 | DataStore: `{roomId → lastShownAt}` |

### 公告弹出时机
```kotlin
onEnterRoom { room ->
    if (room.announcement.isNotBlank()) {
        val last = seenStore.get(room.id)
        if (last == null || now - last > 24.hours) {
            showPopup(room.announcement)
            seenStore.save(room.id, now)
        }
    }
}
```

### RoleBadge
```kotlin
@Composable
fun RoleBadge(role: Role, modifier: Modifier = Modifier) = when (role) {
    OWNER -> Icon(Icons.Crown, tint = Gold, ..)
    ADMIN -> Icon(Icons.Shield, tint = Gold, ..)
    MEMBER -> Unit
}
```
所有渲染用户身份的位置统一使用：
- 麦位卡片昵称
- 观众席行（T-30039）
- 弹幕消息发送者昵称（T-30017 修改）
- 用户操作菜单头部（T-30040）

### WS 订阅
- `RoomInfoUpdated` → 更新 `roomState.announcement/title/category`；若 announcement 变化，重置 seen 并再次弹窗
- `AdminChanged` → 更新 `roomState.adminUserId`；触发所有 RoleBadge 重组（通过 stateflow）

### testTag
`Key('announcement_popup')`, `Key('btn_announcement_close')`, `Key('btn_show_announcement')`, `Key('role_badge_${userId}')`

## 三、TDD 验收用例
- [ ] **AN43-01** 首次进有公告房弹窗
- [ ] **AN43-02** 24h 内再进房不弹窗
- [ ] **AN43-03** 空公告不显示顶部 📄
- [ ] **AN43-04** 顶部 📄 点击手动弹出
- [ ] **AN43-05** AdminChanged 到达后 500ms 内麦位徽章更新
- [ ] **AN43-06** RoomInfoUpdated 改 announcement 后重新弹窗
- [ ] **AN43-07** Owner 👑 / Admin 🛡️ / member 无徽章
- [ ] **AN43-08** 弹窗长文本滚动 + 关闭按钮

## 四、实现结果
- **实际修改的文件**：
- **测试覆盖率**：
- **完成时间**：

## 五、Reviewer意见
### Round 1
- **状态**：
- **审查意见**：
- **改进建议**：
