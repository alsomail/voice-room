# 测试套件：AUTH 用户认证（Android）

> **需求模糊点 (Ambiguity Notes)**：
> - 无

覆盖 Task：T-30001~T-30004（登录页 UI + E2E + JWT 拦截器 + UserRepository）。

---

## TC-AUTH-00002：登录页 UI - +966 前缀 + RTL 布局
**【元数据】**
- **归属模块**：`AUTH`
- **测试类型**：`Compatibility`
- **回归级别**：`P0`

**【前置条件】**
1. Debug 版 App 已安装于 Android 13 设备。
2. 系统语言为阿拉伯语。

**【执行步骤与断言】**
| 步骤序号 | 目标端 | 操作动作 (Action) | 预期结果 (Assertion) |
| :------: | :----- | :---------------- | :------------------- |
| 1 | `Android` | 启动 App，停留在登录页 | 屏幕中央显示手机号输入框，左侧固定 `+966` 前缀，文字方向从右至左 |
| 2 | `Android` | 在手机号框输入 `512345678` | 框内显示 `512345678`（跟随 RTL 仍保持数字左对齐或自然显示） |
| 3 | `Android` | 观察"获取验证码"按钮位置 | RTL 下按钮位于输入框左侧或下方，视觉可见、未被裁切 |
| 4 | `Android` | 切换系统语言为英文重启 App | 登录页变回 LTR 布局，`+966` 前缀仍显示 |

**【数据清理】**
- 无。

---

## TC-AUTH-00003：60 秒倒计时 + 再次点击冷却提示
**【元数据】**
- **归属模块**：`AUTH`
- **测试类型**：`Functional`
- **回归级别**：`P0`

**【前置条件】**
1. 登录页，手机号已填合法值。

**【执行步骤与断言】**
| 步骤序号 | 目标端 | 操作动作 (Action) | 预期结果 (Assertion) |
| :------: | :----- | :---------------- | :------------------- |
| 1 | `Android` | 点击"获取验证码"按钮 | 按钮文字 1 秒内变为 `60s 后重发`，置灰不可点 |
| 2 | `Android` | 等待 60 秒 | 文字每秒递减，到 0 后恢复为"获取验证码"可点状态 |
| 3 | `Android` | 10 秒时强制点击（如通过辅助服务） | 无反应，按钮仍为置灰 |
| 4 | `Android` | 重启 App 后再点击 | Toast / 底部 SnackBar 显示"请稍后再试"（服务端 cooldown） |

**【数据清理】**
- 无。

---

## TC-AUTH-00004：E2E 一键登录 - 新用户进入大厅
**【元数据】**
- **归属模块**：`AUTH`
- **测试类型**：`Integration`
- **回归级别**：`P0`

**【前置条件】**
1. 后端 Mock SMS 固定验证码 `123456`。
2. `users` 表无 `+966500000100`。

**【执行步骤与断言】**
| 步骤序号 | 目标端 | 操作动作 (Action) | 预期结果 (Assertion) |
| :------: | :----- | :---------------- | :------------------- |
| 1 | `Android` | 在手机号框输入 `500000100` | 框内文字更新 |
| 2 | `Android` | 点击"获取验证码"按钮 | 按钮置灰倒计时 |
| 3 | `Android` | 在验证码框输入 `123456` | 框内文字更新 |
| 4 | `Android` | 点击"登录"按钮 | 屏幕中央出现圆形 Loading 指示器 |
| 5 | `AppServer` | 后端日志 | 记录 POST /auth/login 200 |
| 6 | `Android` | 2 秒内 | 自动跳转到大厅页（HallScreen），顶部标题显示"语聊房" |
| 7 | `DB` | users 表 | 出现 phone=`+966500000100` 的新记录 |
| 8 | `Android` | 杀掉 App 后重新启动 | 直接进入大厅，跳过登录页（JWT 持久化） |

**【数据清理】**
- 删除测试 user 记录。

---

## TC-AUTH-00005：验证码错误 - 错误提示
**【元数据】**
- **归属模块**：`AUTH`
- **测试类型**：`Functional`
- **回归级别**：`P1`

**【前置条件】**
1. 手机号已发码，服务端真实验证码为 `123456`。

**【执行步骤与断言】**
| 步骤序号 | 目标端 | 操作动作 (Action) | 预期结果 (Assertion) |
| :------: | :----- | :---------------- | :------------------- |
| 1 | `Android` | 输入错误验证码 `000000`，点击登录 | 登录按钮下方显示红色文字"验证码错误"或 SnackBar |
| 2 | `Android` | 页面停留 | 仍在登录页，未跳转 |

**【数据清理】**
- 无。

---

## TC-AUTH-00006：JWT 拦截器 + 401 自动退出
**【元数据】**
- **归属模块**：`AUTH`
- **测试类型**：`Integration`
- **回归级别**：`P0`

**【前置条件】**
1. U1 已登录，进入大厅。

**【执行步骤与断言】**
| 步骤序号 | 目标端 | 操作动作 (Action) | 预期结果 (Assertion) |
| :------: | :----- | :---------------- | :------------------- |
| 1 | `Android` | 在大厅手动下拉刷新 | 抓包（Charles）可见请求头含 `Authorization: Bearer <token>` |
| 2 | `AppServer` | 服务端手动使 U1 的 token 过期（或下发 token 黑名单） | - |
| 3 | `Android` | 再次下拉刷新 | 触发 HTTP 401，App 自动清除本地 JWT，跳转登录页；Toast 显示"登录已过期，请重新登录" |

**【数据清理】**
- 无。

---

## TC-AUTH-00007：UserRepository - Room 缓存 + Flow
**【元数据】**
- **归属模块**：`AUTH`
- **测试类型**：`Functional`
- **回归级别**：`P1`

**【前置条件】**
1. 已登录 U1，个人中心页已打开过一次（数据已缓存到 Room）。

**【执行步骤与断言】**
| 步骤序号 | 目标端 | 操作动作 (Action) | 预期结果 (Assertion) |
| :------: | :----- | :---------------- | :------------------- |
| 1 | `Android` | 打开飞行模式 | 网络断开 |
| 2 | `Android` | 重新打开 App 进入"我的"页 | 页面立即展示上次缓存的头像/昵称/余额，不出现空白加载态 |
| 3 | `Android` | 关闭飞行模式，手动刷新 | 数据从服务端刷新，Flow 触发 UI 自动重绘 |

**【数据清理】**
- 无。
