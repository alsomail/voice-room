<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建，TDD Agent 只读不写。
2. 创建后必须在 doc/tasks/index.md 对应 Task 的行内补充链接：[TDS](./tds/adminServer/T-10001.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: 管理员表设计 (Task ID: T-10001)

## 一、背景与目标
- **关联需求**：product.md - Admin Server 管理后台
- **前置依赖**：T-00001 (users 表设计)
- **本 Task 的目标**：设计并创建 admins 表和 admin_logs 表，作为 Admin Server 认证系统的数据基础

## 二、方案设计
### 核心数据流
SQLx migration 文件 → PostgreSQL → admins 表 + admin_logs 表创建

### 涉及文件清单
| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `app/adminServer/migrations/001_create_admins.sql` | 新增 | admins 表 DDL |
| `app/adminServer/migrations/002_create_admin_logs.sql` | 新增 | admin_logs 表 DDL |
| `app/adminServer/migrations/003_seed_super_admin.sql` | 新增 | 默认 super_admin 种子数据 |

### 接口 / 信令定义

**前置依赖**：T-00001 (users 表先行)

**数据模型**（参见 doc/protocol.md §六 6.3, 6.4）：

admins 表：
```sql
CREATE TABLE admins (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    username     VARCHAR(50) NOT NULL UNIQUE,
    password_hash VARCHAR(200) NOT NULL,
    role         VARCHAR(20) NOT NULL DEFAULT 'operator',
    display_name VARCHAR(100),
    is_active    BOOLEAN NOT NULL DEFAULT true,
    last_login_at TIMESTAMPTZ,
    created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);
ALTER TABLE admins ADD CONSTRAINT chk_admin_role CHECK (role IN ('super_admin', 'operator', 'cs', 'finance'));
```

admin_logs 表：
```sql
CREATE TABLE admin_logs (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    admin_id    UUID NOT NULL REFERENCES admins(id),
    action      VARCHAR(50) NOT NULL,
    target_type VARCHAR(20),
    target_id   UUID,
    detail      JSONB,
    ip_address  INET,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_admin_logs_admin_id ON admin_logs(admin_id, created_at DESC);
CREATE INDEX idx_admin_logs_action ON admin_logs(action, created_at DESC);
```

## 三、TDD 验收用例
- [ ] 正向：migration 可成功执行，admins 表 + admin_logs 表创建完成
- [ ] 正向：username 有唯一索引
- [ ] 正向：password_hash 使用 bcrypt（通过 shared crate）
- [ ] 正向：role 字段有 CHECK 约束（super_admin, operator, cs, finance）
- [ ] 正向：种子数据包含默认 super_admin 账号
- [ ] 异常：重复 username 插入应触发唯一约束冲突

## 四、实现结果（由 TDD Agent 完成后填写）
- **实际修改的文件**：
  - `app/adminServer/migrations/001_create_admins.sql`（新增）— admins 表 DDL + UNIQUE 约束 + CHECK 约束
  - `app/adminServer/migrations/002_create_admin_logs.sql`（新增）— admin_logs 表 DDL + 两个索引
  - `app/adminServer/migrations/003_seed_super_admin.sql`（新增）— 默认 super_admin 种子数据（bcrypt 散列）
  - `app/adminServer/src/lib.rs`（新增）— `is_valid_admin_role()` 函数 + 16 个单元测试 + 1 个 doctest
  - `app/adminServer/Cargo.toml`（修改）— dev-dependencies 中增加 `chrono`、`uuid`
  - `app/shared/src/models/admin.rs`（新增）— `AdminModel` 结构体（9 个字段，`FromRow` + Serde）
  - `app/shared/src/models/mod.rs`（修改）— 导出 `AdminModel`
- **测试覆盖率**：17 个测试全部通过（16 unit + 1 doctest）；`cargo clippy --workspace -- -D warnings` 零警告
- **完成时间**：2026-04-18

## 五、Reviewer意见
> 状态枚举：🟢 通过 | 🔴 未通过
### Review理由：
> 状态：🟢 通过
>
> **审查人**：Reviewer  
> **审查日期**：2026-04-19
>
> ### 通过理由
>
> 三项验收标准全部满足：
> 1. ✅ **username 唯一索引**：`001_create_admins.sql` 中 `username VARCHAR(50) NOT NULL UNIQUE` 内联约束，等价于唯一索引。
> 2. ✅ **password_hash 使用 bcrypt**：`shared/src/crypto/password.rs` 使用 `bcrypt::DEFAULT_COST`（= 12）；seed 文件使用 `$2b$12$` 格式；`AdminModel` 文档注释明确格式要求；`lib.rs` 有独立的 bcrypt 格式单元测试。
> 3. ✅ **role CHECK 约束**：`chk_admin_role` 包含 `super_admin / operator / cs / finance` 四值，与 `VALID_ADMIN_ROLES` 常量双层守护。
>
> ### 附加评价
>
> - `AdminModel` 9 个字段与 migration schema 精确对齐，类型映射正确。
> - 17 个测试（16 unit + 1 doctest）通过 `include_str!` 对 SQL 文件做编译期内容校验，覆盖率充分。
> - `init-db.sh` 创建独立的 `app_server_user` / `admin_server_user` 双角色，Admin Server 权限隔离设计完整。
> - Migration 均使用 `IF NOT EXISTS` 幂等 DDL，种子数据使用 `ON CONFLICT DO NOTHING`。
>
> ### 待跟进问题（不阻塞，后续迭代处理）
>
> | 级别 | 问题 | 建议处理时机 |
> |------|------|-------------|
> | MEDIUM | `003_seed_super_admin.sql` 中已知明文密码的 bcrypt hash 存在 VCS，需运维层面强制首次部署改密 | T-10002 DoD checklist 中补充操作项 |
> | MEDIUM | `admins.updated_at` 缺少自动更新触发器，UPDATE 时需应用层显式设置 | T-10002 实现登录/更新接口时在 SQL 中显式 `SET updated_at = now()` |
> | LOW | `admin_logs.admin_id` 外键未显式声明 `ON DELETE RESTRICT`，意图不明确 | T-10012 admin_logs 完善时补充 |
> | LOW | `init-db.sh` 未对 `app_server_user` 执行 `REVOKE ALL ON TABLE admins` 显式声明隔离意图 | 基础设施加固迭代中处理 |
