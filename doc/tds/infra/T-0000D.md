<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建，TDD Agent 只读不写。
2. 创建后必须在 doc/Tasks.md 对应 Task 的行内补充链接：[TDS](./tds/infra/T-0000D.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: CI 基础流水线 (Task ID: T-0000D)

## 一、背景与目标
- **关联需求**：product.md - 工程基建
- **本 Task 的目标**：配置 GitHub Actions CI 流水线，PR 触发自动 lint + test + build，确保代码质量门禁

## 二、方案设计

### 文件位置

```
.github/workflows/ci.yml
```

### CI 流水线内容

```yaml
name: CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  SQLX_OFFLINE: true

jobs:
  rust-check:
    name: Rust Lint + Test + Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy lint
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Run tests
        run: cargo test --workspace

      - name: Build
        run: cargo build --workspace --release

  web-check:
    name: Web Lint + Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app/web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: app/web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build
```

### 关键设计决策

1. **`SQLX_OFFLINE: true`**：CI 无数据库，使用 `.sqlx/` 离线缓存
2. **Rust Cache**：`Swatinem/rust-cache@v2` 加速编译
3. **两个独立 Job**：Rust 和 Web 并行执行
4. **零警告策略**：Clippy `-D warnings`

### 涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `.github/workflows/ci.yml` | 新增 | CI 流水线 |

## 三、TDD 验收用例
- [ ] 正向：PR 触发自动检查，GitHub 显示 check status
- [ ] 正向：`cargo clippy --workspace` 零警告
- [ ] 正向：`cargo test --workspace` 全部通过
- [ ] 正向：`cargo fmt --all -- --check` 格式正确
- [ ] 正向：Web 端 `npm run lint` 通过
- [ ] 正向：Web 端 `npx tsc --noEmit` 类型检查通过

## 四、实现结果（由 TDD Agent 完成后填写）
- **实际修改的文件**：`.github/workflows/ci.yml`（新增）
- **测试覆盖率**：本地验收：`cargo fmt --all -- --check` ✅，`cargo clippy --workspace --all-targets -- -D warnings` ✅，`cargo test --workspace` 8/8 ✅；PR 触发后 GitHub Actions 自动执行两个并行 job
- **完成时间**：2026-04-18

## 五、Reviewer意见
> 状态枚举：🟢 通过（第二轮）
### Review理由（第一轮 🔴 未通过，已修复）：
**🟠 HIGH**（已修复）
- `ci.yml:33-34`：`cargo build --workspace --release` 属于 CD 产物构建，不应在 CI 门禁中。编译时间增加 3-5 倍，且测试阶段已做过 debug 编译验证。删除此步骤或移至独立 release job（仅 tag 触发）。

**🟡 MEDIUM**（已修复）
- 缺少 `timeout-minutes`，建议 rust-check: 30，web-check: 15，防止挂起。

### 第二轮结论（通过）：
- `cargo build --workspace --release` 步骤已删除，job 名称更新为 `Rust Lint + Test`。✅
- `timeout-minutes: 30`（rust-check）和 `timeout-minutes: 15`（web-check）已补充。✅