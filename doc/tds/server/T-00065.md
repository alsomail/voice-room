# TDS: 贵族 Schema 与种子 (Task ID: T-00065)

## 一、背景与目标
- **关联需求**：[phase1_nobility.md §1 / §3.5](../../product/phase1_nobility.md) — 6 档贵族数据基础。
- **目标**：建立 `noble_tiers` / `user_nobles` / `noble_history` / `noble_global_broadcast_log` 表 + privileges JSONB 校验函数 + 6 档生产种子。

## 二、方案设计
### 涉及文件
| 文件 | 变更 |
|------|------|
| `app/server/migrations/...create_noble_tiers.up.sql` | 新增 |
| `app/server/migrations/...create_user_nobles.up.sql` | 新增 |
| `app/server/migrations/...create_noble_history.up.sql` | 新增 |
| `app/server/migrations/...create_noble_global_broadcast_log.up.sql` | 新增 |
| `app/server/migrations/...seed_noble_tiers.up.sql` | 新增 6 档种子 |
| `app/shared/src/nobility/mod.rs` | 新增 `TierId` / `NoblePrivileges` Rust 类型 + serde |
| `doc/protocol/schemas/http/NobilityPrivileges.schema.json` | 新增 JSON Schema 2020-12 |

### 🔌 协议路径绑定表
| # | 协议类型 | 入口 | 处理函数 | protocol/ 锚点 |
|---|---------|------|---------|---------------|
| - | N/A — 仅 schema 与种子；无对外通信 | - | - | [nobility_api.md §10.2](../../protocol/nobility_api.md) |

## 三、TDD 验收用例
- [ ] migrate 幂等
- [ ] `user_nobles.user_id` PRIMARY KEY 保证一人一贵族
- [ ] `(expire_at)` 索引 EXPLAIN 走 Index Scan
- [ ] privileges JSONB 写入非法结构（如 percent>100）→ `noble_privileges_validate(jsonb)` 函数 RAISE EXCEPTION
- [ ] 6 档种子完整：knight/baron/viscount/earl/duke/king，level=1..6 唯一
- [ ] noble_global_broadcast_log 唯一约束 `(user_id, kind, broadcast_date)`

### 🔴 协议路径绑定
- [ ] **PROTO-1**：迁移字段名严格匹配 [nobility_api.md §10.2 表结构](../../protocol/nobility_api.md)
- [ ] **PROTO-2**：种子 6 档与 §10.2.4 表 100% 一致（diamonds/usd/discount/mic_weight/stipend/invisibility/bypass_pw/global_bc）

## 四、实现结果
## 五、Reviewer意见
### Round 1（2026-05-10，Reviewer: Dod）
**状态：❌ 打回**

#### [HIGH] P1-3 `monthly_stipend` JSONB 字段名与协议不符

**文件**：`app/shared/src/models/nobility.rs:115-119`、`app/server/migrations/011_create_nobility.sql` 种子数据

协议 §10.2.3 明确定义：
```jsonc
"monthly_stipend": { "percent": 20, "pay_immediately": true }
```
但实现使用了 `diamonds: i64` 字段名（与协议冲突）：
```rust
// ❌ 当前实现
pub struct MonthlyStipendPrivilege {
    pub diamonds: i64,   // 应为 percent: i64
    pub pay_immediately: bool,
}
```
SQL 种子数据同样使用 `"diamonds"` 键，客户端解析协议字段 `percent` 时会静默拿到 null。

#### [HIGH] P1-3 种子数据月津贴数值错误

**文件**：`app/server/migrations/011_create_nobility.sql` L108-L242

产品 §3.5.11 规定各档月津贴返还比例（月费钻石数 × 比例）：

| tier | 协议 percent | 产品应发钻石 | 种子实际 diamonds | 差异 |
|------|------------|------------|-----------------|------|
| knight | 5% | 150 | 0 | ❌ 缺失 |
| baron | 8% | 800 | 0 | ❌ 缺失 |
| viscount | 10% | 3000 | 0 | ❌ 缺失 |
| earl | 12% | 12000 | 0 | ❌ 缺失 |
| duke | 15% | **45000** | **60000** | ❌ 数值错误 |
| king | 20% | 200000 | 200000 | ✅（凑巧一致）|

另，LV1-LV4 种子 `pay_immediately: false`，但产品§3.5.11 所有档位均为"续费成功立即发放"，应改为 `true`。

**修复方向**：
1. 将 `MonthlyStipendPrivilege.diamonds` 改为 `percent: i64`
2. 更新 6 档种子 `monthly_stipend.percent` 为 5/8/10/12/15/20
3. LV1-LV4 `pay_immediately` 改为 `true`

#### [MEDIUM] P1-4 `noble_privileges_validate()` 未绑定为 CHECK 约束

**文件**：`app/server/migrations/011_create_nobility.sql` L76-L93

函数已正确定义（检查 mic_priority / gift_discount / monthly_stipend 键存在），但 `noble_tiers` 建表语句未添加 CHECK 约束：
```sql
-- ❌ 缺少
ALTER TABLE noble_tiers ADD CONSTRAINT chk_privileges
    CHECK (noble_privileges_validate(privileges));
-- 或在 CREATE TABLE 内：
privileges JSONB NOT NULL CHECK (noble_privileges_validate(privileges)),
```
当前任何格式非法的 privileges JSONB 均可直接写入，validate 函数从未被调用。

- **🔴 Changelog 行**：
## 六、DoD
- [ ] `doc/arch/server/nobility.md`（新建）+ schema 落地路径
- [ ] nobility_api.md §10.2 互链
- [ ] Changelog 行追加
