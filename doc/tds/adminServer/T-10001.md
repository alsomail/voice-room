<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建，TDD Agent 只读不写。
2. 创建后必须在 doc/Tasks.md 对应 Task 的行内补充链接：[TDS](./tds/adminServer/T-10001.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: 管理员表设计 (Task ID: T-10001)

## 一、背景与目标
- **关联需求**：product.md - Admin Server 管理后台
- **前置依赖**：T-00001 (users 表设计)
- **本 Task 的目标**：设计并创建 admins 表和 admin_logs 表，作为 Admin Server 认证系统的数据基础

## 二、方案设计
### 核心数据流
SQLx migration 文件 → PostgreSQL → admins 表 + admin_logs 表创建

### 涉及文件清单
| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `app/adminServer/migrations/001_create_admins.sql` | 新增 | admins 表 DDL |
| `app/adminServer/migrations/002_create_admin_logs.sql` | 新增 | admin_logs 表 DDL |
| `app/adminServer/migrations/003_seed_super_admin.sql` | 新增 | 默认 super_admin 种子数据 |

### 接口 / 信令定义

**前置依赖**：T-00001 (users 表先行)

**数据模型**（参见 doc/protocol.md §六 6.3, 6.4）：

admins 表：
```sql
CREATE TABLE admins (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    username     VARCHAR(50) NOT NULL UNIQUE,
    password_hash VARCHAR(200) NOT NULL,
    role         VARCHAR(20) NOT NULL DEFAULT 'operator',
    display_name VARCHAR(100),
    is_active    BOOLEAN NOT NULL DEFAULT true,
    last_login_at TIMESTAMPTZ,
    created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);
ALTER TABLE admins ADD CONSTRAINT chk_admin_role CHECK (role IN ('super_admin', 'operator', 'cs', 'finance'));
```

admin_logs 表：
```sql
CREATE TABLE admin_logs (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    admin_id    UUID NOT NULL REFERENCES admins(id),
    action      VARCHAR(50) NOT NULL,
    target_type VARCHAR(20),
    target_id   UUID,
    detail      JSONB,
    ip_address  INET,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_admin_logs_admin_id ON admin_logs(admin_id, created_at DESC);
CREATE INDEX idx_admin_logs_action ON admin_logs(action, created_at DESC);
```

## 三、TDD 验收用例
- [ ] 正向：migration 可成功执行，admins 表 + admin_logs 表创建完成
- [ ] 正向：username 有唯一索引
- [ ] 正向：password_hash 使用 bcrypt（通过 shared crate）
- [ ] 正向：role 字段有 CHECK 约束（super_admin, operator, cs, finance）
- [ ] 正向：种子数据包含默认 super_admin 账号
- [ ] 异常：重复 username 插入应触发唯一约束冲突

## 四、实现结果（由 TDD Agent 完成后填写）
- **实际修改的文件**：
- **测试覆盖率**：
- **完成时间**：

## 五、Reviewer意见
> 状态枚举：🟢 通过 | 🔴 未通过
### Review理由：