package com.voice.room.android.feature.profile

import com.voice.room.android.common.FeatureDescriptor

object ProfileFeature {
    val descriptor = FeatureDescriptor(
        title = "Profile Placeholder",
        description = "User profile and localization-ready presentation will land in the profile feature package."
    )
}
