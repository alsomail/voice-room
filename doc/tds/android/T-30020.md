<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建。TDD Agent 仅填写第四节【实现结果】，其余章节只读。Review Agent 仅填写第五节【Reviewer意见】。
2. 创建后必须在 doc/Tasks.md 对应 Task 的行内补充链接：[TDS](./tds/android/T-30020.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: MainScreen 底部三Tab框架 (Task ID: T-30020)

## 一、背景与目标
- **关联需求**：Product Phase 0.5 — App 主页框架，底部三 Tab 导航（房间大厅 / 消息 / 我的）
- **本 Task 的目标**：实现 MainScreen 三 Tab 框架，替换当前 `AppNavGraph` 中的 `MainPlaceholderScreen` 占位，使用 Scaffold + NavigationBar 构建底部导航，Tab 切换保持各页面状态

## 二、方案设计

### 核心数据流

```
AppNavGraph("main" 路由)
  └─ MainScreen(appContainer)
       ├─ Scaffold
       │    ├─ content: 内部 NavHost（三 Tab 子页面）
       │    │    ├─ "main/rooms"    → HallScreen (现有 Paging3 大厅页)
       │    │    ├─ "main/messages" → MessagesPlaceholder (占位)
       │    │    └─ "main/profile"  → ProfilePlaceholder (占位)
       │    │
       │    └─ bottomBar: MenaBottomNavigation
       │         ├─ Tab: 房间 (Icons.Default.Home)     → "main/rooms"
       │         ├─ Tab: 消息 (Icons.Default.Chat)     → "main/messages"  [未来: 未读红点]
       │         └─ Tab: 我的 (Icons.Default.Person)   → "main/profile"
       │
       └─ NavController (独立于外层 AppNavGraph 的 NavController)
```

### 设计决策

1. **嵌套导航**：MainScreen 拥有独立的 `NavController` + `NavHost`，与外层 `AppNavGraph` 的 NavController 隔离。外层管控 `splash/login/main` 三大页面，内层管控三个 Tab 子页面。
2. **Tab 状态保持**：使用 `navController.navigate(route) { popUpTo(graph.findStartDestination().id) { saveState = true }; launchSingleTop = true; restoreState = true }` 标准模式。
3. **颜色定义**：选中色 `MenaColors.Primary`（#D4AF37 金色），未选中色 `MenaColors.OnBackgroundTertiary`（#6C6C6C 灰色），导航栏背景 `MenaColors.Background`（#1A1A2E）。
4. **占位页**：消息和我的 Tab 使用简单的居中文字占位 Composable，后续 T-30023、T-30024 替换。
5. **房间 Tab 内容**：复用现有 `HallScreen`（Paging3 无限滚动），需要传入 `onRoomClick` 回调（导航到 RoomScreen 由外层 NavController 处理，暂不实现，传空回调）。

### 数据模型

```kotlin
// MainTab — 底部 Tab 枚举定义
enum class MainTab(
    val route: String,
    val icon: ImageVector,
    val labelEn: String,
    val labelAr: String,
    val testTag: String,
) {
    ROOMS("main/rooms", Icons.Default.Home, "Rooms", "الغرف", "tab_rooms"),
    MESSAGES("main/messages", Icons.Default.Chat, "Messages", "الرسائل", "tab_messages"),
    PROFILE("main/profile", Icons.Default.Person, "Me", "حسابي", "tab_profile"),
}
```

### 涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `app/android/…/feature/main/MainTab.kt` | **新增** | MainTab 枚举（路由、图标、标签、testTag） |
| `app/android/…/feature/main/MainScreen.kt` | **新增** | Scaffold + NavHost + NavigationBar 三 Tab 主页 |
| `app/android/…/feature/main/MenaBottomNavigation.kt` | **新增** | 底部导航栏封装组件（金色选中、灰色未选中） |
| `app/android/…/feature/main/MessagesPlaceholder.kt` | **新增** | 消息 Tab 占位 Composable |
| `app/android/…/feature/main/ProfilePlaceholder.kt` | **新增** | 我的 Tab 占位 Composable |
| `app/android/…/presentation/AppNavGraph.kt` | **修改** | `composable("main")` 内替换 `MainPlaceholderScreen` → `MainScreen` |

> 文件路径缩写 `…` = `app/src/main/java/com/voice/room/android`

### 关键实现细节

#### MainScreen.kt

```kotlin
@Composable
fun MainScreen(appContainer: AppContainer) {
    val navController = rememberNavController()
    val navBackStackEntry by navController.currentBackStackEntryAsState()
    val currentRoute = navBackStackEntry?.destination?.route

    Scaffold(
        modifier = Modifier.testTag("main_screen"),
        containerColor = MenaColors.Background,
        bottomBar = {
            MenaBottomNavigation(
                currentRoute = currentRoute,
                onTabSelected = { tab ->
                    navController.navigate(tab.route) {
                        popUpTo(navController.graph.findStartDestination().id) {
                            saveState = true
                        }
                        launchSingleTop = true
                        restoreState = true
                    }
                }
            )
        }
    ) { innerPadding ->
        NavHost(
            navController = navController,
            startDestination = MainTab.ROOMS.route,
            modifier = Modifier.padding(innerPadding)
        ) {
            composable(MainTab.ROOMS.route) {
                // 复用现有 HallScreen，onRoomClick 暂传空（T-30022 视觉升级时完善）
                HallScreen(…)
            }
            composable(MainTab.MESSAGES.route) {
                MessagesPlaceholder()
            }
            composable(MainTab.PROFILE.route) {
                ProfilePlaceholder()
            }
        }
    }
}
```

#### MenaBottomNavigation.kt

```kotlin
@Composable
fun MenaBottomNavigation(
    currentRoute: String?,
    onTabSelected: (MainTab) -> Unit,
    modifier: Modifier = Modifier,
) {
    NavigationBar(
        modifier = modifier.testTag("bottom_nav"),
        containerColor = MenaColors.Background,
        tonalElevation = 0.dp,
    ) {
        MainTab.entries.forEach { tab ->
            val selected = currentRoute == tab.route
            NavigationBarItem(
                selected = selected,
                onClick = { onTabSelected(tab) },
                icon = { Icon(tab.icon, contentDescription = tab.labelEn, modifier = Modifier.size(24.dp)) },
                label = { Text(tab.labelEn, style = MaterialTheme.typography.labelSmall) },
                colors = NavigationBarItemDefaults.colors(
                    selectedIconColor = MenaColors.Primary,
                    selectedTextColor = MenaColors.Primary,
                    unselectedIconColor = MenaColors.OnBackgroundTertiary,
                    unselectedTextColor = MenaColors.OnBackgroundTertiary,
                    indicatorColor = Color.Transparent,
                ),
                modifier = Modifier.testTag(tab.testTag),
            )
        }
    }
}
```

### 接口 / 信令定义

本 Task 纯客户端 UI 框架，不涉及前后端通信。

## 三、TDD 验收用例

### 正向场景
- [ ] **TB-01**: 默认启动 MainScreen 后，当前路由为 `main/rooms`，房间 Tab 选中状态
- [ ] **TB-02**: 点击三个 Tab 均可正常切换，对应内容区域正确渲染
- [ ] **TB-03**: 选中 Tab 的图标和文字颜色为金色（`MenaColors.Primary` #D4AF37）
- [ ] **TB-04**: 未选中 Tab 的图标和文字颜色为灰色（`MenaColors.OnBackgroundTertiary` #6C6C6C）
- [ ] **TB-05**: Tab 切换后再切回，之前的页面状态保持（如房间列表滚动位置）
- [ ] **TB-06**: 房间 Tab 显示 HallScreen 的内容（如 `hall_loading` 或房间列表）
- [ ] **TB-07**: 消息 Tab 显示占位内容（"Messages" 或 "الرسائل" 占位文本）
- [ ] **TB-08**: 我的 Tab 显示占位内容（"Me" 或 "حسابي" 占位文本）

### 边界场景
- [ ] **TB-09**: 快速连续点击同一 Tab 不产生多个 BackStack 条目（`launchSingleTop = true`）
- [ ] **TB-10**: 底部导航栏始终可见（`bottom_nav` testTag 可定位）
- [ ] **TB-11**: `main_screen` testTag 可在根容器上定位
- [ ] **TB-12**: RTL 布局下 Tab 顺序不反转（从右至左仍为 房间/消息/我的）

### testTag 协议
| testTag | 元素 | 用途 |
|---------|------|------|
| `main_screen` | MainScreen Scaffold | 主页容器定位 |
| `bottom_nav` | NavigationBar | 底部导航栏定位 |
| `tab_rooms` | 房间 Tab | 点击与选中状态断言 |
| `tab_messages` | 消息 Tab | 点击与选中状态断言 |
| `tab_profile` | 我的 Tab | 点击与选中状态断言 |

## 四、实现结果（由 TDD Agent 完成后填写）
- **实际修改的文件**：
  - **新增（业务代码）**：
    - `app/android/app/src/main/java/com/voice/room/android/feature/main/MainTab.kt` — Tab 枚举（route/icon/labelEn/labelAr/testTag）
    - `app/android/app/src/main/java/com/voice/room/android/feature/main/MainScreen.kt` — Scaffold + 内部 NavHost + NavigationBar 三 Tab 主页
    - `app/android/app/src/main/java/com/voice/room/android/feature/main/MenaBottomNavigation.kt` — 底部导航栏封装（金色选中/灰色未选中）
    - `app/android/app/src/main/java/com/voice/room/android/feature/main/MessagesPlaceholder.kt` — 消息 Tab 占位 Composable
    - `app/android/app/src/main/java/com/voice/room/android/feature/main/ProfilePlaceholder.kt` — 我的 Tab 占位 Composable
  - **修改（业务代码）**：
    - `app/android/app/src/main/java/com/voice/room/android/presentation/AppNavGraph.kt` — `composable("main")` 替换 `MainPlaceholderScreen` → `MainScreen`，移除 `MainPlaceholderScreen` 私有函数及其无用 import
  - **新增（测试代码）**：
    - `app/android/app/src/test/java/com/voice/room/android/feature/main/MainTabTest.kt` — 17 个 JVM 单元测试（枚举属性、唯一性、顺序）
    - `app/android/app/src/androidTest/java/com/voice/room/android/feature/main/MainScreenTest.kt` — 8 个 Compose UI 集成测试（TB-01/02/06/07/08/10/11 + 扩展）
  - **修复（预存编译错误）**：
    - `app/android/app/src/androidTest/java/com/voice/room/android/feature/room/HallScreenTest.kt` — 修复 `assertDoesNotExist` 缺失、`createRoom` 接口不匹配
    - `app/android/app/src/androidTest/java/com/voice/room/android/feature/room/HallScreenPagingTest.kt` — 同上
- **测试覆盖率**：单元测试 17/17 通过，UI 测试编译通过（需设备运行）
- **完成时间**：2026-04-21 01:29

## 五、Reviewer意见

> 状态枚举：🟢 通过 | 🔴 未通过
> 每轮 Review 追加一条记录，不要覆盖历史。

### Round 1
- **状态**：🟢 通过
- **审查意见**：
  - 架构合规 ✅：纯 Composable 导航壳层 + 内嵌 NavHost，符合 MVVM + Compose
  - 主题合规 ✅：正确使用 MenaColors.Primary/Background/OnBackgroundTertiary
  - 导航合规 ✅：AppNavGraph "main" 路由正确替换为 MainScreen
  - 状态管理 ✅：saveState/restoreState/launchSingleTop 标准模式
  - RTL 支持 ✅：Icons.AutoMirrored.Filled.Chat + NavigationBar 原生 RTL
  - CancellationException ✅ (N/A)：无新协程 try-catch
  - 无障碍 ✅：contentDescription + label 语义完整
  - 测试覆盖 ✅：17 JVM 单测 + 8 UI 集成测试
  - 包结构 ✅：com.voice.room.android.feature.main
  - [LOW] MainViewModel/MainDestination/MainUiState 为遗留死代码，建议后续清理
  - [LOW] 底部标签未国际化（labelEn/labelAr 仅用 labelEn），Phase 0.5 可接受
  - [LOW] 集成测试未包裹 MenaTheme，不影响功能验证
- **改进建议**：3 个 LOW 级别问题不阻塞，可在后续迭代处理

<!-- 如有多轮 Review，按以下格式追加：
### Round N
- **状态**：
- **审查意见**：
- **改进建议**：
-->
