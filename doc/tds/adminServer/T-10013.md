<!--
[AI 写入规约]
1. 本文件由 Plan Agent 创建。TDD Agent 仅填写第四节【实现结果】，其余章节只读。Review Agent 仅填写第五节【Reviewer意见】。
-->

# TDS: 手动调整余额 API (Task ID: T-10013)

## 一、背景与目标
- **关联需求**：E-07 "封闭内循环"充值通道 ([phase1_gift_economy.md §1.2](../../product/phase1_gift_economy.md))
- **本 Task 的目标**：Admin 手动为任意用户加/减钻石，事务性写余额 + 流水 + admin_logs，并通过 Redis PubSub 通知 App Server 推送 `BalanceUpdated`。

## 二、方案设计

### 核心数据流
```
POST /api/v1/admin/users/:id/wallet/adjust { amount, reason }
   ↓ admin_auth middleware（要求 role ∈ super_admin|operator|finance，拒绝 cs）
   ↓
handler
   BEGIN TX
     1. SELECT users WHERE id=? FOR UPDATE
        - not found / deleted → 404
     2. new_balance = current + amount
        - new_balance < 0 → 400 (40204 INSUFFICIENT_BALANCE)
     3. UPDATE users SET diamond_balance = new_balance
     4. INSERT wallet_transactions (type='admin_adjust', amount, balance_after, operator_id, reason)
     5. INSERT admin_logs (action='wallet_adjust', target_id, detail jsonb)
   COMMIT
   ↓
Redis PUBLISH admin:events {type:"balance_updated", user_id, new_balance, delta, reason}
   ↓ App Server 订阅后推送 WS BalanceUpdated（T-00018）
   ↓
200 { new_balance }
```

### 涉及文件清单
| 文件 | 类型 | 说明 |
|------|------|------|
| `app/adminServer/src/modules/wallet/mod.rs` | 新增 | 模块入口 |
| `app/adminServer/src/modules/wallet/handler.rs` | 新增 | `adjust_balance` |
| `app/adminServer/src/modules/wallet/service.rs` | 新增 | 事务 + publish |
| `app/adminServer/src/bootstrap/router.rs` | 修改 | 挂载路由 + RBAC |
| `app/adminServer/src/modules/audit/service.rs` | 修改 | 新增 `action=wallet_adjust` 枚举 |
| `app/adminServer/tests/wallet_adjust_test.rs` | 新增 | WA01~WA08 |

### HTTP 接口
**POST /api/v1/admin/users/:id/wallet/adjust** （需 Admin JWT + `WalletAdjust` 权限）

Request:
```json
{ "amount": 1000, "reason": "运营补偿：客诉处理 #1234" }
```
- `amount`：非零整数，正/负皆可；绝对值 ≤10,000,000（防误操作）
- `reason`：必填，2-200 字符

Response 200:
```json
{ "code":0, "data":{ "user_id":"uuid", "new_balance":1234, "delta":1000 }, "request_id":"..." }
```

错误码：
| HTTP | code | 说明 |
|------|------|------|
| 400 | 40003 | amount=0 / reason 空 / amount 绝对值超限 |
| 401 | 40101/40102 | 未认证/过期 |
| 403 | 40301 | role=cs（无权限） |
| 404 | 40400 | 用户不存在或已删除 |
| 400 | 40204 | 调整后余额 <0 |

### RBAC
在 `modules/auth/rbac.rs` 中 `WalletAdjust` permission 授权 `super_admin, operator, finance`，`cs` 拒绝。

### 跨服务事件（Redis PubSub）
复用 T-10011 的 `admin:events` 频道，格式：
```json
{ "type":"balance_updated", "user_id":"uuid", "new_balance":1234, "delta":1000, "reason":"...", "admin_id":"uuid", "ts":1720000000 }
```

### admin_logs
```
action = "wallet_adjust"
target_type = "user"
target_id = user_id
detail = { amount, reason, new_balance, delta }
```
由 `audit::middleware` 或服务内显式写入（参考 T-10012 模式）。

## 三、TDD 验收用例
- [ ] **WA01** super_admin 调用成功：余额更新 + 流水 +1 + admin_log +1 + Redis publish 命中
- [ ] **WA02** cs 角色 → 403
- [ ] **WA03** amount=0 → 40003
- [ ] **WA04** reason 空 → 40003
- [ ] **WA05** 用户不存在 → 404
- [ ] **WA06** amount=-1000 当前余额 500 → 40204，事务回滚，流水无新记录
- [ ] **WA07** amount=20,000,000 → 40003（超限）
- [ ] **WA08** 事务原子性：手动在 INSERT admin_logs 阶段抛错，用户余额保持不变

## 四、实现结果
- **实际修改的文件**：
  - `app/shared/src/error/code.rs`（修改）— 新增 `InsufficientBalance = 40204` 错误码
  - `app/adminServer/src/common/auth/context.rs`（修改）— `Permission` 枚举新增 `WalletAdjust`；`has_permission` 在 `operator` 和 `finance` 分支补充 `WalletAdjust`
  - `app/adminServer/src/common/error.rs`（修改）— `AppError` 新增 `InsufficientBalance` 变体；错误码映射 → `ErrorCode::InsufficientBalance`；HTTP 状态映射 → 400
  - `app/adminServer/src/modules/wallet/mod.rs`（新增）— 模块入口，导出 dto / handler / repository / service 四个子模块
  - `app/adminServer/src/modules/wallet/dto.rs`（新增）— `AdjustBalanceRequest`（amount, reason）、`AdjustBalanceResponse`（user_id, new_balance, delta）
  - `app/adminServer/src/modules/wallet/repository.rs`（新增）— `WalletRepository` trait（`find_user_balance` + `adjust_balance_atomic`）、`PgWalletRepository`（真实 DB 事务：SELECT FOR UPDATE → 余额校验 → UPDATE users → INSERT wallet_transactions → INSERT admin_logs → COMMIT）、`FakeWalletRepository`（内存实现，支持 `seed_user` / `get_balance` / `get_transactions` / `get_admin_logs_written` / `set_inject_admin_log_error`）；7 个单元测试（WR-01~WR-07）
  - `app/adminServer/src/modules/wallet/service.rs`（新增）— `WalletService`（调仓库原子操作 + Redis PUBLISH fire-and-forget）；4 个单元测试（WS-01~WS-04）
  - `app/adminServer/src/modules/wallet/handler.rs`（新增）— `adjust_balance_handler`（RBAC 校验 WalletAdjust → 参数校验 → 调 service）、`validate_request`（amount 非零/绝对值 ≤10,000,000 / reason 2~200字符）；7 个单元测试（VR-01~VR-07）
  - `app/adminServer/src/modules/mod.rs`（修改）— 新增 `pub mod wallet;`
  - `app/adminServer/src/bootstrap/mod.rs`（修改）— `AppState` 新增 `wallet_service` 字段；`AppState::new` 新增 `wallet_repo` 参数（+`#[allow(clippy::too_many_arguments)]`）；`for_test()` 注入 `FakeWalletRepository::default()`；`build_app` 挂载 `POST /api/v1/admin/users/{id}/wallet/adjust`；新增集成测试 WA01~WA08 + WA-finance 共 9 条；更新全部 12 处 `AppState::new` 调用（追加 wallet_repo 参数）
  - `app/adminServer/src/main.rs`（修改）— 引入 `PgWalletRepository`；`AppState::new` 追加 `Arc::new(PgWalletRepository::new(pool))`
- **测试覆盖率**：
  - 新增测试：WA01~WA08（集成）+ WA-finance（集成）+ WR-01~WR-07（repository 单元）+ WS-01~WS-04（service 单元）+ VR-01~VR-07（handler 单元）= **27 个新增测试**
  - 全量测试：**286 passed, 0 failed**（含所有前置 Task 的存量测试）
  - TDD 验收标准覆盖：WA01✅ WA02✅ WA03✅ WA04✅ WA05✅ WA06✅ WA07✅ WA08✅
  - Clippy：零警告（`-D warnings`）
- **完成时间**：2025-07-16

## 五、Reviewer意见
### Round 1
- **状态**：
- **审查意见**：
- **改进建议**：
