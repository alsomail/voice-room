<!--
[AI 写入规约]
1. 本文件由 Plan Agent 创建。TDD Agent 仅填写第四节【实现结果】，其余章节只读。Review Agent 仅填写第五节【Reviewer意见】。
2. 创建后必须在 doc/tasks/index.md 对应 Task 的行内补充链接：[TDS](./tds/infra/T-0000G.md)（已存在）
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: Seed/Reset/Preflight 三件套脚本 (Task ID: T-0000G)

> **主设计**：[T-0000E E2E 多环境分层与切换器总设计](./T-0000E.md) — 本 TDS 严格继承其 §2.5（Seed 数据契约）+ §2.6（健康预检契约）+ §2.10（涉及文件清单）+ §2.11 Step 2 验收锚点 + §2.12 R2/R7/R9 风险来源。
> **上游依赖**：[T-0000F 三档 .env 模板](./T-0000F.md) — 本 TDS 复用其 §2.3 字段表（`E2E_VALID_TOKEN` / `E2E_*_ID` 等 24 个字段）作为 Seed 输出口与 Preflight 入参口。
> **下游消费方**：[T-0000H envLoader/globalSetup/globalTeardown](./T-0000H.md) — 本 TDS §2.7 定义 globalSetup 调用本三脚本的接口契约（命令行入参、退出码、stdout 协议）。

---

## 一、背景与目标

- **关联需求**：模块 9（E2E 测试基建）P0 起点之一，落地 T-0000E §2.11 Migration Path **Step 2：Seed/Reset/Preflight 三件套就绪**。
- **本 Task 的目标**：交付 3 个可执行脚本，使 `tests/scripts/**` 在 `E2E_PROFILE=local` 下**冷启动 5 分钟内**可跑通：
  1. `scripts/dev/seed-e2e.sql` — 幂等创建 E2E 测试用户（A/B + 4 角色 admin）/ 测试房间 / JWT Token，并把所有 ID + Token **回填**到 `scripts/dev/.seed-output.env`，由下游 globalSetup 转出到 `process.env`。
  2. `scripts/dev/reset-e2e.sh` — 幂等清空 E2E 测试数据，**不**修改业务表结构、**不**触碰非 E2E 行。
  3. `scripts/dev/preflight.sh` — 5 端（Postgres / Redis / AppServer / AdminServer / Web）健康检查，任一挂掉 **2 秒内**彩色定位并附修复 hint，按检查项分配独立退出码。
- **修复体检缺口**：T-0000E §1 体检表 **C2**（`scripts/dev/seed-e2e.sql` 不存在）+ **C12**（无统一 5 端预检脚本）。
- **不在本 Task 范围内**：
  1. envLoader / globalSetup 实现 → T-0000H；
  2. `package.json` 的 `db:seed` `db:reset` `preflight` 一键命令 → T-0000I；
  3. AppServer / AdminServer 自身 `/health` 端点 — 假设已存在，本 Task 仅消费；
  4. Seed 生成的 JWT 需要的签名 CLI（`shared/bin/sign-jwt.rs`）— 本 Task 提供调用契约，实现可由 TDD Agent 在落地阶段一并完成（非主契约）。

---

## 二、方案设计

### 2.1 核心数据流

```
                       ┌──────────────────────────────────────┐
[ tests/scripts/env/ ] │ globalSetup (T-0000H)                │
   .env.<profile> ─────┤   1) loadE2EEnv()                    │
                       │   2) preflight.sh --profile $P  ◄────┼── 本 Task ③
                       │   3) [local] seed-e2e.sql       ◄────┼── 本 Task ① + sign-jwt CLI
                       │   4) source .seed-output.env    ◄────┼── 本 Task ① 回填
                       │   5) Playwright 起飞                  │
                       └──────────────────────────────────────┘
                       ┌──────────────────────────────────────┐
                       │ globalTeardown (T-0000H)             │
                       │   [local only] reset-e2e.sh     ◄────┼── 本 Task ②
                       └──────────────────────────────────────┘
```

> **不变量**：脚本三件套是 **profile-aware** 但 **profile-agnostic 实现**——逻辑层只读 env 变量（`PGHOST` / `REDIS_URL` / `APP_SERVER_BASE_URL` 等），不内置 profile 分支。Profile 路由完全由上游 envLoader 完成；本脚本对 staging/prod 的安全约束以「拒绝执行」形式存在（见 §2.4 / §2.6）。

### 2.2 涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `scripts/dev/seed-e2e.sql` | **新增** | 幂等 SQL，靠 `psql -v` 接收 `E2E_PROFILE` / `JWT_SIGNED_VALID_TOKEN` 等变量；末尾 `\echo` 输出回填字段对，由 wrapper 脚本捕获到 `.seed-output.env` |
| `scripts/dev/seed-e2e.sh` | **新增（包装器）** | wrapper：先 fail-fast 校验 profile=local，再调 `shared/bin/sign-jwt`（或 `cargo run -p shared --bin sign-jwt --quiet`）拿到 7 个 token，组装成 `psql -v` 入参，执行 `seed-e2e.sql`，最后把回填字段写入 `scripts/dev/.seed-output.env`（gitignored） |
| `scripts/dev/reset-e2e.sh` | **新增** | bash + psql + redis-cli；按 §2.5 表逐项 DELETE / DEL；profile≠local 直接退出码 21 |
| `scripts/dev/preflight.sh` | **新增** | bash；按 §2.6 矩阵串行 5 项检查；任一失败彩色提示并以专属退出码退出（11~15）；2 秒超时硬上限 |
| `scripts/dev/init-db.sh` | **修改** | 启动时增建 `e2e_runner` PG 角色（仅本地 DB，最小权限）— 用于 seed/reset 与业务进程账号隔离 |
| `scripts/dev/grant-permissions.sql` | **修改** | 末尾追加：把 E2E 数据相关表的 `INSERT/UPDATE/DELETE` 授给 `e2e_runner`，禁止访问 `admin_logs` / `payment_orders` 等敏感表 |
| `scripts/dev/.seed-output.env` | **运行时产物（gitignored）** | seed wrapper 产出；globalSetup 读取并 `process.env.X = …`；本 Task 仅在 `.gitignore` 加忽略行，**不**入库 |
| `.gitignore` | **修改** | 新增 `scripts/dev/.seed-output.env` |

### 2.3 入参与环境变量

| 变量 | 来源 | 用法 | Seed | Reset | Preflight |
|------|------|------|------|-------|-----------|
| `E2E_PROFILE` | env (envLoader 注入) | local/staging/prod 路由门禁 | local 强制 | local 强制 | 透传给检查项裁剪（远端跳过 PG/Redis） |
| `PGHOST` `PGPORT` `PGUSER` `PGPASSWORD` `PGDATABASE` | env | psql 连接 | ✅ | ✅ | ✅ (`pg_isready`) |
| `REDIS_URL` | env | redis-cli 连接 | — | ✅ | ✅ (`redis-cli ping`) |
| `APP_SERVER_BASE_URL` `ADMIN_SERVER_BASE_URL` `ADMIN_WEB_URL` | env | curl 目标 | — | — | ✅ |
| `JWT_SECRET` | env | sign-jwt 签发密钥 | ✅ (wrapper 透传) | — | — |
| `E2E_TOKEN_TTL_SECONDS` | env，默认 `86400` | A/B/admin token 寿命 | ✅ | — | — |
| `E2E_ID_NAMESPACE` | 常量 `9b3e0c6a-1ec1-4d3f-93d4-e2e000000000`（UUIDv5 namespace） | 确定性 ID 生成 | ✅ | ✅（按集合删） | — |

### 2.4 Seed 数据契约（实现 T-0000E §2.5）

#### 2.4.1 用户表（`users`）

| 字段（确定性 ID） | phone | role | nickname | 备注 |
|---|---|---|---|---|
| `E2E_USER_A_ID = uuidv5(E2E_NS, 'user_a')` | `+966500000900` | `user` | `E2E User A` | 主测试用户，绑定 `E2E_VALID_TOKEN` |
| `E2E_USER_B_ID = uuidv5(E2E_NS, 'user_b')` | `+966500000901` | `user` | `E2E User B` | 辅助用户（连麦/送礼对象） |

`ON CONFLICT (id) DO UPDATE SET phone=EXCLUDED.phone, nickname=EXCLUDED.nickname, updated_at=now();`

#### 2.4.2 管理员表（`admins`）

| 字段（确定性 ID） | username | role | 备注 |
|---|---|---|---|
| `uuidv5(E2E_NS, 'admin_super')` | `e2e_admin` | `admin` | `E2E_ADMIN_TOKEN` |
| `uuidv5(E2E_NS, 'admin_op')` | `e2e_op` | `op` | `E2E_OP_TOKEN` |
| `uuidv5(E2E_NS, 'admin_cs')` | `e2e_cs` | `cs` | `E2E_CS_TOKEN` |
| `uuidv5(E2E_NS, 'admin_fin')` | `e2e_fin` | `fin` | `E2E_FIN_TOKEN` |

#### 2.4.3 房间表（`rooms`）

| 字段 | 值 | 备注 |
|---|---|---|
| `id` | `E2E_ROOM_ID = uuidv5(E2E_NS, 'room_main')` | 唯一测试房间 |
| `owner_id` | `E2E_USER_A_ID` | 房主=A |
| `name` | `E2E Test Room` | |
| `capacity` | `8` | 麦位 8（与生产默认一致） |
| `is_locked` | `false` | 默认开放 |
| `password` | `null` | 无密码 |
| `created_at` | `'2026-01-01 00:00:00+00'` | **固定**，避免每次跑变化 |

`ON CONFLICT (id) DO UPDATE SET owner_id=EXCLUDED.owner_id, capacity=EXCLUDED.capacity;`

#### 2.4.4 JWT Token 生成策略

| Token | 主体 | 载荷字段 | 过期 | 来源 |
|-------|------|---------|------|------|
| `E2E_VALID_TOKEN` | E2E_USER_A_ID | `{ sub, role:'user', exp }` | `now + E2E_TOKEN_TTL_SECONDS`（默认 24h） | wrapper 调 `shared/bin/sign-jwt` |
| `E2E_ADMIN_TOKEN` / `E2E_OP_TOKEN` / `E2E_CS_TOKEN` / `E2E_FIN_TOKEN` | 各对应 admin id | `{ sub, role:<对应>, exp }` | 同上 | 同上 |
| `E2E_EXPIRED_TOKEN` / `E2E_EXPIRED_ADMIN_TOKEN` | A / super-admin | `exp = now - 90d`（固定） | 已过期 | **不**经 seed，由 `.env.<profile>.example` 直接写常量值（T-0000F §2.3） |

- **密钥**：`JWT_SECRET` 从 env 注入，wrapper **不** echo 该值，**不**写日志，**不**入 `.seed-output.env`。
- **签发器**：本 Task 约束 wrapper 必须调用工作区内的 sign-jwt CLI（推荐路径：`shared/bin/sign-jwt.rs`，由 TDD Agent 落地）。CLI 入参 `--sub <uuid> --role <user|admin|op|cs|fin> --ttl <sec>`，stdout 输出 token 单行。
- **回填文件**（`scripts/dev/.seed-output.env`）格式：
  ```
  # Generated by scripts/dev/seed-e2e.sh at 2026-05-31T01:23:45Z
  E2E_USER_A_ID=...
  E2E_USER_B_ID=...
  E2E_ROOM_ID=...
  E2E_VALID_TOKEN=...
  E2E_ADMIN_TOKEN=...
  E2E_OP_TOKEN=...
  E2E_CS_TOKEN=...
  E2E_FIN_TOKEN=...
  ```
  **不含** `E2E_EXPIRED_*`（来自 .env.example，由 envLoader 直读）。

#### 2.4.5 幂等约束（量化）

- 同一台 DB 执行 `seed-e2e.sh` 第 1 次和第 N 次后：
  - `users`/`admins`/`rooms` 三张表中 `id ∈ E2E ID 集合` 的行数严格相等（=2 / =4 / =1）；
  - 所有 token 字段在第 N 次输出**均能解码并通过签名校验**（exp 可能因 TTL 重置而推迟，这是预期行为）。
- 测试方法：连跑 2 次 `seed-e2e.sh` 后，对比 `psql -c "SELECT id,phone FROM users WHERE phone LIKE '+96650000090%' ORDER BY phone;"` 输出 hash 一致。

### 2.5 Reset 清理范围表

| 目标 | 操作 | 过滤条件 | 保留项 |
|------|------|----------|--------|
| `users` 表 | `DELETE FROM users WHERE phone LIKE '+96650000090%' OR id IN (E2E_USER_A_ID, E2E_USER_B_ID);` | phone 前缀 + 显式 ID 集合 | 表结构、其他生产用户 |
| `admins` 表 | `DELETE FROM admins WHERE username IN ('e2e_admin','e2e_op','e2e_cs','e2e_fin');` | username 集合 | 真实运营管理员 |
| `rooms` 表 | `DELETE FROM rooms WHERE id = E2E_ROOM_ID;` | 显式 ID | 其他房间 |
| 关联子表（`room_members` / `gift_orders` / `wallet_tx` 等） | `DELETE WHERE room_id=E2E_ROOM_ID OR user_id IN (E2E_USER_A_ID, E2E_USER_B_ID);` | 外键集合 | 其他业务行 |
| Redis 业务键 | `redis-cli --scan --pattern 'room:{E2E_ROOM_ID}:*' \| xargs -r redis-cli DEL`；同理 `user:{E2E_USER_*_ID}:*`、`mic_muted:*`、`chat_muted:*`、`kicked:{E2E_ROOM_ID}:*` | 限定 key 前缀含 E2E ID | 其他 key |
| `scripts/dev/.seed-output.env` | `rm -f` | — | — |
| **业务表 schema** | **不动** | — | 全部 DDL |
| `schema_migrations` | **不动** | — | 全部行 |
| 非 E2E 用户/房间/订单 | **绝不删除** | — | — |

**Profile 红线**：
- `E2E_PROFILE != local` → 立即输出 `[reset-e2e] refuse: profile=$P, only 'local' is allowed` 并退出码 **21**。
- `E2E_PROFILE` 未设置 → 同样退出码 **21**（默认拒绝）。
- 受 CI 调用时支持 `--yes` 跳过交互确认；交互模式下默认要求 `y` 确认。

### 2.6 Preflight 健康检查矩阵（实现 T-0000E §2.6）

| # | 检查项 | 命令（最简形式） | 期望结果 | 超时 | 失败退出码 | 失败彩色提示文案 |
|---|--------|-----------------|---------|------|-----------|------------------|
| 1 | Postgres | `pg_isready -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDATABASE" -t 2` | exit 0，stdout 含 `accepting connections` | **2s** | **11** | `[1/5] Postgres   ❌ FAIL  ($PGHOST:$PGPORT)\n        ↳ Hint: docker compose up -d postgres` |
| 2 | Redis | `redis-cli -u "$REDIS_URL" -t 2 ping` | stdout `PONG` | **2s** | **12** | `[2/5] Redis      ❌ FAIL  ($REDIS_URL)\n        ↳ Hint: docker compose up -d redis` |
| 3 | AppServer | `curl -fsS --max-time 2 "$APP_SERVER_BASE_URL/health"` | HTTP 200 | **2s** | **13** | `[3/5] AppServer  ❌ FAIL  $APP_SERVER_BASE_URL/health\n        ↳ Hint: cargo run -p voice-room-server -- --profile local` |
| 4 | AdminServer | `curl -fsS --max-time 2 "$ADMIN_SERVER_BASE_URL/health"` | HTTP 200 | **2s** | **14** | `[4/5] AdminServer ❌ FAIL  $ADMIN_SERVER_BASE_URL/health\n        ↳ Hint: cargo run -p voice-room-admin-server -- --profile local` |
| 5 | Web | `curl -fsS --max-time 2 -o /dev/null "$ADMIN_WEB_URL"` | HTTP 200 / 301 / 302 | **2s** | **15** | `[5/5] Web        ❌ FAIL  $ADMIN_WEB_URL\n        ↳ Hint: cd app/web && npm run dev` |

**全局约束**：
- 总挂壁时间上限 ≤ 10s（5 项 × 2s）。**任一项失败立即退出**（fail-fast），后续项标记为 `⏭ skipped`。
- 颜色：成功 `\033[32m✅\033[0m`，失败 `\033[31m❌\033[0m`，跳过 `\033[33m⏭\033[0m`，hint 用 `\033[36m`。无 TTY（CI）时自动关闭颜色（检查 `[ -t 1 ]`）。
- Profile 裁剪：
  - `E2E_PROFILE=local` → 全部 5 项；
  - `E2E_PROFILE=staging` / `prod` → **跳过 1/2**（远端不直连 PG/Redis），只跑 3/4/5 项，跳过项打印 `⏭ skipped (remote profile)`。
- 全部成功退出码 **0**；至少 1 项失败按**首个失败项**的退出码退出（11~15）。
- `--profile <p>` CLI 入参覆盖 env；缺省读 `E2E_PROFILE`。

**输出范例**（成功）：
```
[1/5] Postgres    ✅ ready (127.0.0.1:5432)
[2/5] Redis       ✅ ready (127.0.0.1:6379)
[3/5] AppServer   ✅ 200  http://localhost:3000/health  (32ms)
[4/5] AdminServer ✅ 200  http://localhost:3001/health  (29ms)
[5/5] Web         ✅ 200  http://localhost:5173         (44ms)
preflight: all 5 checks passed in 0.21s
```

### 2.7 与 T-0000H globalSetup 的接口契约

| 调用 | 命令行 | stdout/stderr 协议 | 退出码语义 | globalSetup 行为 |
|------|--------|-------------------|-----------|------------------|
| Preflight | `bash scripts/dev/preflight.sh --profile $E2E_PROFILE` | 见 §2.6 行格式（每行 `[i/5] …` 可机读） | `0` 通过；`11~15` 对应 5 端 | 非 0 立即抛 `PreflightFailedError`，错误信息 = stderr 末 20 行 |
| Seed | `bash scripts/dev/seed-e2e.sh`（要求 `E2E_PROFILE=local && E2E_ALLOW_WRITES=1`） | 进度行 `[seed] step=N …`；最后 1 行打印 `seed: wrote scripts/dev/.seed-output.env` | `0` 成功；`21` profile 拒绝；`22` JWT 签发失败；`23` SQL 执行失败 | 成功后 `dotenv.parse(scripts/dev/.seed-output.env)` 注入 `process.env` |
| Reset | `bash scripts/dev/reset-e2e.sh --yes` | 每张表 `[reset] users -2 admins -4 rooms -1 …` | `0` 成功；`21` profile 拒绝；`24` 无连接 | globalTeardown 仅 local 调用，失败仅打 warning，不影响测试结果 |

> **契约不可变**：上述 3 条命令名、入参标志、退出码集合作为公共 API 冻结，T-0000H 与 T-0000I 直接消费。本 Task 内部实现可重构（如增加 `--quiet`），但**不得**变更上述四列。

---

## 三、TDD 验收用例

### 3.1 Seed 幂等性
- [ ] **正向**：`bash scripts/dev/seed-e2e.sh` 连跑 2 次，第 2 次退出码 0；对比 `psql -c "SELECT id,phone FROM users WHERE phone LIKE '+96650000090%' ORDER BY phone"` 输出 hash 一致。
- [ ] **正向**：连跑 2 次后 `users` E2E 行数严格 = 2，`admins` E2E 行数 = 4，`rooms` E2E 行数 = 1。
- [ ] **正向**：`.seed-output.env` 含全部 8 个键（`E2E_USER_A_ID/B_ID/ROOM_ID/VALID_TOKEN/ADMIN_TOKEN/OP_TOKEN/CS_TOKEN/FIN_TOKEN`），无空值。
- [ ] **正向**：随机抽 1 个 token base64 解码 payload，`exp - iat` 等于 `E2E_TOKEN_TTL_SECONDS`（默认 86400）。
- [ ] **异常**：`E2E_PROFILE=staging` 调 seed wrapper，立即退出码 21，**不** touch 任何 DB。
- [ ] **异常**：未设 `JWT_SECRET` 调 wrapper，退出码 22，stderr 含 `JWT_SECRET is required`。

### 3.2 Reset 安全
- [ ] **正向**：seed → 在 `users` 表手动 INSERT 一条 phone=`+966599999999` 的非 E2E 行 → reset → 该行仍在，且 E2E 4 行被清。
- [ ] **正向**：reset 后 `psql -c "\d users"` schema 与 reset 前 byte-for-byte 一致；`schema_migrations` 行数不变。
- [ ] **正向**：reset 后 `redis-cli --scan --pattern 'room:{E2E_ROOM_ID}:*' | wc -l` = 0；其他业务键 (`room:{other}:*`) 不动。
- [ ] **异常**：`E2E_PROFILE=prod bash scripts/dev/reset-e2e.sh --yes` 退出码 21，**不**触发任何 DELETE（用 `pg_stat_user_tables` 的 n_tup_del 前后差为 0 验证）。

### 3.3 Preflight 5 端 × 故意打挂
- [ ] **正向**：5 端全活，preflight 退出码 0，全程 < 2s。
- [ ] **异常**：`docker stop vr-postgres` → preflight 在 ≤ 2s 内退出，退出码 11，stdout 含 `[1/5] Postgres   ❌`，hint 含 `docker compose up -d postgres`。
- [ ] **异常**：`docker stop vr-redis` → 退出码 12，stdout 含 `[2/5] Redis`，**且**第 1 项仍打绿 ✅。
- [ ] **异常**：AppServer 未起 → 退出码 13 + hint 含 `cargo run -p voice-room-server`。
- [ ] **异常**：AdminServer 未起 → 退出码 14。
- [ ] **异常**：Web 未起 → 退出码 15。
- [ ] **异常**：5 端全挂 → 仍只在第 1 个失败项停下，退出码 11（fail-fast 行为验证）。
- [ ] **异常**：`E2E_PROFILE=staging` 时跳过 PG/Redis（项 1/2 显示 `⏭ skipped`），仅跑 3/4/5。
- [ ] **正向**：`CI=1` 或非 TTY 环境下颜色字节不出现（`preflight | cat | grep -c $'\033'` = 0）。

### 3.4 与 globalSetup 契约
- [ ] **正向**：模拟 globalSetup 调 `seed-e2e.sh` 后 `dotenv.parse('.seed-output.env')` 解析得到的对象 keys 集合 = §2.4.4 表 8 项。
- [ ] **正向**：`preflight.sh` stdout 每行匹配正则 `^\[\d/5\] [A-Za-z]+\s+(✅|❌|⏭)`（机读稳定）。

---

## 四、实现结果（由 TDD Agent 完成后填写）

### 4.1 改动文件清单

| 文件 | 类型 | LOC | 说明 |
|------|------|-----|------|
| `app/shared/Cargo.toml` | 修改 | +4 | 新增 `[[bin]] sign-jwt` 入口 |
| `Cargo.toml` (workspace) | 修改 | ±1 | `uuid` 增加 `v5` feature |
| `app/shared/src/bin/sign_jwt.rs` | 新增 | 156 | sign-jwt CLI（Token 签发 + UUIDv5 计算） |
| `scripts/dev/seed-e2e.sql` | 新增 | 70 | 幂等 SQL（users/admins/rooms ON CONFLICT DO UPDATE） |
| `scripts/dev/seed-e2e.sh` | 新增 | 130 | wrapper：双 env 校验 → uuid5 → sign-jwt → psql → .seed-output.env |
| `scripts/dev/reset-e2e.sh` | 新增 | 134 | profile-aware 删除；DO $$ + to_regclass 兜底缺表；Redis 前缀 SCAN+DEL |
| `scripts/dev/preflight.sh` | 新增 | 175 | 5 端串行 + fail-fast + 退出码 11~15 + TTY 颜色 |
| `.gitignore` | 修改 | +2 | 新增 `scripts/dev/.seed-output.env`、`.seed.lock` |

合计 ≈ 670 LOC（不含 SQL 注释）。

### 4.2 sign-jwt CLI 落地方案

- **路径选择**：`app/shared/src/bin/sign_jwt.rs`，复用 `voice-room-shared` 已有的 `jwt::token::{AppClaims, AdminClaims, encode_token}`，**不重写**算法。`Cargo.toml` 增加 `[[bin]] name = "sign-jwt" path = "src/bin/sign_jwt.rs"`，bin 名（带连字符）与 wrapper 调用契约一致。
- **依赖增量**：仅在 workspace `Cargo.toml` 给 `uuid` 增 `v5` feature（必须，AppClaims/AdminClaims 算法和 chrono/jsonwebtoken 都已就绪）。`cargo check --workspace` 0 警告 0 错。
- **接口契约**（与 TDS §2.4.4 一致）：
  - `sign-jwt --sub <uuid> --role <user|admin|op|cs|fin> --ttl <sec>` → 从 env `JWT_SECRET` 读密钥（永不 echo），stdout 输出 token 单行。role 映射：`admin→super_admin / op→operator / cs→cs / fin→finance`；`user` 用 `AppClaims iss=voiceroom`，其余用 `AdminClaims iss=voiceroom-admin`。
  - `sign-jwt --uuid5 <name>` → 在 E2E 命名空间 `9b3e0c6a-1ec1-4d3f-93d4-e2e000000000` 下计算 UUIDv5。
  - 退出码：0/2/3/4，安全且 fail-fast。

### 4.3 测试策略执行证据（TDS §三 22 条）

> 本机 macOS 无 `psql` / `redis-cli` / `pg_isready` / `shellcheck`，未安装新依赖；DB-写路径降级为静态 + profile-guard 验证，runtime 写入路径**待 T-0000H 联动验收**。

| # | 验收项 | 命令 | 结果 |
|---|--------|------|------|
| 3.1-1 | seed 幂等（连跑 2 次 hash 一致） | 静态：SQL 全程 `ON CONFLICT (id) DO UPDATE`；UUIDv5 确定性 | ✅ 静态保证；runtime 待联动 |
| 3.1-2 | seed 后 users=2/admins=4/rooms=1 | seed-e2e.sql 末尾 `\echo --seed-counts--` + 三 UNION ALL | ✅ 已落实；runtime 待联动 |
| 3.1-3 | .seed-output.env 含全 8 键 | `cat scripts/dev/seed-e2e.sh \| grep -c '^E2E_'` heredoc 计数 = 8 | ✅ 8/8 |
| 3.1-4 | token exp-iat == TTL | `JWT_SECRET=test-secret target/debug/sign-jwt --role user --ttl 86400` → payload `exp-iat = 86400` | ✅ 实测 |
| 3.1-5 | profile=staging 拒绝 seed → rc=21 | `E2E_PROFILE=staging bash scripts/dev/seed-e2e.sh` → `rc=21`，stderr `refuse: E2E_PROFILE=staging` | ✅ |
| 3.1-6 | 缺 JWT_SECRET → rc=22 | `E2E_PROFILE=local E2E_ALLOW_WRITES=1 bash …seed-e2e.sh` → `rc=22`，stderr `JWT_SECRET is required` | ✅ |
| 3.2-1 | reset 不删非 E2E 行 | `WHERE phone LIKE '+96650000090%' OR id IN (...)` 双重过滤；`DO $$ to_regclass` 兜底缺表 | ✅ 静态；runtime 待联动 |
| 3.2-2 | reset 后 schema 不变 | 仅 DELETE，无 DDL；`schema_migrations` 不在范围 | ✅ 代码审计 |
| 3.2-3 | Redis 仅前缀清 | `redis-cli --scan --pattern 'room:{ROOM_ID}:*'` + `xargs DEL` | ✅ 静态；runtime 待联动 |
| 3.2-4 | profile=prod reset → rc=21 不 DELETE | `E2E_PROFILE=prod bash …reset-e2e.sh --yes` → `rc=21`；该退出在 `psql` 调用之前 | ✅ 实测（PATH=空 移除 psql 仍 rc=21，证明未触达 psql） |
| 3.3-1 | 5 端全活 → rc=0 | macOS 无 psql/redis-cli，PG/Redis fallback 走 `nc` → `[1/5] ✅ port reachable` `[2/5] ✅ port reachable`，AppServer 未起 → rc=13（部分通过：1/2 项实测全绿） | ⚠️ AppServer/Admin/Web 待 T-0000H 联动 |
| 3.3-2 | docker stop postgres → rc=11 在 ≤2s | 用 PGPORT=65530 等价模拟（端口不通） → 输出 `[1/5] Postgres ❌ (127.0.0.1:65530)` + `Hint: docker compose up -d postgres`，rc=11，elapsed 0s | ✅ |
| 3.3-3 | redis 挂 + PG 仍绿 | PG 走真实 5432 ✅ + REDIS_URL=:65531 → rc=12 (fallback 路径 nc 不通) | ✅ 等价模拟 |
| 3.3-4 | AppServer 未起 → rc=13 + hint | 实测 staging profile（PG/Redis 跳过）+ AppServer port 65530 → `[3/5] AppServer ❌ … (got 000)` `Hint: cargo run -p voice-room-server -- --profile local` rc=13 | ✅ |
| 3.3-5 | AdminServer 未起 → rc=14 | 静态：与 AppServer 同模式，`fail_code=14` | ✅ 代码审计 |
| 3.3-6 | Web 未起 → rc=15 | 静态：`fail_code=15`；接受 200/301/302 | ✅ 代码审计 |
| 3.3-7 | 5 端全挂 → 仍 rc=11 fail-fast | 实测：所有 fallback 端口不通 → rc=11，后续 4 项 `⏭ skipped (fail-fast)` | ✅ |
| 3.3-8 | profile=staging 跳过 PG/Redis | 实测：`[1/5] ⏭ skipped (remote profile)`、`[2/5] ⏭ skipped (remote profile)` | ✅ |
| 3.3-9 | CI=1 / 非 TTY 颜色字节为 0 | `CI=1 … bash preflight.sh \| grep -c $'\033'` → `0` | ✅ |
| 3.4-1 | globalSetup 解析 .seed-output.env keys = 8 | wrapper heredoc 写 8 行 `E2E_*=…`，与 §2.4.4 一致 | ✅ |
| 3.4-2 | preflight 行格式机读 | 5 行状态行均匹配 `^\[\d/5\] [A-Za-z]+\s+(✅\|❌\|⏭)` | ✅ 实测 4/4 状态行命中（hint 行不在断言集合内） |
| - | shellcheck 三个脚本 | 本机未安装 `shellcheck` | ⚠️ 待 CI 接入；本地 `bash -n` 三脚本均通过语法检查 |
| - | cargo build sign-jwt 通过 | `cargo build -p voice-room-shared --bin sign-jwt` → Finished | ✅ |
| - | cargo check workspace 0 回归 | `cargo check --workspace` → Finished, 0 警告 | ✅ |
| - | .gitignore 命中 .seed-output.env | `git check-ignore -v scripts/dev/.seed-output.env` → `.gitignore:38:scripts/dev/.seed-output.env` | ✅ |

### 4.4 偏离 TDS 的项及理由

| # | TDS 描述 | 实际实现 | 理由 |
|---|----------|----------|------|
| D1 | TDS §2.4.1 users 表写 `role` 列 | 仅写 `id/phone/nickname/is_banned/coin_balance/vip_level` | 真实 schema（`migrations/001_create_users.sql`）**无** `role` 列；role 由 JWT claim 表达。红线：严禁臆造字段名。 |
| D2 | TDS §2.4.3 rooms 字段 `name/capacity/is_locked/password` | 实际写 `title/max_members/room_type/password_hash` | 真实 schema（`migrations/002_create_rooms.sql`）字段名；`title='E2E Test Room' max_members=8 room_type='normal' password_hash=NULL` 与契约语义等价。 |
| D3 | TDS §2.4.2 admins.role 取 `admin/op/cs/fin` | 实际写 `super_admin/operator/cs/finance` | 真实 schema CHECK 约束（`migrations/001_create_admins.sql`）；CLI 入参仍接受 TDS 简写并在 `sign_jwt.rs` 内做映射。 |
| D4 | TDS §2.2 列出修改 `init-db.sh` 与 `grant-permissions.sql` 创建 `e2e_runner` 角色 | **未实施** | 任务简报 A~G 文件清单未列出；为兜底 G-R1，当前仍依赖 profile-guard。后续如需 DB 角色隔离，留作扩展任务。已在【残余风险】登记。 |
| D5 | TDS §2.4.4 expired token 由 .env.example 直接写常量 | 维持现状（不在本 Task 范围生成） | 与 T-0000F 既有契约一致。 |

### 4.5 未联动的 runtime 锚点（待 T-0000H 验收）

- seed-e2e.sh 实际 psql 写入 + 二次幂等 hash 一致比对
- reset-e2e.sh 实际删除行数计数 + `pg_stat_user_tables.n_tup_del` 不变断言（profile=prod 情况）
- preflight 对真实 AppServer/AdminServer/Web `/health` 端点的 200 响应

### 4.6 完成时间

2026-05-31（v2.11）

---

## 五、Reviewer意见

> 状态枚举：🟢 通过 | 🔴 未通过
> 每轮 Review 追加一条记录，不要覆盖历史。

### Round 1
- **状态**：🟢 通过
- **审查意见**：
  - 安全：`sign_jwt.rs` 仅从 env 读 `JWT_SECRET`、错误信息走 stderr 且不含密钥；`grep -i secret` / `grep -E 'echo.*\$.*JWT|SECRET' scripts/dev/` 均无泄露。wrapper `set +x` + 文件权限 `umask 077` + `.seed-output.env` 不写任何 secret。✅
  - Seed 幂等：`seed-e2e.sql` 三个 INSERT 全部带 `ON CONFLICT (id) DO UPDATE SET ...`，配合 `--uuid5` 确定性 ID，可复跑结果一致；`\echo --seed-counts--` 行数断言机读契约就位。✅
  - Schema 真值核对：核对 `app/server/migrations/001_create_users.sql`、`002_create_rooms.sql`、`app/adminServer/migrations/001_create_admins.sql`，SQL 字段名/枚举值与真实 schema 完全一致（users 无 role 列；rooms = title/room_type/max_members/password_hash；admins.role ∈ {super_admin,operator,cs,finance}）。✅
  - Reset 安全：`E2E_PROFILE != local` 校验在 `psql` 之前生效（实测 `prod` rc=21 即返回，未触达 psql）；DELETE 仅作用于 E2E 三表 + `to_regclass` 兜底子表 + Redis 前缀 `room:{ROOM_ID}:*` / `user:{*}:*` / `kicked|mic_muted|chat_muted:{ROOM_ID}:*`，无 DDL，不动 `schema_migrations`。✅
  - Preflight 体验：`--max-time 2` curl + `pg_isready -t 2` + `redis-cli -t 2`，单项上限 2s；fail-fast 后续 `⏭ skipped`；`CI=1` 与非 TTY 关色实测 `\033` 字节为 0；退出码 11~15 矩阵对齐 TDS §2.6。✅
  - 静态：`bash -n` 三脚本通过；`cargo check --workspace` Finished 0 警告 0 错；`cargo build -p voice-room-shared --bin sign-jwt` OK；token `exp-iat=86400` 实测；UUIDv5 两次调用完全一致。✅
  - `.gitignore` 收口 `scripts/dev/.seed-output.env` 与 `.seed.lock`，`git check-ignore` 命中。✅
- **偏离决议**：
  - D1（users 无 role 列）/ D2（rooms 字段名）/ D3（admins.role 枚举真值）：**接受**。红线"严禁臆造字段"优先于 TDS 描述；TDS §2.4 字段表已被实现侧 §4.4 显式偏离登记，下一轮 TDS 修订时回填 DB 真值即可，不阻塞本任务。
  - D4（init-db.sh / grant-permissions.sql 推迟）：**接受为已知风险**。当前以 profile-guard（`E2E_PROFILE=local` + `E2E_ALLOW_WRITES=1` 双 env）作为首道防线，G-R1 风险可控；e2e_runner DB 角色隔离作为后续扩展任务，未阻塞 T-0000H globalSetup 接口契约。
  - D5（expired token 维持 env 常量）：**接受**，与 T-0000F 既有契约一致。
- **runtime 待联动**：seed 真实 psql 写入 + 二次幂等 hash、reset 真实删除行计数、preflight 对真实 `/health` 200。三项均依赖 docker 服务，将由 T-0000H runtime 联动验收，**不阻塞本任务**。
- **改进建议**：（无必修；下一任务可顺手做）
  1. T-0000H 起 CI 引入 `shellcheck` 三脚本；
  2. 后续 TDS 修订把 §2.4 字段表回填为 DB 真值，避免下游误读；
  3. `reset-e2e.sh` 的 `count_psql` 在无 DB 连接时输出 `?`，可加一行日志显式标注 "skipped/unknown"。

---

## 六、风险矩阵（继承 T-0000E §2.12 R2/R7/R9 + 本 Task 新增）

| ID | 风险 | 概率 | 影响 | 等级 | 主缓解 | 兜底 | 锚点 |
|----|------|------|------|------|--------|------|------|
| G-R1 | reset 在 staging/prod 误执行删除生产数据 | 低 | **致命** | 🔴 P0 | profile≠local 立即退码 21；wrapper 内置二道 env 校验（`E2E_PROFILE` + `E2E_ALLOW_WRITES`） | `--yes` 必须显式提供；DB 用 `e2e_runner` 角色，对生产敏感表无 DELETE 权限（grant-permissions.sql 兜底） | T-0000E §2.12 R1 |
| G-R2 | Seed 不幂等致 phone unique 冲突或 token 重复 | 中 | 高 | 🟠 P1 | 全程 `INSERT ... ON CONFLICT (id) DO UPDATE`；UUIDv5 确定性命名空间 | reset-e2e 作为兜底命令在 seed 前可选调用 | T-0000E §2.12 R2 |
| G-R3 | JWT 密钥泄露（写入日志/文件/git） | 低 | **致命** | 🔴 P0 | wrapper `set +x`，**不** echo `JWT_SECRET`；`.seed-output.env` 仅含已签 token 不含密钥；`.gitignore` 双保险 | code review checklist：grep `JWT_SECRET` 在 seed 产物中应 0 命中 | 新增 |
| G-R4 | 并发跑 seed（CI 多 job 同 DB）导致死锁或部分回填 | 中 | 中 | 🟡 P2 | wrapper 用 `flock scripts/dev/.seed.lock` 串行化；DB 操作单事务 + `ON CONFLICT` | 加 retry-3 + 指数退避 | 新增 |
| G-R5 | preflight 在 CI 跑不到 PG/Redis（远端无直连） | 高 | 低 | 🟢 P3 | profile=staging/prod 时跳过项 1/2（§2.6 矩阵） | T-0000L Runbook 文档明示 | T-0000E §2.12 R7 |
| G-R6 | Token 24h 过期致夜间 CI 红 | 中 | 低 | 🟢 P3 | CI 每日 06:00 重 seed；globalSetup 检测 token 剩余寿命 < 1h 触发 reseed | 文档「token 过期一键 reseed」 | T-0000E §2.12 R9 |
| G-R7 | sign-jwt CLI 未实现（依赖未到位） | 中 | 高 | 🟡 P2 | 本 TDS §2.4.4 已声明 CLI 接口契约，TDD Agent 落地时若 `shared/bin/sign-jwt` 不存在需先补；wrapper 退出码 22 明确报错 | fallback：用 Node 端临时签发（短期权宜，需 review 移除） | 新增 |

---

## 七、与 T-0000E 锚点映射

| T-0000E 锚点 | 本 TDS 对应章节 | 一致性 |
|--------------|------------------|--------|
| §2.5 Seed 数据契约（字段表 + 幂等约束 + ON CONFLICT） | §2.4 全 4 子表 + §3.1 幂等用例 | ✅ 字段集合 1:1 |
| §2.6 健康预检契约（5 项 × 退出码 11~15 × 2s 上限） | §2.6 矩阵 + §3.3 用例 | ✅ 退出码不变 |
| §2.10 文件清单（seed-e2e.sql / reset-e2e.sh / preflight.sh） | §2.2 文件清单（含 wrapper / init-db.sh / grant-permissions.sql 扩展，已在 §1 不在范围内 / §2.2 注释中说明扩展原因） | ✅ 主清单全覆盖；扩展项符合 T-0000E §4.5 「实现扩展（非契约变更）」豁免 |
| §2.11 Step 2 验收锚点（seed 二次执行无副作用 + preflight 5 端通过） | §3.1 / §3.3 | ✅ |
| §2.12 R2 / R7 / R9 | §六 G-R2 / G-R5 / G-R6 | ✅ 一对一映射 |

> **超集声明**：本 TDS 在 §六 新增 G-R1/G-R3/G-R4/G-R7 共 4 项本 Task 局部风险（误删/密钥/并发/CLI），不与主 TDS 冲突，是子 TDS 维度的细化。
