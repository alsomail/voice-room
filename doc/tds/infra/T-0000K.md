<!--
[AI 写入规约]
1. 本文件由 Plan Agent 创建。TDD Agent 仅填写第四节【实现结果】，其余章节只读。Review Agent 仅填写第五节【Reviewer意见】。
2. 创建后必须在 doc/tasks/index.md 对应 Task 的行内补充链接：[TDS](./tds/infra/T-0000K.md)（已存在）
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: Midscene LLM 配置接入文档 + CI Secret 流程 (Task ID: T-0000K)

> **主设计**：[T-0000E](./T-0000E.md)（§2.4.1 envLoader Midscene 字段 / §2.9.2 凭据隔离 / §4.3 K 行契约）
> **上游依赖**：T-0000F（三档 env 模板已落地 `MIDSCENE_*` 4 字段）/ T-0000H（envLoader.midscene + fixtures auto-skip 钩子）
> **下游联动**：T-0000L（RUNBOOK 引用本文 §三 验证命令）

## 一、背景与目标

### 关联需求
- 模块 9：E2E 测试基建（QA Foundation）—— 体检缺口 **C11：Midscene LLM 配置无文档**
- 主 TDS T-0000E §4.3 K 行契约：「Key 缺失时 WEB 用例 skip（非 fail）；CI 仅从 Secrets 取」

### 现状
1. WEB 用例（`tests/scripts/WEB/TC-{AUTH,GIFT,LOG,ROOM,USER,WALLET}.spec.ts` 共 6 个文件）已通过 `import { PlaywrightAgent } from '@midscene/web/playwright'` 接入 Midscene；
2. T-0000F 已在三档 `.env.{local,staging,prod}.example` 留出 `MIDSCENE_MODEL_API_KEY / MIDSCENE_MODEL_NAME=gpt-4o / MIDSCENE_OPENAI_BASE_URL / MIDSCENE_CACHE=1` 4 字段；
3. T-0000H envLoader 已读取 `env.midscene.{apiKey, modelName, baseUrl, cache}` 并经 `writeProcessEnv` 注入 worker；
4. **但**：Key 注入方式（本地 / CI）未文档化；三种部署形态（OpenAI 直连 / Azure / 中转）字段映射未冻结；缺 Key 时的 skip 钩子在 fixtures 层缺失，当前直接 fail。

### 本 Task 的目标
1. 输出 `doc/tests/MIDSCENE_SETUP.md`，冻结**三种部署形态**字段映射 + **CI Secret 注入**示例 + **缺 Key skip** 策略；
2. 在 `tests/scripts/support/fixtures.ts` 增加 `midsceneReady` auto fixture：仅对 WEB 用例（spec 路径 `/tests/scripts/WEB/`）在 `env.midscene.apiKey === ''` 时执行 `testInfo.skip(true, '[MIDSCENE] api key missing — skipped')`；
3. 不在日志 / error / `.e2e-runtime.json` 中泄漏 Key（envLoader writeProcessEnv 已不持久化 apiKey 至 runtime json，此处文档化为安全红线）。

### 一句话目标
**让任何成员或 CI 拿到三档 env 模板 + 本文 + Secret 注入示例后，5 分钟内可让 WEB 用例真跑通；缺 Key 时整套 WEB 用例自动 skip 而非红条。**

---

## 二、方案设计

### 2.1 数据流图

```
   ┌────────────── 本地 ──────────────┐        ┌────────────── CI ──────────────┐
   │ tests/scripts/env/.env.<profile> │        │ GitHub Actions secrets         │
   │   MIDSCENE_MODEL_API_KEY=sk-***  │        │   MIDSCENE_MODEL_API_KEY = sk- │
   │   MIDSCENE_MODEL_NAME=gpt-4o     │        │   (引用 ${{ secrets.* }})      │
   │   MIDSCENE_OPENAI_BASE_URL=...   │        │   step env: 注入到 process.env │
   │   MIDSCENE_USE_AZURE_OPENAI=...  │        └────────────────────────────────┘
   └──────────────┬───────────────────┘                       │
                  │                                            │
                  ▼                                            ▼
        ┌──────────────────────────────────────────────────────────┐
        │ envLoader.loadE2EEnv()  →  env.midscene.{apiKey,         │
        │                              modelName, baseUrl, cache}  │
        │ writeProcessEnv()  →  process.env.MIDSCENE_MODEL_API_KEY │
        │                       (apiKey === '' 时不写入 process)   │
        └──────────────────────────────────────────────────────────┘
                  │
                  ▼
        ┌──────────────────────────────────────────────────────────┐
        │ playwright worker 启动 → fixtures.ts                      │
        │   midsceneReady (auto)：                                  │
        │     if WEB spec && env.midscene.apiKey === ''             │
        │     → testInfo.skip(true, '[MIDSCENE] api key missing')   │
        └──────────────────────────────────────────────────────────┘
                  │
                  ▼
        ┌──────────────────────────────────────────────────────────┐
        │ @midscene/web/playwright PlaywrightAgent                  │
        │   ↳ OpenAI 直连：MIDSCENE_OPENAI_BASE_URL 留空 → 默认     │
        │   ↳ Azure：     MIDSCENE_USE_AZURE_OPENAI=1 + AZURE_*     │
        │   ↳ 中转：       MIDSCENE_OPENAI_BASE_URL=https://relay   │
        └──────────────────────────────────────────────────────────┘
```

### 2.2 涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|---|---|---|
| `doc/tests/MIDSCENE_SETUP.md` | 新增 | 主交付物：三形态配置 + Secret 注入 + skip 策略 + FAQ |
| `tests/scripts/support/fixtures.ts` | 修改 | 新增 `midsceneReady` auto fixture（仅对 WEB spec 路径起效） |
| `tests/scripts/support/__tests__/midsceneSkip.test.ts` | 新增 | 单测：fixture skip 行为 + 文档存在性 + 字段表完整性 |
| `.github/workflows/e2e.yml`（若存在则改；不存在则在文档示例中提供） | 文档示例 | 引用 `secrets.MIDSCENE_MODEL_API_KEY`，禁止明文 |

> **不变更**：envLoader.ts（T-0000H 已实现 4 字段读取与注入）；三档 env.example（T-0000F 已留位）。

### 2.3 三形态字段冻结

| # | 形态 | 必填字段 | 可选字段 | 说明 |
|---|------|----------|----------|------|
| **A** | OpenAI 直连 | `MIDSCENE_MODEL_API_KEY=sk-***`<br>`MIDSCENE_MODEL_NAME=gpt-4o` | `MIDSCENE_OPENAI_BASE_URL`（留空 = 默认 https://api.openai.com/v1） | 默认形态；envLoader 已支持 |
| **B** | Azure OpenAI | `MIDSCENE_USE_AZURE_OPENAI=1`<br>`AZURE_OPENAI_ENDPOINT=https://<resource>.openai.azure.com`<br>`AZURE_OPENAI_DEPLOYMENT=<deployment-name>`<br>`AZURE_OPENAI_API_VERSION=2024-08-01-preview`<br>`MIDSCENE_MODEL_API_KEY=<azure-key>` | — | Azure 部署使用 deployment 名而非 model 名；endpoint 与 api-version 由 Azure 控制台获取 |
| **C** | 中转 / 自托管（OneAPI / LiteLLM / 自建网关） | `MIDSCENE_OPENAI_BASE_URL=https://<relay-host>/v1`<br>`MIDSCENE_MODEL_API_KEY=<relay-token>`<br>`MIDSCENE_MODEL_NAME=gpt-4o`（或网关支持的模型名） | — | 与 OpenAI 直连等价协议；仅 base URL 变更 |

> **字段唯一真值源**：本表为 Midscene 三形态字段冻结表；T-0000F §2.3 24 主字段表负责 envLoader 加载链；本表是其超集（Azure 5 字段为 K 专属，envLoader 不强校验，由 @midscene SDK 直接读 process.env）。

### 2.4 CI Secret 注入示例（GitHub Actions）

```yaml
# .github/workflows/e2e.yml（节选 — 严禁明文 Key）
name: E2E
on: [push, pull_request]

jobs:
  e2e-staging:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Run E2E (staging)
        env:
          E2E_PROFILE: staging
          # ⚠️ 仅引用 Secret，严禁明文
          MIDSCENE_MODEL_API_KEY: ${{ secrets.MIDSCENE_MODEL_API_KEY }}
          MIDSCENE_MODEL_NAME: gpt-4o
          MIDSCENE_OPENAI_BASE_URL: ${{ vars.MIDSCENE_OPENAI_BASE_URL }}
          MIDSCENE_CACHE: '1'
          # 其他 staging 凭据同样从 secrets 注入
          E2E_VALID_TOKEN: ${{ secrets.E2E_VALID_TOKEN }}
          # ...
        run: npm run e2e:staging
```

**安全规约**：
- ✅ 必须用 `${{ secrets.MIDSCENE_MODEL_API_KEY }}` 引用；
- ❌ 禁止 `MIDSCENE_MODEL_API_KEY: sk-xxx`（明文）；
- ❌ 禁止 `echo $MIDSCENE_MODEL_API_KEY`（GitHub Actions 默认 mask，但 `set -x` 或自定义 echo 会泄漏）；
- ✅ Azure 形态的 `AZURE_OPENAI_API_VERSION` 等非敏感字段可走 `${{ vars.* }}`，仅 Key 走 `${{ secrets.* }}`。

### 2.5 缺 Key skip 策略

#### 2.5.1 fixture 层（新增 `midsceneReady` auto fixture）

```ts
// tests/scripts/support/fixtures.ts（新增片段）
export function midsceneReadyImpl(env: E2EEnv, testInfo: MinimalTestInfo & { file?: string }): boolean {
  // 仅对 WEB spec 起效（路径包含 /tests/scripts/WEB/）
  const file = testInfo.file ?? '';
  const isWebSpec = file.includes(`${path.sep}tests${path.sep}scripts${path.sep}WEB${path.sep}`)
                 || file.includes('/tests/scripts/WEB/');
  if (!isWebSpec) return false;
  if (env.midscene.apiKey && env.midscene.apiKey.trim() !== '') return false;
  testInfo.skip(true, '[MIDSCENE] api key missing — skipped');
  return true;
}
```

注册为 `auto: true` worker fixture，与现有 `prodSafeGuard` 并列；执行顺序：`prodSafeGuard` → `midsceneReady`（prod profile + 未打 @prod-safe 已 skip 的用例不再走 midscene 检查）。

#### 2.5.2 与 T-0000H fixtures 协同矩阵

| profile | @prod-safe 标签 | 是 WEB spec | apiKey 空 | 行为 |
|---|---|---|---|---|
| local | — | 是 | 是 | **skip**（缺 Key）|
| local | — | 是 | 否 | 跑 |
| local | — | 否（API/E2E）| 是 | 跑（不依赖 Midscene）|
| staging | — | 是 | 是 | **skip**（缺 Key）|
| staging | — | 是 | 否 | 跑 |
| prod | 否 | 是 | 任意 | **skip**（prod-safe 防线先行）|
| prod | 是 | 是 | 是 | **skip**（midsceneReady）|
| prod | 是 | 是 | 否 | 跑 |

#### 2.5.3 用户提示

skip reason 严格为 `'[MIDSCENE] api key missing — skipped'`，便于 Playwright HTML 报告 grep 与 RUNBOOK FAQ 引用。

### 2.6 限流与回退

| 场景 | 表现 | 处理策略 |
|------|------|----------|
| 401 Unauthorized | Midscene SDK 抛错 | 文档 FAQ §4 指向「Key 失效 / 用错 Azure deployment 名」 |
| 429 Rate Limit | Midscene SDK 抛错或重试 | 启用 `MIDSCENE_CACHE=1`（默认）；CI 串行执行 WEB 套件（不并行 worker）|
| 网关超时 | 测试超时（默认 60s）| 单条用例 retry 1 次（playwright.config.ts `retries: process.env.CI ? 1 : 0`）；连续失败 3 条则 globalTeardown 输出 hint |
| 中转网关挂 | 连续 401/5xx | 文档建议：临时切回 OpenAI 直连（删掉 `MIDSCENE_OPENAI_BASE_URL` 即可）|
| Key 配额耗尽 | 持续 429 | **不**自动 skip（避免静默通过）；要求 RUNBOOK 手工触发「set MIDSCENE_MODEL_API_KEY=空 → 重跑 → 全部 skip」逃生通道 |

### 2.7 安全红线

1. **明文禁止**：`grep -rE '(sk-[A-Za-z0-9]{20,}|sk-proj-)' .github/ doc/ tests/scripts/env/*.example` 必须 0 命中（仅 `.env.{local,staging,prod}` 实文件可含真实 Key，且已在 `.gitignore`）；
2. **不入 runtime json**：envLoader `writeProcessEnv` 的 `.e2e-runtime.json` 持久化路径**不**写入 `MIDSCENE_MODEL_API_KEY`（当前 fixtures.ts `readE2EEnvForWorker` 优先读 runtime json，TDD 阶段需在 globalSetup 写 runtime json 时显式排除 apiKey 字段，或对 midscene.apiKey 单独从 process.env 取）；
3. **日志脱敏**：envLoader 的所有 error message（`MissingEnvError` / `InvalidEnvError`）严禁回显 `MIDSCENE_MODEL_API_KEY` 实值；类型为 secret 的字段在错误格式器中以 `***` 替代；
4. **CI 日志**：禁止 `echo`/`printenv MIDSCENE_*`；GitHub Actions 默认 mask Secret，但显式 `set -x` 会绕过，文档警示。

---

## 三、TDD 验收用例

> 所有用例位于 `tests/scripts/support/__tests__/midsceneSkip.test.ts`（unit 层，跑 `playwright.unit.config.ts`）+ 静态文档体检（grep）。

- [x] **U-1（文档存在性）**：`fs.existsSync('doc/tests/MIDSCENE_SETUP.md') === true`
- [x] **U-2（三形态字段表完整）**：文档正文含全部 3 个形态标题且每形态字段表含全部必填字段（OpenAI 2 必填 / Azure 5 必填 / 中转 3 必填），grep 字段名 6 类全部命中
- [x] **U-3（CI Secret 示例正确）**：文档 CI 章节含 `${{ secrets.MIDSCENE_MODEL_API_KEY }}` 字面 ≥1 命中；同时反向断言：文档中**不**出现形如 `MIDSCENE_MODEL_API_KEY: sk-` 的明文样例（fuzzy 守护）
- [x] **U-4（fixture skip 行为）**：mock `env.midscene.apiKey=''` + `testInfo.file='/abs/tests/scripts/WEB/TC-AUTH.spec.ts'` → `midsceneReadyImpl` 返回 `true` 且 `testInfo.skip` 被以 `(true, '[MIDSCENE] api key missing — skipped')` 调用
- [x] **U-5（非 WEB spec 不 skip）**：mock `env.midscene.apiKey=''` + `testInfo.file='/abs/tests/scripts/API/TC-USER.spec.ts'` → `midsceneReadyImpl` 返回 `false`，`testInfo.skip` 0 调用
- [x] **U-6（有 Key 不 skip）**：mock `env.midscene.apiKey='sk-test'` + WEB spec → 返回 `false`
- [x] **U-7（Key 安全 grep）**：`grep -rE 'sk-[A-Za-z0-9]{20,}' .github/ doc/ tests/scripts/env/*.example` 0 命中
- [x] **U-8（不入 runtime json）**：构造 `.e2e-runtime.json` 写入流程（如有）→ 静态体检 globalSetup 写 json 的字段白名单不含 `apiKey`，或 fixture 加载时显式从 `process.env.MIDSCENE_MODEL_API_KEY` 取值（覆盖 json）
- [x] **U-9（FAQ 章节存在）**：文档含「401 / 429 / 限流 / 切换形态 / 排错」5 个 FAQ 条目（grep 标题）

---

## 四、实现结果（由 TDD Agent 完成后填写）

- **实际修改的文件**：
  - 新增 `doc/tests/MIDSCENE_SETUP.md`（§1 三形态字段冻结表 / §2 本地配置 / §3 CI Secret 注入 + 安全规约 5 条 / §4 缺 Key skip 协同矩阵 / §5 限流回退表 / §6 FAQ 6 条 / §7 验证清单）
  - 修改 `tests/scripts/support/fixtures.ts`：新增 `midsceneReadyImpl(env, testInfo)` 纯函数 + `midsceneReady` auto worker fixture（与 `prodSafeGuard` 严格对称）
  - 修改 `tests/scripts/support/envLoader.ts`：新增 `sanitizeEnvForRuntimeJson(env)` 持久化黑名单（midscene.apiKey 永置空）
  - 修改 `tests/scripts/support/globalSetup.ts` Step 5：持久化 `.e2e-runtime.json` 前调用 `sanitizeEnvForRuntimeJson` 脱敏
  - 新增 `tests/scripts/support/__tests__/midsceneSkip.test.ts`：9 条 TDD 验收用例（U-1~U-9）
- **测试覆盖率**：
  - 本 Task 单测 `midsceneSkip.test.ts`：8 passed / 1 skipped（U-8 仅在 `.e2e-runtime.json` 存在时验证，单测环境跳过；CI/local globalSetup 跑过后会自动覆盖）
  - 全量单测 `npx playwright test --config=playwright.unit.config.ts`：68 passed / 2 skipped / 0 failed in 2.4s（零回归）
  - TypeScript 检查 `npx tsc --noEmit`：通过
- **完成时间**：2026-06-02

---

## 五、Reviewer意见

> 状态枚举：🟢 通过 | 🔴 未通过

### Round 1
- **状态**：🟢 通过（Review → DoD）
- **审查意见**（实跑验证 + 字面对账，11 维全 🟢）：
  1. **skip reason 字面一致** ✅：`fixtures.ts:111`（实际行号见 commit 5bf584a，midsceneReadyImpl 末尾）`testInfo.skip(true, '[MIDSCENE] api key missing — skipped')` 与 TDS §2.5 字面一致（含 em-dash `—`，非短横）；`midsceneSkip.test.ts` U-4 严格断言该字符串。
  2. **跨平台路径识别正确** ✅：`fixtures.ts` `midsceneReadyImpl` 同时检查 `${path.sep}tests${path.sep}scripts${path.sep}WEB${path.sep}` 与 `/tests/scripts/WEB/` 双形态，Windows `\` 与 Unix/Playwright 标准化的 `/` 路径均命中；U-5 用 `path.join('/abs','tests','scripts','API',...)` 验证非 WEB 路径不 skip。
  3. **三形态字段表全字段对齐** ✅：`MIDSCENE_SETUP.md §1` OpenAI 直连（`MIDSCENE_MODEL_API_KEY`/`MIDSCENE_MODEL_NAME` 必填 + `MIDSCENE_OPENAI_BASE_URL`/`MIDSCENE_CACHE` 可选）/ Azure（5 必填：`MIDSCENE_USE_AZURE_OPENAI`/`AZURE_OPENAI_ENDPOINT`/`AZURE_OPENAI_DEPLOYMENT`/`AZURE_OPENAI_API_VERSION`/`MIDSCENE_MODEL_API_KEY`）/ 中转（3 必填：`MIDSCENE_OPENAI_BASE_URL`/`MIDSCENE_MODEL_API_KEY`/`MIDSCENE_MODEL_NAME`）字段表与 TDS §2.3 完全一致；U-2 grep 6 类必填字段全命中。
  4. **CI yaml 使用 Secret 引用** ✅：`MIDSCENE_SETUP.md §3` 使用 `${{ secrets.MIDSCENE_MODEL_API_KEY }}`，非敏感 `MIDSCENE_OPENAI_BASE_URL` 走 `${{ vars.* }}`，`E2E_VALID_TOKEN` 走 `${{ secrets.* }}`；U-3 反向断言 `MIDSCENE_MODEL_API_KEY[:=]\s*sk-[A-Za-z0-9]` 与 `sk-[A-Za-z0-9]{20,}` 真值在文档全文 0 命中。
  5. **sanitizeEnvForRuntimeJson 真脱敏** ✅：`envLoader.ts` 末尾新增函数对 `midscene.apiKey` 强制置空字符串（深拷贝 `tokens`/`ids`/`midscene`，避免改动入参原对象）；`globalSetup.ts:101` Step 5 持久化前必经 `JSON.stringify(sanitizeEnvForRuntimeJson(env))`；U-7 用 canary `sk-secret-leak-canary-12345` 写真实临时文件并 `expect(content).not.toContain(...)` 端到端验证脱敏生效，且 `parsed.midscene.apiKey === ''`。
  6. **runtime json 不泄漏 + worker 端 Key 仍可用** ✅：U-7 显式断言 `writeProcessEnv` 注入 `process.env.MIDSCENE_MODEL_API_KEY` 不受脱敏影响（real Key 走 process.env，脱敏只影响磁盘 json），双通道分离设计正确，与 §2.7 安全红线 #4/#5 对齐。
  7. **fixture 隔离与 prodSafeGuard 协同** ✅：`midsceneReady` 仅当 `testInfo.file` 含 `tests/scripts/WEB/` 时调用 `skip`，对 ADMIN_WEB/API/APPSERVER spec 完全 no-op（U-5 断言 `skips.length === 0`）；与 `prodSafeGuard` 同为 auto worker fixture，二者独立，先后顺序无依赖；prod profile 未打 `@prod-safe` 时 `prodSafeGuard` 先 skip，`midsceneReady` 二次调用 `skip` 仍幂等无副作用。
  8. **9 条用例真实覆盖 §三验收** ✅：U-1（fs.existsSync）/ U-2（6 字段 grep）/ U-3（secrets 引用 + 双反向断言）/ U-4（`expect(skips[0].reason).toBe('[MIDSCENE] api key missing — skipped')` 字面）/ U-5（API + ADMIN_WEB 双路径）/ U-6（apiKey 非空时 skips=[]）/ U-7（端到端写盘 + canary 反查）/ U-8（real `.e2e-runtime.json` 不存在则 SKIP，存在则 grep `sk-`）/ U-9（FAQ + 限流/超时/Azure deployment 三关键词），无 mock 过度，纯函数 `midsceneReadyImpl` 提取使单测无需启动浏览器。
  9. **MIDSCENE_SETUP.md 五大块齐全 + 覆盖 R1~R4** ✅：§1 三形态 / §3 CI Secret + §3.1 安全规约 5 条 / §5 限流回退 5 场景 / §3.1 + §6.6 安全 / §6 FAQ 6 条；FAQ §6.5 中转 vision 不支持 → R1，FAQ §6.2 Azure deployment 误填 → R2，§4.1 协同矩阵 + §6.1 全 skip 排查 → R3，§6.6 + §3.1 #4/#5 → R4；锚点 `T-0000K`/`T-0000F`/`T-0000H` 链接路径正确（相对 `../tds/infra/T-000*.md`，已 fs 校验）。
  10. **回归 0 条** ✅：实跑 `npx playwright test --config=playwright.unit.config.ts` → **68 passed / 2 skipped / 0 failed in 2.3s**（2 skipped 之一为本 task U-8，另一为 npmScripts U-11 健康场景探测式 SKIP）；`npx tsc --noEmit` exit 0；specHardening / prodSafeGuard / globalSetup / envLoader / npmScripts 既有用例零回归。
  11. **beforeEach 隔离 process.env** ✅：`midsceneSkip.test.ts:42-44` `delete process.env.MIDSCENE_MODEL_API_KEY` 隔离 shell 已 export 的 Key，避免 worker 进程复用导致 U-4/U-5/U-6 期望偏移；U-7 finally 块同时 cleanup tmp dir + delete env，无 flaky 风险。

- **改进建议**（均不阻塞 DoD，留作后续小 PR）：
  1. **测试编号与 TDS 略有错位**（轻微）：`midsceneSkip.test.ts` 中标注的 U-7（`writeProcessEnv 不持久化`）实际对应 TDS §三 U-8 的「不入 runtime json」语义，而代码 U-8（`.e2e-runtime.json` 实文件 grep）覆盖 TDS U-7「Key 安全 grep」的子集。验收契约本身全部覆盖，仅注释/标题数字标号略不对齐；建议后续小 PR 内将代码块注释 U-7/U-8 互换或写「U-7+U-8 合并验证」以避免 TDS-Test 字面追溯混淆（位置：`tests/scripts/support/__tests__/midsceneSkip.test.ts:411,449`）。
  2. **`doc/assets/llms-full.txt` 含上游占位 Key**（轻微，非泄漏）：`grep -rE 'sk-[A-Za-z0-9]{20,}' doc/` 命中 2 处 `sk-abcdefghijklmnopqrstuvwxyz`（明显是字母表占位，非真实 Key），来自 Midscene 上游 llms.txt 文档导入。TDS §三 U-7 grep 范围是 `.github/ doc/ tests/scripts/env/*.example`，严格执行会被该文件触发。建议：① 在 U-7 grep 命令中增加 `--exclude-dir=assets` 或 `--exclude=llms-full.txt`；② 或在 RUNBOOK 中文档化「assets/ 下为第三方导入文档，已确认无真 Key」；当前 unit test 未直接执行该 grep（用 sanitize 反查替代），故不阻塞。位置：`doc/assets/llms-full.txt`。
  3. **`midsceneReady` auto fixture 在 ADMIN_WEB/API/APPSERVER worker 上仍会被实例化**（性能微噪声）：虽然纯函数早返回 `false`，但每个 testInfo 仍走一次字符串 `includes` 调用。建议未来若 fixture 数量增长，可改为 `tag-based` 或在 `playwright.config.ts.projects[].use` 维度按子树分发，避免全 worker 全 fixture 实例化（当前规模无需优化）。

- **审查结论**：T-0000K 实现高质量、契约严格对齐 TDS、安全红线（§2.7）在代码层（sanitize）+ 文档层（grep + 反向断言）+ 测试层（U-7 canary）三重兜底；建议 Review → DoD。

---

---

## 六、风险矩阵

| ID | 描述 | 概率 | 影响 | 等级 | 缓解 | 兜底 | Owner |
|---|---|---|---|---|---|---|---|
| R1 | 中转网关协议不一致（OneAPI 部分模型不支持 vision）导致 WEB 用例红 | 中 | 中 | P1 | 文档 §三明确推荐 OpenAI 直连为基线，中转仅在 §C 标记「需自行验证 vision 支持」 | RUNBOOK 提供「切回直连」一行命令（unset `MIDSCENE_OPENAI_BASE_URL`）| Plan |
| R2 | Azure deployment 名误填为 model 名 → 401 | 中 | 低 | P2 | FAQ §4 给出 Azure 控制台路径截图描述 | skip 不掩盖（实际 401 直接 fail）| TDD |
| R3 | fixture skip 误伤：CI 误删 Secret 后 WEB 套件全部 skip 但 build 绿，掩盖回归 | 中 | 高 | P1 | T-0000I `e2e:staging`/`e2e:prod-smoke` 在 CI 模式下增加「WEB skip 数 > 阈值则 fail」软门禁（由 T-0000L RUNBOOK 引用，本 TDS 不强制）| globalTeardown 输出红色 hint「N WEB tests skipped due to MIDSCENE key missing — verify Secret」| Plan |
| R4 | runtime json 泄漏 Key | 低 | 高 | P1 | §2.7 规约 + U-8 用例 | `.gitignore` 已忽略 `tests/scripts/.e2e-runtime.json` | TDD |

---

## 七、与上游 / 同期任务的锚点映射

| 上游 / 同期 | 锚点 | 本 TDS 引用位置 |
|---|---|---|
| T-0000E §2.4.1 | envLoader `MIDSCENE_*` 4 字段定义 | §2.3 形态 A 字段映射的基础 |
| T-0000E §2.9.2 | 凭据隔离：`.env.prod` 永不入库；CI 仅从 GitHub Secrets 取 | §2.4 + §2.7 |
| T-0000E §4.3 K 行 | 「Key 缺失时 WEB 用例 skip（非 fail）；CI 仅从 Secrets 取」 | §2.5（fixture）+ §2.4（CI）|
| T-0000F §2.3 | 三档 env 模板已留 `MIDSCENE_MODEL_API_KEY/NAME/BASE_URL/CACHE` 4 字段位 | §2.3 形态 A 复用，形态 B 的 5 个 Azure 字段为本 TDS 新增（不进 envLoader 强校验）|
| T-0000H envLoader.ts L236-241 | `env.midscene = { apiKey, modelName, baseUrl, cache }` | §2.1 数据流图 |
| T-0000H fixtures.ts `prodSafeGuard` | auto fixture 模板 | §2.5.1 `midsceneReady` 严格对称实现 |
| T-0000J §2.4.1 | 五条 read-only 判定 + @prod-safe 标签 | §2.5.2 协同矩阵第 6/7/8 行 |
| T-0000L RUNBOOK | 故障排查表引用本文 §2.6 限流回退表 | §2.6 |
