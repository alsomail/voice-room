# 全局代码审查报告：模块 9 E2E 测试基建（E2E QA Foundation）

> **当前状态机**：负责人 [-] | 状态 [✅ Passed] | 修复轮次 [1/10]
>
> **批次合并说明**：本文档由原 `batch-e2e-foundation-01.md`（基础批次：12 个任务）与 `batch-e2e-foundation-02.md`（增量批次：T-0000M 双服务共库 Migration 表隔离）合二为一。原文件保留并加重定向 banner，避免破坏历史链接；后续模块 9 审查请直接向本文追加。

---

## 0. 流转规则

- **状态枚举**：负责人 [-] 状态 [✅ Passed] | 负责人 [TDD] 状态 [❌ Failed] | 负责人 [GlobalReview] 状态 [⏳ In Review]
- 每轮 Review 追加一条记录，不要覆盖历史。
- 处于负责人 [GlobalReview] 状态 [⏳ In Review]，则由 [GlobalReview] 进行全局代码审查。
- [GlobalReview] 审查通过 → 修改负责人 [-] 状态 [✅ Passed]。
- [GlobalReview] 审查未通过 → 修改负责人 [TDD] 状态 [❌ Failed]，并将审查意见追加到文档下方。
- [TDD] 修复并自测后 → 状态改为负责人 [GlobalReview] 状态 [⏳ In Review]，触发下一轮复审。

---

## 1. 审查上下文

### 批次 A：基础批次（12 个任务，2026-04-27 闭环）

- **审查范围**：模块 9 E2E 测试基建整体架构与基建可用性审查。
- **包含任务模块**：[模块 9: E2E 测试基建](../tasks/模块9-E2E测试基建%20(E2E%20QA%20Foundation).md)
- **包含任务**：T-0000E、T-0000F、T-0000G、T-0000H、T-0000I、T-0000J、T-0000K、T-0000L、T-00040、T-10020、T-20020、T-30050（共 12 个）
- **关联 TDS**：
  - 基建侧：[T-0000E](../tds/infra/T-0000E.md)、[T-0000F](../tds/infra/T-0000F.md)、[T-0000G](../tds/infra/T-0000G.md)、[T-0000H](../tds/infra/T-0000H.md)、[T-0000I](../tds/infra/T-0000I.md)、[T-0000J](../tds/infra/T-0000J.md)、[T-0000K](../tds/infra/T-0000K.md)、[T-0000L](../tds/infra/T-0000L.md)
  - 各端配置：[T-00040](../tds/server/T-00040.md)、[T-10020](../tds/adminServer/T-10020.md)、[T-20020](../tds/web/T-20020.md)、[T-30050](../tds/android/T-30050.md)

### 批次 B：增量批次（T-0000M，2026-04-27 闭环）

- **审查范围**：批次 A 闭环后，模块 9 在联调阶段又暴露一个架构级阻断（双服务共库共享 `_sqlx_migrations` 表互掐），新增 T-0000M 收口；本批次仅审查 T-0000M 全量 TDD 交付物，不重复审批次 A。
- **包含任务**：T-0000M（共 1 个）
- **关联 TDS / ADR**：
  - [T-0000M](../tds/infra/T-0000M.md)（双服务共库 Migration 表隔离 TDS）
  - [ADR-0001](../adr/ADR-0001-migration-table-isolation.md)（方案 A/B/C 决策记录）
- **代码 diff 范围**：`cf330cf..HEAD`（含 8 个 commit：联调修复 + helper 实现 + 集成测试 + 脚本收口 + ADR-0001 + 任务索引同步 + Round 1 reviewer 意见 + DoD 文档同步）。

---

## 2. 审查关切

### 2.A 批次 A 关切（PO/协调者）

#### 关切 ①：能否快速进行 E2E 测试？
- 新人从 `git clone` 到第一条 smoke 用例全绿，是否真的 5 分钟可达？
- `npm run e2e:local` / `e2e:staging` / `e2e:prod` 链路是否端到端可执行（含 globalSetup → preflight → seed → 跑测 → teardown）？
- preflight 五端探活是否真实生效，失败时报错是否清晰可定位？
- Midscene LLM Key 缺失时 Web 用例是否优雅 skip 而非 fail？

#### 关切 ②：是否具备「一键部署服务 + 一键启动测试」的能力？
- 是否存在 `npm run e2e:up`（或等价命令）一键拉起 docker-compose 全栈？
- 一键命令是否覆盖：拉起服务 → 等待健康 → seed 数据 → 跑 E2E → 失败保留现场 → 成功 teardown？
- 三档 profile（local/staging/prod）切换是否真的「零改代码」？
- `@prod-safe` 标签机制是否能阻止破坏性用例误跑到 prod？

#### 关切 ③：环境变量是否能自动写入 Android 与 AdminWeb（跨端注入能力）？
- AdminWeb：Vite `import.meta.env.VITE_ADMIN_API_BASE_URL` 等是否真的从根 `.env.{profile}` 自动注入？
- Android：`productFlavors {local/staging/prod}` 是否真的将 `BASE_URL` / `WS_URL` / `applicationIdSuffix` 通过 BuildConfig 注入？三档 APK 是否能并存？
- 是否存在统一的 env 单一事实源（根 `.env.{profile}`），还是各端各自维护一份（违反单一事实源原则）？
- envLoader 是否能在 E2E 启动时把同一份配置同时投喂给 Playwright / Midscene / Android / AdminWeb？

#### 关切 ④：基建本身的健壮性
- Seed/Reset 脚本是否幂等？反复跑不会污染数据库？
- globalTeardown 失败时是否会泄漏端口/容器/进程？
- CI 接入 SOP 是否完整（Secret 注入、产物上传、失败重试策略）？
- 文档（E2E_RUNBOOK.md、MIDSCENE_SETUP.md）是否与代码实际行为一致？

### 2.B 批次 B 关切（仅 T-0000M）

#### 关切 ①：双服务共库 Migration 隔离的工程正确性
- `voice_room_shared::migrate::run_migrations_with_table` 自管 SQL 是否完备复刻 sqlx 0.8 原生 `Migrate::run` 的语义边界（version 单调、checksum 校验、缺失迁移检测、advisory lock 互斥、事务/no_tx 分支）？
- `validate_table_name` 白名单是否真正阻断所有 SQL 拼接位置的注入面？
- FNV-1a(table_name) 派生的 advisory lock_id 与 sqlx 默认按 database name 派生的 lock 是否完全错开？

#### 关切 ②：替换覆盖度与回归风险
- 两个 `main.rs` 调用点是否一致？是否还有遗留的 `MIGRATOR.run(&pool)` 字面量？
- `migration_isolation_test`（U-1/U-2/U-3/N-1/N-3）+ `wallet_schema_test`（9 处替换）+ `send_gift_test`（13 处替换）是否充分？测试间 schema 隔离是否健壮？
- 单 Task Reviewer 在 §5 Round 1 提出的 4 条 follow-up 建议，是否需要升级为本批次阻断项？

#### 关切 ③：脚本与文档闭环
- `scripts/dev/init-db.sh` 的 `GRANT CREATE ON SCHEMA public TO app_server_user` 是否幂等？`e2e-up.sh` 的 inline workaround 是否完全撤回？
- AdminServer 子进程作用域的 `DATABASE_URL` 覆盖是否正确？
- 架构文档与 ADR-0001 是否一致地反映「保底方案落地 + Phase 2 切 C 触发条件」？

#### 关切 ④：残余风险的可追溯性
- TDS §4 已记录 E-1/E-2 因 `/health` 端点缺失受限、`ranking_test::r08_response_time_under_100ms` 偶发 perf flake。这两项是否在文档中有明确去向（独立 Task 或 follow-up backlog），不被 T-0000M DoD 隐式吞掉？

---

## 3. 审查与修复日志

> 执行规则：GlobalReview 记录缺陷，TDD 在对应缺陷下方记录修复方案与 PR/Commit。严禁覆盖历史记录，只能向下追加。

---

### 批次 A —— 第 1 轮审查

**@GlobalReview 审查意见：**

#### 一、四个核心关切的逐条结论

| # | 关切 | 结论 | 关键证据 |
|---|---|---|---|
| ① | 5 分钟冷启动 | ⚠️ 部分兑现 | `E2E_RUNBOOK.md:21,29-51` 把「服务起齐 ≈ 1min」计入 5min 预算，但 Step 4 要求新人首次 `cargo run` 冷编译 Rust 工作区（分钟级，远超 1min）。preflight 五端探活实现正确（`scripts/dev/preflight.sh` 含 fail-fast、退出码 11~15、彩色 hint），Midscene 缺 Key 时 WEB 子树 `test.skip` 行为正确。 |
| ② | 一键部署 + 一键测试 | ❌ 未兑现 | `docker-compose.yml` 仅声明 postgres + redis 两个 service，完全不含 AppServer / AdminServer / Web。仓库不存在 `npm run e2e:up`；RUNBOOK §2 Step 4 要求新人开 3 个终端各跑 `cargo run` / `npm run dev`。`@prod-safe` 三道防线已落地 ✅。 |
| ③ | 跨端 env 自动注入 / 单一事实源 | ❌ 未兑现 | (a) AdminWeb：`app/web/.env.{development,staging,production,test}` 4 文件独立维护、URL 全部硬编码；`vite.config.ts` 未配 envDir / loadEnv；根 `.env.example:79-81` 注释明确写「VITE_* 收口于子项目」——有意识的双源设计。(b) Android：`build.gradle.kts:108-134` 中 staging/prod productFlavors 全部字面硬编码，仅 local 通过 env 回退，且环境变量名 `VOICE_ROOM_API_BASE_URL` 与根 `.env` 字段 `APP_SERVER_BASE_URL` 不一致，envLoader 无桥接。(c) envLoader → Playwright/Midscene 链路 ✅。 |
| ④ | 基建健壮性 | ⚠️ 部分兑现 | ✅ Seed 幂等（全表 `ON CONFLICT ... DO UPDATE` + UUIDv5）；✅ Teardown local-only 且失败仅 warn。❌ CI 永红风险：`.github/workflows/playwright.yml` 在 `pull_request`/`push` 直接 `npx playwright test`，不启依赖服务也无 `CI_E2E_READY` 门禁；globalSetup 必走 preflight → 退码 11~15 → 流水线全红。⚠️ RUNBOOK §2 Step 5 推荐 `e2e:prod-smoke -- --list`，但 Step 2 仅复制 `.env.local`，envLoader prod profile 必抛 `MissingEnvError`。 |

#### 二、缺陷清单

- **缺陷 1 [P1]**：CI 工作流 `playwright.yml` 永红——未起依赖服务即跑 Playwright。
  - **文件**：`.github/workflows/playwright.yml:7-22`
  - **修复建议**：(a) 改为 `workflow_dispatch` 手动触发并在 job 中加 `services: postgres/redis` + 启 cargo/vite；(b) 在 `globalSetup` 入口加 `if (CI === 'true' && CI_E2E_READY !== '1') return;` 软门禁。

- **缺陷 2 [P1]**：Android staging/prod productFlavors URL 硬编码，跨端单一事实源破裂。
  - **文件**：`app/android/app/build.gradle.kts:108-134`
  - **修复建议**：staging/prod flavor 改为 `resolveConfigValue(...)` 同 local 写法；envLoader 末尾追加 `VOICE_ROOM_API_BASE_URL` 等桥接键。

- **缺陷 3 [P1]**：AdminWeb env 双源维护，根 `.env.{profile}` 切换不会传导到 Vite。
  - **文件**：`app/web/.env.*` 4 文件 + `app/web/vite.config.ts:1-19`
  - **修复建议**：`vite.config.ts` 接 `loadEnv(mode, path.resolve(__dirname, '../../tests/scripts/env'))` 并通过 `define` 注入；`app/web/.env.*` 清空 URL 字段。

- **缺陷 4 [P2]**：RUNBOOK 5min 预算未计入首次 cargo 冷编译。
  - **文件**：`doc/tests/E2E_RUNBOOK.md:21,42-44`
  - **修复建议**：改为「首次 8~18min / 复跑 ≤ 5min」诚实口径，并加「`cargo build --workspace` 预热」步骤。

- **缺陷 5 [P2]**：RUNBOOK 冷启 Step 5 推荐命令与已复制 env 不匹配。
  - **文件**：`doc/tests/E2E_RUNBOOK.md:36-50`
  - **修复建议**：把 Step 5 改为 `npm run e2e:local -- --list` 或 `--grep "@prod-safe"`，`e2e:prod-smoke` 移到远端凭据流程章节。

- **缺陷 6 [P2]**：docker-compose 不含业务服务，与「一键起全栈」期望落差未在 RUNBOOK 明示。
  - **文件**：`docker-compose.yml:1-41`、`E2E_RUNBOOK.md:39-45`
  - **修复建议**：在 RUNBOOK §2 加「设计取舍」段；新增 `npm run e2e:up` 聚合 `docker compose up -d postgres redis` + 后台 cargo/vite + `wait-on` 五端健康。

#### 三、本轮总结

❌ 存在 P1×3 + P2×3，关切 ①/④ 部分兑现、关切 ②/③ 未兑现。

---

### 批次 A —— 第 2 轮审查（程序性复核）

**@GlobalReview 审查意见：**

实地核验第 1 轮 6 个缺陷在最新代码上**全部仍存在**，无任何 commit 修复痕迹。复核方法：直读源文件行号 + grep 关键变量名（`VOICE_ROOM_API_BASE_URL`、`envDir`、`loadEnv`、`CI_E2E_READY`、`e2e:up`），均无任何匹配变更。本次系 PO 修正主表状态机后的程序性复核，6 个缺陷悉数维持原级别原描述；维持 Failed 状态，**不递增修复轮次计数**。

---

### 批次 A —— 第 1 轮 TDD 修复

> 严格遵循 Red → Green → Refactor。所有改动遵循「单一事实源 = 根 `.env.{profile}`」红线与「零硬编码」约束，Android/Web 端 0 回归。

#### 缺陷 1 [P1] CI 永红 —— 软门禁兜底（方案 b）
- `.github/workflows/playwright.yml`：触发器改为 `workflow_dispatch`，加输入 `e2e_ready`，job env 透传 `CI_E2E_READY`。
- `tests/scripts/support/globalSetup.ts`：`runGlobalSetup` 入口加 `CI=true && CI_E2E_READY!=1` 早退分支。
- `globalSetup.test.ts`：+3 用例（缺旗标早退 / =1 正常跑 / 本地不受影响）。

#### 缺陷 2 [P1] Android staging/prod 硬编码 + envLoader 桥接缺失
- `tests/scripts/support/envLoader.ts`：`writeProcessEnv` 末尾追加 4 个桥接键 `VOICE_ROOM_API_BASE_URL/WS_URL/ANALYTICS_ENDPOINT/ENVIRONMENT`。
- `app/android/app/build.gradle.kts`：staging/prod flavor 改用 `resolveConfigValue(envKey, propKey, default)`，default 保留商店域名以保 0 回归。
- `androidGradle.test.ts`：新建 3 个静态守护用例。
- 自测：staging/prod/local 三 flavor `./gradlew :app:test*DebugUnitTest` 全部 BUILD SUCCESSFUL。

#### 缺陷 3 [P1] AdminWeb env 双源
- `app/web/vite.config.ts`：函数式 defineConfig，自实现 `parseDotenv`（无新依赖），从根 `tests/scripts/env/.env.{profile}.example` + 真值 `.env.{profile}` 读取，通过 `define` 注入 4 个 `import.meta.env.VITE_*`。
- `app/web/.env.{development,staging,production,test}`：清空 URL 字段。
- `viteConfig.test.ts`：新建 3 用例。
- 自测：`cd app/web && npm test` → **517/517 passed**。

#### 缺陷 4 [P2] RUNBOOK 时长预算不实
- `E2E_RUNBOOK.md` §1 改为「首次 8~18min（含 cargo build）/ 复跑 ≤ 5min」。

#### 缺陷 5 [P2] docker-compose 设计取舍未文档化
- `E2E_RUNBOOK.md` §2 加「设计取舍」段；Step 5 改用 `e2e:local --list` / `--grep "@prod-safe"`。

#### 缺陷 6 [P2] 缺一键起栈命令
- `package.json`：scripts 加 `"e2e:up": "bash scripts/dev/e2e-up.sh"`。
- `scripts/dev/e2e-up.sh` / `e2e-down.sh`：新建（docker compose up + 3 个后台 cargo/vite + wait-on 五端健康；PID 写 `.e2e-up.pids`）。

#### 回归测试矩阵

| 测试套件 | 命令 | 结果 |
|---|---|---|
| 根 Playwright unit | `npx playwright test --config=playwright.unit.config.ts` | ✅ 94 passed + 2 skipped |
| AdminWeb vitest | `cd app/web && npm test` | ✅ 517 passed |
| Android staging/prod/local unit | `cd app/android && ./gradlew :app:test*DebugUnitTest --rerun-tasks` | ✅ BUILD SUCCESSFUL |

---

### 批次 A —— 第 3 轮审查（最终通过）

**@GlobalReview 复核意见：**

#### 第 1 轮 6 个缺陷代码现状逐条核对（实地 grep + view，非自述）

| # | 缺陷 | 复核证据 | 结论 |
|---|---|---|---|
| 1 (P1) | CI `playwright.yml` 永红 | `.github/workflows/playwright.yml:6-25` 触发器 `workflow_dispatch` + `inputs.e2e_ready`；`globalSetup.ts:37-41` 早退；`globalSetup.test.ts:202-235` 3 用例。CI 默认 `e2e_ready=0` → 软门禁早退、不再永红。 | ✅ 已修复 |
| 2 (P1) | Android staging/prod 硬编码 + 桥接缺失 | `build.gradle.kts:114-163` staging/prod 全部 `resolveConfigValue` 通道；`envLoader.ts:284-296` `writeProcessEnv` 追加 4 个桥接键。 | ✅ 已修复 |
| 3 (P1) | AdminWeb env 双源 | `vite.config.ts:57-113` 函数式 `defineConfig` + `PROFILE_BY_MODE` 表 + `define` 注入 4 个 `VITE_*`；`app/web/.env.{4文件}` 全部清空 URL；`viteConfig.test.ts` U-V1/V2/V3 全绿。 | ✅ 已修复 |
| 4 (P2) | RUNBOOK 5min 预算 | `E2E_RUNBOOK.md:21-24` 改为「首次 8~18min / 复跑 ≤ 5min」+ `cargo build --workspace` 预热行；`runbook.test.ts` U-12 守护。 | ✅ 已修复 |
| 5 (P2) | RUNBOOK Step 5 命令 | `E2E_RUNBOOK.md:61-66` Step 5 改 `preflight` → `e2e:local --list` → `--grep "@prod-safe"` → `e2e:local`，`e2e:prod-smoke` 移到 §5 远端凭据流程。 | ✅ 已修复 |
| 6 (P2) | docker-compose 不含业务服务 + 缺一键起栈 | §2 顶部「设计取舍」段；`package.json:10` `"e2e:up"`；`e2e-up.sh` 41 行实现 docker up + 3 后台进程 + wait-on 五端健康。 | ✅ 已修复 |

> 复核方法：直读关键源文件 + grep 关键变量名 + 实跑 `npx playwright test --config=playwright.unit.config.ts` → **94 passed + 2 skipped**，0 fail。

#### 四个 PO 关切的最终结论

| # | 关切 | 结论 | 关键变化 |
|---|---|---|---|
| ① | 5min 冷启动 | ✅ 已兑现 | 预算改诚实口径；`npm run e2e:up` + 预热让复跑严格 ≤ 5min。 |
| ② | 一键部署 + 一键测试 | ✅ 已兑现 | `npm run e2e:up` 聚合 docker + 3 后台进程 + wait-on；`@prod-safe` 三道防线维持。 |
| ③ | 跨端 env 自动注入 | ✅ 已兑现 | 单一事实源链路 `根 .env.{profile} → envLoader → process.env → {Vite define / gradle resolveConfigValue}` 闭合可验证。 |
| ④ | 基建健壮性 | ✅ 已兑现 | CI 软门禁双保险解除永红；Seed 幂等、Teardown non-fatal、@prod-safe、envLoader 类型安全 + 脱敏全保留。 |

#### LOW 级观察（不阻断本轮通过，下一批次跟进）

- **观察 1 [P3]**：`writeProcessEnv` 把 `VOICE_ROOM_ANALYTICS_ENDPOINT` 默认置为空串（`envLoader.ts:294`）。当前 E2E 链路不在 globalSetup 内调用 gradlew，影响为 0。
- **观察 2 [P3]**：`vite.config.ts` 自实现 `parseDotenv` 不剥离行尾内联注释（`vite.config.ts:38-55`）。当前 env 文件 grep 验证不含行尾 `#` 注释，无实际影响。

#### 本轮总结

✅ **批次 A 审查通过**：第 1 轮 6 个缺陷全部真实修复；4 个 PO 关切全部从 ⚠️/❌ 升级为 ✅；单测套件 `94 passed + 2 skipped` 实跑全绿；原已落地优点全部保留；2 条 LOW 级观察不阻断通过，作为下批次 NICE-TO-HAVE 跟进。

---

### 批次 B（T-0000M 增量）—— 第 1 轮审查

**@GlobalReview 审查意见：**

> 审查范围：`cf330cf..HEAD`（核心 8 个 T-0000M commit + 文档/索引同步）。已读：T-0000M TDS、ADR-0001、`directory_and_ddd.md` §3.2、`app/shared/src/migrate/mod.rs` 全文、两端 main.rs L40~75、`migration_isolation_test.rs`、`tests/common/mod.rs`、`init-db.sh`、`e2e-up.sh`，并跑通 `cargo test -p voice-room-shared --lib migrate::`（7/7 PASS）。

- **关切 ①（helper 工程正确性）**：核心实现（白名单校验 / FNV-1a advisory lock / checksum & missing 检测 / 事务边界 / 表名注入闭合）与单 Task R1 评审一致，无新增缺陷。
- **关切 ③（脚本与文档闭环）**：`init-db.sh` GRANT 幂等、`e2e-up.sh` inline workaround 已撤（`grep "GRANT CREATE" e2e-up.sh` 0 命中）、AdminServer 子进程作用域不污染父 shell，全部通过。
- **关切 ②、④**：发现下列阻断性问题。

#### 缺陷清单

- **缺陷 1 [P1]**：14 处遗留 `sqlx::migrate!("./migrations").run(&pool)` 直连默认表，违反本批次新立的架构规约。
  - **文件**：`wallet_api_test.rs`（6 处）、`ranking_test.rs`（5 处）、`gift_list_test.rs`（2 处）、`governance_real_repos_test.rs`（1 处）。
  - **问题**：`directory_and_ddd.md` §3.2.3 强制规约「集成测试统一走 `tests/common/mod.rs::run_migrations()`」与遗留代码同 PR 自相矛盾；dev/CI 库会同时存在 `_sqlx_migrations`（旧）+ `_sqlx_app_migrations`（新）两套登记表，规约形同虚设。
  - **修复建议**：14 处全部替换为 `mod common; common::run_migrations(&pool).await?;`。

- **缺陷 2 [P1]**：T-0000M DoD 第 1 条「`npm run e2e:up` 一遍过 5 端绿」客观未达成；`/health` 缺失与 `ranking r08` perf flake 无显式 follow-up Task 接力，模块「13/13 ✅」标记存在对外承诺失实风险。
  - **问题**：`e2e-up.sh:67-68` 等待 `/health`，但 `app/{server,adminServer}/src` grep `/health` 0 命中；TDS §4 自承未通过但仅口头记入「TODO」，无新 Task ID。
  - **修复建议（方案 A 推荐）**：(a) 立 `T-0000N`（AppServer/AdminServer `/health` 端点）+ `T-0000O`（ranking r08 perf flake known-issue）作为 follow-up Task；(b) T-0000M DoD #1 改为「双服务进程冷启动均完成 migrate 且 `_sqlx_app_migrations`=9 / `_sqlx_admin_migrations`=4（5 端 wait-on 全绿依赖 T-0000N）」。

- **缺陷 3 [P2]**：helper 未透传 `Migration::no_tx`（`mod.rs:197-229`）。当前 13 条迁移无 `-- no-transaction`，无运行时 bug；但未来引入 `CREATE INDEX CONCURRENTLY` 时会与 sqlx 原生 `Migrate::run` 行为分叉。
  - **修复建议**：循环内分支 `if m.no_tx { 直连 conn } else { tx 路径 }`，并补 dispatch 单元测试。

- **缺陷 4 [P2]**：`migration_isolation_test.rs:12-14` 顶部注释关于 `app_server_user` 可跑此测试不准确。`init-db.sh` 仅 `GRANT CREATE ON SCHEMA public`（建表权限），未 `GRANT CREATE ON DATABASE voiceroom`（建 schema 权限），`CREATE SCHEMA` 必报权限不足。
  - **修复建议**：注释改为「仅 superuser DATABASE_URL 可跑」。

- **缺陷 5 [P2]**：测试 schema 缺 RAII guard，panic 路径 `t0m_<uuid16>` schema 永久残留。
  - **修复建议**：用 `Drop` impl / `scopeguard::defer!` 包装 schema 名，确保 panic 路径同样 `DROP SCHEMA ... CASCADE`。

- **缺陷 6 [P2]**：N-2（权限缺失显式失败）仅在 TDS §4 手工实测说明，未沉淀为自动化测试。
  - **修复建议**：补一条 N-2 集成用例：superuser 起 schema → `REVOKE CREATE` → 受限连接跑 helper → 断言 `err.contains("_sqlx_app_migrations")`。

#### 本轮总评

❌ **不通过**：(a) 14 处遗留默认表调用与同 PR 架构规约自相矛盾，长期可维护性红线（缺陷 1，P1）；(b) DoD #1 客观未达成、follow-up 无 Task 接力，对外承诺失实风险（缺陷 2，P1）。请 TDD 优先消化 P1 两条，P2 四条可同一轮一并处理。

---

### 批次 B —— 第 1 轮 TDD 修复

#### 缺陷 1 [P1] 14 处遗留收敛
- **commit `1922369`**：`wallet_api_test.rs` 6 处 / `ranking_test.rs` 5 处 / `gift_list_test.rs` 2 处 / `governance_real_repos_test.rs` 1 处全部改走 `mod common; common::run_migrations(&pool).await`。
- **验证**：`grep -rn 'sqlx::migrate!("./migrations").run' app/server/tests/` 仅剩 `tests/common/mod.rs`（`migration_isolation_test.rs` 用 raw migrator 是用例本身需要）。

#### 缺陷 2 [P1] DoD 失实闭环（方案 A）
- **commit `ba00221` + `4295835`**：
  1. 模块 9 task 文件新增 T-0000N（AppServer/AdminServer 暴露 `/health` 端点，🔴 待开发）+ T-0000O（ranking r08 perf flake known-issue，🔴 待开发）。
  2. T-0000M DoD #1 改为「双服务进程冷启动均完成 migrate 且 `_sqlx_app_migrations`=9 / `_sqlx_admin_migrations`=4（5 端 wait-on 全绿依赖 T-0000N）」。
  3. 模块 9 进度由「13/13 ✅」回写为「13/13 ✅ + 2 follow-up（T-0000N/O）」。
  4. T-0000M TDS §3.2 E-1/E-2 标注「依赖 T-0000N，本 Task 不阻塞」+ §4.1 新增「Round 1 修复说明」章节。
  5. `doc/tasks/index.md` v2.51 → v2.52、`doc/product/index.md` v3.13 → v3.14。
- 严守红线：未触动批次 A 已闭环 12 个 Task 状态行；未修改 T-0000M 的 Review/QA/Overall Gate 三列。

#### 缺陷 3 [P2] no_tx 透传
- **commit `39c6dd7`**：`run_inner` step 5 增加 `if m.no_tx { 直连 conn } else { tx 路径 }`，与 sqlx 0.8 原生对齐。新增单元测试 `no_tx_dispatch_executes_without_transaction`。
- **验证**：`cargo test -p voice-room-shared --lib migrate::` → **8/8 PASS**。

#### 缺陷 4 [P2] 注释修正
- **commit `29edae7`**：`migration_isolation_test.rs:12-15` 注释改为「**仅 superuser DATABASE_URL 可跑**；受限账号 `app_server_user` 仅有 `GRANT CREATE ON SCHEMA public`，`CREATE SCHEMA` 会报 `permission denied`」。

#### 缺陷 5 [P2] RAII guard
- **commit `29edae7`**：新增 `SchemaGuard { pool, schema }` 结构，`Drop::drop` 中另起线程 + tokio current_thread rt 跑 `DROP SCHEMA ... CASCADE`。U-1/U-2/U-3/N-1/N-2/N-3 全部改用 `let _guard = SchemaGuard::new(...)`，移除尾部 `drop_schema(...)` 显式调用；panic 路径有 RAII 兜底。纯 std 实现，未引入 `scopeguard` crate。

#### 缺陷 6 [P2] N-2 自动化
- **commit `29edae7`**：新增 `n2_revoke_create_emits_table_name_in_error`：superuser 起 schema → 仅 `GRANT USAGE`（不 `GRANT CREATE`）→ 用 `app_server_user` 受限连接 → 断言 `err.contains("_sqlx_app_migrations")`。SKIP 守卫双层。
- **验证**：`cargo test -p voice-room-server --test migration_isolation_test` → **6/6 PASS**。

---

### 批次 B —— 第 2 轮审查（最终通过）

**@GlobalReview 复审意见：**

> 复审范围：`ac1a2a1..HEAD` 共 6 个 commit（`1922369` / `39c6dd7` / `29edae7` / `ba00221` / `4295835` / `c4cbb2d`）。已实跑 `cargo test -p voice-room-shared --lib migrate::`、`cargo test -p voice-room-server --test migration_isolation_test`、`cargo test -p voice-room-server` 全套（DATABASE_URL=postgres superuser + APP_SERVER_DATABASE_URL=app_server_user）。

#### Round 1 缺陷核验

- **[✅] 缺陷 1（P1，14 处遗留收敛）**：`grep -rn 'sqlx::migrate!("./migrations").run' app/server/tests/` 仅命中 `common/mod.rs:3` 文档注释；`grep -rn "common::run_migrations" app/server/tests/` 命中 26 处覆盖全部目标文件。`cargo test -p voice-room-server` 全套 0 fail（含 `migration_isolation_test 6/6 PASS`）。
- **[✅] 缺陷 2（P1，DoD 失实闭环）**：模块 9 task 文件 line 48/49 完整新增 T-0000N + T-0000O；line 47 T-0000M DoD #1 措辞符合事实；TDS §3.2 与 §4.1 标注一致；`product/index.md` v3.14 + `tasks/index.md` v2.52 changelog 一致。
- **[✅] 缺陷 3（P2，no_tx）**：`mod.rs:197-254` 分支正确，`tests::no_tx_dispatch_executes_without_transaction` 落地；shared crate 单测 **8/8 PASS**。
- **[✅] 缺陷 4（P2，注释）**：`migration_isolation_test.rs:12-15` 措辞符合要求。
- **[✅] 缺陷 5（P2，RAII guard）**：`SchemaGuard` 在 `Drop` 中另起线程跑 `DROP SCHEMA CASCADE` 并 join；6 处用例全用 `SchemaGuard::new(...)`，无裸调 `drop_schema`。纯 std 实现。
- **[✅] 缺陷 6（P2，N-2 自动化）**：`n2_revoke_create_emits_table_name_in_error` 实现路径正确，断言 `err.to_string().contains("_sqlx_app_migrations")`；`migration_isolation_test` **6/6 PASS**。

#### 新增问题

- **[P2 / LOW]** 模块 9 task 文件 line 7 状态行未提 follow-up 字样，与 line 51 汇总口径轻微不一致。**不阻塞本批次放行**——按红线该口径回流由 Coordinator 闭环时同步主表。

#### 本轮总评

🟢 **批次 B 审查通过**：6 条 Round 1 缺陷全部 ✅，无残留 P1，无新增阻断；唯一遗留为 P2/LOW 文档口径小不一致，按红线不修改、留 Coordinator 闭环。关切 ①（helper 工程正确性）+ 关切 ② / ④（替换覆盖度 + 残余风险可追溯性）+ 关切 ③（脚本与文档闭环）本轮全部出清。

实跑回归：shared `migrate::` 8/8 / server `migration_isolation_test` 6/6 / `cargo test -p voice-room-server` 全套 0 fail，无回归。

---

### 批次 C（T-0000N + T-0000O follow-up）—— 第 1 轮审查（最终通过）

> 合并自原 `batch-e2e-foundation-followups.md`。审查范围：`d922e72..HEAD` 共 8 个 commit；实质代码改动仅 `d1454e0`（T-0000N）+ `b793252`（T-0000O），其余为 review / DoD / changelog 文档。

#### 1.C 审查上下文

- **包含任务**：T-0000N（AppServer / AdminServer 暴露 `/health` 端点）、T-0000O（`ranking_test::r08` perf flake known-issue 收口）。
- **关联 TDS**：[T-0000N](../tds/infra/T-0000N.md)、[T-0000O](../tds/infra/T-0000O.md)。
- 改动文件全集：`app/{server,adminServer}/src/bootstrap/mod.rs`、`app/{server,adminServer}/tests/health_endpoint_test.rs`、`app/server/tests/ranking_test.rs`、`doc/tds/infra/T-0000N.md`、`doc/tds/infra/T-0000O.md`、`doc/tests/known-issues.md`、`doc/tests/E2E_RUNBOOK.md`、`doc/arch/server/index.md`、`doc/arch/adminServer/index.md`、`doc/tasks/index.md`、`doc/tasks/模块9-…md`、`doc/review/模块9-E2E测试基建.md`。**未触动**主批次 13 任务的任何业务路径（路由表、模块边界、E2E 配置、scripts 主体均仅在 wait-on/preflight 行做了 `/ping`→`/health` 的最小切换）。

#### 2.C 审查关切

- 关切 ①：`/health` 端点工程正确性（schema、HTTP 语义、免鉴权位置、wait-on / preflight 真切换、文档同步）。
- 关切 ②：T-0000O perf flake 收口（`#[ignore]` 语法、known-issues 5 必填字段、RUNBOOK 链向、自动化护栏）。
- 关切 ③：批次合并风险（不动主批次代码、无横切 K8s readiness/liveness 契约偏移）。

#### 3.C 复核结论

**关切 ①（T-0000N）**：

- **Schema 一致性**（U-1/U-2）✅：双端均挂 `Router::new().route("/health", get(health))`，handler 共用 `HealthResponse { status, service, version }` 形状；`status="ok"` 静态、`service` 分别为 `"app-server"` / `"admin-server"`、`version` 来自 `env!("CARGO_PKG_VERSION")` 编译期注入；零 AppState 读取、零 DB/Redis/下游探测。
- **HTTP 语义**（N-1）✅：仅注册 `get(health)`；`POST /health` 自动 `405 Method Not Allowed`，集成测试 `post_health_returns_405_method_not_allowed` 已覆盖。
- **免鉴权位置**（U-3/U-4）✅：AppServer `build_app` 顶层不加 auth layer，鉴权由业务子模块按需挂载；AdminServer `audit_middleware` 仅命中 `POST .../ban|unban` 与 `DELETE .../rooms/{id}` 白名单（`audit.rs:64-79`），`/health` 不在白名单内必然旁路；`request_context_middleware` 不做鉴权决策。
- **wait-on / preflight 真切换**✅：`scripts/dev/e2e-up.sh:57-58` 切到 `http-get://127.0.0.1:3000/health` + `:3001/health`；`scripts/dev/preflight.sh:152,157` 切到 `${...}/health`；旧 `/ping` 在两脚本中均无残留。
- **文档同步**✅：`doc/arch/server/index.md:36` 与 `doc/arch/adminServer/index.md:254` 均新增 `/health` 契约描述（200 OK / 三字段 schema / 零鉴权零依赖 / 用途）。

**关切 ②（T-0000O）**：

- **`#[ignore]` 落点与语法**（U-1/U-3）✅：`ranking_test.rs:458` 在 `#[tokio::test]` 之后追加 `#[ignore = "perf flake; tracked by T-0000O"]`，cargo test 默认输出 `ignored, "perf flake; tracked by T-0000O"`；`-- --ignored` 显式启用；属性宏顺序与字符串字面量合法；测试体（100ms 阈值 / 测试逻辑）零改动。
- **known-issues.md 5 必填字段**（D-1）✅：新建并以 `<a id="r08"></a>` 显式锚点登记，5 字段完整且可机读，附「跟踪 Task: T-0000O」反向引用，反链双向闭环。
- **RUNBOOK 链向**（D-2）✅：`doc/tests/E2E_RUNBOOK.md:221-223` 新增 §7 Q9，相对路径 `./known-issues.md#r08` 与新建文件锚点匹配可达。
- **回归实测**：commit message 与 TDS 实现结果显示 TDD 已在本地确认 `cargo test -p voice-room-server` 默认 `10 passed; 0 failed; 1 ignored`。Reviewer 沙箱无 docker postgres/redis 未重跑；静态路径上 `#[ignore]` 必然生效，cargo 行为合约稳定。

**关切 ③（横切）**：

- **K8s readiness/liveness 契约对齐**：当前 `/health` 语义 = 「进程存活并能响应 HTTP」（liveness），零依赖故不区分 readiness。未来若引入 K8s 编排且需要 readiness（DB/Redis 连通）时新增 `/readyz` 与之分离 — 不属本批次义务，记入未来工作。
- **doc index 收纳**：`known-issues.md` 已被 RUNBOOK 直链；`doc/tests/index.md` 文件清单未追加新条目（见 P3-1）。

#### 4.C P3 观察（不阻塞放行）

- [ ] **P3-1**：`doc/tests/known-issues.md` 未登记入 `doc/tests/index.md` 的「文件清单」表。
  - **建议**：在表格中追加 `| [known-issues.md](./known-issues.md) | 测试套件已知 flake / 环境性问题登记册（5 字段模板） | T-0000O |`。
- [ ] **P3-2**：`#[ignore = "perf flake; tracked by T-0000O"]` 标签缺少自动化反向校验。
  - **建议**：可选在 `.github/workflows/*` 或 pre-commit hook 加 `grep -q "tracked by T-0000O" app/server/tests/ranking_test.rs` 检查；或在 T-0000O Phase 2 立项时一并交付。

#### 5.C 本轮结论

✅ **批次 C 审查通过**：T-0000N 与 T-0000O 在代码实现、测试覆盖、脚本切换、文档同步、批次隔离等所有关切维度均达标；仅余 2 条 P3 文档/护栏建议，不阻塞放行。

---

### 批次 D（T-0000P + T-0000Q 基建侧架构阻塞）—— 第 1 轮审查（最终通过）

> 合并自原 `batch-arch-blockers-infra.md`。审查范围：T-0000P 主提交 `efd66e7`、T-0000Q 提交链 `52306aa..691221b`，关联 [T-0000P TDS](../tds/infra/T-0000P.md)、[T-0000Q TDS](../tds/infra/T-0000Q.md)。

#### 1.D 审查上下文

- **批次定位**：QA 战报反向拆出的 6 个架构阻塞 Task 中，**基建侧 2 个**的合并审查批次。AppServer 业务侧 4 个（T-00041~44）在另一批次（[批次-架构阻塞修复.md](./批次-架构阻塞修复.md)）承接。
- **包含任务**：
  - **T-0000P**：Midscene env 注入链补齐（envLoader 双注入 `MIDSCENE_MODEL_API_KEY` + fallback `OPENAI_API_KEY` + `.env.{local,staging,prod}.example` 占位 + `.github/workflows/playwright.yml` Secret 注入 + 17 unit tests）
  - **T-0000Q**：`scripts/dev/e2e-up.sh` 端口冲突 preflight（5432/6379/3000/3001/5173 五端，跨平台 lsof/ss/netstat，彩色错误 + PID/进程名 + `kill -9` 提示）

#### 2.D 审查关切

- 关切 ①：T-0000P envLoader Midscene 双注入是否破坏 24 字段冻结契约 / sanitize 流程 / Playwright 进程环境？Midscene 缺失仅 warn 不抛错的策略是否会让 WEB 用例在 CI 上误绿？
- 关切 ②：T-0000P 安全红线（fixture / probe / .env.example 是否真实 Key 泄露；sanitize 是否对 `apiKey` / `_azureApiKey` 全部脱敏写盘）。
- 关切 ③：T-0000P 测试覆盖（17 unit tests + WEB smoke 降级理由是否合理）。
- 关切 ④：T-0000Q `e2e-up.sh` 端口检测的真生效性（lsof/ss/netstat 跨平台、性能 SLA、cleanup 健壮性）。
- 关切 ⑤：T-0000Q 单 Task 已列 3 个 Minor，批次复核是否升级阻断。
- 关切 ⑥：批次内一致性（目录归属、文档同步）。

#### 3.D 复核结论

**关切 ①** ✅ 通过：`envLoader.ts:114-274` 主入口 24 字段必填校验链未变动；Midscene 在 Step 3 独立读取并写入 `midscene` 子对象（既有契约字段，非新增）；新增的 `_azureEndpoint` / `_azureApiKey` 在 `types.ts:54-56` 标注 internal、下划线前缀 + 注释划清边界；`Object.freeze(env)` 仍生效。缺失策略 `envLoader.ts:148-155` 仅 `console.warn` 不进 `missing[]` 不抛错——与 §二既定方针（API/INFRA 测试不应被 WEB 凭据拖垮）一致。CI 误绿风险由 WEB 用例侧 skip 逻辑承担（不在本批次范围）。

**关切 ②** ✅ 通过：`git show efd66e7 | grep -oE "sk-[A-Za-z0-9_-]{20,}"` 命中仅为测试 fixture（`sk-test-*` 等，长度 < 20，无 base64 高熵段）；`.env.*.example` 三档全部为占位符；`sanitizeEnvForRuntimeJson`（`envLoader.ts:343-357`）正确清除 `midscene.apiKey` + `_azureEndpoint` + `_azureApiKey`，Edge 测试 L336-351 已断言原对象保留、副本脱敏；`scripts/dev/midscene-env-probe.ts` `maskApiKey` 仅露首尾 4 字符，<12 字符返回 `[EMPTY or TOO SHORT]`；`.github/workflows/playwright.yml` 仅引用 `secrets.MIDSCENE_MODEL_API_KEY` / `secrets.MIDSCENE_MODEL_BASE_URL`。

**关切 ③** ✅ 接受降级：17 unit tests 覆盖 U-1（直接命中 ×3 优先级层）、U-2（OPENAI_API_KEY fallback + 双注入 + 四层链）、U-3（缺失 warn 不抛）、U-4（baseUrl 透传 + undefined）、U-5（Azure 透传 + 不参与 fallback）、Edge（空字符串 fallback / staging profile / sanitize）；Edge L302-315 显式覆盖「`MIDSCENE_MODEL_API_KEY` 为空字符串 → fallback 到 OPENAI_API_KEY」隐蔽路径（`get()` L132-138 实现 `'' === undefined` 等价语义），是注入链最易翻车点位。WEB smoke 推迟理由（本地无 5 端依赖、CI Secret 待 DevOps 配置）成立；注入逻辑已被 unit 等价覆盖，且 probe 脚本支持 dry-run 验证。

**关切 ④** ✅ 通过（含 1 条 P2 风险记录）：`check-ports.sh:18-30` 跨平台分支结构清晰（macOS → `lsof -ti`，Linux → `ss` 优先 `netstat` fallback）；性能：每端口最多 1 次工具调用（5 端 ≤ 1s），未 spawn shell 子进程循环；`e2e-up.sh:31-32` 在 `docker compose up` **之前**调用，`set -e` 失败即退出，无残留容器。

**关切 ⑤** 保持 Minor 不升级：

- **Minor-1（`grep -oP` BusyBox 不可用）**：实现是 `if ss; elif netstat`（顺序 if，**不是 OR fallback**），ss 存在时不会 fallback 到 netstat。Alpine + iproute2（ss 已装但 grep 无 PCRE）场景下 `grep -oP` 静默返回空 → 端口被误判可用（**false negative**）。CI 为 `ubuntu-latest`（GNU grep 含 -P），dev 主战场 macOS（走 lsof 分支），生产命中概率低。**保留为 P2，不阻断**。
- **Minor-2（TDS §二 `declare -A` 与实现顺序检测不一致）**：实现采用 5 次显式 `check_port` 调用更鲁棒（兼容 bash 3.2 / macOS 默认）。文档未同步是 P3 文档债，不阻断。
- **Minor-3（变量未加引号）**：`$port` / `$pid` 均为纯数字或经 `[[ -n "$pid" ]]` 守卫，且 `IFS='|' read -r` 正确，无 word-splitting 风险。维持 P3。

**关切 ⑥** ✅ 通过：T-0000P 修改 `tests/scripts/support/envLoader.ts` + `scripts/dev/midscene-env-probe.ts`；T-0000Q 修改 `scripts/dev/check-ports.sh` + `scripts/dev/e2e-up.sh`。两 Task 在 `scripts/dev/` 共存但无 import / 文件冲突；TDS §四 / §五均落地。

#### 4.D P2/P3 观察（不阻塞放行）

- [ ] **观察 1 [P2]**：`check-ports.sh` Linux ss 分支的 `grep -oP` 在 BusyBox/Alpine 静默失败。
  - **修复建议**：将 `grep -oP 'pid=\K[0-9]+'` 替换为 POSIX 兼容写法（如 `awk -F'pid=' 'NF>1 {split($2,a,","); print a[1]}'`），并在 ss/netstat 都不可用时 fallback 到 `(echo > /dev/tcp/127.0.0.1/$port) 2>/dev/null` 风格。
- [ ] **观察 2 [P3]**：`e2e-up.sh:10` 头注释退出码声明与实际不符（注释「11~15」与实际 1 冲突）。
  - **修复建议**：注释改为「0 OK / 1 端口冲突（check-ports.sh）/ 78 envLoader CONFIG / 其他 = wait-on 超时」。
- [ ] **观察 3 [P3]**：`T-0000Q.md:46-52` `declare -A` 方案与实现的顺序检测未同步说明。
  - **修复建议**：在 §二末尾追加「实现注：本 Task 最终采用顺序 `check_port` 调用替代 `declare -A`，理由是兼容 macOS 默认 bash 3.2」。
- [ ] **观察 4 [P3]**：`midscene-env-probe.ts:12-18` `maskApiKey` 未区分空字符串与短字符串。
  - **修复建议**：拆分为 `length === 0 → [EMPTY]` / `length < 12 → [TOO SHORT]`。

#### 5.D 本轮结论

🟢 ✅ **批次 D 审查通过**：T-0000P 注入链架构正确、24 字段契约未破坏、安全红线 0 命中、unit 覆盖等价于真实路径，WEB smoke 降级理由成立；T-0000Q 跨平台检测、性能 SLA、`e2e-up.sh` 集成均落地；3 个既有 Minor 不升级为阻断（限定环境 + 已有兜底）。上述 4 条 P2/P3 观察作为技术债登记，不影响放行。

---

## 4. 模块 9 整体结论

| 批次 | 任务范围 | 最终状态 | 修复轮次 | 闭环日期 |
|---|---|---|---|---|
| 批次 A | 12 个任务（T-0000E/F/G/H/I/J/K/L + T-00040/T-10020/T-20020/T-30050） | ✅ Passed | 1/10 | 2026-04-27 |
| 批次 B | T-0000M（双服务共库 Migration 表隔离） | ✅ Passed | 1/10 | 2026-04-27 |
| 批次 C | T-0000N + T-0000O（`/health` 端点 + r08 perf flake 收口） | ✅ Passed | 1/10 | 2026-04-28 |
| 批次 D | T-0000P + T-0000Q（Midscene 注入链 + 端口冲突 preflight） | ✅ Passed | 1/10 | 2026-04-29 |

**模块 9 完成进度**：13 主任务 + 4 follow-up（T-0000N / T-0000O / T-0000P / T-0000Q）= **17/17 ✅ 全闭环**。
