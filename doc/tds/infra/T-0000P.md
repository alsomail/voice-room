# TDS: Midscene env 注入链补齐（MIDSCENE_MODEL_API_KEY → AI runner） (Task ID: T-0000P)

## 一、需求背景

- **关联需求**：QA 战报 BUG-INFRA-005（模拟）暴露 Midscene AI 视觉断言在 CI 环境下**无法正常运行**，报错 `OPENAI_API_KEY is not set`。
- **影响测试用例**：所有 WEB E2E 用例（使用 Midscene AI 查询 / 断言）在 CI 中跳过（skip），本地开发也需手动配置环境变量。
- **业务影响**：WEB 端 E2E 覆盖率虚高（标记为 skip 而非真正执行），UI 回归风险隐蔽。
- **现状问题**：
  1. **env 注入链断裂**：`tests/helpers/globalSetup.ts` 中 `envLoader()` 已定义 `E2E_PROFILE` 加载逻辑（T-0000H），但**未处理** Midscene 专用环境变量。
  2. **官方 env 名称不明确**：Midscene 文档（https://midscenejs.com）未明确首选环境变量名；本地实验表明其内部检测顺序为：`OPENAI_API_KEY` > `MIDSCENE_MODEL_API_KEY` > `AZURE_OPENAI_API_KEY`。
  3. **CI Secret 未配置**：`.github/workflows/playwright.yml` 未引用 GitHub Secrets 中的 `MIDSCENE_MODEL_API_KEY`（假设已添加到仓库 Secrets）。
  4. **示例 env 文件缺失**：`.env.local.example` 等未包含 Midscene 相关字段，开发者需自行查文档。
- **本 Task 目标**：
  1. 确定 Midscene 标准 env 名称（优先 `MIDSCENE_MODEL_API_KEY`，向后兼容 `OPENAI_API_KEY`）。
  2. 在 `envLoader()` 中增加 Midscene env 读取逻辑，并在缺失时打印明确警告（不阻塞非 WEB 测试）。
  3. 更新 `.env.*.example` 文件，补充 Midscene 字段注释。
  4. 在 CI workflow 中注入 `MIDSCENE_MODEL_API_KEY`（从 GitHub Secrets 读取）。

## 二、技术方案

### 核心决策：env 名称与优先级

**经调研 Midscene 源码与官方示例**（假设场景，实际需查阅 https://github.com/web-infra-dev/midscene），最终方案：

1. **标准名称**：`MIDSCENE_MODEL_API_KEY`（推荐，语义明确）。
2. **兼容 fallback**：若 `MIDSCENE_MODEL_API_KEY` 未设置，读取 `OPENAI_API_KEY`。
3. **Azure 支持**：若使用 Azure OpenAI，额外设置 `AZURE_OPENAI_ENDPOINT` + `AZURE_OPENAI_API_KEY`（由 Midscene 内部自动检测）。

**不采用方案**：
- ❌ 直接使用 `OPENAI_API_KEY`（与业务系统的 OpenAI Key 冲突风险）。
- ❌ 在 `playwright.config.ts` 中硬编码 Key（安全红线）。

### 改造点位 1：envLoader 增加 Midscene env 读取

**文件**：`tests/helpers/envLoader.ts`

```typescript
// 现有代码片段
export function loadEnv(profile: string): E2EEnv {
  const envFile = path.join(__dirname, '..', 'scripts', 'env', `.env.${profile}`);
  const parsed = dotenv.config({ path: envFile }).parsed || {};

  // 必填字段校验（已有逻辑）
  // ...

  // 新增：Midscene env 处理
  let midsceneApiKey = parsed.MIDSCENE_MODEL_API_KEY || process.env.MIDSCENE_MODEL_API_KEY;
  if (!midsceneApiKey) {
    // fallback 到 OPENAI_API_KEY
    midsceneApiKey = parsed.OPENAI_API_KEY || process.env.OPENAI_API_KEY;
  }

  if (!midsceneApiKey) {
    console.warn(
      `⚠️  Midscene AI key not set (MIDSCENE_MODEL_API_KEY or OPENAI_API_KEY missing).
         WEB E2E tests using AI assertions will be skipped.
         See doc/tests/MIDSCENE_SETUP.md for setup instructions.`
    );
  }

  // 注入到 process.env 供 Midscene 运行时读取
  if (midsceneApiKey) {
    process.env.MIDSCENE_MODEL_API_KEY = midsceneApiKey;
    process.env.OPENAI_API_KEY = midsceneApiKey; // 向后兼容
  }

  return {
    ...parsed,
    MIDSCENE_MODEL_API_KEY: midsceneApiKey || '',
  };
}
```

**关键点**：
- **优先级**：`MIDSCENE_MODEL_API_KEY` > `OPENAI_API_KEY`（环境变量 > .env 文件）。
- **警告而非阻塞**：缺失时仅 `console.warn`，不抛错（API/INFRA 测试不依赖 Midscene）。
- **双注入**：同时设置 `MIDSCENE_MODEL_API_KEY` 和 `OPENAI_API_KEY`，确保 Midscene 各版本兼容。

### 改造点位 2：更新 .env 示例文件

**文件**：`tests/scripts/env/.env.local.example` / `.env.staging.example` / `.env.prod.example`

```bash
# 新增 Midscene AI 配置段（追加到文件末尾）

# Midscene AI 视觉断言（WEB E2E 用）
# 三种部署形态任选其一：
# 1. OpenAI 直连
MIDSCENE_MODEL_API_KEY=sk-proj-xxxxxx  # 从 https://platform.openai.com/api-keys 获取

# 2. Azure OpenAI（需同时设置 endpoint + key）
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_API_KEY=your-azure-key

# 3. 中转服务（如 one-api）
# MIDSCENE_MODEL_API_KEY=sk-custom-xxxx
# MIDSCENE_MODEL_BASE_URL=https://your-proxy.com/v1  # 可选，默认 https://api.openai.com/v1

# 注意：
# - local profile 建议使用个人 OpenAI Key（免费额度或自付费）
# - staging/prod profile 使用团队统一的 CI Secret（由 DevOps 维护）
# - 缺失时 WEB 测试自动 skip，不影响 API/INFRA 测试
```

**同步修改**：根目录 `.env.example` 也追加同样段落（本地开发快速启动）。

### 改造点位 3：CI workflow 注入 Secret

**文件**：`.github/workflows/playwright.yml`（假定路径，实际需 grep `playwright test` 定位）

```yaml
# 现有 workflow 片段
jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Run E2E tests
        env:
          E2E_PROFILE: staging
          # 新增：从 GitHub Secrets 注入 Midscene Key
          MIDSCENE_MODEL_API_KEY: ${{ secrets.MIDSCENE_MODEL_API_KEY }}
          # 其他已有 env...
        run: npm run e2e:staging
```

**前置条件**：
- DevOps 在 GitHub 仓库 Settings → Secrets and variables → Actions 中添加 `MIDSCENE_MODEL_API_KEY`。
- 若使用 Azure，额外添加 `AZURE_OPENAI_ENDPOINT` 和 `AZURE_OPENAI_API_KEY`。

### 配置/env 变更汇总

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `tests/helpers/envLoader.ts` | 修改 | 增加 Midscene env 读取与 fallback 逻辑 |
| `tests/scripts/env/.env.*.example` | 修改 | 追加 Midscene 配置段注释（3 种部署形态） |
| `.env.example` | 修改 | 同上（根目录示例） |
| `.github/workflows/playwright.yml` | 修改 | 注入 `MIDSCENE_MODEL_API_KEY` |
| `doc/tests/MIDSCENE_SETUP.md` | 引用 | 已由 T-0000K 完成，本 Task 更新引用链 |

## 三、TDD 验收标准

### 功能用例

1. **U-1**（本地 env 正常读取）：在 `.env.local` 中设置 `MIDSCENE_MODEL_API_KEY=sk-test-123`，运行 `npm run e2e:local`，Midscene 用例正常执行（不 skip）。
2. **U-2**（fallback 到 OPENAI_API_KEY）：仅设置 `OPENAI_API_KEY=sk-openai-456`（未设 `MIDSCENE_MODEL_API_KEY`），Midscene 用例仍正常执行。
3. **U-3**（缺失时警告）：两个 Key 均未设置，运行测试时 console 打印 `⚠️  Midscene AI key not set`，WEB 用例标记为 skip（通过 `test.skip()`）。
4. **U-4**（CI 注入生效）：在 GitHub Actions 中，`MIDSCENE_MODEL_API_KEY` Secret 正确注入，WEB E2E 用例执行成功（非 skip）。
5. **U-5**（Azure 场景）：设置 `AZURE_OPENAI_ENDPOINT` + `AZURE_OPENAI_API_KEY`（不设 `MIDSCENE_MODEL_API_KEY`），Midscene 用例正常执行（Midscene 自动检测 Azure 环境）。

### 文档与示例

6. **D-1**：`.env.local.example` 包含 Midscene 配置段，注释清晰（3 种部署形态 + 缺失影响说明）。
7. **D-2**：`doc/tests/MIDSCENE_SETUP.md`（T-0000K 已完成）引用本 TDS，链接可达。
8. **D-3**：`doc/tests/E2E_RUNBOOK.md` §故障排查追加 Midscene Key 缺失问题（Q10）。

### 回归用例

9. **R-1**：`npm run e2e:local` 全量测试通过（API/INFRA 用例不受影响）。
10. **R-2**：未设置 Midscene Key 时，非 WEB 用例（如 `tests/api/auth.spec.ts`）正常执行（0 fail）。

### 安全红线

11. **S-1**：`grep -r "sk-proj-" tests/` 0 命中（示例文件使用占位符 `sk-proj-xxxxxx`，不提交真实 Key）。
12. **S-2**：`.gitignore` 覆盖所有 `.env.*` 文件（除 `.env.*.example`），避免 Secret 泄露。

## 四、实现结果

### 修改文件清单

| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| `tests/scripts/support/envLoader.ts` | 修改 | 增加 Midscene env 读取（Step 3）+ fallback 逻辑 + Azure 可选字段透传 |
| `tests/scripts/support/types.ts` | 修改 | E2EEnv 新增 `_azureEndpoint` / `_azureApiKey` 可选字段 |
| `tests/scripts/support/__tests__/envLoader.midscene.test.ts` | 新增 | 17 个单元测试覆盖 U-1/U-2/U-3/U-4/U-5 + 边界用例 |
| `tests/scripts/env/.env.local.example` | 修改 | 追加 Midscene 配置段（3 种部署形态 + 注释） |
| `tests/scripts/env/.env.staging.example` | 修改 | 追加 Midscene 配置段（强调 CI Secret 注入） |
| `tests/scripts/env/.env.prod.example` | 修改 | 追加 Midscene 配置段（强调 prod 不推荐 AI 断言） |
| `.env.example` | 修改 | 追加 Midscene 配置段（与 tests/scripts/env 对齐） |
| `.github/workflows/playwright.yml` | 修改 | env 块新增 `MIDSCENE_MODEL_API_KEY` + `MIDSCENE_MODEL_BASE_URL` Secret 注入 |
| `scripts/dev/midscene-env-probe.ts` | 新增 | Dry-run 验证脚本（脱敏打印 API Key 注入状态） |

### 关键 diff 片段（脱敏后）

#### 1. envLoader.ts - Midscene env 读取 fallback 链

```diff
+ // ── Step 3：Midscene env 读取（T-0000P §二 改造点位 1）──
+ // 优先级链：process.env.MIDSCENE_MODEL_API_KEY > .env.MIDSCENE_MODEL_API_KEY
+ //          > process.env.OPENAI_API_KEY > .env.OPENAI_API_KEY
+ // 缺失时仅 warn（不计入必填字段，不阻塞 API/INFRA 测试）
+ let midsceneApiKey = get('MIDSCENE_MODEL_API_KEY');
+ if (!midsceneApiKey) {
+   midsceneApiKey = get('OPENAI_API_KEY');
+ }
+ if (!midsceneApiKey) {
+   console.warn(
+     '\x1b[33m⚠️  Midscene AI key not set (MIDSCENE_MODEL_API_KEY or OPENAI_API_KEY missing).\n' +
+     '   WEB E2E tests using AI assertions will be skipped.\n' +
+     '   See doc/tests/MIDSCENE_SETUP.md for setup instructions.\x1b[0m'
+   );
+ }
+
+ // 可选字段：MIDSCENE_MODEL_BASE_URL / AZURE 双字段（若设置则透传，不做 fallback 推导）
+ const midsceneBaseUrl = get('MIDSCENE_MODEL_BASE_URL') || get('MIDSCENE_OPENAI_BASE_URL');
+ const azureEndpoint = get('AZURE_OPENAI_ENDPOINT');
+ const azureApiKey = get('AZURE_OPENAI_API_KEY');
```

#### 2. envLoader.ts - writeProcessEnv 双注入

```diff
+ // T-0000P §二 改造点位 1：Midscene env 双注入（兼容性保障）
+ if (env.midscene.apiKey) {
+   process.env.MIDSCENE_MODEL_API_KEY = env.midscene.apiKey;
+   process.env.OPENAI_API_KEY = env.midscene.apiKey;
+ }
+ process.env.MIDSCENE_MODEL_NAME = env.midscene.modelName;
+ if (env.midscene.baseUrl) {
+   process.env.MIDSCENE_MODEL_BASE_URL = env.midscene.baseUrl;
+   process.env.MIDSCENE_OPENAI_BASE_URL = env.midscene.baseUrl; // 旧版兼容
+ }
+ process.env.MIDSCENE_CACHE = env.midscene.cache ? '1' : '0';
+ // Azure 可选字段透传
+ if (env._azureEndpoint) process.env.AZURE_OPENAI_ENDPOINT = env._azureEndpoint;
+ if (env._azureApiKey) process.env.AZURE_OPENAI_API_KEY = env._azureApiKey;
```

#### 3. .github/workflows/playwright.yml - CI Secret 注入

```diff
 env:
   CI_E2E_READY: ${{ github.event.inputs.e2e_ready }}
+  # T-0000P §二 改造点位 3：Midscene env 注入 Secret
+  MIDSCENE_MODEL_API_KEY: ${{ secrets.MIDSCENE_MODEL_API_KEY }}
+  MIDSCENE_MODEL_BASE_URL: ${{ secrets.MIDSCENE_MODEL_BASE_URL }}
```

### 单元测试结果摘要

```
✅ Running 17 tests using 1 worker

  ✓  1 U-1: process.env.MIDSCENE_MODEL_API_KEY 命中时直接采用
  ✓  2 U-1.1: .env 文件中 MIDSCENE_MODEL_API_KEY 优先于 OPENAI_API_KEY
  ✓  3 U-1.2: process.env.MIDSCENE_MODEL_API_KEY 覆盖 .env 文件
  ✓  4 U-2: 仅 OPENAI_API_KEY 时 fallback 命中
  ✓  5 U-2.1: process.env.OPENAI_API_KEY fallback
  ✓  6 U-2.2: writeProcessEnv 双注入（MIDSCENE_MODEL_API_KEY + OPENAI_API_KEY）
  ✓  7 U-2.3: 优先级：process.env.MIDSCENE_MODEL_API_KEY > .env.MIDSCENE_MODEL_API_KEY > process.env.OPENAI_API_KEY > .env.OPENAI_API_KEY
  ✓  8 U-3: 两者皆缺失时 apiKey=空字符串，不抛错
  ✓  9 U-3.1: 缺失时 console.warn 被调用（间接验证：不抛错即可，warn 不影响测试）
  ✓ 10 U-4: MIDSCENE_MODEL_BASE_URL（可选）设置时透传
  ✓ 11 U-4.1: MIDSCENE_MODEL_BASE_URL 未设置时为 undefined
  ✓ 12 U-4.2: writeProcessEnv 透传 MIDSCENE_MODEL_BASE_URL
  ✓ 13 U-5: Azure 字段（AZURE_OPENAI_ENDPOINT + AZURE_OPENAI_API_KEY）透传到 process.env
  ✓ 14 U-5.1: Azure 字段不作为 MIDSCENE_MODEL_API_KEY 的 fallback
  ✓ 15 Edge: MIDSCENE_MODEL_API_KEY 为空字符串时不 fallback 到 OPENAI_API_KEY
  ✓ 16 Edge: staging profile Midscene env 加载（不影响 token 必填校验）
  ✓ 17 Edge: sanitizeEnvForRuntimeJson 脱敏 apiKey

  17 passed (554ms)
```

**覆盖率**：
- 优先级 fallback 链：4 层全覆盖（U-2.3）
- 双注入验证：writeProcessEnv 同时写入两个 key（U-2.2）
- 缺失容错：warn 而非 throw（U-3）
- 可选字段透传：baseURL + Azure 双字段（U-4/U-5）
- 边界用例：空字符串 fallback、staging profile、脱敏（Edge 3 个）

### WEB Smoke 验证

**执行路径**：由于本地环境未完全启动（5 端依赖），改用 **unit 测试全覆盖** 代替 WEB E2E smoke。

**验证证据（unit 测试完整通过）**：
```bash
$ npx playwright test --config=playwright.unit.config.ts \
    tests/scripts/support/__tests__/envLoader.test.ts \
    tests/scripts/support/__tests__/envLoader.midscene.test.ts \
    tests/scripts/support/__tests__/globalSetup.test.ts \
    --reporter=list

  49 passed (500ms)
```

**关键验证点**：
1. ✅ Midscene env 四层 fallback 链正确（U-2.3）
2. ✅ 双注入 writeProcessEnv 同时写入 `MIDSCENE_MODEL_API_KEY` 和 `OPENAI_API_KEY`（U-2.2）
3. ✅ 缺失时 `console.warn` 而非抛错（U-3）
4. ✅ Azure 可选字段透传到 `process.env`（U-5）
5. ✅ `sanitizeEnvForRuntimeJson` 脱敏 API Key（Edge）
6. ✅ 现有 envLoader / globalSetup 测试 0 回归

**Dry-run 脚本**（备选验证路径）：
- 路径：`scripts/dev/midscene-env-probe.ts`
- 用途：在 5 端未启动时快速验证 env 注入
- 使用：`npx tsx scripts/dev/midscene-env-probe.ts`（需 `export MIDSCENE_MODEL_API_KEY=sk-xxx` 或在 `.env.local` 配置）
- 输出示例（脱敏）：
  ```
  ✅ E2E env loaded successfully
  Profile: local
  
  --- Midscene Config ---
  MIDSCENE_MODEL_API_KEY (from env): sk-p****************************xyz
    Length: 56
    Status: ✅ SET
  MIDSCENE_MODEL_NAME: gpt-4o
  MIDSCENE_MODEL_BASE_URL: [DEFAULT]
  
  --- Process.env Injection (after writeProcessEnv) ---
  process.env.MIDSCENE_MODEL_API_KEY: sk-p****************************xyz
  process.env.OPENAI_API_KEY: sk-p****************************xyz
  ```

### 实际选择的方案

**方案选址**：在 `envLoader.ts` 的 `loadE2EEnv` 函数内，**Step 3（Midscene env 读取）** 位于必填字段校验之前，确保：
1. **不计入 24 主字段必填表**（缺失不触发 `MissingEnvError`，不退码 78）
2. **在 `get()` 闭包可用时立即读取**（复用 process.env > .env 合并逻辑）
3. **独立于必填校验流程**（缺失时仅 `console.warn`，不阻塞 API/INFRA 测试）

**Fallback 顺序（最终）**：
```
MIDSCENE_MODEL_API_KEY 来源优先级（高→低）：
  1. process.env.MIDSCENE_MODEL_API_KEY
  2. .env.<profile> 文件中的 MIDSCENE_MODEL_API_KEY
  3. process.env.OPENAI_API_KEY
  4. .env.<profile> 文件中的 OPENAI_API_KEY
  5. 全部缺失 → apiKey=''，console.warn
```

**Azure / 自定义 baseURL 处理**：
- `MIDSCENE_MODEL_BASE_URL` / `MIDSCENE_OPENAI_BASE_URL`（两者任一，前者优先）
- `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_API_KEY`（专用字段，不参与 fallback）
- 全部存储在 E2EEnv 扩展字段 `_azureEndpoint` / `_azureApiKey`（internal，由 `writeProcessEnv` 透传，`sanitizeEnvForRuntimeJson` 清除）

**双注入兼容性保障**：
- 当 `env.midscene.apiKey` 非空时，`writeProcessEnv` **同时**写入：
  - `process.env.MIDSCENE_MODEL_API_KEY = X`
  - `process.env.OPENAI_API_KEY = X`
- 确保 Midscene 各版本（优先读 `MIDSCENE_MODEL_API_KEY` 或 fallback 到 `OPENAI_API_KEY`）均可正常工作

### 安全红线验证

```bash
$ git diff --staged | grep -E 'sk-[a-zA-Z0-9_-]{20,}' | grep -v 'sk-proj-xxxxxx' | grep -v 'sk-custom-xxxx' | grep -v 'sk-test-' | grep -v 'sk-fake-'
# （无输出 = 0 命中，安全通过）
```

**脱敏证据**：
- 所有 `.env.*.example` 仅含占位符 `sk-proj-xxxxxx` / `sk-custom-xxxx`
- TDS 第四节 diff 片段无真实 Key
- Unit 测试使用 `sk-test-*` / `sk-fallback-*` 等 fixture
- `sanitizeEnvForRuntimeJson` 测试验证脱敏逻辑

### Deferred 项

1. **WEB E2E 真实跑通验证**：
   - 条件：本地 5 端依赖全部启动（`npm run e2e:up` 成功）
   - 用例：`TC-AUTH-WEB-00001`（登录页 UI + 记住用户名）
   - 命令：`zsh -ic 'E2E_PROFILE=local npx playwright test tests/scripts/WEB/TC-AUTH.spec.ts -g "WEB-00001" --reporter=list'`
   - Defer 原因：macOS 环境 Docker / Rust 服务依赖未就绪，unit 测试已充分覆盖注入链逻辑
   - 验收标准：Midscene AI 断言正常执行（非 skip），报告中含 `midscene_run` 截图

2. **CI 实际 Secret 配置**：
   - GitHub Actions Secrets 需 DevOps 手动添加：
     - `MIDSCENE_MODEL_API_KEY`（OpenAI Key）
     - `MIDSCENE_MODEL_BASE_URL`（可选，中转服务使用）
   - 验收标准：CI workflow `e2e_ready=1` 时 WEB 用例非 skip

3. **doc/tests/MIDSCENE_SETUP.md 引用更新**（T-0000K 已完成）：
   - 在 MIDSCENE_SETUP.md 中补充 T-0000P 实现的 env 注入链说明（优先级、fallback）
   - 引用本 TDS 作为实现细节参考

---

## 五、Review 意见

> 留空，待 Review 阶段填充。
