<!--
[AI 写入规约]
1. Plan Agent 创建。
-->

# TDS: 协议治理铁律落地后 — 历史 TDS 协议字段表全量回填 (Task ID: T-00107)

## 一、背景与目标

- **关联需求**：T-00100 落锚 schemas/ 后，所有历史 TDS 仅有「路径级绑定表」（T-0000U 已回填），但**字段级**仍多处失锚。本 Task 借助 T-00106 升级后的字段级审计脚本扫描，找出字段级缺锚 TDS，按优先级（Server WS > REST > Pub/Sub > Web > Android）批量回填字段表引用 schemas/。
- **本 Task 的目标**：审计脚本字段级 0 缺失。

## 二、方案设计

### 涉及文件清单

| 文件 | 变更 | 说明 |
|------|------|------|
| `doc/tds/**/T-*.md` | 批量修改 | 在第二节绑定表下加「字段表锚点：[schemas/ws/MicTaken.schema.json](../../protocol/schemas/ws/MicTaken.schema.json)」 |
| `doc/review/batch-tds-field-binding-backfill.md` | 新增 | 批次记录 |

### 🔌 协议路径绑定表

> N/A — 本 Task 无跨端协议路径，为纯文档清理任务。

## 三、TDD 验收用例

- [ ] T-00106 字段级审计脚本 0 缺失。
- [ ] 所有历史 TDS 第二节存在字段表锚点链接 OR 显式 `N/A` 声明。

## 四、实现结果

### 新增 / 修改文件

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `doc/tds/infra/T-00100.md` | 修改 | N/A 文本改为匹配 `NA_PATTERNS` 的标准格式 |
| `doc/tds/server/T-00103.md` | 修改 | N/A 文本改为匹配 `NA_PATTERNS` 的标准格式 |
| `doc/tds/infra/T-00106.md` | 修改 | N/A 文本改为匹配 `NA_PATTERNS` 的标准格式 |
| `doc/tds/infra/T-00107.md` | 修改 | N/A 文本改为匹配 `NA_PATTERNS` 的标准格式 |
| `doc/tds/adminServer/T-00105.md` | 修改 | 绑定表 client/server 路径补全 `app/` 前缀；补充 schema.json 锚点 |
| `doc/tds/web/T-00102.md` | 修改 | 绑定表 server 路径补全 `app/` 前缀与函数名 |
| `doc/tds/infra/T-00104.md` | 重构 | 非标准表格重构为标准格式（19 行，含完整 schema.json 链接） |
| `scripts/audit/protocol-binding-audit.ts` | 修改 | 修复 3 处 bug：①`\\.route(` 未闭合括号；②新增 adminServer 到服务端 grep；③新增 Web 独立函数与 adminServer Rust 函数到客户端 grep |
| `scripts/audit/validate-tds-field-binding.sh` | 新增 | T-00107 验收脚本，P0=0 且 P1=0 时 exit 0 |

### 测试结果

```
✅ Audit PASSED: No P0 issues. 0 P1 warning(s).
   154 TDS files scanned, 40 bindings found, P0=0, P1=0
```

## 五、Reviewer意见

### Round 1
- **🔴 Changelog 行**：

## 六、DoD 完成记录

- [ ] `doc/tasks/index.md` 标记 ✅ Done
