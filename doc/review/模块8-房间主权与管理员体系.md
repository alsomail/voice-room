# 全局代码审查报告: 模块 8 - 房间主权与管理员体系 (E-10)
> **当前状态机**：负责人 [TDD] | 状态 [❌ Failed] | 修复轮次 [1/10]

---

## 0. 流转规则
- **状态枚举**：负责人 [-] 状态 [✅ Passed] | 负责人 [TDD] 状态 [❌ Failed] | 负责人 [GlobalReview] 状态 [⏳ In Review]
- 每轮 Review 追加一条记录，不要覆盖历史。
- 处于负责人 [GlobalReview] 状态 [⏳ In Review]，则由[GlobalReview]进行全局代码审查
- [GlobalReview]审查通过，则修改负责人 [-] 状态 [✅ Passed]
- [GlobalReview]审查未通过，则修改负责人 [TDD] 状态 [❌ Failed], 并将审查意见填入文档下方
- 处于负责人 [TDD] 状态 [❌ Failed]，则由[TDD]根据审查意见进行代码修复并自测
- [TDD]修复之后，将状态改为负责人 [GlobalReview] 状态 [⏳ In Review]

---

## 1. 审查上下文
- **包含任务**：[模块 8: 房间主权与管理员体系 (E-10)](../tasks/模块8-房间主权与管理员体系%20(E-10).md)
  - App Server：T-00024 / T-00025 / T-00026 / T-00027 / T-00028 / T-00029 / T-00030
  - Admin Server：T-10016
  - Web：T-20014
  - Android：T-30036 / T-30037 / T-30038 / T-30039 / T-30040 / T-30041 / T-30042 / T-30043 / T-30044
- **关联 TDS**：`doc/tds/{server,adminServer,web,android}/` 对应 ID
- **产品规范**：`doc/product/phase1_room_governance.md`、`doc/product/business_flows.md §2.8`、§2.3 权限矩阵
- **开始时间**：2026-04-25

---

## 2. 审查与修复日志

*(执行规则：GlobalReview 记录缺陷，TDD 在对应缺陷下方记录修复方案与 PR/Commit。严禁覆盖历史记录，只能向下追加)*

### 【第 1 轮审查】 - 2026-04-25
**@GlobalReview 审查意见：**

本轮审查覆盖 18 个任务（T-00024 ~ T-00030 / T-10016 / T-20014 / T-30036 ~ T-30044）。结合迁移脚本、Service 层、WS 信令、Admin/Web/Android 端进行交叉对照后，发现 **4 个 P0** 与 **3 个 P1**、**1 个 P2** 级别缺陷，其中 #1 与 #2 直接导致整个治理体系在生产环境中**完全失效**（数据写不进库、重启即丢、跨实例不共享），必须立即返工。

- [ ] **缺陷 1**：[级别 P0] **生产 main.rs 未注入治理模块的真实 Repo / Redis 实现，全部使用 Fake***
  - **文件与行号**：
    - `app/server/src/main.rs:97-112`（仅调用 `with_room_password_redis`，未调用 `with_kick_redis / with_kick_audit_db / with_mute_redis / with_mute_db / with_transfer_admin_repo`）
    - `app/server/src/bootstrap/mod.rs:100-107` `new()` 与 `:147-154` `new_with_managers()` 默认装配 `FakeKickRedis / FakeKickAuditDb / FakeMuteRedis / FakeMuteDb / FakeTransferAdminRepo`
  - **问题说明**：生产二进制启动后，**模块 8 的所有持久化与分布式语义都失效**：
    1. **Kick 冷却（5 min）只存在内存中**：服务重启或多实例部署后，被踢用户立刻可重新进房（违反 T-00028 #4）。
    2. **`room_kick_records` 永远写不进库**：管理员/治理日志查询（T-10016 / T-20014）100% 返回空（违反 T-00028 #5、T-10016 #1）。
    3. **`room_mute_records`、Mute Redis Key 同样仅存在内存中**：Mute 在 SendMessage / TakeMic 拦截会随重启失效，离线/其他节点 Sub 也无法识别 Mute。
    4. **TransferAdmin 数据库 `rooms.admin_user_id` 永远不会被更新**：管理员任命只活在 HashMap 里，重启即丢；`/rooms/{id}` GET 也读不到（违反 T-00030 #4）。
  - **修复建议**：
    1. 在 `app/server/src/modules/governance/{kick,mute,transfer}.rs` 中实现 `RealKickRedis / RealKickAuditDb (PgKickAuditDb) / RealMuteRedis / RealMuteDb (PgMuteDb) / PgTransferAdminRepo`，使用 `redis::Client` + `sqlx::PgPool`。
    2. 在 `app/server/src/main.rs` 中显式调用 `state.with_kick_redis(...).with_kick_audit_db(...).with_mute_redis(...).with_mute_db(...).with_transfer_admin_repo(...)`。
    3. 同时建议 `bootstrap::new()` 中的 Fake 默认值改为 `unimplemented!()` 守护，避免再次漏接线。
  - **TDD 修复记录**：
    - **修复方案**：
      1. 在 `app/server/src/modules/governance/{kick,mute,transfer}.rs` 与 `room/password.rs` 的 `Real*` 实现既已存在；本批次将其在生产路径接线。
      2. 重构 `bootstrap::AppState::new_with_managers()` 签名：从 13 参数扩展为 20 参数，**强制要求注入** `room_password_redis / kick_redis / kick_audit_db / mute_redis / mute_db / mic_lock / transfer_admin_repo` 七个生产仓储；删除内部 Fake 默认值兜底逻辑（无法再"忘记接线"）。
      3. 把 `AppState::new()` 的可见性收敛为 `#[cfg(any(test, feature = "test-utils"))]`，生产 release 二进制不再可达任何 Fake* 默认装配。
      4. `app/server/src/main.rs` 在启动期构造 `RealKickRedis::new(redis_url)? / RealKickAuditDb::new(pool.clone()) / RealMuteRedis::new(redis_url)? / RealMuteDb::new(pool.clone()) / RealMicLock::new(redis_url)? / RealTransferAdminRepo::new(pool.clone())`，使用 `?` 透传错误实现 fail-fast；移除原 `.with_room_password_redis(..)` 链式调用，改为构造时一次性注入。
      5. 新增 `app/server/tests/governance_real_repos_test.rs` 5 项集成测试（GR01–GR05）覆盖 PG round-trip + Redis TTL，与 `events_batch_test.rs` 同骨架（`DATABASE_URL/REDIS_URL` 缺省自动 skip），本地 `vr-postgres+vr-redis` 环境 5/5 通过。
    - **文件清单**：
      - `app/server/src/main.rs`
      - `app/server/src/bootstrap/mod.rs`
      - `app/server/Cargo.toml`（新增 `mute_user_test / transfer_admin_test / force_mic_test / governance_real_repos_test` 四个 `[[test]] required-features=["test-utils"]` 条目）
      - `app/server/tests/governance_real_repos_test.rs`（新增）
    - **验证结果**：`cargo build --release` ✅；`cargo test --features test-utils --lib` 431/431 ✅；`cargo test --features test-utils --tests` 全部通过；带 `DATABASE_URL/REDIS_URL` 跑 `governance_real_repos_test` 5/5 通过。
    - **Commit**：`a69116461a9c478eb6b0d6c0a7cb425ab85e708c`

- [ ] **缺陷 2**：[级别 P0] **`room_mute_records` SQL 列名与迁移不一致，Mute 写入 / 查询全部 panic**
  - **文件与行号**：
    - 迁移：`app/server/migrations/008_room_governance.sql:51` 列名为 `type VARCHAR(8) ... CHECK (type IN ('mic','chat'))`
    - 写入：`app/server/src/modules/governance/mute.rs:421-423` SQL 使用 `INSERT INTO room_mute_records (..., mute_type, ...)`
    - 查询：`app/adminServer/src/modules/governance/repo.rs:338, 354, 362` 在 COUNT/SELECT 中引用 `m.mute_type`
  - **问题说明**：列名 `mute_type` 在物理表中根本不存在。一旦上面缺陷 1 修复并接入真实 Postgres，**任何禁麦/禁言操作和 Admin 查询禁言日志都会返回 `column "mute_type" does not exist` 运行时错误**，整个 T-00029 / T-10016 mute 链路被这一字段名 typo 直接打死。
  - **修复建议**：统一为 `type`（注意 `type` 不是 PG 关键字，但若担心可用 `"type"` 双引号显式引用）；同时检查 ORM 结构体字段映射并补回 SQL 行级测试覆盖。
  - **TDD 修复记录**：
    - **修复方案**：选择**列名向代码侧对齐**（`type` → `mute_type`），原因有二：(a) Rust 写入 (`mute.rs:421`) 与 Admin 读取 (`repo.rs:338,354,362`) 已使用 `mute_type`，改 5 处 SQL 比改 3 处 SQL 风险更小；(b) 避免日后再有人忘记 `"type"` 双引号引用。
      1. 重写 `migrations/008_room_governance.sql:51` `CHECK (mute_type IN ('mic','chat'))`、`:57` 索引 `idx_mute_records_target_type_ts(target_user_id, mute_type, created_at DESC)` 与文件头 S24-05 注释。
      2. 新增 `migrations/009_align_mute_type_column.sql`：用 `DO ... IF EXISTS old AND NOT EXISTS new ... THEN RENAME COLUMN ...` 幂等 ALTER 处理已部署 008 的环境，并 DROP/ADD 重建 `chk_mute_type` CHECK + 必要时 CREATE 索引。
      3. 移除 `app/shared/src/models/governance.rs` 上 `RoomMuteRecord.mute_type` 字段的 `#[sqlx(rename = "type")]`，因列名已与字段名直接匹配（保留 `#[sqlx(type_name="varchar", rename_all="lowercase")]`）。
      4. 更新 `app/server/tests/room_governance_schema_test.rs::s24_05_mute_type_check_excludes_invalid_values` 断言迁移文本含 `CHECK (mute_type IN ('mic','chat'))`。
      5. **真实 PG round-trip 验证**：新增 `governance_real_repos_test.rs::gr02_real_mute_db_uses_mute_type_column`，对真实 Postgres 调 `RealMuteDb.insert_mute_record(...,"mic",...)` 后再 `SELECT mute_type FROM room_mute_records WHERE room_id=$1` 回读，本地 vr-postgres 通过。
    - **文件清单**：
      - `app/server/migrations/008_room_governance.sql`
      - `app/server/migrations/009_align_mute_type_column.sql`（新增）
      - `app/shared/src/models/governance.rs`
      - `app/server/tests/room_governance_schema_test.rs`
      - `app/server/tests/governance_real_repos_test.rs`（新增 GR02）
    - **验证结果**：`cargo test --test room_governance_schema_test` 23/23 ✅；带 `DATABASE_URL` 跑 GR02 通过；adminServer 的 `m.mute_type` SQL 在新 schema 下编译 + 列存在。
    - **Commit**：`a69116461a9c478eb6b0d6c0a7cb425ab85e708c`

- [ ] **缺陷 3**：[级别 P0] **测试用 Fake* 结构体未加 `#[cfg(any(test, feature = "test-utils"))]`，泄漏到生产二进制**
  - **文件与行号**：
    - `app/server/src/modules/room/password.rs:175` `FakeRoomPasswordRedis` / `FakeEntry`
    - `app/server/src/modules/governance/kick.rs:110` `FakeKickRedis`、`:190` `KickRecord`、`:198` `FakeKickAuditDb`
    - `app/server/src/modules/governance/mute.rs:155` `FakeMuteRedis`、`:265` `FakeMuteDb`
    - `app/server/src/modules/governance/transfer.rs:53` `FakeTransferAdminRepo`、`:92` `FailingTransferAdminRepo`
  - **问题说明**：模块 1–7 已确立强制规则——所有 `Fake*` 必须 `#[cfg(any(test, feature = "test-utils"))]` 守门，禁止编入 release 二进制。当前模块 8 的 Fake 结构体均为 `pub` 且无 `cfg` 限制，配合缺陷 1 直接被生产 `main.rs` 调用——这是缺陷 1 能够"无声生效"的根本原因。即便缺陷 1 修复，这些 Fake 仍会被编入产物增加二进制体积、暴露内部实现并污染 doc。
  - **修复建议**：在每个 `pub struct Fake*`、`pub struct Failing*`、其内部辅助 struct（如 `KickRecord`、`FakeEntry`）以及 `impl ... for Fake*` 的代码块上添加 `#[cfg(any(test, feature = "test-utils"))]`；如需在集成测试用，请在 `Cargo.toml` 启用 `test-utils` feature。
  - **TDD 修复记录**：
    - **修复方案**：
      1. 在所有 Fake/Failing 结构体定义、`impl Default`、`#[async_trait] impl Trait for Fake*` 代码块以及它们专用的 helper struct（`KickRedisEntry / KickRecord / MuteRedisEntry / MuteRecord / FakeEntry`）上批量加 `#[cfg(any(test, feature = "test-utils"))]`。
      2. 同步把仅被 Fake 使用的 std 导入（`std::collections::HashMap`、`std::sync::Mutex`、`std::time::Instant`）也 cfg 守门，避免 release 编译产生 unused-import 警告。
      3. `app/server/src/modules/room/mod.rs` 的 `pub use FakeRoomPasswordRedis` 与 `pub use FakeRoomRepository` 重导出同样 cfg 守门。
      4. `bootstrap::AppState::new()` 整体 cfg 收敛到 `#[cfg(any(test, feature = "test-utils"))]`（其内部唯一引用 Fake 的代码路径），生产链路只能通过新签名 `new_with_managers(...)` 进入；移除其中残留的 `FakeTransferAdminRepo::default()` 默认值（参见缺陷 1 修复方案 #2）。
      5. `Cargo.toml` 给 `mute_user_test / transfer_admin_test / force_mic_test / governance_real_repos_test` 显式声明 `required-features = ["test-utils"]`，配合既有 `kick_user_test`、`password_room_test` 形成完整入口约束。
    - **文件清单**：
      - `app/server/src/modules/governance/kick.rs`
      - `app/server/src/modules/governance/mute.rs`
      - `app/server/src/modules/governance/transfer.rs`
      - `app/server/src/modules/room/password.rs`
      - `app/server/src/modules/room/mod.rs`
      - `app/server/src/bootstrap/mod.rs`
      - `app/server/Cargo.toml`
    - **验证结果**：
      - `cargo build --release` ✅ 无 Fake 符号链入。
      - `grep -nE "Fake[A-Z]" app/server/src/main.rs` 0 处命中，确认生产入口已无 Fake 引用。
      - `cargo test --lib`（默认 feature）431/431 通过；`cargo test --features test-utils --lib` 亦 431/431。
      - `cargo test --features test-utils --tests` 全部通过（含 `kick_user_test / mute_user_test / transfer_admin_test / force_mic_test / password_room_test`）。
    - **Commit**：`a69116461a9c478eb6b0d6c0a7cb425ab85e708c`

- [ ] **缺陷 4**：[级别 P0] **Android 模块 8 大量硬编码中文，缺失 `values-ar` 翻译，违反 i18n 强制约定**
  - **文件与行号**（均为新增文件，未走 `stringResource`）：
    - `app/android/app/src/main/java/com/voice/room/android/feature/room/governance/UserKickedDialog.kt:72,81-84,89,108`：标题"你已被移出房间"、Reason 映射"发送垃圾内容/骚扰其他用户/..."、正文模板"原因：%s"、按钮"知道了"
    - `app/android/app/src/main/java/com/voice/room/android/feature/room/governance/MuteStatusChip.kt:42,74`：Label "禁麦"/"禁言"、提示"你已被$label..."
    - `app/android/app/src/main/java/com/voice/room/android/feature/room/governance/KickReasonDialog.kt:59,69-72,98,114,134`：标题"踢出原因"、4 个 preset、占位"请输入原因"、"取消"/"确认"
    - `app/android/app/src/main/java/com/voice/room/android/feature/room/components/AudienceBottomSheet.kt:81,99,114`："观众席"/"麦上"/"观众"
    - `app/android/app/src/main/java/com/voice/room/android/feature/room/components/PasswordInputDialog.kt:138`："密码错误，剩余 X 次"
    - `app/android/app/src/main/java/com/voice/room/android/feature/room/components/UserActionBottomSheet.kt:119-120,156-166`：角色徽标"👑 房主"/"🛡️ 管理员"、9 项操作"任命管理员/卸任管理员/抱上麦/抱下麦/禁麦/禁言/踢出房间/查看资料/举报"
    - `app/android/app/src/main/res/values-ar/strings.xml`：缺失上述键的所有阿语翻译
  - **问题说明**：项目级强制约束（参见 `doc/conventions/i18n.md`、模块 1–7 历轮 Review 结论）要求**所有用户可见文案必须经 `R.string` 抽取并提供 `values-ar/` 真翻译（不接受机翻/Latin 残留）**。当前实现等价于 `app:locale=ar` 时仍展示中文，对中东市场（Phase1 主目标用户群）发布即不可用。
  - **修复建议**：
    1. 抽取上述全部硬编码文案到 `values/strings.xml`，命名空间统一 `room_governance_*` / `room_audience_*` / `room_user_action_*`。
    2. 提供 `values-ar/strings.xml` 的真实阿语翻译（必要时同步更新 `values-en/`）。
    3. 在 Compose 侧改用 `stringResource(R.string.xxx)` 与 `pluralStringResource`（剩余 X 次场景）。
    4. 加 lint：禁止 Compose `Text("中文")` 字面量（参考已有 ktlint baseline）。
  - **TDD 修复记录**：
    - **批 2 修复方案**（2025 R1 批 2，仅做 i18n 抽取与 values-ar 真翻译，不改业务行为/testTag）：
      1. `app/android/app/src/main/res/values/strings.xml`：新增 4 个命名空间的字符串与 plurals 资源（共 30 项）：
         - `room_governance_*`（被踢弹窗 / 禁麦禁言 Chip / 踢人原因弹窗 / 通用确认取消）：`room_governance_kicked_title` / `room_governance_kicked_reason_{spam,harassment,abuse,default}` / `room_governance_kicked_body_format` / `room_governance_kicked_acknowledge` / `room_governance_mute_chip_{mic,chat}_format` / `room_governance_kick_dialog_title` / `room_governance_kick_reason_label_{harassment,spam,abuse,other}` / `room_governance_kick_custom_placeholder` / `room_governance_btn_{cancel,confirm}`，以及 `<plurals name="room_governance_kicked_cooldown_minutes">`。
         - `room_audience_*`（观众席）：`room_audience_title_format` / `room_audience_section_on_mic_format` / `room_audience_section_observers_format` / `room_audience_empty`。
         - `room_user_action_*`（用户操作菜单）：`room_user_action_role_owner` / `room_user_action_role_admin` / `room_user_action_{assign_admin,revoke_admin,force_take_mic,force_leave_mic,mute_mic,mute_chat,kick,view_profile,report}`。
         - `room_password_*`（密码弹窗）：`room_password_dialog_title` / `room_password_btn_{confirm,cancel}` 与 `<plurals name="room_password_error_attempts">`（"剩余 X 次"用 `pluralStringResource`）。
      2. `app/android/app/src/main/res/values-ar/strings.xml`：为上述全部键提供阿拉伯语真翻译；两组 plurals 提供 `zero/one/two/few/many/other` 全量类别（CLDR 阿语规则），不复制中文亦不机翻。
      3. 6 个 Compose 文件全部改用 `stringResource(R.string.xxx)` 与 `pluralStringResource(R.plurals.xxx)`：
         - `UserKickedDialog.kt`：标题 / Reason 映射 / "原因+冷却分钟数"（`room_governance_kicked_body_format` 内嵌 `pluralStringResource` 渲染的冷却文案）/ "知道了"。
         - `MuteStatusChip.kt`：移除原 `label = if(...) "禁麦" else "禁言"` 字面量，按 muteType 选取格式串资源 ID，再 `stringResource(formatRes, timeText)`。
         - `KickReasonDialog.kt`：标题 / 4 个 Radio Label / `OutlinedTextField` placeholder（最大长度参数化 `KICK_REASON_MAX_LENGTH=100`）/ 取消 / 确认。
         - `AudienceBottomSheet.kt`：观众席总数标题、麦上区/观众区 SectionHeader、空状态文案。
         - `PasswordInputDialog.kt`：标题、Error 态 `pluralStringResource(R.plurals.room_password_error_attempts, n, n)`、Locked 态沿用既有 `password_locked_minutes`、确认/取消按钮。
         - `UserActionBottomSheet.kt`：将 `UserAction.toDisplayName()` 改为 `@Composable` 扩展函数返回 `stringResource(R.string....)`；房主/管理员角色徽标也改用 `stringResource`。
      4. testTag 全部保持不变（`dialog_kicked` / `btn_kicked_ack` / `mute_countdown_mic` / `mute_countdown_chat` / `kick_reason_dialog` / `kick_reason_$index` / `kick_custom_input` / `btn_cancel_kick` / `btn_confirm_kick` / `audience_sheet` / `audience_header_on_mic` / `audience_header_observers` / `password_dialog` / `password_input` / `password_digit_$i` / `password_error_text` / `btn_submit_password` / `user_action_sheet` / `user_action_${action.name}`），UI 测试与单测继续通过 `Key('btn_confirm_kick')` 等触达。
    - **新增 string 键清单**（共 30 项资源）：
      - `room_governance_kicked_title` / `room_governance_kicked_reason_spam` / `room_governance_kicked_reason_harassment` / `room_governance_kicked_reason_abuse` / `room_governance_kicked_reason_default` / `room_governance_kicked_body_format` / `room_governance_kicked_acknowledge`
      - `room_governance_mute_chip_mic_format` / `room_governance_mute_chip_chat_format`
      - `room_governance_kick_dialog_title` / `room_governance_kick_reason_label_harassment` / `room_governance_kick_reason_label_spam` / `room_governance_kick_reason_label_abuse` / `room_governance_kick_reason_label_other` / `room_governance_kick_custom_placeholder` / `room_governance_btn_cancel` / `room_governance_btn_confirm`
      - `room_audience_title_format` / `room_audience_section_on_mic_format` / `room_audience_section_observers_format` / `room_audience_empty`
      - `room_user_action_role_owner` / `room_user_action_role_admin` / `room_user_action_assign_admin` / `room_user_action_revoke_admin` / `room_user_action_force_take_mic` / `room_user_action_force_leave_mic` / `room_user_action_mute_mic` / `room_user_action_mute_chat` / `room_user_action_kick` / `room_user_action_view_profile` / `room_user_action_report`
      - `room_password_dialog_title` / `room_password_btn_confirm` / `room_password_btn_cancel`
      - plurals：`room_governance_kicked_cooldown_minutes`、`room_password_error_attempts`
    - **验证结果**：
      - `./gradlew :app:testDebugUnitTest` ✅ BUILD SUCCESSFUL（无新增警告）。
      - 6 个目标文件红线 grep（"你已被|已踢出|剩余|分钟|管理员|普通用户|举报|麦上|观众|确认|取消|禁麦|禁言|抱上麦|抱下麦|踢出|房主|骚扰|刷屏|辱骂|其他|知道了|请输入|密码错误"）：剩余命中均为 KDoc 注释 / testTag / WS payload key 字符串（"mic"/"chat"/"owner"/"admin" 等英文字面量，属允许的 key 字符串）。
      - testTag 0 改动，所有 Compose 测试入口（`Key('btn_confirm_kick')` / `Key('btn_kicked_ack')` / `Key('password_error_text')` 等）保持可达。
    - **Commit**（批 2）：`a21fbfd`（代码 + i18n 资源 + 文档骨架）

- [ ] **缺陷 5**：[级别 P1] **`broadcast_room_info_updated` 绕过统一广播出口，丢失 `msg_id` 与 `recent_broadcasts` 入栈**
  - **文件与行号**：`app/server/src/ws/broadcaster.rs:96-126`
  - **问题说明**：
    - 该函数手动构造 envelope 后直接 `registry.broadcast`，未调用 `broadcast_to_room`。
    - 因此 `RoomInfoUpdated` envelope **没有 `msg_id` 字段**，并且**未写入 `RoomState.recent_broadcasts`**。
    - 而 WS 协议 §6.7 明确将 `RoomInfoUpdated` 列为可广播类型，并要求支持 `last_msg_id` 重连补发；前端 Web/Android 在断线重连时无法补回房间元数据更新，业务上会出现"封面/公告/类目改完后部分客户端不刷新"的偶发现象。
  - **修复建议**：将广播路径改为 `crate::ws::broadcast::broadcast_to_room(room_state, "RoomInfoUpdated", payload)` 统一出口，由其负责注入 `msg_id` 并写入 `recent_broadcasts`。
  - **TDD 修复记录**：[等待 TDD 填写修复逻辑与 Commit ID]

- [ ] **缺陷 6**：[级别 P1] **治理日志 CSV 导出端到端缺失（违反 T-20014 #4 与 T-10016 #5 验收）**
  - **文件与行号**：
    - Web：`app/web/src/features/governance/GovernanceLogsPage.tsx`（无 CSV 按钮、无下载逻辑）
    - Admin Server：`app/adminServer/src/modules/governance/handler.rs`（仅有 `/kicks`、`/mutes` 分页接口，无 `/export.csv` 端点）
  - **问题说明**：
    - T-10016 验收 #5 明确要求"CSV 导出正确编码 UTF-8 BOM"——当前 Admin Server 完全没有该端点。
    - T-20014 验收 #4 明确要求"导出 CSV 含 UTF-8 BOM 表头与全部筛选条件下的记录"——当前 Web 端没有按钮也没有 fetch。
    - 缺失该能力将导致运营无法向监管方/合规岗导出操作审计记录（合规 Phase1 必备）。
  - **修复建议**：
    1. Admin Server 新增 `GET /admin/governance/logs.csv?op=&room_id=&from=&to=`，响应 `Content-Type: text/csv; charset=utf-8`，body 首字节写入 `\xEF\xBB\xBF` BOM；列头与字段口径与现有 `/kicks` `/mutes` 一致；建议复用 `csv` crate；记得带 `audit_logger.log_action("governance.export_csv", ...)`。
    2. Web 端在 `GovernanceLogsPage` 增加"导出 CSV"按钮，点击后将当前筛选条件（包含 tab）拼入 query 调用 Admin Server，使用 `Blob` + `URL.createObjectURL` 触发下载，文件名 `governance-logs-${range}.csv`。
    3. 加 e2e：导出文件首三字节为 `EF BB BF`，含正确表头。
  - **TDD 修复记录**：[等待 TDD 填写修复逻辑与 Commit ID]

- [ ] **缺陷 7**：[级别 P1] **`UserKicked` 与多个 `*Result` 信令未注入 `msg_id`，前端无法去重 / 重放**
  - **文件与行号**：
    - `app/server/src/modules/governance/kick.rs:510-524` `UserKicked` 点对点下发，envelope 无 `msg_id`
    - 同文件 / `mute.rs` / `transfer.rs` / `force_mic.rs` 中各 `*_error` / `*_success` Result 也未填 `msg_id`
  - **问题说明**：协议 §6.3 规定所有出站 envelope 必须携带 `msg_id`（点对点亦然，便于客户端去重 ACK）；§6.7 列出的 `UserKicked` 属可重放类型。当前实现导致：
    1. Android 客户端连接抖动重连时，可能重复弹两次"你已被踢出"对话框；
    2. 服务端无法基于 `msg_id` 做幂等审计；
    3. 客户端 `processed_msg_ids` 集合不工作。
  - **修复建议**：所有出站消息一律走统一构造器（建议提取 `build_outbound_envelope(type, payload)` 帮助函数），自动 `Uuid::new_v4()` 注入 `msg_id` + 服务器 ts。
  - **TDD 修复记录**：[等待 TDD 填写修复逻辑与 Commit ID]

- [ ] **缺陷 8**：[级别 P2] **`*Result` 错误体格式与协议 §6.3 不一致（flat `code/message` vs nested `payload`）**
  - **文件与行号**：`kick.rs:480` `kick_error/kick_success`、`mute.rs:580` `mute_error/mute_success`、`transfer.rs` 与 `force_mic.rs` 同类
  - **问题说明**：协议 §6.3 约定 Result envelope 为 `{ type, msg_id, code, payload: { ...业务字段 / message } }`。当前实现把 `code` / `message` / `timestamp` 平铺在 envelope 顶层，与既有 `MicResult` / `GiftSendResult` 风格不一致，前端解码器分支变多。
  - **修复建议**：统一使用 `payload: { code, message? }`（或将 message 放进 payload，code 留顶层并去掉冗余字段），与既有出站结果统一。
  - **TDD 修复记录**：[等待 TDD 填写修复逻辑与 Commit ID]

---

**审查通过项（无需修复）**：
- 迁移 `008_room_governance.sql`：表/索引齐全，使用 `IF NOT EXISTS` 满足幂等。
- `validator.rs`：6 位密码正则、公告字符长度、封面白名单全部正确。
- `password.rs`：`bcrypt::hash` 与 `bcrypt::verify` 均通过 `tokio::task::spawn_blocking` 卸载到阻塞线程池；`SET NX EX` 防重锁正确。
- `lifecycle.rs::JoinRoom`：踢人冷却检查（步骤 2.5）+ 密码 access_token claim 校验（步骤 3）顺序与拒绝码合理。
- `members_service.rs`：先做权限校验、内存快照排序后再批量 `find_users_by_ids`，无 N+1，分页 clamp 正确。
- `transfer.rs` / `force_mic.rs`：仅房主可任命/卸任管理员；管理员不能强制房主下麦；广播走 `broadcast_to_room`。
- AdminServer 治理 service：调用 `audit_logger.log_action` 记录 query 操作，命中合规审计要求。
- `BoundedMsgIdSet` / `recent_broadcasts` 容量上限正确，无内存泄漏风险。

**本轮结论**: ❌ 存在 **P0 × 4 / P1 × 3 / P2 × 1**。重点是 #1（生产未接真实 Repo）与 #2（SQL 列名不一致）共同导致整个 E-10 在生产环境**完全失效**且无任何错误日志预警，必须优先修复并补集成测试（Postgres + Redis 真实容器）覆盖端到端 happy path 与冷却场景。

*(请在文档头部将状态机修改为：`负责人 [TDD] | 状态 [❌ Failed] | 修复轮次 [1/10]`)*
