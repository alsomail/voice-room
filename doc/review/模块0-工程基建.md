# 全局代码审查报告: 模块 0 - 工程基建 (Infrastructure & Shared)
> **当前状态机**：负责人 [-] | 状态 [✅ Passed] | 修复轮次 [1/10]

---

## 0. 流转规则
- **状态枚举**：负责人 [-] 状态 [✅ Passed] | 负责人 [TDD] 状态 [❌ Failed] | 负责人 [GlobalReview] 状态 [⏳ In Review]
- 每轮 Review 追加一条记录，不要覆盖历史。
- 处于负责人 [GlobalReview] 状态 [⏳ In Review]，则由[GlobalReview]进行全局代码审查
- [GlobalReview]审查通过，则修改负责人 [-] 状态 [✅ Passed]
- [GlobalReview]审查未通过，则修改负责人 [TDD] 状态 [❌ Failed], 并将审查意见填入文档下方
- 处于负责人 [TDD] 状态 [❌ Failed]，则由[TDD]根据审查意见进行代码修复并自测
- [TDD]修复之后，将状态改为负责人 [GlobalReview] 状态 [⏳ In Review]

---

## 1. 审查上下文
- **包含任务**：[模块 0: 工程基建 (Infrastructure & Shared)](../tasks/模块0-工程基建%20(Infrastructure%20&%20Shared).md)
  - T-0000A Docker Compose 开发环境
  - T-0000B 共享 crate (shared/)
  - T-0000C 数据库权限隔离
  - T-0000D CI 基础流水线
- **关联 TDS**：
  - [T-0000A](../tds/infra/T-0000A.md)
  - [T-0000B](../tds/infra/T-0000B.md)
  - [T-0000C](../tds/infra/T-0000C.md)
  - [T-0000D](../tds/infra/T-0000D.md)
- **开始时间**：2026-04-25

---

## 2. 审查与修复日志

*(执行规则：GlobalReview 记录缺陷，TDD 在对应缺陷下方记录修复方案与 PR/Commit。严禁覆盖历史记录，只能向下追加)*

### 【第 1 轮审查】
**@GlobalReview 审查意见：**

本轮审查覆盖 4 个任务的实际产物：
- T-0000A：`docker-compose.yml`、`scripts/dev/init-db.sh`、`.env.example`、`.gitignore`
- T-0000B：`Cargo.toml`（workspace 根）、`app/shared/`（lib + models/jwt/error/crypto/types/auth 共 21 个文件）、`rust-toolchain.toml`
- T-0000C：`scripts/dev/grant-permissions.sql`、`scripts/dev/verify-permissions.sh`
- T-0000D：`.github/workflows/ci.yml`

**审查要点逐项核对**：
1. **架构红线（DDD/分层）**：`app/shared/Cargo.toml` 仅依赖 serde / sqlx / jsonwebtoken / bcrypt / chrono / uuid / thiserror，未引入 axum、tokio、tracing 等 server-only 框架；`src/` 目录仅包含 Models（POD + `FromRow`）、JWT 工具、错误码、密码哈希、UUID 类型别名、room access 鉴权工具，无业务流程编排。**未发现 shared 引入业务依赖的违规。** ✅
2. **凭证与敏感信息**：`docker-compose.yml`、`init-db.sh`、`verify-permissions.sh` 均通过 `${VAR:-default}` / `${VAR:?required}` 从环境注入，`.env.example` 在 `.gitignore` 范围内，未发现硬编码生产凭证。✅
3. **JWT 安全**：`decode_token` 强制 `set_issuer(&[expected_iss])`，新增 `wrong_iss_returns_error` 测试；`room_access.rs` 进一步将 `leeway = 0`、`exp = iat + 60`、独立 ISS `voiceroom-room-access`，跨角色伪造路径已闭合。`exp/iat` 类型为 `u64`，无 2038 风险。✅
4. **bcrypt**：使用 `DEFAULT_COST`（cost=12），`verify_password` 错误密码返回 `Ok(false)` 已有用例覆盖。✅
5. **错误码**：`ErrorCode` 数值与 `doc/protocol/index.md` §1.4 编号空间一致（40xxx/40101/40102/42901…），并有 `error_code_numeric_values_match_protocol` 单测固化。✅
6. **数据库权限**：`grant-permissions.sql` 对 `users` 仅授予 SELECT/INSERT/UPDATE（无 DELETE/TRUNCATE，符合软删除约定），显式 `REVOKE` `admins` / `admin_logs`，并补充了 `ALTER DEFAULT PRIVILEGES` 防止后续 migration 漏授；`verify-permissions.sh` 覆盖 8 项正反用例 + 幂等。最小权限原则达成。✅
7. **CI 门禁**：`fmt --check`、`clippy -D warnings`、`cargo test --workspace` 三道门禁齐全；release 构建已剥离；`timeout-minutes` 已设；`SQLX_OFFLINE=true` 防 CI 连库；Web 端 `lint` + `typecheck` + `build` 完整。`pull_request` + `push:main` 双触发覆盖。无 secrets 泄露风险（workflow 未引用任何 `${{ secrets.* }}`）。✅
8. **代码体量**：shared crate 最大文件 `models/room.rs` 401 行（纯模型 + 文档注释），无超大函数 / 深嵌套。✅

**P2 建议（非阻断）**：
- [ ] **建议 1**：[级别 P2] **`init-db.sh` heredoc 内通过 `'$APP_SERVER_PASS'` 单引号插值密码，若未来生产密码包含单引号会导致 SQL 语法错误或注入。** 当前 `.env.example` 默认值无特殊字符，dev 可接受；上生产前请改为 psql `\set` 变量绑定或预先用 `\047` / 双单引号转义。对应任务：T-0000A。（前一轮已知遗留，登记追踪）
- [ ] **建议 2**：[级别 P2] **CI 未运行前端 `vitest` 用例。** `app/web/package.json` 已声明 `"test": "vitest run"`，但 `web-check` job 仅做 lint/typecheck/build，单元测试不进入门禁。建议在 `npm run typecheck` 之后追加 `- name: Unit test\n  run: npm test`。对应任务：T-0000D。

**本轮结论**: ✅ 审查通过：4 个任务均符合架构总纲与编码红线，无 P0/P1 级缺陷。两项 P2 仅作改进建议，不阻断流转。
*(已将状态机修改为：`负责人 [-] | 状态 [✅ Passed] | 修复轮次 [1/10]`)*

---

