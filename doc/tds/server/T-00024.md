<!--
[AI 写入规约]
-->

# TDS: rooms 表扩字段 + 治理审计表迁移 (Task ID: T-00024)

## 一、背景与目标
- **关联需求**：E-10 房间主权基座 ([phase1_room_governance §4](../../product/phase1_room_governance.md))
- **本 Task 的目标**：扩展 rooms 表字段（封面/分类/密码/公告/管理员），新建 `room_kick_records` 与 `room_mute_records` 审计表。

## 二、方案设计

### 迁移脚本 008_room_governance.sql
```sql
-- rooms 扩展字段（幂等）
ALTER TABLE rooms
    ADD COLUMN IF NOT EXISTS cover_url       TEXT NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS category        VARCHAR(32) NOT NULL DEFAULT 'chat',
    ADD COLUMN IF NOT EXISTS password_hash   VARCHAR(60),
    ADD COLUMN IF NOT EXISTS announcement    TEXT,
    ADD COLUMN IF NOT EXISTS admin_user_id   UUID REFERENCES users(id);

-- category 枚举约束
ALTER TABLE rooms
    DROP CONSTRAINT IF EXISTS chk_room_category,
    ADD CONSTRAINT chk_room_category
        CHECK (category IN ('chat','emotion','music','game','matchmaking','other'));

-- 治理审计表
CREATE TABLE IF NOT EXISTS room_kick_records (
    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    room_id          UUID NOT NULL REFERENCES rooms(id),
    target_user_id   UUID NOT NULL REFERENCES users(id),
    operator_user_id UUID NOT NULL REFERENCES users(id),
    reason           TEXT,
    created_at       TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_kick_records_room_ts ON room_kick_records(room_id, created_at DESC);
CREATE INDEX idx_kick_records_target_ts ON room_kick_records(target_user_id, created_at DESC);

CREATE TABLE IF NOT EXISTS room_mute_records (
    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    room_id          UUID NOT NULL REFERENCES rooms(id),
    target_user_id   UUID NOT NULL REFERENCES users(id),
    operator_user_id UUID NOT NULL REFERENCES users(id),
    type             VARCHAR(8) NOT NULL CHECK (type IN ('mic','chat')),
    duration_sec     INT NOT NULL CHECK (duration_sec >= 0), -- 0 = 解除
    reason           TEXT,
    created_at       TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_mute_records_room_ts ON room_mute_records(room_id, created_at DESC);
CREATE INDEX idx_mute_records_target_type_ts ON room_mute_records(target_user_id, type, created_at DESC);
```

### shared crate 更新
| 文件 | 变更 |
|------|------|
| `app/shared/src/models/room.rs` | `RoomModel` 新增 `cover_url`, `category`, `password_hash: Option<String>`, `announcement: Option<String>`, `admin_user_id: Option<Uuid>` |
| `app/shared/src/models/governance.rs` | 新增 `RoomKickRecord`, `RoomMuteRecord`, `MuteType` |

### 存量兼容
- `cover_url DEFAULT ''` 空串代表无封面，老房间不受影响
- `category DEFAULT 'chat'` 让老行自动归类为"闲聊"
- `admin_user_id NULL` 老行默认无管理员

### 涉及文件清单
| 文件 | 类型 | 说明 |
|------|------|------|
| `app/server/migrations/008_room_governance.sql` | 新增 |  |
| `app/shared/src/models/room.rs` | 修改 | 扩展 |
| `app/shared/src/models/governance.rs` | 新增 |  |
| `app/shared/src/models/mod.rs` | 修改 | 导出 |
| `app/server/tests/room_governance_schema_test.rs` | 新增 | S01~S06 |

## 三、TDD 验收用例
- [ ] **S24-01** 迁移可重入执行两次无报错
- [ ] **S24-02** category='invalid' 被 CHECK 拒绝
- [ ] **S24-03** 存量房间迁移后 cover_url=''、category='chat'
- [ ] **S24-04** room_kick_records (room_id, created_at) 索引存在
- [ ] **S24-05** room_mute_records.type='sms' 被 CHECK 拒绝
- [ ] **S24-06** 软删房间仍可被外键引用（或 ON DELETE CASCADE 视策略；默认 RESTRICT 不删即可）

## 四、实现结果
- **实际修改的文件**：
- **测试覆盖率**：
- **完成时间**：

## 五、Reviewer意见
### Round 1
- **状态**：
- **审查意见**：
- **改进建议**：
