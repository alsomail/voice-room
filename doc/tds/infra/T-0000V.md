# T-0000V — architecture/websocket_and_state §8.2 重写 + ARCHITECTURE.md 物理删除归档

> **版本**: v1.0  
> **创建日期**: 2026-05-06  
> **归属模块**: 模块9 - E2E测试基建  
> **优先级**: P2  
> **预估工时**: 3h

## 一、任务背景

### 1.1 问题来源

BUG-CHAT-WS Round 16 暴露了协议漂移的系统性根因：项目中存在两个事实源：
- `ARCHITECTURE.md`（已废弃但未物理删除）
- `doc/architecture/` 目录（当前维护的架构文档）

同时，`doc/architecture/websocket_and_state.md §8.2` 可能包含过时的 JSON 信令格式示例（`APPLY_SEAT`/`SEAT_UPDATED` 等旧格式），未能清晰指向 `doc/protocol/index.md` 作为唯一权威定义。

### 1.2 影响范围

- `ARCHITECTURE.md`：废弃文件，经 v2.83 版本添加废弃横幅后等待物理删除
- `doc/architecture/websocket_and_state.md §8.2`：信令格式定义章节需重写指向单一事实源
- `doc/adr/`：需新增 ADR-0002 沉淀协议治理铁律的架构决策

### 1.3 背景历史

v2.83（2026-05-05）在 `ARCHITECTURE.md` 添加废弃横幅，同时重写了 `websocket_and_state §8.2` 指向 `protocol/`，但未完成物理删除和 ADR 记录。本 Task 完成这最后一步。

---

## 二、协议路径绑定表

> **N/A — 纯文档清理任务，不涉及跨端 API/WS 路径变更**

本 Task 仅处理文档层面的清理与重组，不新增、不修改任何 API 端点或 WebSocket 信令路径。无需填写协议路径绑定表。

---

## 三、验收标准

| ID | 验收条件 | 验证方式 |
|----|---------|---------|
| AC-01 | `ARCHITECTURE.md` 文件不存在（已物理删除） | `[ ! -f doc/ARCHITECTURE.md ]` |
| AC-02 | `doc/adr/ADR-0002-protocol-single-source-and-binding-table.md` 存在且内容完整 | `[ -f doc/adr/ADR-0002-protocol-single-source-and-binding-table.md ]` |
| AC-03 | `doc/architecture/websocket_and_state.md §8.2` 明确指向 `doc/protocol/index.md`，无过时 JSON 信令格式 | grep 检查 §8.2 内容 |
| AC-04 | 项目中无对废弃 ARCHITECTURE.md 的悬空引用（ADR-0002 及本 TDS 自身的历史记录除外） | `grep -rn "doc/ARCHITECTURE\.md" . --include="*.md" \| grep -v ".git" \| grep -v "ADR-0002" \| grep -v "T-0000V"` |

---

## 四、实现说明

### 4.1 引用处理策略

删除废弃的 `ARCHITECTURE.md` 前，将所有引用它的文件更新为指向对应的 `doc/architecture/` 子文件：
- `doc/architecture/index.md` 第 3 行注释 → 更新为已完成物理删除（移除"待物理删除"措辞）
- `doc/tds/infra/T-0000C.md` 中的 `ARCHITECTURE.md §7` → 历史性引用，更新为 `doc/architecture/api_and_auth.md`

### 4.2 §8.2 重写原则

v2.83 已完成基础重写，本次做最终强化：
- 保留架构层面的描述（信令流转机制、状态机设计原则）
- 确认已删除具体 JSON 信令格式示例
- 强化单一事实源声明，明确指向 `doc/protocol/index.md` 和 `doc/protocol/websocket_signals.md`

### 4.3 ADR-0002 结构

参照 `doc/adr/ADR-0001-migration-table-isolation.md` 格式，包含：
- 状态、背景、决策内容（D-1/D-2/D-3 三条）、结果与影响、反例参考

---

## 五、实现结果

完成于 2026-05-06：

1. **悬空引用处理**：
   - `doc/architecture/index.md` 第 3 行：移除 `doc/ARCHITECTURE.md` 精确路径，改为历史说明并链接 ADR-0002
   - `doc/tds/infra/T-0000C.md`：将 `ARCHITECTURE.md §7` 引用改为 `doc/architecture/api_and_auth.md`
2. **§8.2 增强**：在 `doc/architecture/websocket_and_state.md §8.2` 唯一契约源声明中增加 `doc/protocol/index.md` 的直接引用（原仅指向 `websocket_signals.md`）
3. **ADR-0002 新增**：`doc/adr/ADR-0002-protocol-single-source-and-binding-table.md`，包含 D-1/D-2/D-3 三条决策及 BUG-CHAT-WS Round 16 反例
4. **物理删除**：`doc/ARCHITECTURE.md` 已删除，`git rm` 记录入 TDD commit

---

## 六、Review 意见

**审查日期**: 2026-05-06  
**结论**: ✅ **通过**

### 审查要点

1. **ADR-0002 内容准确性** ✅
   - 背景基于真实项目历史（v2.83 废弃横幅、BUG-CHAT-WS Round 16 真实发生）
   - D-1/D-2/D-3 三条决策清晰，每条有可操作标准
   - 反例参考基于 `cf899bd` commit 真实记录

2. **§8.2 修改质量** ✅
   - 保留了架构层语义描述（信令流转机制、状态机概念）
   - 无任何 JSON 格式示例（`APPLY_SEAT`/`SEAT_UPDATED` 仅出现于"禁止"说明语境）
   - 新增 `doc/protocol/index.md` 的直接引用，从 "仅指向 websocket_signals.md" 升级为同时锚定 "protocol/index.md（顶层入口）"

3. **引用处理完整性** ✅
   - AC-04 验证通过：无悬空 `doc/ARCHITECTURE.md` 路径引用（ADR-0002 + T-0000V 自身历史文档除外）
   - `doc/architecture/index.md` 历史记录已更新为已完成物理删除的措辞

4. **TDS 结构完整性** ✅
   - 包含背景（1.1/1.2/1.3）、协议绑定表（N/A 声明）、验收标准、实现说明、实现结果
