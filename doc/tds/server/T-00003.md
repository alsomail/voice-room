<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建，TDD Agent 只读不写。
2. 创建后必须在 doc/Tasks.md 对应 Task 的行内补充链接：[TDS](./tds/server/T-00003.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: 手机号一键登录接口 (Task ID: T-00003)

## 一、背景与目标
- **关联需求**：product.md - 手机号一步登录
- **前置依赖**：T-00002 (短信验证码发送接口)
- **本 Task 的目标**：实现 POST `/api/v1/auth/login` 一步登录接口，验证码正确后登录，手机号不存在自动注册

## 二、方案设计
### 核心数据流
Client → POST /api/v1/auth/login {phone, code} → Controller → AuthService → Redis HGET sms:code:{phone} 校验 → 校验通过 → 查 users 表 → 不存在则 INSERT (自动注册) → shared::jwt::encode_jwt() → 返回 {token, user, is_new} → DEL sms:code:{phone}

### 涉及文件清单
| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `app/server/src/modules/auth/controller.rs` | 修改 | 增加 login handler |
| `app/server/src/modules/auth/service.rs` | 修改 | 登录 + 自动注册逻辑 |
| `app/server/src/modules/auth/repository.rs` | 新增 | users 表 CRUD |
| `app/server/src/modules/auth/dto.rs` | 新增 | LoginRequest, LoginResponse |
| `app/shared/src/jwt/encode.rs` | 引用 | JWT 签发 |

### 接口 / 信令定义
参见 `doc/protocol.md` §二 2.2 POST /api/v1/auth/login

## 三、TDD 验收用例
- [ ] 正向：验证码正确，已有用户登录成功，返回 JWT + 用户信息 + is_new=false
- [ ] 正向：验证码正确，新手机号自动注册，返回 JWT + 用户信息 + is_new=true
- [ ] 正向：新用户默认昵称格式为 "用户XXX"（随机后缀）
- [ ] 正向：JWT 有效期 30 天，iss="voiceroom"
- [ ] 异常：验证码错误返回 401 (40103)
- [ ] 异常：验证码过期返回 401 (40104)
- [ ] 异常：验证码尝试超过 5 次返回 401 (40105)
- [ ] 异常：校验成功后 Redis key 被删除（一次性作废）

## 四、实现结果（由 TDD Agent 完成后填写）
- **实际修改的文件**：
  - `app/server/src/modules/auth/dto.rs`：`LoginResponse.token`（去 `access_token`/`token_type`），新增 `LoginUserInfo { is_new, created_at }`，`UserResponse` 删 `is_banned` + 加 `created_at`
  - `app/server/src/modules/auth/service.rs`：`login` 追踪 `is_new`；`issue_token` 只调用 `now_secs()` 一次；新增 `From<(UserModel, bool)> for LoginUserInfo`；`From<UserModel> for UserResponse` 加 `created_at`
  - `app/server/src/modules/auth/routes.rs`：路径加 `/api/v1` 前缀
- **测试覆盖**：`login_correct_code_creates_user_and_returns_token` 断言 `is_new=true`；`login_existing_user_reuses_account` 断言 `is_new=false`；31 个测试全绿
- **完成时间**：2 次（初版 + 返修）

## 五、Reviewer意见
> 状态枚举：🟢 通过 | 🔴 未通过
### Review理由：🔴 未通过
1. 🔴 路由路径缺少 `/api/v1` 前缀
2. 🔴 LoginResponse 字段名不符合 protocol：应为 `token`（非 `access_token`），缺少 `is_new`
3. 🔴 UserResponse 缺少 `created_at` 字段，多余 `is_banned`（不应暴露给客户端）
4. 🟠 `issue_token` 调用 `now_secs()` 两次可能产生偏差
### 返修记录（TDD Agent）
- ✅ 路由路径加 `/api/v1` 前缀
- ✅ `LoginResponse.token`（去 `access_token`/`token_type`）
- ✅ 新增 `LoginUserInfo` 含 `is_new: bool` + `created_at`，login service 追踪 `is_new`
- ✅ `UserResponse` 删 `is_banned`，加 `created_at`（RFC 3339 格式）
- ✅ `issue_token` 合并为 `let now = now_secs()` 单次调用

---

### 第二轮 Review（Review Agent，commit `df327b9`）
> 状态枚举：🟢 通过 | 🔴 未通过

#### Review 结论：🔴 未通过（1 HIGH）

**H-01（跨 T-00002/03/05）错误响应 `request_id` 永远为空字符串**  
- 位置：`src/common/error.rs:116`；影响 `login` handler 的所有错误路径（401/400/500）  
- 同 T-00002 H-01 描述。controller 的 `?` 操作符会调用 `AppError::into_response()`，request_id 为空。  
- 修复：同 T-00002 H-01。

**TDS §三 验收用例备注**：用例"新用户默认昵称格式为 '用户XXX'"与实现不符——实现为 `"User{末4位}"`，protocol 示例为 `"User_a1b2"`，待 PM 确认后对齐。当前不阻塞。
---

### 第二轮返修记录（TDD Agent，第三轮 TDD）
> H-01 修复（同 T-00002）：login handler 返回 `axum::response::Response`，错误路径调用 `err_response(e, rc.request_id())`。  
> 详细记录见 T-00002.md 第二轮返修记录。
