<!--
[AI 写入规约]
-->

# TDS: 创建房间表单升级 (Task ID: T-30036)

## 一、背景与目标
- **关联需求**：E-10 创建房间完整化 ([设计文档](../../design/android/T-30036.md))
- **本 Task 的目标**：扩展 `CreateRoomScreen`：房名+封面选择器+分类+公告+密码开关。

## 二、方案设计

### 涉及文件
| 文件 | 类型 | 说明 |
|------|------|------|
| `feature/room/create/CreateRoomScreen.kt` | 修改 | 重构主表单 |
| `feature/room/create/CreateRoomViewModel.kt` | 修改 | 新字段 state + 校验 |
| `feature/room/create/components/CategoryDropdown.kt` | 新增 |  |
| `feature/room/create/components/PasswordInputRow.kt` | 新增 | 6 位分格输入 |
| `feature/room/create/components/AnnouncementField.kt` | 新增 | 200 字计数 |
| `data/api/RoomApi.kt` | 修改 | `createRoom` 请求体 |
| `data/model/CreateRoomRequest.kt` | 修改 | 新增字段 |
| `test/.../CreateRoomViewModelTest.kt` | 修改 | C36-01~C36-08 |

### 表单 State
```kotlin
data class CreateRoomUiState(
    val title: String = "",
    val coverUrl: String = "",           // 从 T-30037 返回
    val category: RoomCategory = CHAT,   // 枚举
    val announcement: String = "",
    val passwordEnabled: Boolean = false,
    val password: String = "",           // 6 位数字
    val submitting: Boolean = false,
    val error: String? = null
) {
    val canSubmit: Boolean
        get() = title.isNotBlank() && title.length <= 30
             && announcement.length <= 200
             && (!passwordEnabled || password.matches(Regex("\\d{6}")))
             && coverUrl.isNotEmpty()
             && !submitting
}
```

### 布局
- 顶部 TopAppBar 返回
- 封面选择：点击 CoverPreview → 弹 T-30037
- TextField 房名 (testTag: `Key('input_room_title')`)
- `CategoryDropdown` ExposedDropdownMenu 6 项
- `AnnouncementField` 200 字
- Switch 密码开关 → 显隐 `PasswordInputRow`
- 底部按钮 `GoldButton` `Key('btn_submit_create_room')`

### 提交
```kotlin
fun submit() {
    viewModelScope.launch {
        _state.update { it.copy(submitting = true) }
        val req = CreateRoomRequest(
            title = state.title,
            room_type = if (state.passwordEnabled) "password" else "normal",
            cover_url = state.coverUrl,
            category = state.category.key,
            announcement = state.announcement.ifBlank { null },
            password = state.password.ifBlank { null }
        )
        runCatching { roomApi.createRoom(req) }
            .onSuccess { navigateToRoom(it.room_id) }
            .onFailure { handleError(it) }
    }
}
```

### 分类枚举
```kotlin
enum class RoomCategory(val key: String, @StringRes val labelRes: Int) {
    CHAT("chat", R.string.cat_chat),
    EMOTION("emotion", R.string.cat_emotion),
    MUSIC("music", R.string.cat_music),
    GAME("game", R.string.cat_game),
    MATCHMAKING("matchmaking", R.string.cat_matchmaking),
    OTHER("other", R.string.cat_other)
}
```

## 三、TDD 验收用例
- [ ] **C36-01** 空房名按钮置灰
- [ ] **C36-02** 公告 >200 字 counter 置红 + 按钮置灰
- [ ] **C36-03** 密码开关关闭时不传 password 字段
- [ ] **C36-04** 密码开关开但输入 5 位按钮置灰
- [ ] **C36-05** 密码 6 位非数字（含字母）按钮置灰
- [ ] **C36-06** 未选封面按钮置灰
- [ ] **C36-07** 创建成功 navigate 到 RoomScreen
- [ ] **C36-08** 409 错误（已有活跃房）显示 Snackbar

## 四、实现结果
- **实际修改的文件**：
- **测试覆盖率**：
- **完成时间**：

## 五、Reviewer意见
### Round 1
- **状态**：
- **审查意见**：
- **改进建议**：
