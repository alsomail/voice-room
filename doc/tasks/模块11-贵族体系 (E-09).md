# 模块 11: 贵族体系 (E-09)

> 返回 [任务总索引](./index.md)
> **产品方向**: [phase1_nobility.md](../product/phase1_nobility.md)
> **依赖**: 模块 6 (E-07 钱包) ✅ Done / 模块 10 (E-08 真支付) 🟡 设计中

```
T-00065 (noble schema) ──┬─► T-00066 (tiers 列表 API) ──► T-30070 (贵族中心 UI)
                         ├─► T-00067 (钻石购买 API) ──────► T-30071 (购买流程)
                         ├─► T-00068 (cron 续费/过期) ───► T-30075 (续费提醒弹窗)
                         └─► T-00069 (徽章/特效 WS 字段下发) ──► T-30072 (进场特效) + T-30073 (徽章组件)
T-00070 (特权钩子: 隐身/优先抢麦/折扣) ──► 复用 T-00012 / 治理 / 礼物
T-10030 (Admin tier CRUD) ──► T-20035 (Web 贵族管理页)
T-10031 (Admin 手动赠送) ──► T-20036 (Web 用户贵族 Tab)
T-30074 (资料卡贵族框)
```

---

## App Server

| Task ID | 模块 | 任务名称 | 前置依赖 | 核心描述 | TDD 验收标准 | 预估工时 | 研发负责人 | 研发状态 | Review Gate | QA Gate | Overall Gate |
|---------|------|----------|----------|----------|-------------|----------|------------|----------|-------------|---------|--------------|
| **T-00065** | Noble | Schema 与种子 | T-00017 | 新建 `noble_tiers` (tier_id PK / name_en / name_ar / level INT UNIQUE / monthly_diamonds / monthly_usd / privileges JSONB / icon_url / entrance_animation_url / bgm_url / badge_color / is_active) 与 `user_nobles` (user_id FK / tier_id FK / start_at / expire_at / auto_renew BOOL / source ENUM / created_at)；插入 6 档贵族种子 | 1. 迁移幂等<br>2. user_nobles UNIQUE(user_id) 约束（用户只能持有一个贵族）<br>3. (expire_at) 索引<br>4. privileges JSONB 校验函数 | 4h | Done | Done | ✅ | - | - |
| **T-00066** | Noble | 贵族列表 API | T-00065 | GET `/api/v1/nobles/tiers` 返回上架 tier 列表（按 level 升序）；GET `/api/v1/nobles/me` 返回当前用户贵族信息 | 1. Accept-Language 切换 name_en/name_ar<br>2. /me 未持有返回 200 + tier=null<br>3. 响应 < 100ms（缓存） | 4h | Done | Done | ✅ | - | - |
| **T-00067** | Noble | 钻石购买/续费 API（强事务） | T-00065, T-00018 | POST `/api/v1/nobles/purchase { tier_id, msg_id, auto_renew }`：事务扣钻 + upsert user_nobles（已持有同级 expire_at += 30d；升级按剩余天数补差扣款；降级 40911 拒绝）；推送 BalanceUpdated + NobleChanged | 1. 同 msg_id 幂等<br>2. 升级补差扣款公式正确（剩余天数比例换算）<br>3. 降级返回 40911<br>4. 余额不足 INSUFFICIENT_BALANCE 回滚<br>5. 事务断电状态自洽 | 8h | Done | Done | ✅ | - | - |
| **T-00068** | Noble | 续费/过期 cron | T-00067 | Tokio cron 每小时：① 自动续费扫描（auto_renew=true & expire_at within 24h）尝试扣钻 ② 过期清理（expire_at < now → delete + NobleExpired）；连续 3 次扣款失败关闭 auto_renew | 1. 续费成功推送 NobleRenewSuccess<br>2. 失败 NobleRenewFailed<br>3. 过期清理 5min 内完成<br>4. 失败计数写 user_nobles.failed_renew_count | 6h | Done | Done | ✅ | - | - |
| **T-00069** | Noble | 进场广播 + 徽章字段下发 | T-00067 | 用户 JoinRoom 时房间内广播 `UserJoined` 包含 `noble = { tier_id, badge_color, entrance_animation_url, bgm_url, expire_at }`；MemberSnapshot 同步增字段；新增 `NobleEntrance` WS 信令（房间专用进场特效广播，含发送给非进场者的可见性） | 1. UserJoined payload 含 noble 字段<br>2. 隐身贵族不广播（仅广播给管理员/房主）<br>3. 高等级触发全服 NobleEntranceGlobal Pub/Sub<br>4. 协议路径绑定表完整 | 6h | Done | Done | ✅ | - | - |
| **T-00070** | Noble | 特权钩子（隐身/优先抢麦/礼物折扣） | T-00069 | ① 观众席 API 过滤 invisible=true ② T-00012 抢麦 Lua 脚本增贵族权重 ③ T-00020 SendGift 应用折扣前对账（结算价=原价*(1-discount)，写流水）④ 入房密码校验跳过（公爵+） | 1. 隐身：观众席不返回；管理员/房主仍可见<br>2. 抢麦并发 100ms 内贵族优先成功<br>3. 折扣计算精准（无浮点误差，按"千"取整）<br>4. 公爵+ 进入密码房 200 直接入<br>5. 全部以 noble_tier.privileges JSON 配置驱动 | 10h | Done | Done | ✅ | - | - |

---

## Admin Server

| Task ID | 模块 | 任务名称 | 前置依赖 | 核心描述 | TDD 验收标准 | 预估工时 | 研发负责人 | 研发状态 | Review Gate | QA Gate | Overall Gate |
|---------|------|----------|----------|----------|-------------|----------|------------|----------|-------------|---------|--------------|
| **T-10030** | Noble | tier CRUD API | T-00065, T-10012 | `/api/v1/admin/nobles/tiers` GET/POST/PUT/DELETE；privileges JSON Schema 校验；上架开关；写 admin_logs；上传图标 / 进场动画文件 | 1. JSON Schema 校验未通过返回 400 + 字段路径<br>2. level 唯一性<br>3. 软删后已持有用户保留贵族至 expire_at<br>4. 文件类型白名单 | 5h | TDD | Done | ✅ Passed | - | - |
| **T-10031** | Noble | 手动赠送/撤销贵族 API | T-10030 | POST `/api/v1/admin/users/:id/noble/grant { tier_id, days, reason }`；POST `/revoke { reason }`；写 user_nobles + admin_logs；触发 NobleChanged 广播 | 1. 仅 super_admin<br>2. 赠送支持任意天数<br>3. 撤销立即生效<br>4. 操作可追溯 | 4h | TDD | Done | ✅ Passed | - | - |
| **T-10032** | Noble | 贵族用户查询 API | T-10030 | GET `/api/v1/admin/nobles/users`：按等级/到期时间筛选；导出 CSV | 1. 分页 < 200ms<br>2. 含 source 字段（区分钻石/真金/赠送）<br>3. CSV 含 user_id/tier/start/expire/source | 4h | TDD | Done | ✅ Passed | - | - |

---

## Web Admin

| Task ID | 模块 | 任务名称 | 前置依赖 | 核心描述 | TDD 验收标准 | 预估工时 | 研发负责人 | 研发状态 | Review Gate | QA Gate | Overall Gate | UI 设计 |
|---------|------|----------|----------|----------|-------------|----------|------------|----------|-------------|---------|--------------|---------|
| **T-20035** | Noble | 贵族管理页 | T-10030 | 路由 `/admin/nobles/tiers`：表格 CRUD + privileges JSONB 可视化编辑器（Form 模式与 JSON 模式切换） + 文件上传（图标/Lottie/BGM） | 1. JSON 编辑器实时校验<br>2. 上传文件预览<br>3. 上下架开关 + 二次确认<br>4. 软删行灰显示 | 8h | TDD | **Done** | ✅ Passed | - | - | 待补 |
| **T-20036** | Noble | 用户贵族 Tab + 手动操作 | T-10031, T-10032 | 用户详情新增"贵族"Tab：当前贵族卡片 + 历史记录；按钮"赠送贵族""撤销贵族" 弹窗（super_admin only） | 1. 历史按时间倒序<br>2. 按钮权限隐藏<br>3. 操作后实时刷新<br>4. 失败 Toast errorCode | 5h | TDD | **Done** | ✅ Passed | - | - | 待补 |

---

## Android

| Task ID | 模块 | 任务名称 | 前置依赖 | 核心描述 | TDD 验收标准 | 预估工时 | 研发负责人 | 研发状态 | Review Gate | QA Gate | Overall Gate | UI 设计 |
|---------|------|----------|----------|----------|-------------|----------|------------|----------|-------------|---------|--------------|---------|
| **T-30070** | Noble | 贵族中心页（列表 + 当前状态） | T-00066, T-30018 | 新建 `NobleCenterScreen`：顶部当前贵族卡片（动态 Lottie 头像框 + 到期倒计时）；下方等级列表（横向 Pager 滑动切换 6 档，每档卡片含特效预览）；底部"立即开通"按钮 | 1. 当前贵族 lottie 框正确加载<br>2. Pager 横滑流畅 60fps<br>3. 已持有等级显示"已开通"<br>4. 未开通显示"立即开通"<br>5. RTL 兼容 | 10h | Plan | Todo | - | - | - | [T-30070.md](../design/android/T-30070.md) |
| **T-30071** | Noble | 钻石/真金购买流程 | T-30070, T-00067, T-30060 | 选档位 → "立即开通"弹窗（30 天/3 月/年；钻石/真金切换）→ 钻石走 T-00067 直购；真金走 T-30062 创建 noble_pack SKU 订单 → 成功返回贵族中心刷新 | 1. 钻石不足跳 T-30060 充值<br>2. 真金完整走 E-08 链路<br>3. 升级补差弹窗显示折算<br>4. 降级被拒 Toast<br>5. 成功播放专属庆祝动画 | 8h | Plan | Todo | - | - | - | [T-30070.md](../design/android/T-30070.md) |
| **T-30072** | Noble | 进场特效播放器 | T-00069, T-30031 | 收到 `UserJoined.noble` 字段 → 房间内顶部全宽 Lottie 横幅 + BGM（按等级 3-15s）；可叠加多个进场（FIFO 队列）；房主可在房间设置中关闭进场特效 | 1. 复用 T-30031 ILottiePlayer<br>2. URL 由 Server 下发不硬编码<br>3. 队列防止特效叠加爆炸<br>4. 设置关闭后不播放<br>5. 进场补偿事件不重播 | 8h | Plan | Todo | - | - | - | [T-30072.md](../design/android/T-30072.md) |
| **T-30073** | Noble | 用户贵族徽章组件（全局） | T-00069 | 抽离 `NobleBadge` Composable：传 `noble_tier_id` 渲染 6 档颜色徽章；在房间观众席项 / 聊天消息发送者 / 资料卡 / 麦位下方统一使用 | 1. 6 档颜色样式正确<br>2. tier=null 时 size=0 不占布局<br>3. 复用同一 testTag 模式 `noble_badge_${userId}`<br>4. 单元测试覆盖 6 档渲染 | 5h | Plan | Todo | - | - | - | 待补 |
| **T-30074** | Noble | 资料卡贵族框 | T-30073 | 用户头像组件 `AvatarWithFrame` 升级：基于 noble_tier 渲染对应外框（图片由 Server 下发 frame_url）；房间内麦位/观众席/聊天/资料卡全局生效 | 1. 等级越高框越炫<br>2. URL 缓存（Coil）<br>3. tier=null 不渲染<br>4. RTL 不影响 | 4h | Plan | Todo | - | - | - | 待补 |
| **T-30075** | Noble | 续费/过期/失败提醒 | T-00068 | WS `NobleRenewFailed/NobleExpired` 监听 → AlertDialog 提示 + 跳贵族中心；过期前 24h 推送本地通知（如开启）；DataStore 记录已弹过避免重复打扰 | 1. 失败弹窗含"立即充值"按钮<br>2. 过期弹窗含"续费"<br>3. 同一事件 24h 内不重复弹<br>4. 通知权限缺失静默 | 5h | Plan | Todo | - | - | - | 待补 |

---

## 模块汇总
- **App Server**: 6 Tasks（38h）
- **Admin Server**: 3 Tasks（13h）
- **Web Admin**: 2 Tasks（13h）
- **Android**: 6 Tasks（40h）
- **总计**: 17 Tasks ≈ 104h
