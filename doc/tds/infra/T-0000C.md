<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建，TDD Agent 只读不写。
2. 创建后必须在 doc/Tasks.md 对应 Task 的行内补充链接：[TDS](./tds/infra/T-0000C.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: 数据库权限隔离 (Task ID: T-0000C)

## 一、背景与目标
- **关联需求**：product.md - 工程基建 / ARCHITECTURE.md §7 双数据库账号
- **本 Task 的目标**：对 T-0000A 创建的两个 PG Role 做表级细粒度 GRANT，确保 app_server_user 只能操作 C 端业务表，不可访问管理表

## 二、方案设计

### 权限矩阵

| 表名 | app_server_user | admin_server_user |
|------|----------------|-------------------|
| `users` | SELECT, INSERT, UPDATE | ALL |
| `rooms` | SELECT, INSERT, UPDATE, DELETE | ALL |
| `admins` | — (无权限) | ALL |
| `admin_logs` | — (无权限) | ALL |
| 其他业务表 | SELECT, INSERT, UPDATE | ALL |

### 实现方式

权限脚本作为独立 SQL 文件，在所有建表 migration 执行完成后运行。

### 文件：`scripts/dev/grant-permissions.sql`

```sql
-- ============================================================
-- 权限隔离脚本：在所有业务表创建完成后执行
-- 执行方式：psql -h localhost -U postgres -d voiceroom -f scripts/dev/grant-permissions.sql
-- ============================================================

-- app_server_user: C 端业务表权限
GRANT USAGE ON SCHEMA public TO app_server_user;
GRANT SELECT, INSERT, UPDATE ON TABLE users TO app_server_user;
GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO app_server_user;

-- app_server_user: 禁止访问管理表
REVOKE ALL ON TABLE admins FROM app_server_user;
REVOKE ALL ON TABLE admin_logs FROM app_server_user;

-- admin_server_user: 全权（已在 init-db.sh 中设置 DEFAULT PRIVILEGES）
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO admin_server_user;
GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public TO admin_server_user;
```

> **注意**：此脚本需要在 T-00001（users 表）和 T-10001（admins 表）的 migration 之后执行。

### 涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `scripts/dev/grant-permissions.sql` | 新增 | 表级权限 GRANT 脚本 |

## 三、TDD 验收用例
- [ ] 正向：`app_server_user` 可 SELECT/INSERT/UPDATE `users` 表
- [ ] 正向：`admin_server_user` 可对所有表执行任意操作
- [ ] 异常：`app_server_user` 无法 SELECT `admins` 表（权限拒绝）
- [ ] 异常：`app_server_user` 无法 SELECT `admin_logs` 表（权限拒绝）
- [ ] 正向：脚本可重复执行（幂等）

## 四、实现结果（由 TDD Agent 完成后填写）
- **实际修改的文件**：`scripts/dev/grant-permissions.sql`（新增），`scripts/dev/verify-permissions.sh`（新增验收脚本）
- **测试覆盖率**：`verify-permissions.sh` 8/8 通过（INSERT/SELECT/UPDATE 正向3项，权限拒绝2项，admin全权2项，幂等1项）
- **完成时间**：2026-04-18

## 五、Reviewer意见
> 状态枚举：🟢 通过（第二轮）
### Review理由（第一轮 🔴 未通过，已修复）：
**🔴 CRITICAL**（已修复）
- `verify-permissions.sh:12,13`：密码 `app_server_pass`/`admin_server_pass` 硬编码。需改为从环境变量读取（与 T-0000A 同步修复）。

**🟡 MEDIUM**（已修复）
- `grant-permissions.sql`：缺少 `ALTER DEFAULT PRIVILEGES` for `app_server_user`，未来 migration 新建的表不会自动授权。需补充：
  ```sql
  ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT, INSERT, UPDATE ON TABLES TO app_server_user;
  ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT USAGE, SELECT ON SEQUENCES TO app_server_user;
  ```

### 第二轮结论（通过）：
- `verify-permissions.sh` 密码改为 `${APP_SERVER_PASS:-app_server_pass}` 读取环境变量。✅
- `grant-permissions.sql` 补充了 `ALTER DEFAULT PRIVILEGES` 两条语句。✅