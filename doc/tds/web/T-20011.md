<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建。TDD Agent 仅填写第四节【实现结果】，其余章节只读。Review Agent 仅填写第五节【Reviewer意见】。
2. 创建后必须在 doc/Tasks.md 对应 Task 的行内补充链接：[TDS](./tds/web/T-20011.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: Web 端活水房间监控增强 (Task ID: T-20011)

## 一、背景与目标

- **关联需求**：[doc/design/adminWeb/T-20011.md](../../design/adminWeb/T-20011.md)
- **前置依赖**：T-20004（RoomsPage + useRoomsPage + RoomsTable 已就绪）、T-20005（RoomDetailModal 已就绪）、T-10004（房间列表接口已提供 `member_count`、`status`、`created_at` 字段）
- **本 Task 的目标**：在不新增后端接口的前提下，在现有房间管理页面 `/rooms` 增加"活水监控"能力：新增活跃状态 Tag 列与持续时长列、工具栏加活跃度前端筛选、异常行高亮，帮助运营人员快速识别疑似异常房间。

---

## 二、方案设计

### 2.1 核心数据流

```
AdminRoomItem[]（来自 T-10004 API）
        │
        ▼
useRoomsPage（已有）
  ├─ items: AdminRoomItem[]           ← API 数据（不变）
  ├─ activityFilter: ActivityFilter   ← 新增 state（默认 'all'）
  ├─ setActivityFilter(v)             ← 新增 setter
  └─ filteredItems: AdminRoomItem[]   ← useMemo 前端过滤结果（新增）
        │
        ▼
RoomsPage（index.tsx）
  └─ <RoomsTable
       items={filteredItems}           ← 由 filteredItems 替换原 items
       activityFilter={activityFilter}
       onActivityFilterChange={setActivityFilter}
       .../>
        │
        ▼
RoomsTable（已有，扩展）
  ├─ 工具栏：新增 <Select data-testid="activity-filter">
  ├─ 新列：<RoomActivityTag> (data-testid="room-activity-tag-{roomId}")
  ├─ 新列：formatDuration(created_at) (data-testid="room-duration-{roomId}")
  └─ onRow：getActivityStatus === 'abnormal' → style 行背景高亮
```

**设计原则**：
- **活跃度筛选纯前端**：复用 T-10004 已有数据，不增加任何新 API，`filteredItems` 在 `useRoomsPage` 内以 `useMemo` 计算
- **活跃状态计算为纯函数**：抽离至 `roomUtils.ts`，便于单独单元测试，与组件解耦
- **最小化 diff**：`RoomsTable` Props 仅新增 2 个字段，`useRoomsPage` 返回值追加 3 个字段，原有接口契约不变
- **分页行为**：服务端分页的 `total` 字段不受前端活跃度筛选影响（当前页过滤，分页控件按 API 数据计算）

### 2.2 涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `src/pages/rooms/roomUtils.ts` | **新增** | 纯函数：`getActivityStatus` / `formatDuration` / `filterByActivity`；类型：`ActivityLevel` / `ActivityFilter` |
| `src/pages/rooms/roomUtils.test.ts` | **新增** | `getActivityStatus` 和 `formatDuration` 单元测试（U01–U14） |
| `src/pages/rooms/RoomActivityTag.tsx` | **新增** | 活跃状态 Tag 组件，使用 i18n |
| `src/pages/rooms/RoomActivityTag.test.tsx` | **新增** | RoomActivityTag 组件测试（C01–C08） |
| `src/pages/rooms/useRoomsPage.ts` | **修改** | 新增 `activityFilter` state、`setActivityFilter`、`filteredItems`（useMemo） |
| `src/pages/rooms/useRoomsPage.test.ts` | **修改** | 追加 H11–H17 测试（活跃度筛选逻辑） |
| `src/pages/rooms/RoomsTable.tsx` | **修改** | 工具栏加 activity-filter Select；新增两列；onRow 行高亮；新增 Props 2 项 |
| `src/pages/rooms/RoomsTable.test.tsx` | **修改** | 追加 C15–C26 测试（新列、筛选、行高亮） |
| `src/pages/rooms/index.tsx` | **修改** | 透传 `filteredItems`、`activityFilter`、`onActivityFilterChange` 给 RoomsTable |
| `src/pages/rooms/RoomsPage.test.tsx` | **修改** | 追加 I08–I13 集成测试 |
| `src/i18n/locales/zh.ts` | **修改** | 新增 `rooms.activity.*` 翻译键 |
| `src/i18n/locales/en.ts` | **修改** | 新增 `rooms.activity.*` 翻译键 |

### 2.3 类型定义 — `roomUtils.ts`

```typescript
export type ActivityLevel = 'active' | 'quiet' | 'abnormal' | 'normal';
export type ActivityFilter = 'all' | 'active' | 'quiet' | 'abnormal';

/** 活跃状态计算（now 参数支持测试注入，保证时间确定性） */
export function getActivityStatus(
  room: Pick<AdminRoomItem, 'member_count' | 'status' | 'created_at'>,
  now: Date = new Date(),
): ActivityLevel {
  const { member_count, status, created_at } = room;
  if (member_count >= 5) return 'active';
  if (member_count === 0 && status === 'active') return 'abnormal';
  if (member_count >= 1 && member_count <= 4) {
    const durationMs = now.getTime() - new Date(created_at).getTime();
    if (durationMs > 60 * 60 * 1000) return 'quiet'; // > 1h
  }
  return 'normal';
}

/**
 * 格式化持续时长
 * < 1min  → "0m"
 * < 1h    → "35m"
 * ≥ 1h    → "2h 35m"
 * ≥ 24h   → "3d 2h"
 */
export function formatDuration(createdAt: string, now: Date = new Date()): string {
  const diffMs = now.getTime() - new Date(createdAt).getTime();
  if (diffMs < 0) return '0m';
  const totalMinutes = Math.floor(diffMs / 60000);
  const days = Math.floor(totalMinutes / 1440);
  const hours = Math.floor((totalMinutes % 1440) / 60);
  const minutes = totalMinutes % 60;
  if (days > 0) return `${days}d ${hours}h`;
  if (hours > 0) return `${hours}h ${minutes}m`;
  return `${minutes}m`;
}

/** 根据 ActivityFilter 过滤房间列表 */
export function filterByActivity(
  items: AdminRoomItem[],
  filter: ActivityFilter,
  now: Date = new Date(),
): AdminRoomItem[] {
  if (filter === 'all') return items;
  return items.filter((room) => getActivityStatus(room, now) === filter);
}
```

### 2.4 组件 — `RoomActivityTag`

```typescript
interface RoomActivityTagProps {
  level: ActivityLevel;
  roomId: string;
}

const colorMap: Record<ActivityLevel, string> = {
  active:   'success',
  quiet:    'warning',
  abnormal: 'error',
  normal:   'processing',
};

const i18nKeyMap: Record<ActivityLevel, string> = {
  active:   'rooms.activityLevelActive',
  quiet:    'rooms.activityLevelQuiet',
  abnormal: 'rooms.activityLevelAbnormal',
  normal:   'rooms.activityLevelNormal',
};

// 渲染：
// <Tag color={colorMap[level]} data-testid={`room-activity-tag-${roomId}`}>
//   {t(i18nKeyMap[level])}
// </Tag>
```

### 2.5 Hook 修改 — `useRoomsPage`

新增 state：`activityFilter: ActivityFilter`（默认 `'all'`）

新增 useMemo：
```typescript
const filteredItems = useMemo(
  () => filterByActivity(items, activityFilter),
  [items, activityFilter],
);
```

新增返回字段：`activityFilter`、`setActivityFilter`、`filteredItems`

### 2.6 组件修改 — `RoomsTable`

**新增 Props**：
```typescript
activityFilter: ActivityFilter;          // 默认值 'all'（向后兼容）
onActivityFilterChange: (v: ActivityFilter) => void;  // 默认 noop
```

**工具栏**：在状态下拉右侧插入活跃度 Select（`data-testid="activity-filter"`）

**新增列**（在 member_count 后、created_at 前）：
- 活跃状态：`<RoomActivityTag level={getActivityStatus(record)} roomId={record.room_id}/>`
- 持续时长：`<span data-testid={`room-duration-${record.room_id}`}>{formatDuration(record.created_at)}</span>`

**行高亮**（`onRow` 扩展）：`getActivityStatus(record) === 'abnormal'` → `style: { background: 'rgba(231, 76, 60, 0.1)' }`

**columns 和 activityOptions 用 useMemo 包裹**（与 UsersTable 保持一致）

### 2.7 入口修改 — `index.tsx`

将 `RoomsTable` 的 `items` prop 改为 `filteredItems`，并透传 `activityFilter`、`onActivityFilterChange`

### 2.8 i18n 键

```typescript
// zh.ts / en.ts 追加：
'rooms.activityFilter'       // 活跃度 / Activity
'rooms.activityAll'          // 全部 / All
'rooms.activityLevelActive'  // 活跃 / Active
'rooms.activityLevelQuiet'   // 冷清 / Quiet
'rooms.activityLevelAbnormal'// 异常 / Abnormal
'rooms.activityLevelNormal'  // 正常 / Normal
'rooms.columnActivityStatus' // 活跃状态 / Activity Status
'rooms.columnDuration'       // 持续时长 / Duration
```

---

## 三、TDD 验收用例

### U01–U14 — roomUtils 纯函数

#### `getActivityStatus`

| ID | 输入条件 | 期望输出 |
|----|----------|---------|
| U01 | `member_count=5, status='active'` | `'active'` |
| U02 | `member_count=10, status='active'` | `'active'` |
| U03 | `member_count=0, status='active'` | `'abnormal'` |
| U04 | `member_count=0, status='closed'` | `'normal'` |
| U05 | `member_count=3, status='active'`，created_at 比 now 早 90 分钟 | `'quiet'` |
| U06 | `member_count=3, status='active'`，created_at 比 now 早 30 分钟 | `'normal'` |
| U07 | `member_count=1, status='active'`，created_at 比 now 早 61 分钟 | `'quiet'` |
| U08 | `member_count=4, status='active'`，created_at 比 now 早 59 分钟 | `'normal'` |

#### `formatDuration`

| ID | 输入（diffMs） | 期望输出 |
|----|----------------|---------|
| U09 | 0ms | `'0m'` |
| U10 | 45min | `'45m'` |
| U11 | 2h35m | `'2h 35m'` |
| U12 | 3d2h | `'3d 2h'` |
| U13 | 负数（未来时间） | `'0m'` |
| U14 | 恰好 1min | `'1m'` |

### C01–C08 — RoomActivityTag 组件

| ID | 描述 | 验证点 |
|----|------|--------|
| C01 | `level='active'` → color='success' | color 属性 |
| C02 | `level='quiet'` → color='warning' | color 属性 |
| C03 | `level='abnormal'` → color='error' | color 属性 |
| C04 | `level='normal'` → color='processing' | color 属性 |
| C05 | `roomId='r1'` → `data-testid="room-activity-tag-r1"` | testid |
| C06 | `level='active'` zh 环境 → 文字"活跃" | 内容 |
| C07 | `level='abnormal'` zh 环境 → 文字"异常" | 内容 |
| C08 | `level='normal'` zh 环境 → 文字"正常" | 内容 |

### H11–H17 — useRoomsPage Hook

| ID | 描述 | 验证点 |
|----|------|--------|
| H11 | 初始 activityFilter='all'，filteredItems 与 items 相同 | 长度相等 |
| H12 | setActivityFilter('active') → 只含 member_count≥5 的房间 | filter 正确 |
| H13 | setActivityFilter('abnormal') → 只含 0人+active 的房间 | filter 正确 |
| H14 | setActivityFilter('quiet') → 只含冷清条件的房间 | filter 正确 |
| H15 | 连续切换 all→active | filteredItems 正确变化 |
| H16 | API 数据刷新后 filteredItems 随之重新计算 | 依赖 items 更新 |
| H17 | setActivityFilter 不触发 adminGetRooms 重新请求 | mock 调用次数不变 |

### C15–C26 — RoomsTable 组件（新增）

| ID | 描述 | 验证点 |
|----|------|--------|
| C15 | 3 行数据 → 每行有 room-activity-tag testid | testid ×3 |
| C16 | member_count=5 → Tag level='active'（绿） | color |
| C17 | member_count=0+active → level='abnormal'（红） | color |
| C18 | member_count=2, 90min前 → level='quiet'（黄） | color |
| C19 | 正常房间 → level='normal'（蓝） | color |
| C20 | 每行含 room-duration testid，格式正确 | testid + 格式 |
| C21 | 异常房间行背景 rgba(231, 76, 60, 0.1) | style.background |
| C22 | 非异常房间无高亮背景 | style 无 rgba |
| C23 | 工具栏含 activity-filter Select | testid |
| C24 | 选择"异常" → onActivityFilterChange('abnormal') 被调用 | spy |
| C25 | activityFilter='active' → Select 显示受控值"活跃" | value |
| C26 | 点击行 onRowClick 正常触发，不受过滤影响 | spy |

### I08–I13 — RoomsPage 集成

| ID | 描述 | 验证点 |
|----|------|--------|
| I08 | mock 混合数据 → 三行渲染，活跃状态正确 | Tag testid ×3 |
| I09 | 筛选"异常" → 只显示异常行，不发新 API 请求 | 行数+mock次数 |
| I10 | 异常房间行高亮背景可见 | style.background |
| I11 | 持续时长格式正确（模拟 created_at 为 2h35m 前） | text "2h 35m" |
| I12 | 状态筛选与活跃度筛选可同时生效 | 联合验证 |
| I13 | **回归**：原有搜索/分页/关闭/详情/刷新全部正常 | 原有 I01–I07 通过 |

---

## 四、实现结果（由 TDD Agent 完成后填写）

- **实际修改的文件**：
- **测试覆盖率**：
- **完成时间**：

---

## 五、Reviewer意见

> 状态枚举：🟢 通过 | 🔴 未通过
> 每轮 Review 追加一条记录，不要覆盖历史。

### Round 1
- **状态**：（🟢 通过 / 🔴 未通过）
- **审查意见**：
- **改进建议**：（仅未通过时填写）
