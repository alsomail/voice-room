<!--
[AI 写入规约]
1. Plan Agent 创建。TDD 仅填第四节，Review 仅填第五节。
-->

# TDS: Web Admin Zod schema 接入（HTTP DTO 强校验，WS 暂缓）(Task ID: T-00102)

## 一、背景与目标

- **关联需求**：用户决策 #3 — 跨语言 E2E 不强制 Web，但 Web 仍存在 HTTP DTO 字段失配风险（手写 TypeScript interface vs server `dto.rs`）。本 Task 范围仅覆盖 **HTTP REST 层**，WS 层留 follow-up（Web 当前 WS 使用面较窄）。
- **本 Task 的目标**：基于 T-00100 的 `doc/protocol/schemas/http/*.schema.json` 在 Web 侧建立 **Zod 校验层**：所有 `apiClient.*` 出/入参出栈/入栈强校验；schema mismatch 立刻在开发模式抛错（生产模式打 sentry 替代品的 console.error）。

## 二、方案设计

### 涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `app/web/src/api/schemas/*.ts` | 新增 | 由 schemas/http/*.schema.json 转换的 Zod schema（先用 `json-schema-to-zod` codegen 一次，产物入仓） |
| `app/web/src/api/apiClient.ts` | 修改 | 拦截器中 `responseSchema.parse()` / `requestSchema.parse()` |
| `app/web/src/api/apiClient.test.ts` | 修改 | 全量加 schema 失配测试 |

### 🔌 协议路径绑定表

| # | 协议类型 | 入口 | 客户端 | 服务端 | 锚点 |
|---|---------|------|--------|--------|------|
| 1 | HTTP REST | `GET /api/v1/admin/users` ⭐ | `apiClient.adminUsers.list` | `adminServer/users/controller.rs` | [admin_api.md](../../protocol/admin_api.md) |
| 2 | HTTP REST | `POST /api/v1/admin/users/:id/ban` ⭐ | `apiClient.adminUsers.ban` | 同上 | 同上 |
| 3-N | HTTP REST | 其余全部 admin endpoint | apiClient | adminServer | admin_api.md |

## 三、TDD 验收用例

- [ ] **ZOD-1**：构造 mock server 响应缺少必填字段（如 `users[0].id`），apiClient 抛 `ZodError`，业务层 catch 并打 console.error。
- [ ] **ZOD-2**：构造响应字段类型错误（如 `total: "100"` 字符串），抛 `ZodError`。
- [ ] **ZOD-3**：所有现有 8+ apiClient endpoint 覆盖率 100%（grep `z.object` 命中数 ≥ endpoint 数）。
- [ ] **ZOD-4**：`additionalProperties: false` 等价 — Zod `.strict()` 拒绝未知字段，但允许 codegen 配置时降级为 warn（避免 server 滚动添加字段就 break web）。

## 四、实现结果

## 五、Reviewer意见

### Round 1
- **状态**：
- **🔴 Changelog 行**：

## 六、DoD 完成记录

- [ ] `doc/arch/web/index.md` 协议入口索引补 ⭐ 锚点
- [ ] `doc/tasks/index.md` 标记 ✅ Done
