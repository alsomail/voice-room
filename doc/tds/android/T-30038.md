<!--
[AI 写入规约]
-->

# TDS: 密码房进房弹窗 (Task ID: T-30038)

## 一、背景与目标
- **关联需求**：E-10 密码房进房流程 ([设计文档](../../design/android/T-30038.md))
- **本 Task 的目标**：`PasswordInputDialog` 6 位分格输入 + 剩余次数提示 + 锁定提示。

## 二、方案设计

### 涉及文件
| 文件 | 类型 | 说明 |
|------|------|------|
| `feature/hall/components/PasswordInputDialog.kt` | 新增 |  |
| `feature/hall/HallViewModel.kt` | 修改 | 点击密码房触发弹窗 |
| `data/api/RoomApi.kt` | 修改 | `verifyPassword(roomId, body)` |
| `feature/room/RoomViewModel.kt` | 修改 | JoinRoom 携带 access_token |

### 弹窗交互
```kotlin
@Composable
fun PasswordInputDialog(
    state: PasswordDialogState,   // idle / verifying / error / locked
    onSubmit: (String) -> Unit,
    onDismiss: () -> Unit
)
```
- 6 个 OutlinedTextField 单字符分格，输入自动跳下一格；最后一格输完自动 submit
- `state=error`：底部红字"密码错误，剩余 N 次"
- `state=locked`：弹窗变只读 + "已被锁定，{remaining_min} 分钟后重试"
- 返回键 `onDismiss` 关闭

### Flow
```
Hall: 点击 has_password=true 的房 card
  ↓
HallViewModel.openPasswordDialog(roomId)
  ↓ 用户输 6 位
verifyPassword(roomId, {password}) → 成功：获 access_token
  ↓
Navigate to RoomScreen(roomId, accessToken)
  ↓
RoomViewModel 发 JoinRoom { access_token } 经 WS
```

### 错误处理
| HTTP code | UI 反馈 |
|-----------|--------|
| 40103 | 红字 + 剩余次数；清空输入 |
| 42910 | 切 locked 状态，显示剩余分钟 |
| 40400 | Toast "房间不存在" + 关闭 |
| 其它 | Toast "网络错误，请重试" |

### testTag
`Key('password_dialog')`, `Key('password_input')`, `Key('password_digit_0')..5`, `Key('btn_submit_password')`, `Key('password_error_text')`

## 三、TDD 验收用例
- [x] **P38-01** 6 位输完自动提交（state 先变 Verifying）
- [x] **P38-02** 错误密码清空输入 + 显示剩余 4 次
- [x] **P38-03** 连续错误后切 locked 状态，输入不可编辑
- [x] **P38-04** 锁定文案正确展示剩余分钟
- [x] **P38-05** 成功后 Navigation 到 RoomScreen 且 access_token 传入
- [x] **P38-06** 返回键 dismiss 不进房

## 四、实现结果
- **实际修改的文件**：
  | 文件 | 类型 |
  |------|------|
  | `feature/room/PasswordDialogState.kt` | 新增 — 密封类 Idle/Verifying/Error/Locked |
  | `feature/room/HallEvent.kt` | 新增 — 大厅一次性事件 NavigateToRoom/ShowToast |
  | `feature/room/components/PasswordInputDialog.kt` | 新增 — 6 位分格 Composable |
  | `domain/room/PasswordExceptions.kt` | 新增 — 领域异常 PasswordWrongException / PasswordLockedException / RoomNotFoundException |
  | `data/remote/model/VerifyPasswordModels.kt` | 新增 — DTO VerifyPasswordRequest / VerifyPasswordResponseData |
  | `data/remote/api/RoomApiService.kt` | 修改 — 新增 `verifyPassword(roomId, body)` |
  | `data/room/RetrofitRoomRepository.kt` | 修改 — 实现 `verifyPassword`，错误码映射 40103/42910/40400 |
  | `data/room/FakeRoomRepository.kt` | 修改 — 新增 `verifyPasswordResult` 可控属性 |
  | `domain/room/IRoomRepository.kt` | 修改 — 新增 `verifyPassword` 接口 |
  | `feature/room/HallViewModel.kt` | 修改 — 新增 `passwordDialogState`、`hallEvents`、`openPasswordDialog`、`verifyPassword`、`dismissPasswordDialog` |
  | `feature/room/RoomViewModel.kt` | 修改 — `joinRoom` 新增 `accessToken: String? = null` 参数 |
  | `test/.../HallPasswordDialogTest.kt` | 新增 — P38-01~P38-08 共 8 个单元测试 |

- **测试覆盖率**：
  | 文件 | 行覆盖率 |
  |------|---------|
  | `HallEvent.kt` | 100% |
  | `PasswordDialogState.kt` | 100% |
  | `PasswordExceptions.kt` | 100% |
  | `HallViewModel.kt` | 79% |
  | `FakeRoomRepository.kt` | 74% |
  | P38-01~P38-08 测试通过 | **8/8** |

- **完成时间**：2026-04-23

## 五、Reviewer意见
### Round 1
- **状态**：
- **审查意见**：
- **改进建议**：
