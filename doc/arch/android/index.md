<!-- 
[AI 读写指令与维护规约 (Doc Management Skill)]
1. 本文件是 Android 架构的总路由，严禁在此文件内编写具体的业务逻辑或冗长的代码片段。
2. 架构拆分为独立的子 Markdown 文件存放于本目录下。
3. [索引规则]：当你在本目录新增了 `.md` 子文件，必须立即同步更新本文件的【二、子模块索引】。
4. [状态规则]：当某项能力完成开发，必须同步更新本文件的【三、当前能力全景与状态】。
5. 所有的相对路径链接必须真实有效，禁止生成无法点击的死链接。
-->

# Android 端架构总索引与状态盘点

## 一、 架构概述
本项目 Android 端当前采用 Kotlin + 分层骨架（`core / common / data / domain / presentation / feature`）推进建设，`ViewModel + MainUiState` 已在壳层页面落地，完整业务级状态管理仍在后续演进中。详情参见 `doc/architecture/android_architecture.md`。

## 二、 子模块索引 (Module Router)
> ⚠️ AI 寻路提示：请点击以下具体模块查看详细架构说明、API 映射和代码存放路径。
### 实际目录：
- ⚙️ [构建系统与多环境 productFlavors](./build.md) - `productFlavors {local/staging/prod}` 维度、`applicationIdSuffix` 后缀、BuildConfig 三域名注入、NetworkSecurityConfig 双锁机制、flavor-specific test sourceset（T-30050，Review Round 1 通过）。
- 🧱 [启动装配与壳层页面](./bootstrap.md) - `Application`、`AppContainer`、`MainActivity`、`MainViewModel` 的当前链路。
- 🌐 [核心基建与防腐层骨架](./foundation.md) - 环境配置、HTTP、WebSocket、遥测、媒体、IM 与调试适配器现状。
- 🧩 [业务骨架与测试现状](./features.md) - `auth/room/profile` 能力现状：room 模块已完成大厅页完整链路（T-30005）、Paging3 无限滚动（T-30006）、大厅页黑金视觉升级（T-30022，RoomCard/OnlineCountBadge/HallTopBar/CategoryTabRow）、**房间页黑金视觉升级（T-30025，HostMicSlot/MicSlotCard/EmptyMicSlot/MicSlotsGrid/ChatMessageList）**及**房间底部操作栏升级（T-30026，RoomBottomBar：GoldOutlinedTextField+MicButton三态+GiftButton/EmoteButton灰禁+ExitButton二次确认）**及**创建房间表单升级（T-30036，CreateRoomScreen+CoverPreview+CategoryDropdown+AnnouncementField+PasswordInputRow+IRoomRepository接口升级+38测试）**，`profile` 已完成个人中心完整链路（T-30024，ProfileScreen/ProfileViewModel/ProfileUiState/ProfileEvent）；测试覆盖面说明；**Compose UI 测试约定（T-30099）**：组件最外层 `Modifier.semantics(mergeDescendants=true)` 合并子树 + `testTag` 稳定标识（命名规约 `<feature>_<element>`），测试查询内部子节点用 `useUnmergedTree=true`；`performTextInput` 前需先 `performClick() + waitForIdle()` 确保软键盘就绪；AnimatedVisibility 内组件用 `assertExists()` 替代 `assertIsDisplayed()` 避免动画期间 flaky。
- 📡 [房间 WS 信令与协议绑定](./room.md) - WS 消息反序列化层（T-00101）：28 个 sealed class 子类型 + RuntimeTypeAdapterFactory + RoomViewModel `when(sealed)` dispatch，消灭 27+ 野生 `json.get()` 取字段，payload-nested + PROTO-BINDING 注释完整落锚（762 tests / 0 新增失败）。
- 💳 [钱包模块](./wallet.md) - 余额显示 + 流水列表（Paging3 分页）+ WebSocket 实时更新（T-30027，Review R2 通过，22 tests 全部通过）。
- 🎁 [礼物模块](./gift.md) - 礼物面板 BottomSheet（4列网格 + Tab 分类 + 数量选择器 + Mutex 缓存）+ WebSocket 实时余额更新 + 错误重试（T-30028，Review R2 通过，27 新单元测试全部通过）。
- 🏆 [榜单模块](./ranking.md) - 魅力/财富 × 日/周四组 Tab 榜单展示 + Top3 金银铜光圈 + Top1 王冠 + 我的排名 Sticky Footer + 下拉刷新（T-30033，Review R2 通过，18 个单元测试全部通过，HIGH-01 竞态取消机制已实现）。
- 📊 [Analytics 防腐层 + 事件上报客户端](./analytics.md) - `AnalyticsPort` 接口 + `SentryAnalytics`（DefaultSentryHub Stub，MVP 阶段）+ `SensitiveFilter`（手机号/JWT 脱敏）+ `ConsentMode`（FULL/CRASH_ONLY/NONE）+ `NoopAnalytics` + `BuildConfig.SENTRY_DSN` 注入 + CI 静态检查脚本（T-30034）；`EventReportClient`（本地队列 + Throttler + WS/HTTP Transport）+ `SessionManager`（30s 后台新 UUID）+ `CommonPropsProvider`（6 个公共字段含 `network_type`）+ `ConsentRepository`/`DataStoreConsentStore`（Properties 持久化）+ `PrivacyConsentDialog`（Compose，3 个 testTag）+ 20+ 核心事件埋点（T-30035，Review R2 通过，42 个单元测试全部通过）。
- 🔐 [Auth 认证模块](./auth.md) - 登录页组件结构（LoginScreen / LoginViewModel / LoginUiState）、+966 手机号输入、60s 倒计时、RTL 布局支持、StateFlow 数据流；**登录页视觉升级（T-30021）**：MenaTheme 黑金渐变背景 + GoldOutlinedTextField + GoldButton，纯 UI 改造功能不变。
- 🎨 [中东黑金主题系统](./theme.md) - MenaColors / MenaTypography / MenaShapes / MenaTheme + GoldButton / GoldOutlinedTextField / AvatarWithFrame 通用组件（T-30018）；**GoldButton / GoldOutlinedTextField 测试可达性强化（T-30099）**：外层 Box 加 `semantics(mergeDescendants=true)` 合并子树暴露统一语义节点，GoldOutlinedTextField 确保 onValueChange 回调单向数据流（内部不截断事件，直接透传到调用方 state holder）。
- 🚀 [Splash 启动页与 Compose Navigation](./bootstrap.md) - SplashScreen / SplashViewModel / SplashNavEvent / AppNavGraph / MainActivity Compose 改造（T-30019）。
- 🧭 [导航架构与 MainScreen 三Tab框架](./navigation.md) - AppNavGraph 三路由骨架（splash/login/main）、MainScreen 底部三Tab（Hall/Messages/Profile）、MenaBottomNavigation 金色主题导航、saveState/restoreState 策略（T-30020）。

## 🔌 协议入口索引 (Protocol Entry Index)

> **铁律**：每个跨端 Task 的 DoD 阶段必须把 TDS「协议路径绑定表」中**本端涉及的行**反向写入此表。本表是 android 端**所有**对外协议调用方的汇总，供 global-review、新人 onboarding 和重构变更影响面分析使用。

### 🔌 Schema 索引
- [Protocol Schemas](../../protocol/schemas/) — WS/HTTP/Pub/Sub 三协议层机器可读 Schema（T-00100 落锚）
  - `schemas/ws/` — 34 个 WebSocket 信令 Schema（含 Ping/Pong/JoinRoom/SendMessage 等）
  - `schemas/http/` — HTTP DTO Schema（含 RoomDetail.mic_slots 强类型）
  - `schemas/pubsub/` — Redis admin:events Schema（BanUser/UnbanUser/CloseRoom/BroadcastNotice）

| 协议类型 | 调用方法 | 客户端实调用方（文件:函数）| protocol/ 锚点 | 关联 Task | 服务端实现 |
|----------|---------|----------------------------|---------------|-----------|------------|
| WS S→C `MicTaken` ⭐ | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.MicTaken` | [websocket_signals.md §6.10](../../protocol/websocket_signals.md#610-mictaken) | T-00101 | `server/src/room/handler/mic.rs::handle_take_mic` |
| WS S→C `MicLeft` ⭐ | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.MicLeft` | [websocket_signals.md §6.11](../../protocol/websocket_signals.md#611-micleft) | T-00101 | `server/src/room/handler/mic.rs::handle_leave_mic` |
| WS S→C `UserJoined` ⭐ | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.UserJoined` | [websocket_signals.md §6.12](../../protocol/websocket_signals.md#612-userjoined) | T-00101 | `server/src/room/handler/lifecycle.rs::handle_join_room` |
| WS S→C `UserLeft` ⭐ | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.UserLeft` | [websocket_signals.md §6.13](../../protocol/websocket_signals.md#613-userleft) | T-00101 | `server/src/room/handler/lifecycle.rs::handle_leave_room` |
| WS S→C `UserMuted` ⭐ | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.UserMuted` | [websocket_signals.md §6.14](../../protocol/websocket_signals.md#614-usermuted) | T-00101 | `server/src/room/handler/governance/mute.rs::handle_mute_user` |
| WS S→C `AdminChanged` ⭐ | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.AdminChanged` | [websocket_signals.md §6.15](../../protocol/websocket_signals.md#615-adminchanged) | T-00101 | `server/src/room/handler/governance/transfer.rs::handle_transfer_admin` |
| WS S→C `RoomInfoUpdated` ⭐ | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.RoomInfoUpdated` | [websocket_signals.md §6.16](../../protocol/websocket_signals.md#616-roominfoupdated) | T-00101 | Multiple server handlers |
| WS S→C `Pong` ⭐ | `OkHttpWebSocketClient.onMessage` | `core/ws/OkHttpWebSocketClient.kt::pong 检测` | [websocket_signals.md §6.2](../../protocol/websocket_signals.md#62-心跳) | T-00108 | `server/src/ws/connection.rs::handle_ping` |
| WS S→C `RoomMessage` | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.RoomMessage` | [websocket_signals.md §6.9](../../protocol/websocket_signals.md#69-roommessage) | T-00101 | `server/src/room/handler/chat.rs::handle_send_message` |
| WS S→C `JoinRoomResult` | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.JoinRoomResult` | [websocket_signals.md §6.4](../../protocol/websocket_signals.md#64-joinroomresult) | T-00101 | `server/src/room/handler/lifecycle.rs::handle_join_room` |
| WS S→C `TakeMicResult` | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.TakeMicResult` | [websocket_signals.md §6.8](../../protocol/websocket_signals.md#68-takemicresult) | T-00101 | `server/src/room/handler/mic.rs::handle_take_mic` |
| WS S→C `SendGiftResult` | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.SendGiftResult` | [websocket_signals.md §6.19](../../protocol/websocket_signals.md#619-sendgiftresult) | T-00101 | `server/src/room/handler/gift.rs::handle_send_gift` |
| WS S→C `Unknown` | `RoomViewModel.handleWsMessage(msg: WsServerMessage)` | `feature/room/RoomViewModel.kt::handleWsMessage::is WsServerMessage.Unknown` | N/A (forward-compat) | T-00101 | N/A |
| WS C→S `Ping` ⭐ | `OkHttpWebSocketClient.startHeartbeat` | `core/ws/OkHttpWebSocketClient.kt::startHeartbeat` | [websocket_signals.md §6.2](../../protocol/websocket_signals.md#62-心跳) | T-00108 | `server/src/ws/connection.rs::handle_ping` |
| WS C→S `JoinRoom` | `RoomViewModel.joinRoom` | `feature/room/RoomViewModel.kt::joinRoom` | [websocket_signals.md §6.3](../../protocol/websocket_signals.md#63-joinroom) | T-00101 | `server/src/room/handler/lifecycle.rs::handle_join_room` |
| WS C→S `SendMessage` ⭐ | `RoomViewModel.sendMessage` | `feature/room/RoomViewModel.kt::sendMessage` | [websocket_signals.md §6.6](../../protocol/websocket_signals.md#66-sendmessagecs) | T-30054 | `server/src/room/handler/chat.rs::handle_send_message` |
| WS C→S `TakeMic` | `RoomViewModel.takeMic` | `feature/room/RoomViewModel.kt::takeMic` | [websocket_signals.md §6.5](../../protocol/websocket_signals.md#65-takemiccs) | T-00101 | `server/src/room/handler/mic.rs::handle_take_mic` |
| WS C→S `LeaveMic` | `RoomViewModel.leaveMic` | `feature/room/RoomViewModel.kt::leaveMic` | [websocket_signals.md §6.7](../../protocol/websocket_signals.md#67-leavemiccs) | T-00101 | `server/src/room/handler/mic.rs::handle_leave_mic` |
| _无（纯 UI）_ | _N/A_ | `feature/room/ChatMessageList.kt:UserMessageItem` | _N/A_ | T-30053 | _无（纯 Android UI 长按复制，不动协议）_ |

### T-00104 跨语言 E2E 入口索引（Android 侧）

**说明**：以下表格是 T-00104 跨语言 WebSocket E2E 测试套中涉及的 Android 侧主调用路径映射。所有测试遵循 **PROTO-BINDING** 规约，对所有收到的 server 消息进行 JSON Schema 校验。详见 [T-00104 TDS](../../tds/infra/T-00104.md) 和 [CROSS_LANG_WS_RUNBOOK](../../tests/CROSS_LANG_WS_RUNBOOK.md)。

| # | 场景 | Android 调用入口（⭐主路径） | 测试文件 | 协议锚点 |
|---|------|--------------------------|---------|---------|
| CROSS-1 | Ping | ⭐ `OkHttpWebSocketClient.startHeartbeat` → `{"type":"Ping",...}` | `tests/cross-lang/android-server-ws/CROSS-1-ping-pong.spec.ts` | [websocket_signals.md §6.5.1](../../protocol/websocket_signals.md#651-pingcs) |
| CROSS-1 | Pong | ⭐ `OkHttpWebSocketClient.onMessage` → pong 检测 | `CROSS-1-ping-pong.spec.ts` | [websocket_signals.md §6.6.1](../../protocol/websocket_signals.md#661-pongsc) |
| CROSS-2 | JoinRoom | ⭐ `RoomViewModel.joinRoom` → `{"type":"JoinRoom",...}` | `CROSS-2-join-room.spec.ts` | [websocket_signals.md §6.5.2](../../protocol/websocket_signals.md#652-joinroomcs) |
| CROSS-2 | UserJoined（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.UserJoined` | `CROSS-2-join-room.spec.ts` | [websocket_signals.md §6.7.1](../../protocol/websocket_signals.md#671-userjoinedsroom) |
| CROSS-3 | TakeMic | ⭐ `RoomViewModel.takeMic` → `{"type":"TakeMic",...}` | `CROSS-3-take-mic.spec.ts` | [websocket_signals.md §6.5.4](../../protocol/websocket_signals.md#654-takemiccs) |
| CROSS-3 | MicTaken（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.MicTaken` | `CROSS-3-take-mic.spec.ts` | [websocket_signals.md §6.7.3](../../protocol/websocket_signals.md#673-mictakensroom) |
| CROSS-4 | LeaveMic | ⭐ `RoomViewModel.leaveMic` → `{"type":"LeaveMic",...}` | `CROSS-4-leave-mic.spec.ts` | [websocket_signals.md §6.5.5](../../protocol/websocket_signals.md#655-leavemiccs) |
| CROSS-4 | MicLeft（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.MicLeft` | `CROSS-4-leave-mic.spec.ts` | [websocket_signals.md §6.7.4](../../protocol/websocket_signals.md#674-micleftsroom) |
| CROSS-5 | SendMessage | ⭐ `RoomViewModel.sendMessage` → `{"type":"SendMessage",...}` | `CROSS-5-send-message.spec.ts` | [websocket_signals.md §6.5.6](../../protocol/websocket_signals.md#656-sendmessagecs) |
| CROSS-5 | RoomMessage（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.RoomMessage` | `CROSS-5-send-message.spec.ts` | [websocket_signals.md §6.7.5](../../protocol/websocket_signals.md#675-roommessagesroom) |
| CROSS-6 | SendGift | ⭐ `GiftPanelViewModel.sendGift` → `{"type":"SendGift",...}` | `CROSS-6-send-gift.spec.ts` | [websocket_signals.md §6.5.7](../../protocol/websocket_signals.md#657-sendgiftcs) |
| CROSS-6 | GiftReceived（广播）| ⭐ `GiftPanelViewModel.handleWsMessage` → `is WsServerMessage.GiftReceived` | `CROSS-6-send-gift.spec.ts` | [websocket_signals.md §6.8.1](../../protocol/websocket_signals.md#681-giftreceivedsroom) |
| CROSS-7 | MuteUser | ⭐ Admin WS → `{"type":"MuteUser",...}` | `CROSS-7-mute-user.spec.ts` | [websocket_signals.md §6.5.10](../../protocol/websocket_signals.md#6510-muteusercs) |
| CROSS-7 | UserMuted（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.UserMuted` | `CROSS-7-mute-user.spec.ts` | [websocket_signals.md §6.7.6](../../protocol/websocket_signals.md#676-usermuted) |
| CROSS-8 | KickUser | ⭐ Admin WS → `{"type":"KickUser",...}` | `CROSS-8-kick-user.spec.ts` | [websocket_signals.md §6.5.9](../../protocol/websocket_signals.md#659-kickusercs) |
| CROSS-8 | UserLeft（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.UserLeft` | `CROSS-8-kick-user.spec.ts` | [websocket_signals.md §6.7.2](../../protocol/websocket_signals.md#672-userleftsroom) |

## 三、 当前能力全景与状态 (Capability Matrix)
> 状态枚举：🟢 已完成 | 🟡 开发/调试中 | 🔴 待开发 

### 核心能力
- 🟢 Application 启动装配、`BuildConfig` 环境注入与 `AppContainer` 依赖装配
- 🟢 productFlavors 多环境构建体系（T-30050，Review Round 1 通过）：`flavorDimensions += "env"` + `productFlavors { local / staging / prod }`；local flavor `isDefault=true` 保护迁移；3 个 flavor 各自 `buildConfigField` 注入 `API_BASE_URL`/`WS_URL`/`ANALYTICS_ENDPOINT`/`APP_ENVIRONMENT` + 独立 `applicationIdSuffix`（`.local`/`.stg`/无）；**双锁机制**：manifestPlaceholder 编译期 + `src/{local,staging,prod}/res/xml/network_security_config.xml` 运行时（local 允许明文，staging/prod 强制 HTTPS）；**flavor-specific test sourceset**（`src/test{Local,Staging,Prod}/`）物理隔离 BuildConfig 断言避免虚假失败；`local.properties` 三级 fallback 保留（D-1 开发体验不退化）；同设备可并存三档 APK；详见 [build.md](./build.md)
- 🟢 HTTP 客户端工厂、`RoomSocketRequestFactory` 与物理机 Loopback 预警
- 🟢 `MainActivity` Compose Navigation 改造 + `AppNavGraph` 三路由骨架 + Splash 启动页完整链路（T-30019）：`SplashScreen` / `SplashViewModel` / `SplashNavEvent` / `AppNavGraph`（splash/login/main），详见 [bootstrap.md](./bootstrap.md)
- 🟢 `auth` Feature 登录完整链路已完成（T-30001 + T-30002）：LoginScreen / LoginViewModel / IAuthRepository / RetrofitAuthRepository / TokenManager / NavEvent，详见 [auth.md](./auth.md)
- 🟢 登录页视觉升级（T-30021）：LoginScreen 改用 MenaTheme 黑金风格——`verticalGradient(Background→Surface)` 渐变背景 + `GoldOutlinedTextField`（PhoneInput/CodeInput）+ `GoldButton`（CountdownButton/登录按钮），功能逻辑不变，18 个 androidTest 覆盖，详见 [auth.md § T-30021](./auth.md#十一t-30021-登录页视觉升级黑金风格)
- 🟢 Retrofit 2.11.0 HTTP 客户端接入，`RetrofitAuthRepository` 实现登录 / 发码接口调用与错误映射
- 🟢 DataStore 1.1.1 JWT Token 持久化，`TokenManager` 线程安全读写
- 🟢 OkHttp JWT 拦截器（T-30003）：`AuthInterceptor` 自动注入 `Authorization: Bearer` header；`DefaultUnauthorizedHandler` 用 `AtomicBoolean.compareAndSet` 保证并发 401 只处理一次；登录成功后 `resetUnauthorized()` 重置，详见 [auth.md § T-30003](./auth.md#七t-30003-jwt-拦截器)
- 🟢 用户信息 Repository（T-30004）：`IUserRepository.getMe()` 领域接口 + `RetrofitUserRepository` 实现；`UserProfile` 领域模型与 DTO（`UserMeResponseData`）解耦；`coin_balance` 下划线映射、`vipLevel` 默认 0、401 由 `AuthInterceptor` 透传，详见 [auth.md § T-30004](./auth.md#十t-30004-用户信息-repository)
- 🟢 大厅页 UI（T-30005）：`IRoomRepository` + `RetrofitRoomRepository` + `HallViewModel` + `HallScreen`（`LazyVerticalGrid` + Coil + `RoomCard`）；`FakeRoomRepository` 供测试；`AppContainer` 新增 `roomRepository` 属性
- 🟢 大厅页视觉升级（T-30022）：`HallScreen` 改用 MenaTheme 黑金风格——深色背景 `MenaColors.Background` + `HallTopBar`（金色标题顶部栏）+ `CategoryTabRow`（分类横滑占位 Tab）+ `RoomCard`（深色底 `MenaColors.Surface` + 圆角 16dp + 房间封面/标题/房主昵称/在线人数）+ `OnlineCountBadge`（绿点 + 在线人数标签）+ 创建房间 FAB（金色渐变 `MenaColors.Primary`）；Paging3 分页逻辑不变（`collectAsLazyPagingItems` + `PullToRefreshBox`），纯 UI 改造功能不变，详见 [theme.md § T-30022](./theme.md)
- 🟢 房间列表 Paging3 分页（T-30006）：`RoomPagingSource`（`PagingSource<Int, RoomItem>`，`load()` 按页加载，`getRefreshKey()` 标准实现）+ `RoomListViewModel`（`PagingConfig(pageSize=20, initialLoadSize=20, enablePlaceholders=false, prefetchDistance=5)`，`cachedIn(viewModelScope)`）；`HallScreen` 升级为 `collectAsLazyPagingItems()` + Material3 `PullToRefreshBox`（`@OptIn(ExperimentalMaterial3Api::class)`，accompanist 已废弃）；**关键设计决策**：`initialLoadSize = pageSize = 20`，防止 Paging3 默认值 3×pageSize=60 导致 Refresh 与 Append 数据重叠
- 🟢 创建房间对话框（T-30007）：`CreateRoomUiState`（sealed interface：Idle/Loading/Success/Error）+ `CreateRoomViewModel`（输入校验 + `createRoom()` + `resetState()` + Factory）+ `CreateRoomBottomSheet`（Material3 ModalBottomSheet：标题输入框 1-30 字符 Unicode codePointCount 计算、房间类型 RadioButton normal/password/paid、密码框仅 type=password 时显示、`LaunchedEffect(Unit)` 进入时 resetState 防旧 Success 重播）；Data 层新增 `CreateRoomRequest.kt` / `CreateRoomResponseData.kt` DTO；`IRoomRepository` 扩展 `createRoom(title, type, password?): Result<String>`；`RoomApiService` 新增 `@POST("rooms")`；`AppContainer` 为 `roomRetrofit` 注入 `AuthInterceptor`（解决 POST /rooms 401）；**关键设计决策**：`resetState()` 模式防止 BottomSheet 重开时 `LaunchedEffect(uiState)` 重播旧 Success 导航；`MAX_TITLE_LENGTH = 30` 作为 ViewModel `internal const val`，UI 层直接引用避免漂移
- 🟢 创建房间表单升级（T-30036，Review R2 通过）：`CreateRoomScreen`（全屏 Scaffold，`CoverPreview` 封面选择区 testTag `cover_preview`）+ `CreateRoomFormState`（`data class`，`canSubmit` 计算属性：房名 `codePointCount` ≤30、公告 ≤200 字、密码开启时 `Regex("\\d{6}")` 6 位纯数字、封面 `coverUrl.isNotEmpty()`）+ `CategoryDropdown`（`ExposedDropdownMenu` 6 项 `RoomCategory` 枚举：CHAT/EMOTION/MUSIC/GAME/MATCHMAKING/OTHER）+ `AnnouncementField`（200 字计数，超限字数计数器变红）+ `PasswordInputRow`（6 位分格纯数字，Switch 开启时显示）；`IRoomRepository.createRoom()` 新增 `coverUrl/category/announcement` 参数（修复 HIGH-01）；`CreateRoomRequest` 新增 `cover_url/category/announcement` 可选字段；38 个单元测试全部通过（C36-01~C36-08c 含 C36-07c 验证三字段传仓库），Review R2 ✅；详见 [features.md § T-30036](./features.md#创建房间表单升级模块🟢-已完成t-30036review-r2-通过)
- 🟢 个人中心页（T-30024）：`feature/profile/ProfileScreen.kt`（"我的"Tab 完整页面：头像 `AvatarWithFrame` 80dp 金色光圈 + 昵称 `titleMedium` + ID 行 `ClipboardManager.setText()` 一键复制 + 余额行金色 `Primary` 色展示；退出登录 `AlertDialog` 二次确认 → `tokenManager.clearToken()` → `ProfileEvent.NavigateToLogin`；网络异常时回退展示本地缓存数据，缓存为空则显示加载失败占位）+ `feature/profile/ProfileViewModel.kt`（`StateFlow<ProfileUiState>` 驱动 UI + `SharedFlow<ProfileEvent>` 一次性事件（`NavigateToLogin`）；`loadProfile()` 调用 `IUserRepository.getMe()`，成功更新缓存与 State，异常时回退 `cachedProfile`；退出登录 `logout()` 调用 `ITokenManager.clearToken()` 后发射 `NavigateToLogin`；`Factory(userRepository, tokenManager)` 手动注入）+ `feature/profile/ProfileUiState.kt`（`data class`：`isLoading: Boolean`、`profile: UserProfile?`、`error: String?`）+ `feature/profile/ProfileEvent.kt`（`sealed class`：`NavigateToLogin`）；包路径 `com.voice.room.android.feature.profile`，详见 [features.md](./features.md)
- 🟢 Analytics 防腐层（T-30034，Review R1 通过）：`core/analytics/AnalyticsPort`（接口 + `ConsentMode` 枚举）+ `SentryAnalytics`（内含 `SentryHub` 内部接口，`DefaultSentryHub` MVP Stub，`FakeSentryHub` 测试注入）+ `SensitiveFilter`（手机号 `+?\d{7,15}` + JWT `eyJ...` 脱敏）+ `NoopAnalytics` + `BuildConfig.SENTRY_DSN` 注入 + `check_no_sentry_imports.sh` CI 静态约束脚本；42 个单元测试全部通过；详见 [analytics.md](./analytics.md)
- 🟢 EventReportClient + 核心事件埋点 + 隐私弹窗（T-30035，Review R2 通过）：`EventReportClient`（`track()` ConsentMode 门控 + `flush()` 最多 100 条批次）+ `InMemoryEventQueueDao`（队列，容量上限 1000 条淘汰最旧）+ `Throttler`（≥8 条 OR ≥2min OR onStop OR WS 重连四触发）+ `WsTransport`/`HttpTransport`（WS 优先 HTTP fallback，指数退避 2s→60s，HttpTransport `withContext(Dispatchers.IO)`）+ `SessionManager`（前台 UUID，30s 后台后新建，Clock 抽象）+ `CommonPropsProvider`（6 个公共字段：`device_id/app_version/os_version/locale/network_type/session_id`，`networkTypeProvider` 函数注入）+ `ConsentRepository` + `DataStoreConsentStore`（Java Properties 文件持久化，冷重启同意状态不丢）+ `PrivacyConsentDialog`（Compose，`Key("privacy_consent_dialog")`/`Key("btn_privacy_crash_only")`/`Key("btn_privacy_agree")` testTag，双语标题）+ 26 个核心事件埋点（从 `app_launch` 到 `logout_click`）；42 个单元测试全部通过；详见 [analytics.md § 十二](./analytics.md)
- 🟡 Telemetry / Media / IM 已通过接口与 `NoOp*` 适配器隔离第三方依赖
- 🟢 WebSocket 连接封装（T-30008）：`WebSocketState`（sealed class: Connecting/Connected/Disconnected/Error）+ `IWebSocketClient`（接口: connect/disconnect/send/state/messages）+ `OkHttpWebSocketClient`（指数退避重连 1s→60s、30s 心跳保活、`SharedFlow<String>` 消息流）+ `FakeWebSocketClient`（测试辅助），详见 [foundation.md § WebSocket](./foundation.md)
- 🟢 房间页 UI（T-30009）：`RoomUiState`（roomId/roomName/onlineCount/micSlots/messages）+ `MicSlotUi`（index/userId/nickname/avatarUrl/isMuted/isOccupied）+ `ChatMessageUi`（messageId/senderNickname/content/timestamp）；`RoomTopBar`（房间名+在线人数+返回按钮）+ `MicSlotsGrid`（`LazyVerticalGrid` 固定 3 列，高度 240dp，`userScrollEnabled=false`，`testTag("mic_slots_grid")`）+ `MicSlotItem`（空麦/有人/静音三态，Coil AsyncImage 头像，muted icon `testTag`）+ `ChatMessageList`（`LazyColumn` + `weight(1f)` 撑满剩余高度，`LaunchedEffect` 自动滚到底）+ `BottomInputBar`（TextField + 发送按钮）
- 🟢 房间 ViewModel（T-30010）：`RoomViewModel`（管理房间 WS 状态，`joinRoom(roomId)` / `leaveRoom()` / `sendMessage(text)`，`uiState: StateFlow<RoomViewState>` + `events: SharedFlow<RoomEvent>`，`viewModelScope` 内监听 `IWebSocketClient.messages` 解析服务端广播被动渲染）+ `RoomViewState`（sealed class：`Loading` / `Success(RoomUiState)` / `Error(message)`）+ `RoomEvent`（sealed class：`NavigateBack` / `ShowToast(message)`）；Data 层新增 `IRoomSnapshotRepository`（HTTP 获取房间初始快照）+ `RoomSnapshot`（领域模型：roomId/roomName/onlineCount/micSlots/recentMessages，DTO 与领域模型解耦）+ `RetrofitRoomSnapshotRepository`（`GET /rooms/{id}/snapshot` 实现，401 由 `AuthInterceptor` 透传）；**关键设计决策**：进入房间先 HTTP 拉取快照渲染首屏，再 WS joinRoom 追增量事件，防止 WS 连接建立前白屏；`leaveRoom()` 在 `onCleared()` 兜底，防止后退键绕过导致资源泄漏

  - **Round 13 更新（wsClient.connect 真正注入）**：
    
    #### RoomViewModel WS 连接生命周期（Round 13 更新）
    
    **连接时序**：
    1. `joinRoom(roomId)` 调用时，先通过 `tokenManager.getToken()` 获取 JWT
    2. 构造 `RoomSocketRequestSpec`（baseWsUrl + ?token=JWT）
    3. **调用 `wsClient.connect(spec)` 启动 WS 握手（Round 12 实证 cf899bd 为死代码，本轮真正激活）**
    4. 等待 `wsClient.state` 变为 `Connected`（超时 5 秒）
    5. 连接成功后发送 `JoinRoom` envelope
    6. `leaveRoom()` 调用 `wsClient.disconnect()`
    
    **幂等性保护**：
    - `joiningRoomId`：连接进行中，防止同房间重复 connect（Connecting 期间）
    - `joinedRoomId`：连接成功后设置，允许 Connected/Message 状态幂等跳过
    - 失败路径（超时/异常）清理两个字段，允许重试
    
    **切换房间**：自动 `disconnect()` 旧连接 + `cancel()` 旧 joinJob
- 🟢 麦位组件（T-30011）：`MicSlotCard.kt`（三态麦位卡片：`MicSlotState.EMPTY` / `OCCUPIED` / `MUTED`，`AnimatedVisibility` 驱动头像与 muted icon 进出场动画，双层 semantics 架构：外层 `clickable` + 内层 `semantics { contentDescription = … }` 精确描述当前状态，CompositionLocalProvider 保证 RTL 布局一致性）+ `MicSlotsGrid.kt`（新增 `onMicSlotClick: (index: Int) -> Unit = {}` 回调参数，事件上浮至 ViewModel 层）；`MicSlotCardTest.kt`（androidTest，MC-01 ～ MC-12 共 12 个测试用例，编译通过，需实体设备或 API 31+ AVD 运行）
- 🟢 麦克风权限请求（T-30012）：`MicPermissionHandler.kt`（权限守卫 Composable，content lambda 模式：权限已授予时直接渲染 content，否则触发请求流程；`MicPermissionHelper` 接口隔离 Accompanist，`permissionRequested` 状态防止首次请求被误判为永久拒绝）+ `MicPermissionRationaleDialog`（引导用户授权的说明对话框）+ `MicPermissionSettingsDialog`（永久拒绝后引导跳转系统设置的对话框）；`RoomScreen.kt` 新增 `onMicPermissionGranted` 参数；`RoomViewModel.kt` 新增 stub 方法；`AndroidManifest.xml` 新增 `RECORD_AUDIO` 权限声明；依赖新增 `accompanist-permissions 0.36.0`；测试：androidTest MP-01～MP-07（权限守卫交互流程）+ 单元测试 MP-08（ViewModel stub 验证）
- 🟢 上麦/下麦逻辑（T-30013）：`core/media/IMediaService.kt`（防腐层接口：`joinChannel(roomId, userId)` / `leaveChannel()` / `startPublishAudio()` / `stopPublishAudio()` 共 4 个 suspend 方法）+ `core/media/NoOpMediaService.kt`（生产 MVP 占位实现，4 个方法均返回 `Result.success(Unit)`）+ `core/media/FakeMediaService.kt`（测试注入，记录调用次数；`joinChannelResult` / `startPublishAudioResult` / `stopPublishAudioResult` / `leaveChannelResult` 支持注入失败场景）；`RoomViewModel.kt` 完整实现上麦/下麦：`onMicPermissionGranted(slotIndex)` 发送 `TakeMic` WS 消息 → 收到 `MicTaken` 广播且 `userId == currentUserId` → `joinChannel` + `startPublishAudio`（两步均检查 `Result`，失败时 `ShowToast`，`CancellationException` 正确 re-throw）；`onMicSlotClick(slotIndex)` 三路路由：空麦位静默 / 自己的麦位 → `leaveMic` / 他人麦位静默；`leaveMic(slotIndex)` 发送 `LeaveMic` WS 消息 → 收到 `MicLeft` 广播（先读 `leavingUserId` 再更新 UI）且 `leavingUserId == currentUserId` → `stopPublishAudio` + `leaveChannel`（try-catch 静默，`CancellationException` re-throw）；`currentUserId` 字段在 `joinRoom(roomId, userId = "")` 时写入，默认空串保持向后兼容；测试：TM-01～TM-08 + TM-03B（`startPublishAudio` 失败路径），`RoomViewModelTest` 共 22 个单元测试全部通过
- 🟢 聊天列表 Compose（T-30014）：`MessageType` 枚举（定义于 `RoomUiState.kt`：`USER_TEXT` / `SYSTEM_NOTICE`）+ `ChatMessageUi` 数据类（`messageId: String` / `senderNickname: String?` / `content: String` / `timestamp: Long` / `messageType: MessageType`，`messageType` 默认 `USER_TEXT` 向后兼容）；`ChatMessageList.kt`（`LazyColumn` 聊天列表：`USER_TEXT` → `UserMessageItem` 左对齐 Row，昵称 labelSmall primary 色 + 内容 bodyMedium；`SYSTEM_NOTICE` → `SystemNoticeItem` Box 居中，`onSurface 60%` 灰色）；**去重**：`remember(messages) { messages.distinctBy { it.messageId } }` 防止 `LazyColumn` key 冲突；**自动滚动**：`LaunchedEffect(deduplicated.size) { listState.animateScrollToItem(deduplicated.lastIndex) }`；**RTL 支持**：`UserMessageItem` 使用 `Row`，天然遵从 `CompositionLocal LayoutDirection`，无需手动镜像；**testTag 协议**：`LazyColumn` → `"chat_message_list"`，`USER_TEXT` 子项 → `"user_message_{index}"`，`SYSTEM_NOTICE` 子项 → `"system_message_{index}"`（均通过 `semantics { testTag = … }` 注入，`mergeDescendants = true` 确保文本内容可通过父节点断言）；测试：`ChatMessageListTest.kt`（androidTest，CL-01 空列表无崩溃、CL-02 USER_TEXT 昵称+内容可见、CL-03 SYSTEM_NOTICE 居中内容可见、CL-04 混合消息全部渲染、CL-05 testTag 协议三标签均可定位、CL-06 20 条消息自动滚动末尾、CL-07 RTL 布局无崩溃、CL-08 重复 messageId 去重渲染），编译通过
- 🟢 聊天输入框组件（T-30015）：`ChatInputBar.kt`（无状态 Composable：`inputText: String` / `onInputTextChange` / `onSendMessage` / `isSending: Boolean = false`；`canSend = inputText.isNotBlank() && !isSending`；`TextField(singleLine=true, testTag="chat_input_field", KeyboardOptions(ImeAction.Send), KeyboardActions(onSend { if(canSend) onSendMessage(inputText) })`；`IconButton(enabled=canSend, testTag="chat_send_button")`；布局 `Row`：`TextField(weight=1f)` + `Spacer(8dp)` + `IconButton`，`padding(horizontal=12.dp, vertical=8.dp)`）；**禁用规则**：空消息 / 纯空白字符 / `isSending=true` 三种场景均禁用发送按钮与 IME 回车触发；`RoomScreen.kt` 修改：`Scaffold(modifier=modifier.imePadding())` 软键盘适配 + `bottomBar` 替换旧 `BottomInputBar` → `ChatInputBar` + `var localInputText by remember { mutableStateOf("") }` 本地状态（T-30016 将提升至 ViewModel）+ 发送后 `localInputText = ""` 清空；`RoomScreenTest.kt` 同步更新 testTag `message_input`/`send_button` → `chat_input_field`/`chat_send_button`；**testTag 协议**：`chat_input_field`（原 `message_input` 废弃）/ `chat_send_button`（原 `send_button` 废弃）；测试：`ChatInputBarTest.kt`（androidTest，CI-01 空文本禁用、CI-02 空白字符禁用、CI-03 有效文本启用、CI-04 点击发送回调正确、CI-05 IME Send 触发回调、CI-06 testTag 可定位、CI-07 isSending=true 禁用 + CI-05b 空白 IME 不发送 + CI_onChange 回调验证，共 9 个测试），`compileDebugKotlin` ✅ / `compileDebugAndroidTestKotlin` ✅ / `testDebugUnitTest` 30 tests passed
- 🟢 发送消息逻辑（T-30016）：`RoomViewModel.sendMessage(content)` 完整实现：**空消息守卫** `content.isBlank()` 提前返回；**发送中状态** `isSendingMessage = true`（`try-finally` 保证复位，无论成功或异常均清除 `isSendingMessage = false`）；**WS 发送** `IWebSocketClient.send(SendMessage JSON)`；**成功路径** 发出 `RoomEvent.ClearInput` 通知 UI 清空输入框；**失败路径** `catch(e: Exception)` → `ShowToast(e.message)` + `isSendingMessage` 复位后按钮恢复可点击（可重试）；**`CancellationException` re-throw** 在 catch 块内 `if (e is CancellationException) throw e` 保证协程取消语义正确传播不被 ShowToast 吞掉；新增 `RoomEvent.ClearInput`（`RoomEvent` sealed class 新增子类，无附加数据，`RoomScreen` 的 `LaunchedEffect(Unit)` 监听 `events` 流，`is RoomEvent.ClearInput → localInputText = ""`，替换旧 T-30015 的发送后本地清空逻辑）；新增 `RoomUiState.isSendingMessage: Boolean`（默认 `false`，向后兼容，`RoomScreen` 传递给 `ChatInputBar(isSending = uiState.isSendingMessage)` 实现发送期间按钮自动禁用）；`FakeWebSocketClient` 新增 `sendThrowable: Throwable? = null` 字段（`send()` 实现：非 null 时抛出该异常，供单元测试注入发送失败与 `CancellationException` 场景）；`RoomScreen.kt` 集成：`onSendMessage = { vm.sendMessage(it) }` 替换旧回调 + `ClearInput` 事件监听清空输入 + `isSending` 状态传递；测试：`RoomViewModelTest`（单元测试：SM-01 空字符串不触发 WS send、SM-02 纯空白字符不触发 WS send、SM-03 正常发送过程中 `isSendingMessage` 先 `true` 后 `false`、SM-04 发送成功后 `events` 收到 `ClearInput`、SM-05 WS `send` 抛 `IOException` 后收到 `ShowToast` 且 `isSendingMessage` 复位、SM-06 `CancellationException` re-throw 不被 `ShowToast` 拦截，共 6 个单元测试），`compileDebugKotlin` ✅ / `testDebugUnitTest` ✅
- 🟢 房间页视觉升级（T-30025）：`RoomScreen` 完整改造为黑金风格，WS/上下麦逻辑不变。新增/改造组件：**HostMicSlot**（主麦 80dp `AvatarWithFrame` + Canvas `GoldGlowRing` 金色光圈，stroke 6dp `MenaColors.Primary`，居中突出展示）；**MicSlotCard**（副麦 60dp，深色背景 `MenaColors.Surface`，三态 EMPTY/OCCUPIED/MUTED）；**EmptyMicSlot**（虚线圆圈 + "+" 图标，可点击上麦触发 `onMicSlotClick`）；**MicSlotsGrid**（`LazyVerticalGrid` 固定 4 列，替换原 3 列布局，`userScrollEnabled=false`）；**ChatMessageList** 弹幕样式升级：`USER_TEXT` 消息昵称改为 `MenaColors.Primary` 金色，`SYSTEM_NOTICE` 系统消息金黄居中展示；`RoomScreen` 整体背景改为 `MenaColors.Background` 深色，顶部栏沿用 `RoomTopBar` 黑金风格。RoomViewModel 未修改，WS 消息格式与上下麦逻辑完全不变。详见 [features.md § T-30025](./features.md#房间页视觉升级模块t-30025)
- 🟢 房间底部操作栏升级（T-30026）：`RoomBottomBar` Composable 替换原 `ChatInputBar`，采用 `Row` 布局：**GoldOutlinedTextField**（输入框，`weight(1f)` 撑满剩余宽度，复用 T-30018 通用组件，软键盘 `imePadding()` 适配）+ **MicButton**（🎤 三态：`isOnMic=false` → 灰色禁用 `MenaColors.OnBackgroundTertiary`；`isOnMic=true && !isMuted` → 绿色激活 `Color(0xFF4CAF50)`；`isOnMic=true && isMuted` → 红色静音 `MenaColors.Error`；点击触发 `RoomViewModel.toggleMicMute()`）+ **GiftButton**（🎁 灰色禁用 + 点击弹 `Toast("敬请期待")`）+ **EmoteButton**（❤️ 灰色禁用 + 点击弹 `Toast("敬请期待")`）+ **ExitButton**（🚪 点击弹 `AlertDialog` 二次确认，确认后 `RoomViewModel.leaveRoom()` + 导航返回）；`RoomViewModel` 新增 `toggleMicMute()` 方法（仅在 `isCurrentUserOnMic=true` 时切换 `isCurrentUserMuted` 状态，不在麦上时为 no-op）+ `RoomUiState` 新增 `isCurrentUserOnMic: Boolean`（默认 `false`）与 `isCurrentUserMuted: Boolean`（默认 `false`）字段；`RoomScreen.kt` 中 `Scaffold.bottomBar` 替换为 `RoomBottomBar`；**testTag 协议**：`room_bottom_bar` / `mic_button` / `gift_button` / `emote_button` / `exit_button` / `exit_confirm_dialog`；测试：单元测试 BB-01（不在麦时 toggleMicMute 为 no-op）/ BB-02（在麦未静音时 toggleMicMute → isMuted=true）/ BB-03（在麦已静音时 toggleMicMute → isMuted=false），androidTest BB-04（MicButton 灰色不可点态）/ BB-05（GiftButton 点击显示 Toast）/ BB-06（EmoteButton 点击显示 Toast）/ BB-07（ExitButton 点击弹 AlertDialog）/ BB-08（AlertDialog 取消后不退出）/ BB-09（AlertDialog 确认后触发 leaveRoom），详见 [features.md § T-30026](./features.md#房间底部操作栏升级模块t-30026)
- 🟢 接收消息逻辑（T-30017）：`RoomViewModel.handleNewMessage(payload)` 新增/完善：**msgId 守卫** `msgId ?: return`（null msgId 直接丢弃，防止无法去重的匿名消息进入列表）；**seenMsgIds 去重** `if (!seenMsgIds.add(msgId)) return`（`LinkedHashSet<String>` 成员变量，`add` 返回 false 表示重复，在 content 校验之前执行；**设计意图：即使 content 为空也消耗该 msgId，防止同一 msgId 携带不同 content 二次投递**）；**content 守卫** `content ?: return`（**修复 bug**：旧实现 `content ?: ""` 会渲染空白气泡，现改为 `?: return` 直接丢弃）；**ChatMessageUi 构建** `ChatMessageUi(messageId=msgId, senderNickname=sender, content=content, timestamp=System.currentTimeMillis(), messageType=MessageType.USER_TEXT)`；**不可变追加** `state.messages + newMsg`（产生新 List，Compose `remember(messages)` 能感知引用变化触发重组，避免 `MutableList.add` 的静默失效）；**状态更新** 仅在 `uiState.value is RoomViewState.Success` 时执行 `copy(roomUiState = roomUiState.copy(messages = …))`，其余状态（Loading/Error）静默忽略；**测试**：`RoomViewModelTest` 新增 RM-03（正常消息追加到列表）/ RM-04（重复 msgId 去重不追加）/ RM-05（content 为 null 丢弃不渲染空白气泡）共 3 个新用例；VM-07（`seenMsgIds.add` 在 content 验证之前消耗 msgId，防止 msgId 重用）/ VM-08（空 content 与重复 msgId 同时到达时的边界行为）等效覆盖 RM-01/RM-02；**单元测试总计 235 个**，`compileDebugKotlin` ✅ / `testDebugUnitTest` ✅
- 🟢 中东黑金主题系统（T-30018）：`MenaColors`（11 个黑金色彩令牌：Background/Surface/Primary/Error 等，`darkColorScheme` 构建）+ `MenaTypography`（5 级排版规范 Roboto 22sp/16sp/14sp/12sp/11sp）+ `MenaShapes`（3 级圆角：大卡片 16dp / 按钮 24dp / 输入框 12dp）+ `MenaTheme`（主题入口 Composable，`darkColorScheme` + RTL 自动检测 `TextUtils.getLayoutDirectionFromLocale`）+ `GoldButton`（金色渐变胶囊按钮，`clickable(enabled, role=Button)` 语义正确）+ `GoldOutlinedTextField`（深色底 + 金色边框输入框）+ `AvatarWithFrame`（Coil 圆形头像 + 可选金色光圈）；测试 33 个用例（7 JVM + 26 androidTest）全部通过，详见 [theme.md](./theme.md)
- 🟢 MainScreen 底部三Tab框架（T-30020）：`feature/main/MainScreen.kt`（Scaffold + NavHost + MenaBottomNavigation）+ `MainTab` enum（Hall/Messages/Profile 三个 Tab）+ `MenaBottomNavigation.kt`（MenaTheme 金色主题底部导航栏，选中项金色高亮 `MenaColors.Primary`，未选中灰色 `MenaColors.OnSurface`）；Tab 切换使用 `saveState = true` / `restoreState = true` / `launchSingleTop = true` 策略保持各页面状态；默认显示 Hall Tab；包路径 `com.voice.room.android.feature.main`，详见 [navigation.md](./navigation.md)
- 🟢 消息Tab占位页（T-30023）：`core/ui/PlaceholderScreen.kt`（通用可复用占位组件：`PlaceholderScreen(title, icon?, subtitle?, modifier?)` + `PlaceholderScreenDefaults` 设计规范常量；背景 `MenaColors.Background`，图标 64dp `MenaColors.OnBackgroundTertiary`，标题 `titleMedium` `MenaColors.OnBackgroundSecondary`，副标题 `bodySmall`；居中 `Column`；testTag `"placeholder_screen"`）+ `feature/main/MessagesPlaceholder.kt`（委托 `PlaceholderScreen`，传入 `icon=Icons.AutoMirrored.Outlined.Chat`、`title="消息功能即将上线"`、`subtitle="敬请期待"`；外层 `Box(testTag("messages_placeholder"))` 保持 T-30020 兼容性）；测试 18 个用例（7 JVM + 11 androidTest），详见 features.md
- 🟢 Splash 启动页与 Compose Navigation（T-30019）：`feature/splash/SplashScreen.kt`（品牌 Splash 页 Composable：深色背景 `MenaColors.Background` + 居中 Logo `ic_logo.xml` 金色麦克风矢量图 120dp + 缩放 `Animatable(0.5f→1f)` + 淡入 `Animatable(0f→1f)` 并行动画 800ms `EaseOut` + 底部版本号；动画完成后 `delay(500L)` 再调 `checkAuth()`；`LaunchedEffect(Unit)` 监听 `navEvent` 驱动 `NavController` 导航；testTag 协议：`splash_screen` / `splash_logo` / `splash_version`）+ `feature/splash/SplashViewModel.kt`（JWT 检测逻辑：`checkAuth()` 调用 `ITokenManager.getToken()`，非 null 且非空白 → `NavigateToMain`，否则 → `NavigateToLogin`；`CancellationException` 正确 re-throw 保持协程取消语义；`MutableSharedFlow<SplashNavEvent>` 发射一次性导航事件；`Factory(tokenManager: ITokenManager)` 支持手动依赖注入）+ `feature/splash/SplashNavEvent.kt`（sealed class：`NavigateToMain` / `NavigateToLogin`）+ `presentation/AppNavGraph.kt`（Compose Navigation `NavHost` 全局导航骨架：`startDestination = "splash"`，三路由 `splash` / `login` / `main`；`popUpTo("splash") { inclusive = true }` 防止返回键回退到 Splash；`main` 路由当前为 `MainPlaceholderScreen` 占位，后续 T-30020 替换为三 Tab 框架）+ `presentation/MainActivity.kt`（改造为 `ComponentActivity` + `setContent { MenaTheme { AppNavGraph(appContainer) } }`，移除旧 XML 壳层绑定）+ `common/AppContainer.kt`（`tokenManager: ITokenManager` 提升为公开 `val` 属性，供 `SplashViewModel.Factory` 注入）+ `res/drawable/ic_logo.xml`（金色麦克风矢量图标）；测试：6 个单元测试 `SplashViewModelTest.kt`（SP-01 有效 token→NavigateToMain、SP-02 null token→NavigateToLogin、SP-03 空白 token→NavigateToLogin、SP-04 getToken 异常→NavigateToLogin、SP-05a/SP-05b CancellationException re-throw）+ 4 个 UI 测试 `SplashScreenTest.kt`（SP-06 Logo 可见、SP-07 版本号可见、SP-08 深色背景、SP-09 testTag 定位），编译通过
- 🟢 钱包页完整链路（T-30027，Review R2 通过）：`WalletUiState`（balance/loadingBalance/refreshing/error）+ `WalletEvent`（ShowToast/NavigateToLogin/RefreshTransactions）+ `WalletViewModel`（Manual Factory + StateFlow + SharedFlow；init → `loadBalance` + `subscribeToWsEvents`；WS `BalanceUpdated` 按 protocol §6.4.1 读取嵌套 `payload.diamond_balance` → 更新余额 + RefreshTransactions；401 → NavigateToLogin；CancellationException re-throw）+ `WalletScreen`（Scaffold + TopAppBar + `PullToRefreshBox` + `LazyColumn`：余额大卡片 + 流水分页列表 + 空状态占位；testTag 协议：`wallet_balance_value`/`btn_wallet_recharge`/`wallet_txn_list` 挂在 LazyColumn/`wallet_empty`）+ `WalletTxnPagingSource`（`PagingSource<Int, WalletTxn>`，1-based 页码，`lastPage = size < loadSize`）+ `RetrofitWalletRepository`（`getBalance`→`Long` / `listTxns`→`TxnsPage`，与 RetrofitUserRepository 统一 parseBody 错误处理策略；`refresh()` 补充 401 → NavigateToLogin 处理）+ `WalletApiService`（GET `wallet/balance` + GET `wallet/transactions`）+ 领域模型 `WalletTxn`/`TxnsPage`；`IWalletRepository` 扩展 `getBalance()`/`listTxns()` 接口，`walletPreviewLabel()` 向后兼容；`AppContainer` 注入 `RetrofitWalletRepository`；`ProfileContent.kt` 余额行新增 `onNavigateToWallet` clickable（W27-09）；`MainScreen` 内部 NavHost 新增 "wallet" composable；WS 协议 `payload.diamond_balance` 嵌套字段读取（R1-CRITICAL 修复）；单元测试 22 个全部通过（WalletViewModelTest 15 个：W27-01~08 + R1-CRITICAL-1/1b + R1-HIGH-3/3b + WalletTxnPagingSourceTest 7 个），`compileDebugKotlin` ✅ / `assembleDebug` ✅，Review R2 ✅，详见 [tds/android/T-30027.md](../../tds/android/T-30027.md) + [wallet.md](./wallet.md)
- 🟢 ChatMessageList 气泡样式（T-30052）：`UserMessageItem` Surface 容器（`shape=MaterialTheme.shapes.medium` + `color=MenaColors.ChatBubble #2A2A2A` + `widthIn(max=280.dp)` + `padding(horizontal=10.dp, vertical=6.dp)` + `testTag("chat_bubble")`），供 Midscene 视觉 AI 识别"气泡形状"；可见性 `private→internal` 供 androidTest（CB-01/02/03）直调；MenaColors 新增 `ChatBubble` 令牌；`SystemNoticeItem` 与业务逻辑、5 节点 WS 日志（`ws: received` / `rvm: onWsMessage` / `ws: parse` / `ws: dispatch` / `ui: chatMessages`）完整保留
- 🟢 ChatMessageList 长按复制菜单（T-30053）：`UserMessageItem` 接入 `combinedClickable(onLongClick)` + `DropdownMenu`，外层 `Row→Box` 作为菜单锚点；菜单含「复制」项（`testTag("chat_msg_copy")` + `contentDescription="chat_msg_copy"`），点击后通过 `LocalContext.current` 获取 `ClipboardManager` 调用 `setPrimaryClip(ClipData.newPlainText(...))` 写入消息原文 + `Toast.makeText(...).show()` 提示；`DropdownMenu` 自身 `contentDescription="chat_msg_long_press_menu"`；保留 T-30052 气泡 Surface/`MenaColors.ChatBubble`/`testTag("chat_bubble")` 与 T-30051 5 节点 `Log.d("ChatMessageList", ...)` 可观测性日志；`@OptIn(ExperimentalFoundationApi::class)` 注解；新增 `ChatLongPressTest.kt`（5 用例覆盖 LP-01/02/03/05/08）；详见 [TDS T-30053](../../tds/android/T-30053.md)
- 🔴 WebSocket 长连接状态机真实鉴权接入、房间同步、RTC/IM 接入
- 🔴 礼物、麦位、家族、CP、VIP、背包、小游戏等业务页面与数据链路


### 遗留技术债 (Tech Debt)
- `auth` 模块已完成 UI + ViewModel + Repository + DataStore + OkHttp JWT 拦截器 + 用户信息 Repository 完整链路（T-30001 / T-30002 / T-30003 / T-30004）；`room` 模块已完成大厅页完整链路（T-30005）、Paging3 无限滚动分页（T-30006）、创建房间对话框（T-30007，含 `CreateRoomViewModel` / `CreateRoomBottomSheet` / `IRoomRepository.createRoom` / DTO / `AppContainer` AuthInterceptor 修复）、房间页 UI（T-30009，`RoomScreen` / `RoomUiState` / `MicSlotsGrid(240dp)` / `MicSlotItem` / `ChatMessageList` / `BottomInputBar`）与房间 ViewModel（T-30010，`RoomViewModel` + `RoomViewState` sealed class + `RoomEvent` sealed class + `IRoomSnapshotRepository` / `RoomSnapshot` HTTP 快照 + WS joinRoom/leaveRoom/sendMessage 完整链路）；`feature/mic` 已完成麦位组件（T-30011，`MicSlotCard.kt` 三态 EMPTY/OCCUPIED/MUTED + `AnimatedVisibility` 动画 + 双层 semantics + RTL 支持；`MicSlotsGrid.kt` 新增 `onMicSlotClick` 回调；`MicSlotCardTest.kt` MC-01～MC-12 androidTest）与麦克风权限请求（T-30012，`MicPermissionHandler.kt` 权限守卫 Composable content lambda 模式 + `MicPermissionHelper` 接口隔离 Accompanist + `permissionRequested` 防首次误判；`MicPermissionRationaleDialog` + `MicPermissionSettingsDialog` 两个对话框子组件；`RoomScreen.kt` 新增 `onMicPermissionGranted` 参数；`RoomViewModel.kt` 新增 stub 方法；`AndroidManifest.xml` 新增 `RECORD_AUDIO`；依赖新增 `accompanist-permissions 0.36.0`；androidTest MP-01～MP-07 + 单元测试 MP-08）；`core/ws` 已完成 WebSocket 连接封装（T-30008，`WebSocketState` sealed class / `IWebSocketClient` 接口 / `OkHttpWebSocketClient` 指数退避重连+心跳保活 / `FakeWebSocketClient` 测试辅助）；`core/media` 已完成上麦/下麦逻辑（T-30013，`IMediaService.kt` 防腐层接口 4 个 suspend 方法；`NoOpMediaService.kt` 生产 MVP 占位实现；`FakeMediaService.kt` 测试注入含 4 个可注入失败字段；`RoomViewModel.kt` 完整上麦流程 `onMicPermissionGranted` → TakeMic WS → MicTaken → `joinChannel`+`startPublishAudio`，完整下麦流程 `onMicSlotClick` → `leaveMic` → LeaveMic WS → MicLeft → `stopPublishAudio`+`leaveChannel`，所有媒体协程 `CancellationException` 正确 re-throw；单元测试 TM-01～TM-08 + TM-03B 共 22 个全部通过）；`feature/room` 已完成聊天列表 Compose（T-30014，`MessageType` 枚举 `USER_TEXT`/`SYSTEM_NOTICE` 定义于 `RoomUiState.kt`；`ChatMessageUi` 扩展 `messageType` 字段默认 `USER_TEXT` 向后兼容；`ChatMessageList.kt` LazyColumn 双类型渲染 + `distinctBy { messageId }` 去重 + `LaunchedEffect(deduplicated.size) animateScrollToItem` 自动滚动 + Row 天然 RTL 支持；testTag 协议 `chat_message_list` / `user_message_N` / `system_message_N`；`ChatMessageListTest.kt` androidTest CL-01～CL-08 全部通过）与聊天输入框组件（T-30015，`ChatInputBar.kt` 无状态组件：`canSend = inputText.isNotBlank() && !isSending` 三场景禁用发送；`TextField(ImeAction.Send + KeyboardActions)` IME 回车守卫发送；`IconButton(enabled=canSend)` 按钮禁用；`RoomScreen.kt` 新增 `Scaffold(modifier.imePadding())` 软键盘适配 + `bottomBar` 替换旧 `BottomInputBar` → `ChatInputBar` + 本地 `var localInputText by remember` 管理状态；testTag 协议 `chat_input_field` / `chat_send_button`（原 `message_input`/`send_button` 废弃）；`ChatInputBarTest.kt` androidTest CI-01～CI-07 + 2 个边界测试共 9 个，`compileDebugKotlin` ✅ / `compileDebugAndroidTestKotlin` ✅ / `testDebugUnitTest` 30 tests passed）与发送消息逻辑（T-30016，`RoomViewModel.sendMessage(content)` 空消息守卫 + `try-finally isSendingMessage` 状态管理 + WS send + 成功发 `ClearInput` + 失败 `ShowToast` 可重试 + `CancellationException` re-throw；`RoomEvent.ClearInput` 新增；`RoomUiState.isSendingMessage: Boolean` 新增默认 `false`；`FakeWebSocketClient.sendThrowable` 注入发送异常；`RoomScreen` 监听 `ClearInput` 清空输入 + 传递 `isSending` 给 `ChatInputBar`；单元测试 SM-01～SM-06，`compileDebugKotlin` ✅ / `testDebugUnitTest` ✅）与接收消息逻辑（T-30017，`RoomViewModel.handleNewMessage(payload)` 完整实现：`msgId ?: return` 守卫 + `seenMsgIds.add(msgId)` 去重（`LinkedHashSet`，在 content 验证之前执行，防止同一 msgId 二次投递）+ `content ?: return` 守卫修复空白气泡 bug（旧 `content ?: ""` 已废弃）+ `ChatMessageUi` 构建 + 不可变 `state.messages + newMsg` 追加保证 Compose 重组感知；单元测试新增 RM-03/RM-04/RM-05 + VM-07/VM-08（等效覆盖 RM-01/RM-02），**总计 235 个单元测试**，`compileDebugKotlin` ✅ / `testDebugUnitTest` ✅）；`feature/splash` 已完成 Splash 启动页与 Compose Navigation（T-30019，`SplashScreen` / `SplashViewModel` / `SplashNavEvent` / `AppNavGraph` / `MainActivity` Compose 改造 / `AppContainer.tokenManager` 公开化）；`feature/main` 已完成 MainScreen 底部三Tab框架（T-30020，`MainScreen.kt` Scaffold + NavHost + MenaBottomNavigation、`MainTab` enum Hall/Messages/Profile、`MenaBottomNavigation.kt` 金色主题底部导航栏选中金色/未选中灰色、Tab 切换 `saveState/restoreState/launchSingleTop` 状态保持策略）；`feature/profile` 已完成个人中心完整链路（T-30024，`ProfileScreen.kt` 头像/昵称/ID/余额展示 + 复制ID + 退出登录 AlertDialog 二次确认、`ProfileViewModel.kt` StateFlow<ProfileUiState> + SharedFlow<ProfileEvent> + 网络异常缓存回退、`IUserRepository.getMe()` 复用 T-30004 用户信息 Repository）；`AppContainer.fromBuildConfig()` 中 `tokenManager` 已提升为公开 `val` 属性（T-30019），供 `SplashViewModel.Factory` 注入，debug 占位实现仍为匿名 `ITokenManager`。
- `core` 层已完成接口隔离；`core/theme` 已完成中东黑金主题系统（T-30018，MenaColors 11 色彩令牌 + MenaTypography 5 级排版 + MenaShapes 3 级圆角 + MenaTheme 入口 Composable + GoldButton / GoldOutlinedTextField / AvatarWithFrame 通用组件，33 个测试全部通过）；远程配置、本地存储、安全、日志等能力还只有骨架或占位。
