# TDS: 贵族列表 API (Task ID: T-00066)

## §0 产品验收（Given-When-Then）

- **关联 Spec**：[`doc/specs/nobility_purchase.md`](../../specs/nobility_purchase.md)
- **关联状态机**：[`state_machines.md#noble`](../../product/state_machines.md#noble)
- **关联用户旅程**：[`user_journeys.md#j4-noble-renewal`](../../product/user_journeys.md#j4-noble-renewal)
- **关联业务约束**：[`business_constraints.md`](../../product/business_constraints.md)
- **必须满足的验收条款**（GWT 全文以 spec §5 为唯一事实源，**禁止本地改写**）：GWT-NP1 / GWT-NP4

---

## 一、背景与目标
- **关联需求**：[phase1_nobility.md §3.1 / §3.5](../../product/phase1_nobility.md)。
- **目标**：实现 `GET /nobles/tiers`（公开）+ `GET /nobles/me`（鉴权），含 i18n（Accept-Language）+ 100ms 缓存。

## 二、方案设计
### 涉及文件
| 文件 | 变更 |
|------|------|
| `app/server/src/modules/nobility/controller.rs` | 新增 list_tiers / get_me handler |
| `app/server/src/modules/nobility/service.rs` | 新增 |
| `app/server/src/modules/nobility/repo.rs` | 新增 |
| `app/server/src/modules/nobility/cache.rs` | 新增（in-memory `tiers` 缓存 + Redis pub/sub invalidation 由 T-10030 admin 触发）|
| `app/server/src/modules/nobility/dto.rs` | 新增 严格匹配 [§10.3.1/10.3.2](../../protocol/nobility_api.md) |
| `app/server/tests/nobility_list_test.rs` | 新增 |

### 🔌 协议路径绑定表
| # | 协议类型 | 入口 | 客户端调用方 | 处理函数 | protocol/ 锚点 |
|---|---------|------|------------|---------|---------------|
| 1 | HTTP REST | `GET /api/v1/nobles/tiers` ⭐ | `app/android/.../noble/NobleApi.kt::getTiers` (T-30070) + `app/web` Admin 公开预览 | `nobility::controller::list_tiers_handler` | [nobility_api.md §10.3.1](../../protocol/nobility_api.md) |
| 2 | HTTP REST | `GET /api/v1/nobles/me` ⭐ | `NobleApi.kt::getMe` | `nobility::controller::get_me_handler` | [nobility_api.md §10.3.2](../../protocol/nobility_api.md) |

## 三、TDD 验收用例
- [ ] Accept-Language=ar-SA → name=name_ar；en-US → name=name_en；缺省 en-US
- [ ] /me 未持有 → 200 + `data.tier_id=null`
- [ ] /me 持有 → 完整字段含 `days_remaining` / `in_grace_period`
- [ ] 缓存命中后响应 P95 < 30ms
- [ ] /tiers 无需鉴权 → 200
- [ ] /me 无 token → 401

### 🔴 协议路径绑定
- [ ] **PROTO-1**：URL 字面量 `/api/v1/nobles/tiers` 与 `/api/v1/nobles/me`
- [ ] **PROTO-2**：响应字段 `tier_id/level/monthly_diamonds/monthly_usd/usd_sku_id/icon_url/frame_url/entrance_animation_url/bgm_url/badge_color/bubble_style_id/privileges` 全字段严格对齐

## 四、实现结果

### Round 2（2026-05-11）

**TDD RED→GREEN 完成。T-00065 修复后 `NoblePrivileges` serde 层自动受益，`TierDto.privileges` 中 `monthly_stipend.percent` 字段已正确序列化。**

#### 依赖传递修复

| 影响点 | 修复结果 |
|--------|---------|
| `GET /api/v1/nobles/tiers` 响应中 `privileges.monthly_stipend.percent` | ✅ 正确返回（duke=15, king=20 等） |
| `FakeNobilityService.make_fake_tier()` 种子值 | ✅ `percent: 5/8/10/12/15/20`，`pay_immediately: true` |
| `dto03_tier_dto_privileges_percent` 测试断言 | ✅ 断言 percent=15 + duke stipend=45000 |

#### 测试列表（新增/修复）

| ID | 测试名 | 文件 |
|----|--------|------|
| dto03 | `dto03_tier_dto_privileges_percent` | `nobility/dto.rs`（duke percent=15, 45000 钻断言）|
| dto05 | `dto05_noble_changed_signal_format` | `nobility/dto.rs` |
| ns08~ns11 | FakeNobilityService 种子正确性 | `nobility/service.rs` |

## 五、Reviewer意见
### Round 1（2026-05-10，Reviewer: Dod）
**状态：❌ 打回**

#### [通过] P0-1 URL 字面量

`GET /api/v1/nobles/tiers` 和 `GET /api/v1/nobles/me` 与协议 §10.3.1/§10.3.2 完全一致 ✅

#### [通过] P1-1 Accept-Language i18n

`parse_lang()` 正确拆分 `ar-SA,ar;q=0.9` → `"ar"`，默认回退 `"en"`；测试 NC05/NC07 覆盖 ✅

#### [通过] P1-2 /me 未持有返回 `tier_id: null`

`MyNobleResponse::none()` 显式设 `tier_id: None`，序列化后 JSON 为 `{"tier_id":null,...}`；测试 NC04/DTO-02 验证 ✅

#### [HIGH] 依赖 T-00065 monthly_stipend 字段名错误（传递影响）

本任务 `list_tiers_handler` 返回的 `TierDto.privileges` 中 `monthly_stipend` 字段使用 `diamonds` 而非协议要求的 `percent`，导致客户端调用 `GET /api/v1/nobles/tiers` 拿到不符合协议 §10.2.3 的 JSONB 结构。待 T-00065 修复后，本任务 `NoblePrivileges` serde 层自动受益，无需额外改动，但在 T-00065 修复合并前此 API **不可联调**。

#### [建议] P2 缓存策略尚未实现

TDS 要求 tier 列表 60 s 缓存（`repo.rs` / `cache.rs`），当前 `FakeNobilityService` 每次 list 均重新构建，生产实现须补充。

- **🔴 Changelog 行**：

### Round 2（2026-05-11，TDD Agent）
**状态：✅ 通过 → Review**

T-00065 修复后本任务自动受益：`TierDto.privileges.monthly_stipend.percent` 现在正确。测试 dto03 断言 duke `percent: 15` 且运行时 stipend=45000 钻（15%×300000）✅。无需额外改动 controller/service 层。

### Round 2（2026-05-10，Reviewer: everything:code-reviewer）
- **状态**：✅ 通过
- **审查意见**：
  - T-00065 `percent` 字段修复已正确传递到 `list_tiers_handler`（`NoblePrivileges` serde 自动序列化 `percent`）✅
  - `dto03` 测试断言 duke `percent=15`，stipend 计算 45000（15%×300000）✅
  - `GET /api/v1/nobles/tiers` NC01/NC02 返回 200 + 6 个 tier ✅
  - 延后项：60s 缓存 P2，可接受 ✅

## 六、DoD
- [x] `GET /api/v1/nobles/tiers` 响应 `privileges.monthly_stipend.percent` 字段正确
- [x] dto03 测试更新（percent 断言 + stipend 断言）
- [ ] 60s 缓存实现（延后）
- [x] Changelog 行追加（v3.26）
