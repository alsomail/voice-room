<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建。TDD Agent 仅填写第四节【实现结果】，其余章节只读。Review Agent 仅填写第五节【Reviewer意见】。
2. 创建后必须在 doc/tasks/index.md 对应 Task 的行内补充链接：[TDS](./tds/android/T-30016.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: 发送消息逻辑 (Task ID: T-30016)

## 一、背景与目标

- **关联需求**：语聊房聊天体验——用户发送文字消息完整交互流程（Chat 模块）
- **前置依赖**：
  - T-00016（App Server 文本消息广播 ✅）：服务端处理 `SendMessage` 信令，广播 `MessageReceived` 给房间内所有用户
  - T-30015（输入框组件 ✅）：`ChatInputBar` 已预留 `isSending: Boolean` 参数
- **本 Task 目标**：
  1. 重写 `RoomViewModel.sendMessage`：异步发送 + 发送中状态管理 + 成功/失败事件分发
  2. `RoomUiState` 新增 `isSendingMessage` 字段，驱动 `ChatInputBar.isSending` 禁用状态
  3. `RoomEvent` 新增 `ClearInput`，成功发送后通知 UI 清空输入框
  4. `RoomScreen` 适配：传递 `isSendingMessage`，监听 `ClearInput` 事件清空输入框

---

## 二、方案设计

### §2.1 WS 消息格式（SendMessage 信令）

基于现有代码约定（参见 `RoomViewModel.kt` 同级信令格式）：

| 方向 | type | 字段 |
|------|------|------|
| Client → Server | `SendMessage` | `roomId`, `content`, `msgId`（UUID） |

JSON 示例：
```json
{"type":"SendMessage","roomId":"room-1","content":"Hello","msgId":"uuid-xxx"}
```

### §2.2 RoomViewModel.sendMessage 重写

```kotlin
fun sendMessage(content: String) {
    if (content.isBlank()) return
    val roomId = currentRoomId ?: return
    viewModelScope.launch {
        updateSendingState(true)
        try {
            val msgId = UUID.randomUUID().toString()
            wsClient.send("""{"type":"SendMessage","roomId":"$roomId","content":"$content","msgId":"$msgId"}""")
            _events.trySend(RoomEvent.ClearInput)  // 成功：通知清空输入
        } catch (e: CancellationException) {
            throw e  // 必须 rethrow
        } catch (e: Exception) {
            _events.trySend(RoomEvent.ShowToast("发送失败：${e.message}"))
            // 失败：不清空输入，允许重试
        } finally {
            updateSendingState(false)
        }
    }
}

private fun updateSendingState(isSending: Boolean) {
    val current = _uiState.value as? RoomViewState.Success ?: return
    _uiState.value = RoomViewState.Success(current.uiState.copy(isSendingMessage = isSending))
}
```

### §2.3 新增 / 修改的数据类型

**`RoomUiState`**（新增字段）：
```kotlin
data class RoomUiState(
    // ... existing fields ...
    val isSendingMessage: Boolean = false,  // ← 新增：发送中禁用标志
)
```

**`RoomEvent`**（新增成员）：
```kotlin
sealed class RoomEvent {
    object NavigateBack : RoomEvent()
    data class ShowToast(val message: String) : RoomEvent()
    object ClearInput : RoomEvent()   // ← 新增：通知 UI 清空输入框
}
```

### §2.4 FakeWebSocketClient 扩展

为支持 SM-05（发送失败）测试，`FakeWebSocketClient` 新增可注入的异常钩子：
```kotlin
/** 设置后，下一次 send() 调用将抛出该异常（用于模拟网络发送失败） */
var sendThrowable: Throwable? = null

override fun send(message: String): Boolean {
    sendThrowable?.let { throw it }
    if (_state.value !is WebSocketState.Connected) return false
    sentMessages.add(message)
    return true
}
```

### §2.5 RoomScreen 适配

```kotlin
@Composable
fun RoomScreen(
    uiState: RoomUiState,
    events: Flow<RoomEvent> = emptyFlow(),   // ← 新增：事件流，默认为空
    onBack: () -> Unit = {},
    onSendMessage: (String) -> Unit = {},
    onMicPermissionGranted: (slotIndex: Int) -> Unit = {},
    modifier: Modifier = Modifier,
) {
    var localInputText by remember { mutableStateOf("") }

    // 监听 ClearInput 事件，清空输入框
    LaunchedEffect(Unit) {
        events.collect { event ->
            if (event is RoomEvent.ClearInput) {
                localInputText = ""
            }
        }
    }

    Scaffold(
        modifier = modifier.imePadding(),
        bottomBar = {
            ChatInputBar(
                inputText = localInputText,
                onInputTextChange = { localInputText = it },
                isSending = uiState.isSendingMessage,  // ← 新增
                onSendMessage = { text ->
                    onSendMessage(text)
                    // 不再立即清空，等待 ViewModel ClearInput 事件
                },
            )
        },
    ) { ... }
}
```

### §2.6 涉及文件清单

| 文件路径（相对 `app/android/app/src/`） | 变更类型 | 说明 |
|----------|----------|------|
| `main/.../feature/room/RoomUiState.kt` | **修改** | `RoomUiState` 新增 `isSendingMessage: Boolean = false` |
| `main/.../feature/room/RoomEvent.kt` | **修改** | 新增 `object ClearInput : RoomEvent()` |
| `main/.../feature/room/RoomViewModel.kt` | **修改** | `sendMessage` 重写为异步版本 + `updateSendingState` 私有方法 |
| `main/.../core/ws/FakeWebSocketClient.kt` | **修改** | 新增 `sendThrowable` 可注入异常支持 |
| `main/.../feature/room/RoomScreen.kt` | **修改** | 新增 `events` 参数，监听 `ClearInput`，传递 `isSendingMessage` |
| `test/.../feature/room/RoomViewModelTest.kt` | **修改** | 追加 SM-01~SM-06 测试；更新 VM-12（新增 `advanceUntilIdle()`) |

---


### 🔌 协议路径绑定表

N/A — 本 Task 无跨端协议路径，仅 Android 端内部改造

## 三、TDD 验收用例

| ID | 描述 | 验证重点 |
|----|------|---------|
| SM-01 | `sendMessage("")` → wsClient 不发送，无 ClearInput 事件 | 空白内容早期返回 |
| SM-02 | `sendMessage("hello")` → wsClient 发送包含正确 type/roomId/content/msgId 的 JSON | WS 消息格式正确性 |
| SM-03 | 发送成功 → `RoomEvent.ClearInput` 事件发出 | 成功后清空输入 |
| SM-04 | 发送过程中 `isSendingMessage = true`，发送完成后 `= false` | 发送中禁用状态 |
| SM-05 | `wsClient.send` 抛异常 → 发出 `ShowToast` 事件，不发出 `ClearInput` | 失败错误处理 |
| SM-06 | 发送失败后再次 `sendMessage` → 可以正常发送（`isSendingMessage` 无永久锁定） | 失败可重试性 |

---

## 四、实现结果（由 TDD Agent 完成后填写）

- **实际修改的文件**：

  | 文件路径（相对 `app/android/app/src/`） | 变更类型 | 说明 |
  |----------|----------|------|
  | `main/.../feature/room/RoomUiState.kt` | **修改** | `RoomUiState` 新增 `isSendingMessage: Boolean = false` |
  | `main/.../feature/room/RoomEvent.kt` | **修改** | 新增 `object ClearInput : RoomEvent()` |
  | `main/.../feature/room/RoomViewModel.kt` | **修改** | `sendMessage` 重写为异步（`viewModelScope.launch`），新增 `updateSendingState()` 私有方法；发送中状态 true→false 管理；成功发 `ClearInput`，失败发 `ShowToast` |
  | `main/.../core/ws/FakeWebSocketClient.kt` | **修改** | 新增 `sendThrowable: Throwable? = null`，`send()` 中检测并抛出（SM-05/SM-06 测试用） |
  | `main/.../feature/room/RoomScreen.kt` | **修改** | 新增 `events: Flow<RoomEvent> = emptyFlow()` 参数；`LaunchedEffect(Unit)` 监听 `ClearInput`；`localInputText` 提升至 Scaffold 外；`ChatInputBar.isSending = uiState.isSendingMessage`；移除立即清空 `localInputText = ""` |
  | `test/.../feature/room/RoomViewModelTest.kt` | **修改** | 追加 SM-01~SM-06 共 6 个测试；VM-12 补加 `advanceUntilIdle()`；import 新增 `UnconfinedTestDispatcher`、`assertFalse` |

- **sendMessage 异步重写关键设计**：
  - `content.isBlank()` 早期返回 → 不发送，不设 `isSendingMessage=true`（SM-01）
  - `currentRoomId == null` 早期返回（未加房间时静默忽略）
  - `viewModelScope.launch` + `updateSendingState(true/false)` in `finally` 保证状态复位（SM-04 / SM-06）
  - 成功路径：`wsClient.send()` → `_events.trySend(ClearInput)`（SM-03）
  - 失败路径：catch → `_events.trySend(ShowToast(...))`（SM-05），不发 `ClearInput`

- **SM-04 测试技术**：使用 `UnconfinedTestDispatcher(testScheduler)` 收集 `uiState`；StateFlow 每次 `value` 变更时立即在线分发，收集器在 `isSendingMessage=true` 和 `=false` 之间均能获取，记录完整历史 `[false, true, false]`

- **编译状态**：
  - `compileDebugKotlin` ✅ BUILD SUCCESSFUL
  - `compileDebugUnitTestKotlin` ✅ BUILD SUCCESSFUL
  - `testDebugUnitTest` ✅ **28 tests passed** in `RoomViewModelTest`（全套 232 tests，0 failures）

- **测试覆盖**（新增）：
  - SM-01：空白内容早期返回（空字符串 + 纯空白字符双重验证）
  - SM-02：正确 JSON 格式（type / roomId / content / msgId）
  - SM-03：成功后 `ClearInput` 事件发出
  - SM-04：`isSendingMessage` 状态 true→false 完整迁移（`UnconfinedTestDispatcher` 捕获中间状态）
  - SM-05：`send()` 抛异常 → `ShowToast` 发出，`ClearInput` 不发出
  - SM-06：失败后重试可以正常发送（无永久锁定）

- **完成时间**：2025-01

---

## 五、Reviewer意见

> 状态枚举：🟢 通过 | 🔴 未通过
> 每轮 Review 追加一条记录，不要覆盖历史。

### Round 1 — 2025-01 | 审查人: Review Agent | 结论: 🟢 通过

#### 审查清单逐项结论

| # | 审查重点 | 结论 | 证据（文件:行号） |
|---|---------|------|----------------|
| 1 | **CancellationException re-throw** | ✅ PASS | `RoomViewModel.kt:184-185`：`catch (e: CancellationException) { throw e }` 位于通用 `catch (e: Exception)` 之前，顺序正确，协程取消语义完整保留 |
| 2 | **isSendingMessage finally 复位** | ✅ PASS | `RoomViewModel.kt:189-191`：`updateSendingState(false)` 在 `finally` 块中，无论成功/失败/取消均复位，无永久锁定风险 |
| 3 | **空消息守卫** | ✅ PASS | `RoomViewModel.kt:174`：`if (content.isBlank()) return` 位于函数入口第一行，在 `viewModelScope.launch` 之前提前返回，无冗余协程创建 |
| 4 | **ClearInput 事件逻辑** | ✅ PASS | `RoomViewModel.kt:183`：`ClearInput` 仅在 `wsClient.send()` 成功后（try 正常路径）发出；失败路径（catch）只发 `ShowToast`，两者互斥 |
| 5 | **isSending=true 期间阻止重复发送** | ✅ PASS (设计意图明确) | `FakeWebSocketClient.send()` 为同步非挂起方法，`viewModelScope.launch` 协程体内无挂起点，Main Dispatcher 保证原子性执行，UI 层 `isSendingMessage` 禁用按钮为设计意图（TDS §2.2 明确）|
| 6 | **SM-01~SM-06 测试完整性** | ✅ PASS (全覆盖) | 见下方测试覆盖详情 |

#### 测试覆盖详情 (SM-01~SM-06)

| ID | 测试方法名 | 覆盖验证点 | 质量评价 |
|----|-----------|-----------|---------|
| SM-01 | `SM-01 sendMessage blank content...` | `""` 和 `"   "` 双重验证，`sentMessages.isEmpty()` + `!any { ClearInput }` | ✅ 完整 |
| SM-02 | `SM-02 sendMessage valid content...` | 验证 4 字段：`type/roomId/content/msgId` 均 contains 断言 | ✅ 完整 |
| SM-03 | `SM-03 sendMessage success - ClearInput event emitted` | `any { it is RoomEvent.ClearInput }` 断言 | ✅ 完整 |
| SM-04 | `SM-04 isSendingMessage transitions true during send then false after completion` | `UnconfinedTestDispatcher` 收集历史，`contains(true)` + `!last()` 双断言 | ✅ 完整，中间状态可被捕获 |
| SM-05 | `SM-05 wsClient send throws exception...` | `ShowToast` 有 + `ClearInput` 无 + toast `isNotBlank()` 三断言 | ✅ 完整 |
| SM-06 | `SM-06 sendMessage after failure - retry succeeds...` | 失败后重置 `sendThrowable=null`，验证重试成功 + `ClearInput` 发出 + `isSendingMessage=false` | ✅ 完整 |

#### 细节观察（非阻塞）

- **`sendThrowable` 跨测试隔离**：每个测试通过 `@Before setup()` 重新创建 `FakeWebSocketClient` 实例，测试隔离完整，无跨测试污染。
- **SM-04 调度器细节**：收集协程使用 `UnconfinedTestDispatcher`，ViewModel 协程使用 `mainDispatcherRule.testDispatcher`；`send()` 同步执行使 StateFlow 两次 `value` 赋值均在 `advanceUntilIdle()` 中被完整捕获，测试方案与实现匹配。
- **`seenMsgIds` 内存增长**（继承自 T-30010 `TODO` 已标注）：与本 Task 无关，已有文档注释。

#### 总结

实现与 TDS §2.2 设计方案完全一致，无 CRITICAL/HIGH 问题，6 项审查要点全部通过，SM-01~SM-06 测试覆盖完整且质量合格。

**结论：🟢 通过 — 可流转至 DoD**
