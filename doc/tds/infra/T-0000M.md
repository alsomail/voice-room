<!--
[AI 写入规约]
1. 本文件由 Plan Agent 创建。TDD Agent 仅填写第四节【实现结果】，其余章节只读。Review Agent 仅填写第五节【Reviewer意见】。
2. 创建后必须在 doc/tasks/index.md 对应 Task 的行内补充链接：[TDS](./tds/infra/T-0000M.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: 双服务共库 Migration 表隔离 (Task ID: T-0000M)

## 一、背景与目标

- **关联需求**：模块 9 E2E 测试基建后置缺陷修复（M1 本地全栈一键起服务跑通）。
- **本 Task 的目标**：消除 AppServer 与 AdminServer 共用同一 PostgreSQL 库 `voiceroom` 时，对默认 `_sqlx_migrations` 登记表互相覆盖、互相校验失败的架构级阻断。

### 1.1 实测阻断现象（来自 2026-04-27 e2e:up 联调）

冷启动 `docker compose down -v && npm run e2e:up` 必现：

| 服务 | 现象 | 根因 |
|------|------|------|
| AppServer | `permission denied for table _sqlx_migrations`（即便补 `GRANT CREATE ON SCHEMA`） | AdminServer 已抢先创建该表并归属 `admin_server_user`，AppServer 用 `app_server_user` 写入 |
| AdminServer | `migration 5 was previously applied but is missing in the resolved migrations` | AppServer 已写入 `version=5..9`，AdminServer `migrations/` 只有 1..4，sqlx 视为「迁移消失」拒绝启动 |

**核心结论**：只要双服务对同一库执行 `sqlx::migrate!("./migrations")`，默认共享 `_sqlx_migrations` 表 → 版本号 / checksum 必然互掐，docker `down -v` 全清也无解。

### 1.2 为何不选「双库分离（方案 A）」

经对 AdminServer 源码盘点，`app/adminServer/src/modules/**` 共 13 处直接 `SELECT ... FROM users / rooms / events / wallets`：

- [`modules/user/repository.rs`](../../../app/adminServer/src/modules/user/repository.rs) 3 处
- [`modules/room/repository.rs`](../../../app/adminServer/src/modules/room/repository.rs) 3 处
- [`modules/wallet/repository.rs`](../../../app/adminServer/src/modules/wallet/repository.rs) 2 处
- [`modules/stats/repository.rs`](../../../app/adminServer/src/modules/stats/repository.rs) 2 处
- [`modules/event/query_repo.rs`](../../../app/adminServer/src/modules/event/query_repo.rs) 3 处

「物理双库」会要求把上述 13 处全部改写为 RPC 调 AppServer 或启用 `postgres_fdw`，对一个 MVP 阶段的「模块化单体 + 双进程」是过度工程，且偏离当前 [`doc/architecture/directory_and_ddd.md`](../../architecture/directory_and_ddd.md) 中「共享 DB、按 schema/table 权限隔离」的既定架构方向。**故否决方案 A**。

### 1.3 为何不直接上「方案 C（启动期跳 migrate + sqlx-cli）」

方案 C 的「跳过 runtime migrate、改由 CI/脚本 `sqlx migrate run` 一次性执行」必须先解决「同一 `_sqlx_migrations` 表里两份历史并存」的语义问题——其唯一手段仍然是 `--migrations-table` 自定义表名。换言之 **C 是 B 的超集**，B 是必经路径。本 Task 先收敛 B，把 C 作为后续上线 staging/prod 时的演进项（参见第六节演进路线）。

### 1.4 决策

采纳 **方案 B：自定义 sqlx migration 表名**。

| 服务 | 自定义迁移表名 |
|------|---------------|
| AppServer (`voice-room-server`) | `_sqlx_app_migrations` |
| AdminServer (`voice-room-admin-server`) | `_sqlx_admin_migrations` |

---

## 二、方案设计

### 2.1 核心数据流

```
┌─────────────────────────────────────────────────────────────────┐
│ Postgres voiceroom (single DB)                                  │
│                                                                 │
│  ┌───────────────────────┐   ┌──────────────────────────┐       │
│  │ _sqlx_app_migrations  │   │ _sqlx_admin_migrations   │       │
│  │  v=001..009           │   │  v=001..004              │       │
│  │  owner=app_server_user│   │  owner=admin_server_user │       │
│  └───────────▲───────────┘   └─────────────▲────────────┘       │
│              │                              │                   │
│  ┌───────────┴────────────┐    ┌────────────┴────────────┐      │
│  │ AppServer  main.rs     │    │ AdminServer main.rs     │      │
│  │  Migrator               │    │  Migrator              │      │
│  │   .set_table_name(...)  │    │   .set_table_name(...) │      │
│  │   .run(pool)            │    │   .run(pool)           │      │
│  └────────────────────────┘    └─────────────────────────┘      │
│                                                                 │
│  共享业务表（users / rooms / wallets / events / ...）          │
└─────────────────────────────────────────────────────────────────┘
```

启动期两个进程独立 migrate，互不感知对方版本；业务表保持共享与原有读写权限。

### 2.2 sqlx API 选型与降级路径

`sqlx::migrate!("./migrations")` 宏返回 `sqlx::migrate::Migrator`（编译期嵌入迁移文件）。`Migrator` 提供 `set_table_name`（≥ sqlx 0.7）。但默认情况下宏返回的是 `&'static Migrator`，**不可变借用**——直接 `.set_table_name(...)` 在 sqlx 0.8 会编译失败。

**TDD 实施时必须按以下顺序验证**：

1. **首选**：检查 `sqlx::migrate!` 是否提供 mut 入口（如 `let mut m = sqlx::migrate!(...).clone()` 或 0.8 新增的 `MigrationSource` API），优先使用宏 + 克隆。
2. **降级**：若克隆方案不可行，则改用 `Migrator::new(std::path::Path::new("./migrations")).await?` 运行时加载（牺牲编译期校验）。
3. **保底**：仍不可行时，绕过 Migrator，直接执行 SQL：先 `CREATE TABLE IF NOT EXISTS _sqlx_app_migrations (...)` + 自管 hash 比对（不推荐，仅作兜底）。

> Plan 阶段已通过快速代码检索确认 sqlx 0.8.x 的 `Migrator` 字段 `pub table_name: Cow<'static, str>` 在 `migrate!` 宏返回值上确实是 const 静态。**TDD 阶段须先做 1 行 spike**：写 `let m: sqlx::migrate::Migrator = sqlx::migrate!("./migrations"); /* 是否可 .clone() 后 set_table_name */`，红→定方案；20 分钟内未得绿则切到降级方案 2。

### 2.3 涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `app/server/src/main.rs` | 修改 | 替换 L56 单行 migrate 调用，改为自定义表名 `_sqlx_app_migrations` |
| `app/adminServer/src/main.rs` | 修改 | 替换 L62 单行 migrate 调用，改为自定义表名 `_sqlx_admin_migrations` |
| `app/server/tests/common/mod.rs` | 新增 | 抽取 `run_migrations(pool: &PgPool)` helper，统一表名 |
| `app/server/tests/wallet_schema_test.rs` | 修改 | 8 处 `sqlx::migrate!("./migrations").run(&pool).await?` 切换到 helper |
| `app/server/tests/send_gift_test.rs` | 修改 | 7 处同上 |
| `scripts/dev/init-db.sh` | 修改 | 在 `GRANT CONNECT` 之后补 `GRANT CREATE ON SCHEMA public TO app_server_user`（幂等） |
| `scripts/dev/e2e-up.sh` | 修改 | 删除 L37–L42 inline `GRANT CREATE ON SCHEMA public` 临时补丁（已上移到 init-db.sh） |
| `doc/arch/server/structure.md` | 修改 | §3 注明自定义表名 `_sqlx_app_migrations` |
| `doc/arch/server/status.md` | 修改 | 同步迁移机制描述 |
| `doc/arch/adminServer/structure.md` | 修改 | §3 注明自定义表名 `_sqlx_admin_migrations` |
| `doc/arch/adminServer/auth.md` | 修改 | L262 启动序列描述同步 |
| `doc/architecture/directory_and_ddd.md` | 修改 | 「数据库共享策略」节追加双服务迁移隔离规约 |
| `doc/adr/ADR-0001-migration-table-isolation.md` | 新增 | ADR 记录决策（A/B/C 对比 + 选 B 的事实依据 + 后续切 C 的触发条件） |
| `doc/tasks/index.md` | 修改 | 追加 v2.50 变更记录 + Phase 1.7 新模块入口 |
| `doc/tasks/模块9-E2E测试基建 (E2E QA Foundation).md` | 修改 | 在表格末新增 T-0000M 行，并把模块状态由「12/12 ✅」更新为「13/13 ✅（待 DoD）」 |

> 注：`app/adminServer/tests/**` 当前未直接调用 `sqlx::migrate!`（已确认 grep 0 命中），无需改动。

### 2.4 接口 / 信令定义

无对外接口变更。仅内部 sqlx Migrator 表名约定：

| 进程 | 迁移登记表 | 创建者角色 |
|------|------------|------------|
| AppServer | `_sqlx_app_migrations` | `app_server_user` |
| AdminServer | `_sqlx_admin_migrations` | `admin_server_user` |

### 2.5 安全与权限红线

- **必须**通过 init-db.sh（仅 dev / docker-compose 起的本地 PG）授予 `app_server_user` 在 `public` schema 上的 `CREATE` 权限，否则它无法建立自有的 `_sqlx_app_migrations` 表。
- **不得**让 `app_server_user` 写入 `_sqlx_admin_migrations`（默认无权限即可，无需显式 REVOKE）。
- staging / prod 环境本 Task 不直接覆盖；切 C 方案后由 sqlx-cli 一次性执行迁移，运行账号无需 schema CREATE 权限。

---

## 三、TDD 验收用例

### 3.1 单元 / 集成层

- [ ] **U-1（迁移记录隔离）**：在 fresh 库上分别启动 AppServer / AdminServer 后，`SELECT version FROM _sqlx_app_migrations` 返回 9 行（1..9），`SELECT version FROM _sqlx_admin_migrations` 返回 4 行（1..4）。
- [ ] **U-2（启动顺序无关）**：先启 AdminServer 再启 AppServer，与反序启动结果一致；两边均成功无 panic。
- [ ] **U-3（重复启动幂等）**：AppServer 连续启动 3 次，`_sqlx_app_migrations` 行数恒为 9；AdminServer 同理为 4。
- [ ] **U-4（既有 server 集成测试 0 回归）**：`cargo test -p voice-room-server` 全绿（含 wallet_schema_test.rs / send_gift_test.rs 共 15 处迁移调用）。
- [ ] **U-5（既有 adminServer 集成测试 0 回归）**：`cargo test -p voice-room-admin-server` 全绿（≥ 474 passed，参 T-10020 基线）。

### 3.2 系统 / E2E 层

- [ ] **E-1（一键冷启动绿）**：`docker compose down -v && npm run e2e:up` 一遍过，5 端 wait-on 全绿。
- [ ] **E-2（preflight 全绿）**：`npm run preflight` 1 秒内全绿。
- [ ] **E-3（E2E 全量回归）**：`npm run e2e:local` 通过率与改动前持平（不引入新红用例）。
- [ ] **E-4（脚本侧 workaround 已撤）**：`grep -n "GRANT CREATE ON SCHEMA public" scripts/dev/e2e-up.sh` 0 命中（已上移 init-db.sh）。

### 3.3 异常 / 边界用例

- [ ] **N-1（残留库兼容）**：在已有旧 `_sqlx_migrations` 表（混杂版本号）的库上启动 AppServer，应**不报错**地新建 `_sqlx_app_migrations` 并使用之；旧表保持原样不污染新表。
- [ ] **N-2（权限缺失显式失败）**：临时移除 `app_server_user` 的 `CREATE ON SCHEMA public` 后启动 AppServer，应在第一时间得到包含 `_sqlx_app_migrations` 表名的明确报错（用于排障可识别性）。
- [ ] **N-3（迁移文件回滚不允许）**：故意删除 `app/server/migrations/009_*.sql` 后再启动，sqlx 应报「missing migration」并明确指出表名 `_sqlx_app_migrations`。

---

## 四、实现结果（由 TDD Agent 完成后填写）

- **实际修改的文件**：
- **测试覆盖率**：
- **完成时间**：
- **关键决策与坑点**：
  - sqlx Migrator 改写采用的具体方案（首选 / 降级 / 保底，见 §2.2）：
  - 既有库残留 `_sqlx_migrations` 表的处理方式（保留不动 / 提供清理脚本 / 文档警示）：

---

## 五、Reviewer 意见

> 状态枚举：🟢 通过 | 🔴 未通过
> 每轮 Review 追加一条记录，不要覆盖历史。

### Round 1
- **状态**：（🟢 通过 / 🔴 未通过）
- **审查意见**：
- **改进建议**：（仅未通过时填写）

---

## 六、演进路线（非本 Task 范围，仅备忘）

| 阶段 | 触发条件 | 演进动作 |
|------|----------|----------|
| **现在（B）** | 本 Task | 双 migration 表，runtime migrate-on-startup |
| **Phase 2（C）** | 上 staging / prod 时 | 引入 `sqlx-cli`，CI/部署脚本一次性 `sqlx migrate run --migrations-table _sqlx_app_migrations`；两服务 main.rs 通过 env flag `MIGRATE_ON_START=0` 跳过 runtime migrate |
| **Phase 3（A）** | 出现以下任一信号：①AdminServer 跨域查询 < 3 处；②C / B 端数据安全审计要求物理隔离；③多租户 SaaS 化 | 物理双库 + AdminServer 改造为 RPC / FDW |
