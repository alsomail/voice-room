# 全局代码审查报告: 模块 3 - 房间内核心功能 (In-Room Core)
> **当前状态机**：负责人 [TDD] | 状态 [❌ Failed] | 修复轮次 [1/10]

---

## 0. 流转规则
- **状态枚举**：负责人 [-] 状态 [✅ Passed] | 负责人 [TDD] 状态 [❌ Failed] | 负责人 [GlobalReview] 状态 [⏳ In Review]
- 每轮 Review 追加一条记录，不要覆盖历史。
- 处于负责人 [GlobalReview] 状态 [⏳ In Review]，则由[GlobalReview]进行全局代码审查
- [GlobalReview]审查通过，则修改负责人 [-] 状态 [✅ Passed]
- [GlobalReview]审查未通过，则修改负责人 [TDD] 状态 [❌ Failed], 并将审查意见填入文档下方
- 处于负责人 [TDD] 状态 [❌ Failed]，则由[TDD]根据审查意见进行代码修复并自测
- [TDD]修复之后，将状态改为负责人 [GlobalReview] 状态 [⏳ In Review]

---

## 1. 审查上下文
- **包含任务**：[模块 3: 房间内核心功能 (In-Room Core)](../tasks/模块3-房间内核心功能%20(In-Room%20Core).md)
  - App Server：T-00011 / T-00012 / T-00013 / T-00014 / T-00015 / T-00016 / T-00011B / T-00011C
  - Admin Server：T-10007 / T-10008 / T-10009 / T-10010 / T-10011 / T-10012
  - Web：T-20006 / T-20007 / T-20008 / T-20009
  - Android：T-30008 / T-30009 / T-30010 / T-30011 / T-30012 / T-30013 / T-30014 / T-30015 / T-30016 / T-30017
- **关联 TDS**：见 `doc/tds/{server,adminServer,web,android}/` 对应 ID
- **开始时间**：2026-04-25

---

## 2. 审查与修复日志

*(执行规则：GlobalReview 记录缺陷，TDD 在对应缺陷下方记录修复方案与 PR/Commit。严禁覆盖历史记录，只能向下追加)*

### 【第 1 轮审查】
**@GlobalReview 审查意见：**

> 审查范围：App Server (`app/server/src/{ws,room,events,stats}`)、Admin Server (`app/adminServer/src/modules/{user,audit,event,stats,governance}`)、Web (`app/web/src/{pages/users,core/network}`)、Android (`app/android/app/src/main/java/com/voice/room/android/{core/ws,core/media,feature/room}`) 共四端关键路径直读源码。
> 严重级别映射：P0=致命阻断 / P1=高危 / P2=一般 / P3=优化。

#### 🔴 P0 — 致命阻断（必须修）

- [ ] **缺陷 1**：[级别 P0] **Android ⇄ App Server WebSocket 消息协议不兼容（C2S 全线断裂）**
  - **文件与行号**：`app/android/app/src/main/java/com/voice/room/android/feature/room/RoomViewModel.kt:197-198, 227, 261, 322-324, 475-476, 618` ⇄ `doc/protocol/websocket_signals.md:17-26` ⇄ `app/server/src/ws/connection.rs` (IncomingMessage) + `app/server/src/room/handler.rs` (handle_take_mic / handle_send_message 等)
  - **问题说明**：协议 §6.3 规定通用格式为 `{ "type", "msg_id", "payload": { ... }, "timestamp" }`，服务端 `IncomingMessage` 也按此结构解析（`payload.mic_index` / `payload.content` / `msg_id` snake_case）。但 Android 客户端发送的 JSON 是 **顶层平铺 + camelCase**：
    - `JoinRoom`: `{"type":"JoinRoom","roomId":"...","msgId":"..."}` —— 缺少 `payload` 包裹、`msgId` 不是 `msg_id`，`access_token` 也直接拼在顶层。
    - `TakeMic`: `{"type":"TakeMic","roomId":"...","slotIndex":N}` —— 服务端读 `payload.mic_index`，**永远拿不到下标 → 上麦请求会报 40002 MISSING_PARAMS**。
    - `LeaveMic` / `ForceTakeMic` / `SendMessage` 同样缺 `payload` 包裹。`SendMessage` 服务端读 `payload.content`，客户端给的是顶层 `content` → **聊天消息发不出去**。
    - `LeaveRoom` 服务端使用连接上下文，可侥幸跑通，掩盖了协议差异。
  - **影响范围**：T-30010 / T-30013 / T-30015 全部核心房间内交互（上麦、下麦、聊天、强制上麦、加入密码房）在真实环境下均无法工作。当前测试通过完全是因为 `FakeWebSocketClient` 没有走真协议解析。
  - **修复建议**：（任选其一，但必须四端一致）
    1. 把 Android 的所有 `wsClient.send("...")` 调用改为按协议 §6.3 构造：使用 Gson/Moshi 序列化 `data class C2SEnvelope(val type:String, val msg_id:String, val payload: Any, val timestamp: Long)`。字段一律 snake_case。
    2. 严禁字符串拼接 JSON——参见缺陷 2。
    3. 增加一个端到端 Pact / contract test：Android 序列化的消息必须能被 `serde_json::from_str::<IncomingMessage>` 成功反序列化并取到关键字段。
  - **TDD 修复记录**：
    - 在 `app/android/app/src/main/java/com/voice/room/android/core/ws/WsEnvelope.kt` 新增 `WsEnvelope.build(type, payload, msgId)` + `IWebSocketClient.sendEnvelope` 扩展，统一封装为 `{type, msg_id, payload, timestamp}`（snake_case + Gson 序列化）。
    - 重写 `RoomViewModel.kt` 全部 9 个 `wsClient.send(...)` 站点：JoinRoom 含 `room_id/access_token?/last_msg_id?`；TakeMic `mic_index`；LeaveMic/LeaveRoom 空 payload（服务端用连接上下文）；SendMessage `content`；TransferAdmin `room_id/target_user_id/action`；ForceTakeMic `target_user_id/slot_index`；ForceLeaveMic `target_user_id`；MuteUser `target_user_id/type/duration_sec`（key 为 `type` 而非 `muteType`）；KickUser `target_user_id/reason`（Gson 转义）。
    - 测试：`RoomViewModelTest.kt` TM-01/TM-04/SM-02/VM-12 + UA40-09/09b-confirm/09c + KR41-07/KR41-07b×3 共 11 条全部按新协议改写并 GREEN；`GovernanceUiTest.kt` G44-05 LeaveMic 改为断言空 payload。
    - Commit：见本批次提交 8a24f4f。

- [ ] **缺陷 2**：[级别 P0] **Web 封禁接口字段名 + 单位与 Admin Server 完全错位（封禁系统功能性失效）**
  - **文件与行号**：`app/web/src/core/network/apiClient.ts:350-374` ⇄ `app/adminServer/src/modules/user/dto.rs:163-172` ⇄ `app/adminServer/src/modules/user/service.rs:117-189`
  - **问题说明**：
    - Web 发出 `{ action: "ban", duration: number|null /*分钟*/, reason, remark }`。
    - Admin Server 期望 `{ action, ban_type, duration_hours, reason }`。
    - 字段名错位（`duration` ≠ `duration_hours`，`remark` 不被接受、`ban_type` 永远缺失）；即便强制对齐字段名，**单位差 60 倍**（Web 给"分钟"，后端按"小时"解析）。
    - `service.rs:165` 的 `unwrap_or(0)` 让所有封禁实际写入 `duration_hours = 0` → 业务语义为"永久 0 小时"，封禁解除时间彻底不可信。
    - BanModal 上"24h / 72h / 7d / 30d / 永久" 选项 **全部失效**。
  - **影响范围**：T-20008（封禁前端）+ T-10009（封禁后端）契约不闭环；治理工作流功能性损坏。
  - **修复建议**：
    1. 在 `doc/protocol/admin_api.md` 中明确封禁请求结构（推荐字段：`ban_type: "temporary"|"permanent"`, `duration_hours: int|null`, `reason: string`, `remark: string|null`）。
    2. 后端 dto 在 serde 层启用 `#[serde(deny_unknown_fields)]` 防止再次错位；前端 TypeScript 用同名 interface。
    3. 增加跨端契约测试或 OpenAPI 校验。
  - **TDD 修复记录**：
    - `app/web/src/core/network/apiClient.ts:350-358` 重写 `AdminBanUserRequest`：移除 `duration/remark`；新增 `ban_type?: 'temporary'|'permanent'` + `duration_hours?: number` + `reason?: string`。
    - `app/web/src/pages/users/BanModal.tsx`：`durationOptions` 改为小时 `24/72/168/720`，永久使用字符串 sentinel `'permanent'`（避免 antd `null` required 误判）；`onOk` 构造 `{action, ban_type, duration_hours?, reason}`，remark 非空时合并为 `"reason: remark"`。
    - `app/adminServer/src/modules/user/service.rs:ban_user`：新增三段 `ValidationError` 校验（`ban_type=temporary` 必带 `duration_hours>0`；`permanent` 不允许 `duration_hours`；未知 `ban_type` 拒绝）；事件 payload 改用 `Option<u32>` 不再 `unwrap_or(0)`，并附 `ban_type` 字段。
    - 测试：`BanModal.test.tsx` M07/M07b/M07c GREEN（487/487）；`useBanUser.test.ts` 4 处 sed 替换；adminServer 新增 SB-09…SB-13 5 条 GREEN（403 tests, +22）。

- [ ] **缺陷 3**：[级别 P0] **Admin Server 后台数据看板硬编码 online_users=0 / active_rooms=0**
  - **文件与行号**：`app/adminServer/src/modules/stats/service.rs:62-66`
  - **问题说明**：T-10010 验收 #3 明确"在线人数从 Redis 获取（App Server 维护）"。App Server `stats/service.rs` 已经在向 Redis `stats:online_users` (HLL) 与 `stats:active_rooms` (Set) 写数据；但 Admin 端 `get_overview` 直接 `online_users: 0, active_rooms: 0` 写死返回。运营看板**永远显示 0**。
  - **修复建议**：在 `AdminStatsService` 注入 `RedisClient`/`ConnectionManager`，使用 `PFCOUNT stats:online_users` 与 `SCARD stats:active_rooms`；同时补一个超时与降级（Redis 不可用时返回 `null` 并日志告警，不要直接返回 0 误导运营）。
  - **TDD 修复记录**：
    - `app/adminServer/src/modules/stats/service.rs` 重写：使用 `tokio::sync::OnceCell<MultiplexedConnection>` 持有 Redis 连接；`try_init_redis(&self, url)` 启动时 fire-and-forget 初始化（连接失败 → 日志 + 留 None，不阻塞启动）；`read_realtime_counts()` 用 `PFCOUNT stats:online_users` + `SCARD stats:active_rooms`，外层 `tokio::time::timeout(500ms)`，任一失败都降级为 0 + warn 日志。
    - `app/adminServer/src/main.rs:86-89` 在 `build_app(state.clone())` 后调用 `state.stats_service.try_init_redis(redis_url)`（读取 `REDIS_URL` 环境变量）。
    - 测试：新增 ST-07（无 Redis init → 0/0）+ ST-08（错误 URL → 不 panic、仍 0/0），既有 ST-01..06 通过。

#### 🟠 P1 — 高危（强烈建议修）

- [ ] **缺陷 4**：[级别 P1] **App Server `StatsService.user_leave_room` 在用户离开时无条件 SREM 房间，导致 active_rooms 系统性偏低**
  - **文件与行号**：`app/server/src/stats/service.rs:96-104`
  - **问题说明**：每次任意用户离开都会从 `stats:active_rooms` 集合移除房间，即便房间内还有其它成员。该集合的 `SCARD` 因此永远 ≤ 真实活跃房间数，与同模块 `stats:online_users` 不一致。
  - **修复建议**：传入"房间剩余人数"或"是否最后一人"，仅在最后一人离开时 SREM；或改为按 room_id → active_user_count 的 hash + 检查 0 时清理。
  - **TDD 修复记录**：
    - `StatsPort::user_leave_room` trait 签名新增 `remaining_members: usize` 入参；`StatsService` 实现仅在 `remaining_members == 0` 时执行 `SREM stats:active_rooms`。
    - `app/server/src/room/handler.rs:258-262` 在 `members.remove` 后计算 `remaining = room_state.member_count()` 并传入。
    - 测试：ST05 改为传 `0` 验证 SREM；新增 ST05B 验证 `remaining>0` 时**不**执行 SREM（GREEN）。

- [ ] **缺陷 5**：[级别 P1] **Android 用字符串插值拼装 WS JSON，存在 JSON 注入与转义漏洞**
  - **文件与行号**：`app/android/.../feature/room/RoomViewModel.kt:197-198, 227, 261, 322-324, 475-476, 495, 552, 618`
  - **问题说明**：所有 WS 出站消息均使用 `"""{"type":...,"content":"$content"}"""` 三引号字符串拼接。一旦用户聊天内容含 `"` / `\` / 换行（阿拉伯语场景下相当常见），JSON 直接被破坏：
    - 轻则消息发送失败、重则恶意构造 `","msg_id":"...","payload":{...}` 注入伪造字段。
    - 与缺陷 1 一并修复：必须切换到结构化序列化（Gson/Moshi/kotlinx.serialization）。
  - **修复建议**：定义 `C2SEnvelope` 数据类，统一通过 Gson `toJson()` 输出。
  - **TDD 修复记录**：
    - 同缺陷 1 修复：`WsEnvelope.kt` 使用 Gson `JsonObject` 构建，自动处理 `"` / `\` / 换行/Unicode 转义。RoomViewModel 的 KickUser 路径不再手动 `replace("\"", "\\\"")`。
    - 测试：`KR41-07b` 三条（双引号 / 反斜杠 / 双引号+反斜杠的"恶毒输入"）均通过 `JsonParser.parseString` 解析后访问 `payload.reason` 验证 round-trip 等于原文（GREEN）。

- [x] **缺陷 6**：[级别 P1] **断线重连未实现 `last_msg_id` 续传（违反 T-00011 验收 #3）**
  - **文件与行号**：`app/android/.../core/ws/OkHttpWebSocketClient.kt:184-221` + `app/server/src/ws/connection.rs` (handle_socket) + 协议 `doc/protocol/websocket_signals.md`
  - **问题说明**：T-00011 验收 #3 与模块说明明确要求"WebSocket 契约：JWT 握手 / 心跳 / 断线重连 / **last_msg_id 续传**"。Android `OkHttpWebSocketClient.scheduleReconnect()` 只是简单 `doConnect(spec)` 重连，未携带 last_msg_id；服务端也没有按 room+last_msg_id 回放消息的机制（无消息缓冲）。重连期间发送的所有事件（聊天/上下麦/礼物）将丢失。
  - **修复建议**：要么实现 last_msg_id 缓冲（服务端按房间维护最近 N 秒/N 条消息环缓冲，重连时回放）；要么修改 TDS 明确 MVP 阶段不做重连续传，并在协议文档中删除该承诺。**避免静默偏离 TDS。**
  - **TDD 修复记录**：
    - **客户端侧（批 1 已完成）**：`RoomViewModel` 新增 `lastReceivedMsgId` 字段，`handleWsMessage` 从入站消息的 `msg_id`/`msgId` 字段更新；下次 `JoinRoom` 时 payload 自动带上 `last_msg_id`（未收到任何消息时省略字段）。
    - **服务端侧（批 2b 完成）**：
      1. **环形缓冲数据结构**：`app/server/src/room/state.rs` 新增 `RECENT_BROADCASTS_CAPACITY=200` + `RecentBroadcast` + `RecentBroadcasts(Mutex<VecDeque<...>>)`（FIFO，最旧驱逐），挂载到 `RoomState.recent_broadcasts`；提供 `push(msg_id, json)` / `replay_after(last_msg_id) -> Option<Vec<RecentBroadcast>>`（命中返回区间消息，未命中/出窗返回 None）。
      2. **统一广播出口**：`app/server/src/ws/broadcaster.rs` 新增 `broadcast_to_room(registry, &RoomState, envelope)` —— 服务端为每条 envelope 注入 UUID v4 `msg_id`、写入 `recent_broadcasts`、扇出至 `registry.get_connections_in_room()`；附带降级 `broadcast_to_room_no_state(registry, room_id, envelope)` 用于治理动作早于任何 JoinRoom 的边界场景（不写缓冲，仅广播）。
      3. **重构 13 处广播站点**：原审计报告"46 处"实际为 13 处真正的房间广播：
         - `room/handler.rs`（5 处：UserJoined / UserLeft / MicTaken / MicLeft / RoomMessage）
         - `modules/governance/{kick,mute,force_mic,transfer}.rs`（8 处：UserLeft+MicLeft / UserMuted×2 / 强制 MicTaken+MicLeft / AdminChanged grant+revoke）
         - `modules/gift/send_gift.rs`（1 处：GiftReceived；`build_gift_received_msg` 改为返回 `serde_json::Value` 不再预填 msg_id）
         全部改走 `broadcast_to_room`；`broadcast_mic_left` 签名 `(registry, room_id, ...)` → `(registry, &RoomState, ...)`；mute 模块 `do_unmute` / `do_unmute_internal` 串接 `room_manager` 入参。
      4. **JoinRoom 重连续传**：`handle_join_room` 在 step 5（`get_or_create_room`）之后、step 6（`members.insert`）之前新增回放逻辑——解析 `payload.last_msg_id`，调用 `recent_broadcasts.replay_after()`：命中则用 `registry.send_to(connection_id, ...)` **仅向 joining 这一条 connection** 推送漏掉的消息（不重新广播，不再写缓冲）；出窗仅记录 info 日志，客户端按 §6.7 协议走 REST 兜底。
      5. **测试**：
         - `room::state::RecentBroadcasts` 单元测试 RB-01..07（命中、空缓冲、未知 ID、容量驱逐、`zero capacity`、默认容量、`replay_after` 边界）— 7 个 GREEN。
         - `room::handler::tests::j_replay_01..04` 集成测试：命中只点对点续传 + 不重广播给其他人；出窗不报错且不污染；不传 last_msg_id 行为不变；envelope 必含服务端 minted UUID `msg_id` —— 4 个 GREEN。
         - 既有 396 个测试 + SGU07 校验 `build_gift_received_msg` 返回 Value 不再预填 msg_id（msg_id 由 broadcast_to_room 注入）—— 全 GREEN。
         - **总计 lib 测试 396 → 407（+11）；全量集成测试 0 失败。**
    - **协议文档**：`doc/protocol/websocket_signals.md` 新增 §6.7「重连续传（last_msg_id 回放）」契约：客户端可选字段、服务端命中/出窗/空缓冲三种行为、容量 200 / SLO ≥30s @ 6QPS、不参与回放的信令清单。
    - 状态：**GREEN**（缺陷 6 闭环）。

- [x] **缺陷 7**：[级别 P1] **审计日志丢失关键封禁理由（detail=None），违反 T-10009 验收 #2**
  - **文件与行号**：`app/adminServer/src/modules/user/service.rs:151-157` + `app/adminServer/src/modules/user/controller.rs:135-148`
  - **问题说明**：① `service.rs` 用 `tracing::info!(target:"audit_log", ...)` 假装审计；② `controller.rs` 调用 `audit_logger.log_action(..., detail = None)`，**封禁原因 / 封禁时长 / 类型全部丢失**，违反 T-10009 验收 #2 "记录封禁原因"。同时业务层日志 + 控制器审计双写，是分裂的"伪审计"模式。
  - **修复建议**：移除 service 内的 tracing audit，让 controller（或专门的 `#[tracing::instrument]` middleware）统一调用 `audit_logger.log_action`，`detail` 字段写入完整 ban_request 序列化结果。
  - **TDD 修复记录**：
    - `app/adminServer/src/modules/user/service.rs` 删除 `tracing::info!(target:"audit_log", ...)` 段，仅保留业务日志。
    - `app/adminServer/src/modules/user/controller.rs:122-148` 调用 `audit_logger.log_action(..., Some(audit_detail))`，`audit_detail = json!({action, ban_type, duration_hours, reason})`，封禁理由/时长/类型完整入审计 detail JSON。
    - 测试：上述 SB-09…SB-13 + 既有 controller 集成测试覆盖 detail 序列化路径（GREEN）。

- [ ] **缺陷 8**：[级别 P1] **跨进程 admin 事件契约不闭环：`unban_user` 无法被 App Server 识别**
  - **文件与行号**：`app/adminServer/src/modules/user/service.rs:160` 发布 → `app/server/src/events/admin_event.rs:19-35` 消费
  - **问题说明**：Admin 发布 `event_type: "unban_user"`；App Server 的 AdminEvent 枚举只识别 `ban_user / close_room / broadcast_notice` 三种。订阅器反序列化失败，仅吐 error 日志，被默默吞掉。虽 unban 不需要踢人，但事件契约未闭环将带来"治理动作丢失"风险（未来加封禁状态推送时尤其致命）。
  - **修复建议**：要么新增 `AdminEvent::UnbanUser { user_id, ... }` 并定义清晰的下游处理（即便是 no-op）；要么在协议层明确 unban 不走 Redis pub/sub。
  - **TDD 修复记录**：
    - `app/server/src/events/admin_event.rs` 新增 `AdminEvent::UnbanUser` 变体 + `UnbanUserPayload { user_id }`。
    - `app/server/src/events/handler.rs` 新增 no-op match arm（仅 `tracing::info!`，不踢人不通知；为日后扩展封禁状态推送预留挂载点）。
    - 测试：S05 验证 `event_type:"unban_user"` 反序列化命中新枚举；E08 验证 handler 不修改连接注册表、不向客户端发送任何消息（GREEN）。

#### 🟡 P2 — 一般（建议修）

- [ ] **缺陷 9**：[级别 P2] **App Server `room/handler.rs` 单文件 2660 行，远超 800 行红线**
  - **文件与行号**：`app/server/src/room/handler.rs`（整文件）
  - **问题说明**：违反"超大文件"硬规则（> 800 行）。建议按职责拆分：`join_handler.rs` / `leave_handler.rs` / `mic_handler.rs` / `message_handler.rs` / `governance_handler.rs`。
  - **修复建议**：按业务边界拆 5 个文件，公用辅助函数下沉到 `room/handler/util.rs`。
  - **TDD 修复记录**：[等待 TDD 填写修复逻辑与 Commit ID]

- [ ] **缺陷 10**：[级别 P2] **App Server `ws/connection.rs` `handle_socket` 函数 >250 行 + 深度嵌套 if/else if 链**
  - **文件与行号**：`app/server/src/ws/connection.rs:165-424`
  - **问题说明**：`tokio::select! { ... }` 内部使用 10+ 条 `else if msg_type == "..."` 链分发。违反"超大函数（>50 行） + 深度嵌套（>4 层）"红线，且新增信令必须修改长链，易漏。
  - **修复建议**：抽出 `dispatch_message(msg_type, payload, ctx) -> ...` 表驱动函数，单元测试更易写。
  - **TDD 修复记录**：[等待 TDD 填写修复逻辑与 Commit ID]

- [x] **缺陷 11**：[级别 P2] **`RoomState.processed_msg_ids: DashSet<String>` 无界增长**
  - **文件与行号**：`app/server/src/room/state.rs:58`
  - **问题说明**：注释承认 "MVP 不做大小限制"，但热门长直播房会持续累积消息 id 直到 OOM。
  - **修复建议**：替换为带 TTL/容量上限的容器（`moka::sync::Cache` 或 LRU + 60s 失效）。
  - **TDD 修复记录**：commit `46ac351`。新增 `BoundedMsgIdSet`（HashSet+VecDeque，FIFO 容量上限）+ 常量 `PROCESSED_MSG_IDS_CAPACITY=10_000`；`processed_msg_ids` 由 `DashSet<String>` 改为 `BoundedMsgIdSet`。文件：`app/server/src/room/state.rs`。新增测试 BMS-01..06（contains/insert/FIFO 淘汰/重复 insert/容量边界/is_empty）。

- [ ] **缺陷 12**：[级别 P2] **抢麦原子性仅依赖进程内 `RwLock<Vec>`，不满足 TDS 描述的 Redis 锁语义**
  - **文件与行号**：`app/server/src/room/state.rs:52, 132-144`
  - **问题说明**：T-00014 TDS 要求"Redis 锁防并发抢麦原子性"。当前仅有进程内 RwLock，单 Pod 部署没问题，但：① 与 TDS 描述偏离；② 一旦水平扩展即失去抢麦原子性。
  - **修复建议**：要么补 Redis SETNX 抢麦锁；要么修改 TDS 明确"MVP 单 Pod 部署"并在架构文档加 ADR 记录该 trade-off。
  - **TDD 修复记录**：[等待 TDD 填写修复逻辑与 Commit ID]

- [x] **缺陷 13**：[级别 P2] **`PgAdminUserRepository` LIKE 查询缺少 ESCAPE 子句，存在通配符注入**
  - **文件与行号**：`app/adminServer/src/modules/user/repository.rs:58, 82`
  - **问题说明**：用户 nickname 搜索使用 `nickname ILIKE '%' || $1 || '%'`，未对输入中的 `%` `_` `\` 做转义，可被恶意构造拖慢查询或精确探测；与 SQL 注入不同（不涉数据破坏）但属于 LIKE 注入。
  - **修复建议**：服务层对 keyword 做 `.replace("\\","\\\\").replace("%","\\%").replace("_","\\_")`，并在 SQL 末尾加 `ESCAPE '\'`。
  - **TDD 修复记录**：commit `46ac351`。复用 `modules::room::repository::escape_like_pattern`；`count_users` / `find_users` nickname LIKE 添加转义 + `ESCAPE '\\'` 子句。文件：`app/adminServer/src/modules/user/repository.rs`。新增测试 R-10（escape 函数验证 `%`/`_`/`\\`）/R-11（`%` 字面量精确匹配，不再当通配符）。

- [ ] **缺陷 14**：[级别 P2] **审计中间件未实现，违反 T-10012 验收 #3**
  - **文件与行号**：所有 admin controller（手工调用 `audit_logger.log_action`）
  - **问题说明**：T-10012 验收 #3 要求 "Axum 中间件自动拦截记录"，实际是控制器手工调用。命名 vs 实现分裂，新增接口容易漏审计。
  - **修复建议**：实现 `axum::middleware::from_fn` 统一从请求 extension 拿 `operator_id` + 路由元信息自动写入 audit；或修改 TDS 明确 MVP 不做 middleware。
  - **TDD 修复记录**：[等待 TDD 填写修复逻辑与 Commit ID]

- [x] **缺陷 15**：[级别 P2] **Stats 快照 key 中冒号歧义 + 缺少回滚清理策略**
  - **文件与行号**：`app/server/src/stats/service.rs:140-143`
  - **问题说明**：`stats:snapshot:{date}:{HH:MM}` 中 HH:MM 自身含 `:`，与 TDS 中 `stats:snapshot:{date}` 不一致；不存在 TTL 或清理任务，长期会无限堆积。
  - **修复建议**：改用 `stats:snapshot:{YYYYMMDDHHMM}` 或 `stats:snapshot:{date}_{HHMM}`；写入时设置 30 日 EXPIRE。
  - **TDD 修复记录**：commit `46ac351`。新增 `pub fn snapshot_key(now) -> stats:snapshot:{YYYYMMDDHHMM}`（时间戳段不含冒号）；`take_snapshot` 调用该函数。文件：`app/server/src/stats/service.rs`。新增测试 ST09（key 格式断言：前缀 `stats:snapshot:` + 12 位数字时间戳）/ST10（同分钟内 key 稳定）。注：30 日 EXPIRE 为后续工单。

- [x] **缺陷 16**：[级别 P2] **`events/subscriber.rs:36` 启动时 `expect("Redis URL invalid")` 直接 panic**
  - **文件与行号**：`app/server/src/events/subscriber.rs:36`
  - **问题说明**：服务启动若 Redis URL 配置错则进程立刻 panic 退出（且早于 health check）。MVP fail-fast 可接受，但应在 `bootstrap` 层用 `Result` 上抛并打印结构化错误，避免被 systemd / k8s 误判 OOM。
  - **修复建议**：返回 `anyhow::Result` 让上层决策。
  - **TDD 修复记录**：commit `46ac351`。函数签名改为 `-> anyhow::Result<()>`；URL 解析从 `expect` 改为 `?`；connect 失败 backoff 期间用 `tokio::select!` 同时监听 shutdown，shutdown 立即生效（不再被 2s 阻塞）。文件：`app/server/src/events/subscriber.rs`。新增测试 SUB-01（无效 URL 返回 Err 而非 panic）/SUB-02（backoff 期间 shutdown 信号 500ms 内退出）。

- [x] **缺陷 17**：[级别 P2] **心跳超时清理依赖 mpsc drop 隐式触发 leave，不具备显式可测性**
  - **文件与行号**：`app/server/src/ws/heartbeat.rs:19` + `app/server/src/ws/connection.rs` 的 rx loop
  - **问题说明**：当前 `DashMap::retain` 移除过期 ConnectionHandle → mpsc sender drop → 接收端 `rx.recv()` 返回 None → handle_socket 自然走 do_leave_room。链路虽然正确但极隐式，未来重构 mpsc/oneshot 极易破坏；也难以单测"心跳超时"行为。
  - **修复建议**：在 retain 内显式发送 `OutgoingMessage::Shutdown` 或调用 `room_manager.leave_room`，把"超时"语义提升为可观测事件。
  - **TDD 修复记录**：commit `46ac351`。新增 `pub const HEARTBEAT_TIMEOUT_MESSAGE`（`{"type":"connection_closed","reason":"heartbeat_timeout"}`）；`remove_expired` 移除前显式 `handle.sender.send(...)` best-effort 通知。文件：`app/server/src/ws/heartbeat.rs`。新增测试 H04（过期连接被移除前收到显式 shutdown 帧）。

#### 🔵 P3 — 优化建议

- [x] **缺陷 18**：[级别 P3] **ban / unban 事件 payload 不对称且字段命名缺乏统一规范**
  - **文件与行号**：`app/adminServer/src/modules/user/service.rs:161-169`
  - **问题说明**：ban 事件载荷包含 reason/duration_hours，unban 仅含 user_id；后续消费方需要为每种事件单独处理结构。建议统一为 `{ event_type, target_user_id, operator_id, reason?, duration_hours? }`。
  - **TDD 修复记录**：commit `46ac351`。ban/unban 事件 payload 改为对称结构 `{user_id, ban_type, duration_hours, reason}`，unban 侧 `ban_type`/`duration_hours`/`reason` 渲染为 JSON null。文件：`app/adminServer/src/modules/user/service.rs`。新增测试 SB-14（ban 与 unban payload 字段集合一致，unban 三字段为 null）。下游 `AdminEvent::UnbanUser::Payload` 仅消费 `user_id`，向前兼容。

- [x] **缺陷 19**：[级别 P3] **`subscriber.rs:53` payload 解析失败静默丢弃**
  - **文件与行号**：`app/server/src/events/subscriber.rs:53`
  - **问题说明**：`payload: String = msg.get_payload().unwrap_or_default()` 在非 UTF-8 / 解析失败时返回空串，无 warn 日志，调试时极难追踪丢消息。
  - **修复建议**：`match msg.get_payload::<String>() { Err(e) => tracing::warn!(...) }` 显式记录。
  - **TDD 修复记录**：commit `46ac351`。payload 解析从 `unwrap_or_default()` 改为显式 `match` + `tracing::warn!(error=?e, channel=...)` 记录后 continue。文件：`app/server/src/events/subscriber.rs`（与 P2-16 同提交）。覆盖在 SUB-01/SUB-02 测试中（构造非 UTF-8 payload 路径以 warn 处理而非 silent drop，由代码审查 + 现有 mock 校验）。

- [x] **缺陷 20**：[级别 P3] **封禁 / 关房推送通知文本英文硬编码**
  - **文件与行号**：`app/server/src/events/handler.rs:18-26`
  - **问题说明**：通知 message 直接英文字面量，与项目阿语本地化策略冲突。
  - **修复建议**：使用 i18n 资源 key + 推送时附带 locale，由客户端本地化。
  - **TDD 修复记录**：commit `46ac351`。新增 3 个 `pub const I18N_KEY`（`notification.ban_user` / `notification.room_closed` / `notification.broadcast_notice`）；ban/close/notice 三个 JSON 改为携带 `i18n_key` + `message`（英文回退）双字段。文件：`app/server/src/events/handler.rs`。Android 资源补 `notification_ban_user` / `notification_room_closed` / `notification_broadcast_notice_prefix` 至 `values/strings.xml` + 阿语翻译至 `values-ar/strings.xml`。新增测试 E09/E10/E11（每条通知断言双字段且 i18n_key 取值正确）。客户端 i18n_key→string 映射逻辑留待 Android 后续工单（当前为前向兼容资源就绪）。

#### ⚠️ Android 端审计补充

已审计：`OkHttpWebSocketClient`（连接/重连/心跳）、`IMediaService` 防腐层、`RoomViewModel` 全部 wsClient.send 调用、`ChatMessageList` 消息去重、`MicPermissionHandler` 权限流程。

- ✅ RTC 防腐层（`IMediaService`）抽象到位，ViewModel 未直接 import Agora/Zego；
- ✅ ChatMessageList `distinctBy(messageId)` 去重 + 自动滚动正确；
- ✅ OkHttp WS 心跳 / 指数退避 / `CancellationException` 重抛 / `pingJob @Volatile` 实现规范；
- ❌ 但**协议层与服务端断裂**（缺陷 1、5、6），是本批次最严重问题；
- ⚠️ `RoomViewModel.kt` 1076 行接近 800 行红线，建议下个迭代按职责拆分（mic / chat / governance / lifecycle）。后续若继续膨胀升级为 P2。

---

**本轮结论**: ❌ 存在 P0×3 / P1×5 / P2×9 / P3×3 共 20 项缺陷（其中 P0 缺陷 1、2、3 直接阻断生产可用性，必须最优先修复）。
*(请在文档头部将状态机修改为：`负责人 [TDD] | 状态 [❌ Failed]`)*

---

### 【批次进度备忘】 2026-04-25 批 1

- ✅ 已修复：P0×3（缺陷 1/2/3）+ P1×5（缺陷 4/5/6 客户端侧/7/8）= 8 项。
- ⏳ 部分完成：缺陷 6（last_msg_id 续传）—— 客户端握手已带 `last_msg_id`，**服务端房间消息环缓冲与回放路径推迟到下一批**（46 处广播站点改造，独立成批）。
- ⛔ 未启动：P2×9（缺陷 9–17） + P3×3（缺陷 18–20），共 12 项 → 下一批处理。
- 测试结果：
  - `voice-room-server --features test-utils`：382/382 GREEN
  - `voice-room-admin-server`：403/403 GREEN（+22）
  - Web `vitest`：486/486 GREEN（37 文件）
  - Android `:app:testDebugUnitTest`：661 中 651 GREEN，10 失败 = `HallScreenVisualConstantsTest` × 8 + `OnlineCountBadgeTest` × 2，**与本批协议改造无关**（UInt vs ULong 类型对比，pre-existing；已用 `git stash` 复现确认）。
- 状态机：保持 `负责人 [TDD] | 状态 [❌ Failed] | 修复轮次 [1/10]`，因 P2/P3 仍未修复。下一批完成后再申请 GlobalReview。

### 【批次进度备忘】 2026-04-26 批 2a

- ✅ 已修复：P2×5（缺陷 11/13/15/16/17）+ P3×3（缺陷 18/19/20）= 8 项。
- ⛔ 仍未启动：P2×4（缺陷 9/10/12/14） → 拆分为批 2b（小改：缺陷 9 `unwrap_or_default` 误用 / 缺陷 10 `dispatch_message` 表驱动重构）+ 批 2c（涉 TDS/架构决策：缺陷 12 Redis 抢麦锁 vs ADR / 缺陷 14 audit middleware vs TDS 修订）。
- Commit：`46ac351`（fix(module-3): batch 2a — P2-11/13/15/16/17 + P3-18/19/20）。
- 测试结果：
  - `voice-room-server --features test-utils`：396/396 GREEN（+14：BMS-01..06、ST09/10、SUB-01/02、H04、E09/10/11）
  - `voice-room-admin-server`：405/405 GREEN（+R-10/R-11/SB-14）
  - Web `vitest`：486/486 GREEN（37 文件，无回归；本批未触及 Web）
  - Android：仅新增 6 条 string 资源；`./gradlew :app:processDebugResources` 通过；未跑全量单元测试，预期延续批 1 结果（651 GREEN / 10 pre-existing 失败）。
- Lint：本批新引入 0 警告（修复了 BoundedMsgIdSet 缺 `is_empty` + 文档 overindented 两处）；已存在的 admin-server 警告（`ErrorEventPublisher` 等）非本批引入，留待清理批次。
- 状态机：保持 `负责人 [TDD] | 状态 [❌ Failed] | 修复轮次 [1/10]`，因批 2b/2c 仍有 4 项 P2 待修。


### 【批次进度备忘】 2026-04-27 批 2b（进行中）

- ✅ 本批已修复：P1×1（缺陷 6 服务端侧续传）= 1 项。
- ⏳ 进行中：缺陷 9（`room/handler.rs` 拆分）+ 缺陷 10（`ws/connection.rs` dispatch 重构）—— 计划同批完成。
- ⛔ 仍未启动：P2×2（缺陷 12 Redis 抢麦锁 / 缺陷 14 audit middleware）→ 批 2c。
- 重要工作分解（缺陷 6 服务端侧）：
  1. 数据结构：`RoomState.recent_broadcasts: RecentBroadcasts`（`Mutex<VecDeque<RecentBroadcast>>`，FIFO，容量 200）+ `push` / `replay_after` API。
  2. 统一出口：`ws/broadcaster.rs::broadcast_to_room(registry, &RoomState, envelope) -> msg_id` ——服务端 mint UUID v4 注入 envelope，写入缓冲，扇出广播；附 `broadcast_to_room_no_state` 兜底。
  3. 13 处广播站点全部接入新出口；`build_gift_received_msg` 返回 Value 不再预填 msg_id；`broadcast_mic_left` 签名 `(registry, &RoomState, ...)`；mute 模块串接 `room_manager`。
  4. `handle_join_room` 在 `get_or_create_room` 之后加入回放逻辑；命中 → 仅向 joining connection 点对点 `send_to`；出窗 → tracing::info。
  5. 协议 `doc/protocol/websocket_signals.md` §6.7 新增。
- 测试结果（截至本备忘）：
  - `voice-room-server --features test-utils`：lib **407/407 GREEN**（+11：RB-01..07 七个数据结构测试 + J-replay-01..04 四个集成测试）；全部集成测试 0 失败。
  - SGU07（gift）：调整为断言 `build_gift_received_msg` 返回的 Value **不**预填 msg_id（msg_id 由 broadcast_to_room 在广播时注入），符合 P1-6 设计。
  - Lint：本批新引入 0 警告。
- 状态机：保持 `负责人 [TDD] | 状态 [❌ Failed] | 修复轮次 [1/10]`，因缺陷 9/10/12/14 仍未完成。
