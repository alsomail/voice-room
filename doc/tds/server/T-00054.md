# TDS: 待 ACK 订单后台对账 cron (Task ID: T-00054)

## §0 产品验收（Given-When-Then）

- **关联 Spec**：[`doc/specs/recharge_order.md`](../../specs/recharge_order.md)
- **关联状态机**：[`state_machines.md#order`](../../product/state_machines.md#order)
- **关联用户旅程**：[`user_journeys.md#j1-recharge-gift-noble`](../../product/user_journeys.md#j1-recharge-gift-noble)
- **关联业务约束**：[`business_constraints.md`](../../product/business_constraints.md)（`ORDER_PENDING_TTL_SEC` / `REFUND_NEGATIVE_BALANCE_LIMIT_GOLD` / `ORDER_DAILY_COUNT_LIMIT`）
- **必须满足的验收条款**（GWT 全文以 spec §5 为唯一事实源，**禁止本地改写**）：GWT-O1 / GWT-O2 / GWT-O3 / GWT-O4 / GWT-O5

---

## 一、背景与目标
- **关联需求**：[phase1_payment_billing.md §6.4 / §0 acknowledgePurchase 必须 3 天内完成](../../product/phase1_payment_billing.md) — Google 规定未 ack 的购买 3 日后自动退款，必须有兜底 cron。
- **本 Task 的目标**：Tokio cron 每 5min 扫 `state IN (VERIFYING, CREDITED) AND age > 阈值` 订单，重新查 Google 状态强制推进至 ACKED 或 FAILED。

## 二、方案设计

### 核心数据流
```
tokio_cron_scheduler 每 5min
  ↓
SELECT id FROM payment_orders WHERE
  (state='VERIFYING' AND created_at < now()-INTERVAL '10min')
  OR (state='CREDITED' AND acked_at IS NULL AND credited_at < now()-INTERVAL '1h')
  LIMIT 100
  ↓
逐单调 GooglePlayBillingPort::get_product_purchase 重新校验
  ├─ purchaseState=0 + 未 acked → adapter.acknowledge → state=ACKED
  ├─ purchaseState=0 + 已 acked → state=ACKED（仅更新本地状态）
  ├─ purchaseState=1 (CANCELED) → state=FAILED + 反向 wallet（仅当 CREDITED）
  └─ Google 5xx → 写 Sentry + 下次 cron 重试（最多 24h）
```

### 涉及文件清单
| 文件 | 变更 | 说明 |
|------|------|------|
| `app/server/src/modules/payment/reconcile_cron.rs` | 新增 | cron 主循环 |
| `app/server/src/main.rs` | 修改 | 注册 cron job |
| `app/server/tests/payment_reconcile_test.rs` | 新增 | 注入 FakeGooglePlayBillingPort + 假时间推进 |

### 🔌 协议路径绑定表

| # | 协议类型 | 入口 | 处理函数 | protocol/ 锚点 |
|---|---------|------|---------|---------------|
| - | N/A — 内部 cron，不直接对外通信；间接复用 T-00052 的 `BalanceUpdated` 推送 | - | `reconcile_cron::tick` | [payment_api.md §8 状态机](../../protocol/payment_api.md) |

## 三、TDD 验收用例
- [ ] **VERIFYING 超时**：mock 一笔 12 分钟前 VERIFYING 订单 → cron 推进至 ACKED 或 FAILED
- [ ] **CREDITED 未 ACK**：mock 一笔 1.5h 前 CREDITED 未 ack 订单 → 调 acknowledge → state=ACKED
- [ ] **Google 5xx**：mock 临时错误 → 订单状态保留，下次 tick 重试；3 次失败写 Sentry
- [ ] **不影响主流程**：cron 运行期间正常 verify 请求 P95 不退化（用 SELECT FOR UPDATE SKIP LOCKED）
- [ ] **写 admin_logs**：每次推进结果落审计

### 🔴 协议路径绑定的强制验收
- [ ] **PROTO-1**：N/A（无跨端）；但需断言 cron 触达的外部 HTTP URL 仍指向 Google `purchases.products:get` / `:acknowledge`，与 T-00052 复用同一 Adapter（不允许另起一个 client）

## 四、实现结果
- 实际修改的文件：
  - `app/server/src/modules/payment/cron.rs`：添加 step3 (PENDING > 24h → CANCELLED)、step4 (PENDING_GOOGLE > 72h re-check)
  - `app/server/src/modules/payment/repo.rs`：添加 find_stale_pending_orders、cancel_stale_pending_order、find_stale_pending_google_orders
- 测试覆盖率：lib tests 591 passed, 0 failed（Round2 GREEN）；新增 C02~C04 测试
- 完成时间：2026-05-11
## 五、Reviewer意见
### Round 1
- **状态**：❌ 未通过（2026-05-10，Reviewer: everything:code-reviewer / gpt-5.4-mini）
- **审查意见**：
  - ⚠️ HIGH：`cron.rs` 当前仅处理 `VERIFYING/CREDITED`，未覆盖协议要求的超时 `PENDING/VERIFYING` 订单终态推进；与 `PENDING_GOOGLE` 未持久化问题叠加后，存在长期悬挂订单。
  - ⚠️ HIGH：需补充超时扫描、终态落库与告警日志，确保超过窗口的订单能进入 `CANCELLED/FAILED/VOIDED` 等明确终态。
- **🔴 Changelog 行**：`v3.23 | 2026-05-10 | [T-00050~55 Review Fail ❌] 模块10 Server端支付审查打回：RTDN 端点未验签可伪造、verify 并发双充/终态回退、RTDN 幂等与时间戳错误、超时订单未终态推进、dev_mock feature 编译失败、schema 测试失配；状态 Review→TDD`

### Round 2
- **状态**：❌ 未通过（2026-05-10，Reviewer: everything:code-reviewer）
- **审查意见**：
  - ✅ P0-5 PENDING 超时：`cron.rs` step 3 调用 `repo.find_stale_pending_orders()`（SQL: `state='PENDING' AND created_at < now() - INTERVAL '24 hours'`）→ `cancel_stale_pending_order()`（SQL: `WHERE order_id=$1 AND state='PENDING'`），逻辑幂等，24h 超时 PENDING → CANCELLED 已到位。
  - ✅ P0-5 PENDING_GOOGLE 超时：step 4 扫描 `state='PENDING_GOOGLE' AND created_at < now() - INTERVAL '72 hours'` 并调用 Google API 重查，超时后仅记录日志（实际 credit 委托 RTDN/verify 路径），策略合理。
  - ❌ P0-6（T-00055 关联）：模块级联动打回，整体轮次 ❌。
- **🔴 修复要点**：见 T-00055 Round 2。本 Task 自身无需改动。

### Round 3
- **状态**：✅ 通过（2026-05-10，Reviewer: everything:code-reviewer）
- **审查意见**：P0-6 dev_mock 编译已修复（Uuid+4参数签名对齐），P0-1 RS256+JWKS 验签已实现（T53-08/12/13/14），全部7项P0通过，607测试全绿。

## 六、DoD 完成记录
- [ ] `doc/arch/server/payment.md` cron 章节
- [ ] Changelog 行追加
