# TDS: REST/WS Chat 双路径 envelope 等价回归集成测试 (Task ID: T-00048)

## 一、背景与目标

基于 T-00047 已完成的基础（REST POST /api/v1/chat-messages + WS SendMessage 双路径落锚），本 Task 新增更全面的双路径集成测试，要求真实 WS 客户端连接、多客户端并发场景、字段逐项断言、死连接清理验证。

T-00047 已验证（PROTO-2-A/B/C，13/13 全绿），T-00048 在此基础上新增：
- 两个 WS 观察者 + REST 发送者并发场景
- 字段逐项断言（msg_id UUID v4、content、timestamp、from_user、type）
- 死连接清理逻辑在双路径下的一致性验证

## 二、方案设计

### 核心数据流

```
WS 路径：  WS Client → ws::handler → handle_send_message → broadcast_to_room → WS Observer A/B
REST 路径：REST Client → POST /api/v1/chat-messages → send_chat_message_handler → broadcast_to_room → WS Observer A/B
```

### 涉及文件清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `app/server/tests/chat_dual_path_equivalence.rs` | 新增 | 双路径等价回归集成测试（纯测试，不改业务代码）|
| `app/server/Cargo.toml` | 修改 | 注册 [[test]] chat_dual_path_equivalence |

### 🔌 协议路径绑定表（Plan 必填）

| # | 协议类型 | 入口/信令名 | 客户端调用方 | 服务端处理函数 | 广播/响应 | protocol/ 锚点 |
|---|---------|------------|-------------|--------------|---------|--------------|
| 1 | WS C→S | SendMessage ⭐ | RoomViewModel.sendMessage → wsClient.sendEnvelope | app/server/src/room/handler/chat.rs::handle_send_message | 广播 RoomMessage 到房间所有 WS 连接 | websocket_signals.md §6.8.1 / [schemas/ws/SendMessage.schema.json](../../protocol/schemas/ws/SendMessage.schema.json) |
| 2 | WS S→Room 广播 | RoomMessage | 客户端接收 onMessage | app/server/src/ws/broadcaster.rs::broadcast_to_room | 房间所有 WS 连接（含观察者 A/B）| websocket_signals.md §6.8.2 / [schemas/ws/RoomMessage.schema.json](../../protocol/schemas/ws/RoomMessage.schema.json) |
| 3 | HTTP REST | POST /api/v1/chat-messages（备路径） | 无客户端调用（运营工具/测试） | app/server/src/modules/chat/controller.rs::send_chat_message_handler | 同房间广播 RoomMessage | room_api.md §3.6.1 / [schemas/ws/SendMessage.schema.json](../../protocol/schemas/ws/SendMessage.schema.json) |

### 实现约束

- **纯测试任务**：不修改任何业务代码（handler/controller/broadcaster）
- 测试文件位置：`app/server/tests/chat_dual_path_equivalence.rs`（与现有测试平级）
- 复用现有辅助函数（common/ 模块或 chat_rest_broadcast_test.rs 中的辅助函数）
- UUID 验证：使用 `uuid::Uuid::parse_str()` 验证合法 v4 格式
- 测试需要 `test-utils` feature
- WS 路径通过 `handle_send_message` 函数直接调用（与 PROTO-2-B 相同模式），REST 路径通过 `build_app + oneshot`

## 三、TDD 验收用例

### DUAL-1（双 WS 观察者均收到等价 envelope）
- **前置**：两个 WS 观察者 A/B 同时连接同一房间（通过 register_conn），一个 WS 发送者，一个 REST 发送者
- **步骤**：
  1. WS 发送者发一条消息（handle_send_message）
  2. REST 发送者发一条消息（POST /api/v1/chat-messages）
  3. 观察者 A/B 分别收到两条 RoomMessage
- **验证**：WS 发的 envelope vs REST 发的 envelope（在同一观察者视角）除 `envelope.msg_id`/`payload.msg_id` 外逐字段相等
- **通过条件**：A/B 两个观察者均通过字段比对

### DUAL-2（payload 字段逐项断言）
- **验证字段**：
  - `payload.msg_id`：合法 UUID v4（Uuid::parse_str 不报错），WS 路径和 REST 路径的值互不相同
  - `payload.content`：两路径相等（均经 filter_content 处理）
  - `payload.timestamp`：int64 ms（值允许不同，但均 > 0）
  - `payload.user_id`：两路径相等
  - `type`：均等于 "RoomMessage"
- **通过条件**：所有字段断言均通过

### DUAL-3（死连接清理逻辑一致性）
- **步骤**：
  1. 关闭一个 WS 观察者连接（不优雅断开，直接 drop receiver）
  2. 通过 WS 路径发消息 → 确认服务器不 panic、不阻塞（有存活观察者收到消息）
  3. 通过 REST 路径发消息 → 确认服务器不 panic、不阻塞（存活观察者收到消息）
- **通过条件**：两次发消息均完成，无 panic，存活观察者均收到消息

## 四、实现结果

- **实现时间**: 2026-05-05
- **新增文件**: `app/server/tests/chat_dual_path_equivalence.rs`
- **修改文件**: `app/server/Cargo.toml`（新增 [[test]] chat_dual_path_equivalence）
- **测试结果**: DUAL-1/2/3 全部通过（3/3）
- **测试命令**: `cargo test --test chat_dual_path_equivalence --features test-utils`
- **全量测试**: server 无新增失败（admin-server `wa06` 为既有缺陷，与本 Task 无关）

## 五、Reviewer 意见

### Round 1 - 状态：🟢 通过

**审查日期**: 2026-05-05

**协议路径绑定校验**：
- 行 #1（WS SendMessage ⭐）：DUAL-1/2/3 均通过 `handle_send_message` 直接调用覆盖 WS 路径 ✅
- 行 #2（RoomMessage 广播）：DUAL-2 对 type/payload.msg_id/payload.content/timestamp/payload.user_id 逐项断言 ✅
- 行 #3（REST POST /chat-messages）：DUAL-1/2/3 均通过 `build_app + oneshot` 覆盖 REST 路径 ✅

**测试质量**：
- DUAL-1 两观察者 A/B 并发收到双路径广播：✅
- DUAL-2 字段逐项断言（msg_id UUID v4 互不相同、content 等价、timestamp 数值、user_id 相等、type="RoomMessage"）：✅
- DUAL-3 死连接清理双路径一致性（drop receiver → WS 路径 + REST 路径均完成，存活观察者均收到）：✅
- Cargo.toml 注册 `[[test]] chat_dual_path_equivalence` required-features test-utils：✅
- 业务代码洁净（纯测试 + Cargo.toml [[test]] 注册，无业务代码改动）：✅

**结论**：代码质量良好，测试覆盖完整，通过审查。

---

### Round 1 GlobalReview 独立纪要（@GlobalReview，2026-05-05）

**审查方式**：独立源码审计（不依赖 TDD 内部审查记录，直接比对协议文档 + 服务端实现 + 测试代码）

**协议路径绑定独立核验结论：PASS ✅**

> grep 证据：
> - `grep -n "handle_send_message" app/server/tests/chat_dual_path_equivalence.rs` → 命中行 26/131/225/363
> - `grep -n "post(send_chat_message_handler)" app/server/src/modules/chat/routes.rs` → 命中行 24
> - `grep -n "pub fn broadcast_to_room" app/server/src/ws/broadcaster.rs` → 命中行 30
> - `grep -n "filter_content" app/server/src/room/handler/chat.rs` → 命中行 40/111（WS路径）
> - `grep -n "filter_content" app/server/src/modules/chat/controller.rs` → 命中行 113（REST路径）
> - 字段名 `payload.user_id`：协议/实现/测试三端一致，无 `from_user`/`sender_id` 命名错位

**独立发现的缺陷**：

| 级别 | 位置 | 描述 |
|------|------|------|
| P2 | `chat_dual_path_equivalence.rs:304-311` | DUAL-2 `timestamp` 仅做 `is_number()` 检查，未验证 `> 0`，与 TDS §三规格偏差（注释写"int64 > 0"但实现不符） |
| P3 | `chat_dual_path_equivalence.rs:12` | `mod common;` 声明但文件内零调用，为无效引入（不影响编译，`common/mod.rs` 有 `#![allow(dead_code)]` 兜底） |
| P3 | `chat_dual_path_equivalence.rs:171-174` | DUAL-1 `type` 断言为相对等值（WS==REST），非绝对值（== "RoomMessage"），DUAL-2 补充覆盖，联合可接受 |

**整体结论**：✅ 通过。无 P0/P1 问题。1 条 P2 规格偏差有纠正价值，2 条 P3 优化建议可选处理。测试实现与 TDS 设计意图高度吻合，DUAL-1/2/3 三用例逻辑清晰，双路径协议契约验证完整。
