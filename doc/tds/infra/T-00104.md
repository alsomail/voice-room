<!--
[AI 写入规约]
1. Plan Agent 创建。
-->

# TDS: Android × Server 跨语言 WS 回环测试（8 场景）(Task ID: T-00104)

## 一、背景与目标

- **关联需求**：BUG-CHAT-WS 复盘 R3 — server 自己写 envelope 自己 parse 永远过单测，但 server 字符串与 Android 反序列化字符串脱钩。本 Task 用**真 Android 客户端 + 真 server 进程**跑 8 个核心场景的端到端回环。
- **用户决策 #3**：仅做 Android × Server，Web 不强制（留 follow-up）。
- **本 Task 的目标**：建立 `tests/cross-lang/android-server-ws/` 测试套，每个场景：
  1. 启动真 server（test profile + 临时 PG）
  2. 启动 Android Instrumented Test 触发 WS 入站消息
  3. 断言 Android UI state 与 server 数据库状态一致
  4. 断言双方 envelope 在 schema 校验下互通

## 二、方案设计

### 8 个核心场景

| # | 场景 | C→S 触发 | S→C 验证 |
|---|------|---------|---------|
| 1 | Ping/Pong 心跳 | Android 客户端发 `Ping` | 收 `Pong`，5 次往返不掉线 |
| 2 | JoinRoom→UserJoined | Android `JoinRoom` | 收 `JoinRoomResult` + 第二客户端收 `UserJoined` |
| 3 | TakeMic→MicTaken | Android `TakeMic` | 收 `TakeMicResult` + 同房间收 `MicTaken` |
| 4 | LeaveMic→MicLeft | 同上 | `LeaveMicResult` + `MicLeft` |
| 5 | SendMessage→RoomMessage | Android `SendMessage` | 同房间收 `RoomMessage` |
| 6 | SendGift→GiftReceived+BalanceUpdated | Android `SendGift` | 收 `SendGiftResult` + 房间收 `GiftReceived` + 自己收 `BalanceUpdated` |
| 7 | MuteUser→UserMuted | Admin 通过 WS 触发 `MuteUser` | 同房间收 `UserMuted` |
| 8 | KickUser→UserLeft | Admin `KickUser` | 被踢者收 `connection_close` + 同房间收 `UserLeft` |

### 涉及文件清单

| 文件 | 变更 | 说明 |
|------|------|------|
| `tests/cross-lang/android-server-ws/runner.sh` | 新增 | 启动 server + 跑 instrumented test |
| `tests/cross-lang/android-server-ws/scenarios/*.kt` | 新增 | 8 个场景类 |
| `app/android/.../androidTest/.../CrossLangWsTest.kt` | 新增 | Espresso/Compose Test 测试入口 |
| `package.json` | 修改 | 加 `test:cross-lang:ws` script |
| `.github/workflows/cross-lang.yml` | 新增 | CI workflow（夜间跑） |

### 🔌 协议路径绑定表

| 场景 | Android 客户端调用入口（⭐ = 主路径） | protocol/ 锚点 | Server 处理函数 |
|------|--------------------------------------|---------------|----------------|
| CROSS-1: Ping 发送 | ⭐ `OkHttpWebSocketClient.startHeartbeat` → `wsClient.send({"type":"Ping",...})` | [websocket_signals.md §6.5.1](../../protocol/websocket_signals.md#651-pingcs) | `app/server/src/ws/connection.rs::ping_pong_responses` |
| CROSS-1: Pong 接收 | ⭐ `OkHttpWebSocketClient.onMessage` → pong 检测 | [websocket_signals.md §6.6.1](../../protocol/websocket_signals.md#661-pongsc) | `app/server/src/ws/connection.rs::ping_pong_responses` |
| CROSS-2: JoinRoom | ⭐ `RoomViewModel.joinRoom` → `wsClient.send({"type":"JoinRoom",...})` | [websocket_signals.md §6.5.2](../../protocol/websocket_signals.md#652-joinroomcs) | `app/server/src/room/handler/lifecycle.rs::handle_join_room` |
| CROSS-2: JoinRoomResult | ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.JoinRoomResult` | [websocket_signals.md §6.6.2](../../protocol/websocket_signals.md#662-joinroomresultsc) | `app/server/src/room/handler/lifecycle.rs::handle_join_room` |
| CROSS-2: UserJoined（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.UserJoined` | [websocket_signals.md §6.7.1](../../protocol/websocket_signals.md#671-userjoinedsroom) | `app/server/src/room/handler/lifecycle.rs::handle_join_room` |
| CROSS-3: TakeMic | ⭐ `RoomViewModel.takeMic` → `wsClient.send({"type":"TakeMic",...})` | [websocket_signals.md §6.5.4](../../protocol/websocket_signals.md#654-takemiccs) | `app/server/src/room/handler/mic.rs::handle_take_mic` |
| CROSS-3: TakeMicResult | ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.TakeMicResult` | [websocket_signals.md §6.6.4](../../protocol/websocket_signals.md#664-takemicresultsc) | `app/server/src/room/handler/mic.rs::handle_take_mic` |
| CROSS-3: MicTaken（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.MicTaken` | [websocket_signals.md §6.7.3](../../protocol/websocket_signals.md#673-mictakensroom) | `app/server/src/room/handler/mic.rs::handle_take_mic` |
| CROSS-4: LeaveMic | ⭐ `RoomViewModel.leaveMic` → `wsClient.send({"type":"LeaveMic",...})` | [websocket_signals.md §6.5.5](../../protocol/websocket_signals.md#655-leavemiccs) | `app/server/src/room/handler/mic.rs::handle_leave_mic` |
| CROSS-4: LeaveMicResult | ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.LeaveMicResult` | [websocket_signals.md §6.6.5](../../protocol/websocket_signals.md#665-leavemicresultsc) | `app/server/src/room/handler/mic.rs::handle_leave_mic` |
| CROSS-4: MicLeft（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.MicLeft` | [websocket_signals.md §6.7.4](../../protocol/websocket_signals.md#674-micleftsroom) | `app/server/src/room/handler/mic.rs::handle_leave_mic` |
| CROSS-5: SendMessage | ⭐ `RoomViewModel.sendMessage` → `wsClient.send({"type":"SendMessage",...})` | [websocket_signals.md §6.5.6](../../protocol/websocket_signals.md#656-sendmessagecs) | `app/server/src/room/handler/chat.rs::handle_send_message` |
| CROSS-5: RoomMessage（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.RoomMessage` | [websocket_signals.md §6.7.5](../../protocol/websocket_signals.md#675-roommessagesroom) | `app/server/src/room/handler/chat.rs::handle_send_message` |
| CROSS-6: SendGift | ⭐ `GiftPanelViewModel.sendGift` → `wsClient.send({"type":"SendGift",...})` | [websocket_signals.md §6.5.7](../../protocol/websocket_signals.md#657-sendgiftcs) | `app/server/src/modules/gift/send_gift/handler.rs::handle_send_gift` |
| CROSS-6: GiftReceived（广播）| ⭐ `GiftPanelViewModel` 接收 `is WsServerMessage.GiftReceived` | [websocket_signals.md §6.8.1](../../protocol/websocket_signals.md#681-giftreceivedsroom) | `app/server/src/modules/gift/send_gift/handler.rs::handle_send_gift` |
| CROSS-7: MuteUser | ⭐ Admin 通过 WS → `wsClient.send({"type":"MuteUser",...})` | [websocket_signals.md §6.5.10](../../protocol/websocket_signals.md#6510-muteusercs) | `app/server/src/modules/governance/mute.rs::handle_mute` |
| CROSS-7: UserMuted（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.UserMuted` | [websocket_signals.md §6.7.6](../../protocol/websocket_signals.md#676-usermutedsroom) | `app/server/src/modules/governance/mute.rs::handle_mute` |
| CROSS-8: KickUser | ⭐ Admin 通过 WS → `wsClient.send({"type":"KickUser",...})` | [websocket_signals.md §6.5.9](../../protocol/websocket_signals.md#659-kickusercs) | `app/server/src/modules/governance/kick.rs::handle_kick` |
| CROSS-8: UserLeft（广播）| ⭐ `RoomViewModel.handleWsMessage` → `is WsServerMessage.UserLeft` | [websocket_signals.md §6.7.2](../../protocol/websocket_signals.md#672-userleftsroom) | `app/server/src/modules/governance/kick.rs::handle_kick` |

## 三、TDD 验收用例

- [ ] **CROSS-1~8**：8 个场景全部 PASS（≥3 次稳定通过）
- [ ] **CROSS-FLAKE**：Schema 校验在每场景断言 envelope 双向校验
- [ ] **CROSS-CI**：`npm run test:cross-lang:ws` 接入 GitHub Actions 夜间 cron

## 四、实现结果

## 五、Reviewer意见

### Round 1
- **🔴 Changelog 行**：

## 六、DoD 完成记录

- [ ] `doc/tests/CROSS_LANG_WS_RUNBOOK.md` 新增
- [ ] `doc/tasks/index.md` 标记 ✅ Done
