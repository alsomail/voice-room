<!--
[AI 写入规约]
1. 本文件由 Plan Agent 创建。TDD Agent 仅填写第四节【实现结果】，其余章节只读。Review Agent 仅填写第五节【Reviewer意见】。
-->

# TDS: 礼物 CRUD 管理 API (Task ID: T-10014)

## 一、背景与目标
- **关联需求**：E-07 礼物配置运营化（上下架、调价、新增）
- **本 Task 的目标**：在 Admin Server 提供礼物表 CRUD 与上传图片/Lottie 接口，所有写操作入审计日志并让 App Server 清除缓存。

## 二、方案设计

### 接口清单
| Method | Path | 权限 | 说明 |
|--------|------|------|------|
| GET | `/api/v1/admin/gifts?include_inactive=true&page=&size=` | `super_admin/operator` | 列表（含未上架/已软删选项） |
| POST | `/api/v1/admin/gifts` | `super_admin/operator` | 新增 |
| PUT | `/api/v1/admin/gifts/:id` | `super_admin/operator` | 更新（含 is_active 切换） |
| DELETE | `/api/v1/admin/gifts/:id` | `super_admin` | 软删（is_deleted=true） |
| POST | `/api/v1/admin/gifts/upload` | `super_admin/operator` | multipart 上传 icon/animation |

### POST /api/v1/admin/gifts
```json
// req
{
  "code":"unicorn_01", "name_en":"Unicorn", "name_ar":"يونيكورن",
  "icon_url":"/uploads/xxx.png", "price":66, "tier":3, "effect_level":3,
  "animation_url":null, "sort_order":35, "is_active":true
}
// resp 201
{ "code":0, "data":{ "id":"uuid", ...} }
```
**校验**：
- `code` 唯一，长度 1-32，仅英文数字下划线
- `price >= 1`，`tier ∈ [1,5]`，`effect_level ∈ [1,5]`
- `name_en` `name_ar` 1-64 字符
- `icon_url` 必须指向受控目录 `/uploads/gifts/` 或 CDN 白名单前缀

### 上传接口 POST /api/v1/admin/gifts/upload
- multipart/form-data，字段 `file`、`kind(icon|animation)`
- 白名单 MIME：`image/png`, `image/jpeg`, `image/webp`, `application/json`（Lottie JSON）
- Size ≤ 1MB（图）/ 2MB（Lottie）
- 存储：本地 `app/server/static/uploads/gifts/`（MVP）或 S3 兼容存储（后续）
- 返回 `{ url: "/uploads/gifts/2026-04-21/xxxx.png" }`

### 涉及文件清单
| 文件 | 类型 | 说明 |
|------|------|------|
| `app/adminServer/src/modules/gift/mod.rs` | 新增 | 模块入口 |
| `app/adminServer/src/modules/gift/handler.rs` | 新增 | CRUD + upload |
| `app/adminServer/src/modules/gift/service.rs` | 新增 | 业务逻辑 |
| `app/adminServer/src/modules/gift/repo.rs` | 新增 | SQLx |
| `app/adminServer/src/common/storage/local.rs` | 新增 | 本地文件保存（后续可替换为 S3 适配器，遵循防腐层） |
| `app/adminServer/src/bootstrap/router.rs` | 修改 | 挂载路由 |
| `app/adminServer/tests/gift_crud_test.rs` | 新增 | GC01~GC12 |

### 缓存失效
CRUD 写操作后 Admin Server 通过 Redis `PUBLISH admin:events {type:"gifts_invalidate"}` 通知 App Server 清除 `gifts:list:*`（App Server T-00019 订阅）。或 Admin Server 直连 Redis `DEL gifts:list:ar gifts:list:en`（更简单，MVP 推荐）。

### admin_logs
- `action`: `gift_create` / `gift_update` / `gift_delete` / `gift_upload`
- `target_id`: gift_id（upload 无 target）
- `detail`: 变更前后差异 diff

## 三、TDD 验收用例
- [ ] **GC01** POST 创建成功，201 + 返回 id
- [ ] **GC02** duplicate `code` 返回 40900
- [ ] **GC03** GET 默认仅返回 `is_active=true, is_deleted=false`；`include_inactive=true` 返回全部（除已软删）
- [ ] **GC04** PUT `is_active=false` 切换后 C 端 `/gifts/list` 不再返回（缓存已失效）
- [ ] **GC05** DELETE 软删后 `is_deleted=true`，不可再查到；再次 DELETE 返回 404
- [ ] **GC06** DELETE 非 super_admin → 403
- [ ] **GC07** upload 非白名单 MIME（如 gif）→ 400
- [ ] **GC08** upload 文件 >1MB → 400
- [ ] **GC09** price=0 → 40003
- [ ] **GC10** tier=6 → 40003
- [ ] **GC11** 每次写操作 admin_logs +1 记录
- [ ] **GC12** 创建成功后 Redis `gifts:list:ar` key 不存在（已清除）

## 四、实现结果

### 实际修改的文件

| 文件 | 类型 | 变更说明 |
|------|------|---------|
| `app/adminServer/src/modules/gift/mod.rs` | 新增 | 模块入口，导出 dto/handler/repo/service |
| `app/adminServer/src/modules/gift/dto.rs` | 新增 | 请求/响应 DTO：ListGiftsQuery、CreateGiftRequest、UpdateGiftRequest（Default）、GiftResponse、ListGiftsResponse、UploadGiftFileResponse |
| `app/adminServer/src/modules/gift/repo.rs` | 新增 | GiftRepository trait（6 方法）+ PgGiftRepository（SQL 实现）+ FakeGiftRepository（内存 Fake，含 seed/get_all）+ 6 条 Repo 单元测试 |
| `app/adminServer/src/modules/gift/service.rs` | 新增 | GiftService（list/create/update/delete + 缓存失效事件发布）+ validate_create_request/validate_update_request/validate_file_upload 校验函数 + 12 条 Service 单元测试 |
| `app/adminServer/src/modules/gift/handler.rs` | 新增 | 5 个 Handler：list_gifts/create_gift/update_gift/delete_gift/upload_gift_file（multipart）+ 10 条 Handler 单元测试 |
| `app/adminServer/src/modules/mod.rs` | 修改 | 添加 `pub mod gift` |
| `app/adminServer/src/common/auth/context.rs` | 修改 | 新增 Permission::GiftWrite（super_admin + operator）、Permission::GiftDelete（仅 super_admin） |
| `app/adminServer/src/common/error.rs` | 修改 | 新增 AppError::DuplicateCode(String) → HTTP 409 / ErrorCode::Conflict(40900) |
| `app/adminServer/src/bootstrap/mod.rs` | 修改 | AppState 新增 gift_service/gift_upload_dir 字段；AppState::new 新增 gift_repo 参数；build_app 注册 5 条礼物路由；for_test 使用 FakeGiftRepository；更新全部 14 处 AppState::new 调用；新增 GC01~GC12 + 3 条 extra 集成测试 |
| `app/adminServer/src/main.rs` | 修改 | 注入 PgGiftRepository |
| `app/adminServer/Cargo.toml` | 修改 | 添加 axum multipart feature |
| `doc/Tasks.md` | 修改 | T-10014 负责人 TDD → Review |

### 测试覆盖摘要

- **新增集成测试**：GC01~GC12（12 条）+ extra 01~03（3 条）= **15 条** HTTP 层验收用例
- **新增单元测试**：DTO 3 条 + Repo 6 条 + Service 12 条 + Handler 10 条 = **31 条**
- **全部测试**：332 passed, 0 failed（含全历史回归）
- **Clippy**：`cargo clippy -- -D warnings` 零警告

### TDD 验收点确认

| 验收标准 | 对应测试 | 状态 |
|---------|---------|------|
| 上架/下架通过 is_active 字段切换 | GC04、GS-05 | ✅ |
| 删除为软删（is_deleted=true） | GC05、GS-07、GR-05 | ✅ |
| 文件类型白名单校验（非白名单返回 400） | GC07、GS-10、HV-06 | ✅ |
| 价格必须 >= 1 | GC09、GS-03、HV-02、HV-03 | ✅ |
| 所有写操作落 admin_logs | GC11 | ✅ |
| 重复 code 返回 40900 | GC02、GS-02 | ✅ |
| tier ∈ [1,5] 校验 | GC10、GS-04、HV-04 | ✅ |
| 上传文件 >1MB 返回 400 | GC08、GS-11、HV-07 | ✅ |
| Redis 缓存失效事件发布 | GC12、GS-01 | ✅ |
| DELETE 仅 super_admin | GC06 | ✅ |

### 完成时间

2025-07-17（TDD Agent 实现，`cargo test` 全绿，`cargo clippy -- -D warnings` 零警告）

## 五、Reviewer意见
### Round 1
- **状态**：
- **审查意见**：
- **改进建议**：
