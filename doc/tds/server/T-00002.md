<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建，TDD Agent 只读不写。
2. 创建后必须在 doc/Tasks.md 对应 Task 的行内补充链接：[TDS](./tds/server/T-00002.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: 短信验证码发送接口 (Task ID: T-00002)

## 一、背景与目标
- **关联需求**：product.md - 手机号一步登录
- **前置依赖**：T-00001 (数据库表设计)
- **本 Task 的目标**：实现 POST `/api/v1/auth/send-code` 接口，通过 SmsProvider 防腐层发送验证码，Redis 存储

## 二、方案设计
### 核心数据流
Client → POST /api/v1/auth/send-code → Controller → AuthService → 检查 Redis cooldown/daily → 生成6位验证码 → SmsProvider.send() → 写入 Redis (code + cooldown + daily incr) → 返回 {expires_in, cooldown}

### 涉及文件清单
| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `app/server/src/modules/auth/controller.rs` | 新增 | send_code handler |
| `app/server/src/modules/auth/service.rs` | 新增 | 验证码生成 + Redis 读写逻辑 |
| `app/server/src/infrastructure/third_party/sms/mod.rs` | 新增 | SmsProvider trait |
| `app/server/src/infrastructure/third_party/sms/twilio.rs` | 新增 | Twilio 实现 |
| `app/server/src/infrastructure/third_party/sms/mock.rs` | 新增 | Mock 实现（固定 000000） |

### 接口 / 信令定义
参见 `doc/protocol.md` §二 2.1 POST /api/v1/auth/send-code

### Redis Key 设计
参见 protocol.md §六 6.2：
- `sms:code:{phone}` Hash (TTL 300s)
- `sms:cooldown:{phone}` String (TTL 60s)
- `sms:daily:{phone}:{date}` INCR (TTL 86400s)

## 三、TDD 验收用例
- [ ] 正向：发送成功返回 {expires_in: 300, cooldown: 60}
- [ ] 正向：验证码为 6 位数字，存入 Redis
- [ ] 异常：同一手机号 60 秒内重复发送返回 429 (42901)
- [ ] 异常：当日超过 10 次返回 429 (42902)
- [ ] 异常：手机号格式无效返回 400 (40001)
- [ ] 异常：SmsProvider 失败时返回 500，不写入 Redis

## 四、实现结果（由 TDD Agent 完成后填写）
- **实际修改的文件**：
  - `app/server/src/modules/auth/dto.rs`：新增 `SendCodeResponse { expires_in, cooldown }`
  - `app/server/src/modules/auth/service.rs`：`send_code` 返回 `SendCodeResponse`
  - `app/server/src/modules/auth/controller.rs`：handler 返回 `ApiResponse<SendCodeResponse>`
  - `app/server/src/modules/auth/routes.rs`：路径改为 `/api/v1/auth/verification-codes`
  - `app/server/src/infrastructure/redis_store/mod.rs`：`RedisCodeStore` 改为缓存 `MultiplexedConnection`（异步构造函数 `async fn new`）
  - `app/server/src/main.rs`：更新 `RedisCodeStore::new(...).await?`
- **测试覆盖**：`send_code_success_stores_and_sends` 断言 `expires_in=300` & `cooldown=60`，31 个测试全绿
- **完成时间**：2 次（初版 + 返修）

## 五、Reviewer意见
> 状态枚举：🟢 通过 | 🔴 未通过
### Review理由：🔴 未通过
1. 🔴 路由路径错误：实现 `/auth/send-code`，protocol 要求 `/api/v1/auth/verification-codes`
2. 🔴 响应结构缺失：protocol 要求返回 `{expires_in: 300, cooldown: 60}`，实现返回 `null`
3. 🟠 RedisCodeStore 每次操作新建连接，应缓存 MultiplexedConnection
> 状态枚举：🟢 通过 | 🔴 未通过
### 返修记录（TDD Agent）
- ✅ 路由路径修正为 `/api/v1/auth/verification-codes`
- ✅ 新增 `SendCodeResponse { expires_in: 300, cooldown: 60 }`，controller 返回类型改为 `ApiResponse<SendCodeResponse>`
- ✅ `RedisCodeStore` 改为缓存 `MultiplexedConnection`，每次操作 `.clone()` 复用（MultiplexedConnection 实现了 Clone，内部共享 TCP 连接）

---

### 第二轮 Review（Review Agent，commit `df327b9`）
> 状态枚举：🟢 通过 | 🔴 未通过

#### Review 结论：🔴 未通过（3 HIGH）

**H-01（跨 T-00002/03/05）错误响应 `request_id` 永远为空字符串**  
- 位置：`src/common/error.rs:116`  
- `AppError::into_response()` 硬编码 `request_id: String::new()`；注释说"middleware 会注入"，但 `request_context_middleware` 只写入 response header，不修改 body JSON。  
- `err_response()` 函数存在（L123）但从未被调用——死代码。  
- 修复：controller 中 error 路径改用 `err_response(e, rc.request_id())`，或引入 `AppErrorWithContext(AppError, String)` wrapper。

**H-02 SMS 发送失败后 Redis 状态未回滚**  
- 位置：`src/modules/auth/service.rs:53-54`  
- `save_code` 已写入 Redis（code + cooldown + daily），SMS 失败时 Redis 状态不回滚。  
- 用户进入冷却期但从未收到验证码，60s 内无法重发。  
- 修复：先发 SMS（成功后再 `save_code`）；或 SMS 失败后调用 `code_store.undo_save_code(phone, today)`。

**H-03 `save_code` 冷却/日限检查非原子 TOCTOU**  
- 位置：`src/infrastructure/redis_store/mod.rs:58-71`  
- 三步非原子：EXISTS cooldown → GET daily → pipeline(write)。并发请求可双重通过检查，同一手机号接收两条验证码。  
- 修复：用 Lua 脚本将检查+写入原子化（EVALSHA）。

**M-01 `err_response` 死代码**（配合 H-01 一起修复或删除）  
**M-02 `is_in_cooldown`/`daily_count` trait 方法 service 层不调用，暴露冗余接口**  
**M-03 `DatabaseError`/`SmsSendFailed` 原始错误字符串输出客户端，泄露内部信息**
---

### 第二轮返修记录（TDD Agent，第三轮 TDD）
> 对应 commit：待提交

#### 修复内容汇总

**H-01（controller.rs）**  
- 三个 handler 返回类型从 `Result<Json<ApiResponse<T>>, AppError>` 改为 `axum::response::Response`  
- 错误路径统一调用 `err_response(e, rc.request_id())`，确保 body 中 `request_id` 正确注入  
- `AppError::IntoResponse` 改为调用 `err_response(self, "")` 作为兜底（非 handler 路径）

**M-01（error.rs）**  
- `err_response()` 现在被 controller 调用，不再是死代码  
- 使用 `ErrorBody { code, message, request_id }` 统一错误响应结构

**M-03（error.rs）**  
- 新增 `safe_message()` 方法：5xx 错误对外返回通用文本 "internal server error"，通过 `tracing::error!` 记录原始细节；4xx 返回原始消息  
- `err_response()` 调用 `safe_message()` 确保敏感信息不泄露客户端

**H-02（service.rs）**  
- `send_code` 流程改为：快速预检（`is_in_cooldown` + `daily_count`） → 先发 SMS → 再 `save_code`  
- SMS 失败时 Redis 完全无写入，用户不会进入无效冷却状态  
- M-02 随之解决：`is_in_cooldown`/`daily_count` 在 service 预检阶段被实际调用

**H-03（redis_store/mod.rs）**  
- `RedisCodeStore::save_code` 改为 Lua 脚本（`SAVE_CODE_LUA`）原子化三步：EXISTS cooldown → GET daily → pipeline 写入  
- Lua 错误通过 `e.to_string().contains("COOLDOWN")` / `"DAILY_LIMIT"` 模式匹配转换为对应 `AppError`

#### 新增测试
- `bootstrap::tests::error_response_body_contains_request_id`（H-01 集成验证）
- `bootstrap::tests::success_response_body_contains_request_id`（H-01 回归）
- `service::tests::send_code_sms_failure_does_not_set_cooldown`（H-02 单测）

#### 最终状态
- 34 个测试全部通过，`cargo clippy -- -D warnings` 零警告

---

### 第三轮 Review（Review Agent，commit `86a8fd9`）
> 状态枚举：🟢 通过 | 🔴 未通过

#### Review 结论：🔴 未通过（2 HIGH）

**H-01 `verify_and_consume` 非原子 TOCTOU（安全）**
- 位置：`src/infrastructure/redis_store/mod.rs:118-147`
- HGETALL → 判断 → HSET/DEL 三步非原子。并发两个正确验证码请求均能通过，同一 OTP 可被消费两次。
- 修复：用 Lua 脚本原子化 verify_and_consume（同 save_code H-03 方案）

**H-02 `daily_count` 静默返回 0 绕过限流（安全/可靠性）**
- 位置：`src/infrastructure/redis_store/mod.rs:159`
- `.unwrap_or(0)` 在 Redis 不可用时静默返回 0，导致限流失效，短信可被无限发送。
- 修复：改为传播 `AppError::RedisError`，`None` 时才返回 0。

**M-01 Lua 错误字符串匹配脆弱**
- 位置：`src/infrastructure/redis_store/mod.rs:107-110`
- `contains("COOLDOWN")` 可能误匹配 Redis 代理返回的包含该词的错误。
- 修复：Lua 使用项目前缀 `VR:COOLDOWN` / `VR:DAILY_LIMIT`，Rust 侧精确匹配。

**M-02 `now_secs()` 使用 `unwrap()` 潜在 panic 路径**
- 位置：`src/modules/auth/service.rs:171-175`
- `duration_since(UNIX_EPOCH).unwrap()` 在系统时钟异常时 panic。
- 修复：改为 `.unwrap_or_default()`。

---

### 第三轮返修记录（TDD Agent，第四轮 TDD）
> 对应 commit：待提交

#### 修复内容汇总

**H-01（redis_store/mod.rs）— verify_and_consume Lua 原子化**
- 新增 `VERIFY_CODE_LUA` 脚本：`HGETALL → 判断 → HSET/DEL` 原子化
- 错误类型：`VR:EXPIRED` / `VR:MAX_ATTEMPTS` / `VR:INVALID`，前缀防止与 Redis 内部错误混淆
- 消除 HGETALL→判断→DEL 三步非原子导致的 OTP 双重消费风险

**H-02（redis_store/mod.rs）— daily_count 错误传播**
- `conn.get(&key).await.unwrap_or(0)` → `conn.get(&key).await.map_err(...)? ; Ok(val.unwrap_or(0))`
- Redis 不可用时返回 `Err(AppError::RedisError(...))` 而非静默返回 0，防止限流被绕过

**M-01（redis_store/mod.rs）— Lua 错误字符串加 VR: 前缀**
- `COOLDOWN` → `VR:COOLDOWN`，`DAILY_LIMIT` → `VR:DAILY_LIMIT`
- Rust 侧 `contains("COOLDOWN")` → `contains("VR:COOLDOWN")`（精确匹配）

**M-02（service.rs）— now_secs() unwrap_or_default**
- `duration_since(UNIX_EPOCH).unwrap()` → `.unwrap_or_default()`，消除系统时钟异常时的 panic 路径

#### 新增测试（行为契约 / 防回归）
- `redis_store::tests::verify_and_consume_reuse_code_returns_expired`（H-01 契约）
- `redis_store::tests::verify_and_consume_wrong_then_right_works`（H-01 复杂路径）
- `redis_store::tests::daily_count_returns_correct_value`（H-02 契约）
- `service::tests::login_reuse_code_returns_expired`（H-01 服务层回归）

#### 最终状态
- 38 个测试全部通过，`cargo clippy -- -D warnings` 零警告

---

### 第四轮 Review（对应第四轮 TDD 返修，commit d16d674）

#### ✅ 已修复验证（上轮所有问题通过）
- H-01 VERIFY_CODE_LUA 原子化：正确消除双重消费 TOCTOU
- H-02 daily_count 错误传播：`Option<u64>` + `map_err(RedisError)` 正确
- M-01 Lua 错误前缀 VR: ：所有自定义错误均已加前缀
- M-02 now_secs unwrap_or_default：不再 panic

#### 🟡 发现问题（须修复）

**M-01 [MEDIUM] is_in_cooldown 错误映射不一致**
- 文件：`redis_store/mod.rs` L184
- 问题：`AppError::Internal` 应改为 `AppError::RedisError`，与 `daily_count` 保持一致
- 修复：`.map_err(|e| AppError::RedisError(e.to_string()))`

**M-02 [MEDIUM] send_code 并发 SMS 双发 TOCTOU**
- 文件：`service.rs` L54-66
- 问题：两个并发请求均通过预检 → 均发 SMS → Lua 拒绝第二个 save_code，但用户已收到第二条 SMS 的 code 而 Redis 存的是第一条 code → 登录失败
- 决策选项：A) 删除服务层预检，仅依赖 Lua 返回值；B) 接受此风险并文档化
- 当前倾向：选项 A（先 save_code 占位，成功后再发 SMS）

**L-01 [LOW] 服务层魔法数字 >= 10**
- 文件：`service.rs` L57
- 建议：提升为常量 `MAX_DAILY_LIMIT` 或与 redis_store 层常量对齐

**L-02 [LOW] 测试注释误导**
- 文件：`redis_store/mod.rs` L333，注释"save_code 后增加到 1"与实际不符

#### 裁定
Review **未通过**，T-00002/03/05 负责人回流 TDD 处理 M-01 + M-02。
