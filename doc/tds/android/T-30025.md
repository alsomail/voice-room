<!--
[AI 写入规约]
1. 本文件由 Plan Agent 创建。TDD Agent 仅填写第四节【实现结果】，其余章节只读。Review Agent 仅填写第五节【Reviewer意见】。
2. 创建后必须在 doc/Tasks.md 对应 Task 的行内补充链接：[TDS](./tds/android/T-30025.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: 房间页视觉升级 (Task ID: T-30025)

## 一、背景与目标

- **关联需求**：product.md Phase 0.5 — 交互壳体与基础体验 / 房间页黑金风格
- **前置依赖**：T-30018（MenaTheme 主题系统 ✅）
- **已有基础**：T-30009（RoomScreen UI）、T-30011（MicSlotCard 三态）、T-30013（上麦/下麦 WS 逻辑）、T-30014（ChatMessageList）
- **本 Task 的目标**：将 RoomScreen 从 Material3 默认主题改造为中东黑金风格——深色背景 + 主麦突出（80dp 金色光圈）+ 副麦 4 列（60dp AvatarWithFrame）+ 空麦位虚线"+"+ 弹幕金色昵称 + 系统消息金黄色居中。**WS/上下麦逻辑完全不变**。

## 二、方案设计

### 核心设计原则

1. **纯 UI 改造**：仅修改 Composable 渲染层，不触碰 `RoomViewModel`、`IWebSocketClient`、`IMediaService`、`MicSlotUi` 数据结构及任何 WS 信令逻辑
2. **复用 MenaTheme**：所有颜色取 `MenaColors.*`，头像使用现有 `AvatarWithFrame` 组件
3. **主麦/副麦分离**：`slots[0]` 为主麦（HostMicSlot），`slots[1..8]` 为副麦（4 列 LazyVerticalGrid）
4. **testTag 兼容**：保留所有现有 testTag（`mic_slot_empty_${index}`、`mic_slot_occupied_${index}`、`mic_slot_muted_icon_${index}`、`chat_message_list`、`user_message_{index}`、`system_message_{index}`），新增标签不冲突

### 核心数据流

```
RoomViewModel.uiState (不变)
       │  uiState.micSlots[0]     → HostMicSlot  (80dp 金色光圈)
       │  uiState.micSlots[1..8]  → GuestMicSlotsGrid (60dp 4列)
       │  uiState.messages        → ChatMessageList (金色昵称 / 金黄系统消息)
       ▼
RoomScreen (纯 UI 改造，Scaffold 结构不变)
```

### 布局结构

```
RoomScreen (Scaffold, modifier.imePadding)
├── topBar: RoomTopBar (不变)
├── bottomBar: ChatInputBar (不变)
└── content: Column
    ├── Box(深色背景 MenaColors.Background)
    │   ├── HostMicSlot(slots[0])          // 主麦：居中, 80dp + 金色光圈
    │   └── GuestMicSlotsGrid(slots[1..8]) // 副麦：4列, 每格 60dp
    └── ChatMessageList(messages)           // 弹幕列表 (weight=1f)
```

### 涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `app/android/app/src/main/java/com/voice/room/android/feature/room/HostMicSlot.kt` | **新增** | 主麦 Composable：80dp AvatarWithFrame + Canvas 金色光圈动画 |
| `app/android/app/src/main/java/com/voice/room/android/feature/room/MicSlotCard.kt` | **修改** | 副麦卡片：60dp AvatarWithFrame 替换旧头像；空麦位改为虚线圆形边框 + 金色"+" |
| `app/android/app/src/main/java/com/voice/room/android/feature/room/MicSlotsGrid.kt` | **修改** | 拆分为 HostMicSlot(slot[0]) + 副麦 4 列 LazyVerticalGrid(slots[1..8])；高度自适应 |
| `app/android/app/src/main/java/com/voice/room/android/feature/room/ChatMessageList.kt` | **修改** | UserMessageItem 昵称色改为 `MenaColors.Primary`；SystemNoticeItem 改为 `MenaColors.SystemMessage` |
| `app/android/app/src/main/java/com/voice/room/android/feature/room/RoomScreen.kt` | **修改** | MicSlotsGrid 容器背景设为 `MenaColors.Background`；移除 `height(240.dp)` 硬编码，改用 `wrapContentHeight` |

### 组件设计细节

#### 1. HostMicSlot（主麦 — 新文件）

```kotlin
// HostMicSlot.kt
@Composable
fun HostMicSlot(
    slot: MicSlotUi,
    modifier: Modifier = Modifier,
    onClick: (index: Int) -> Unit = {},
) {
    // 布局：居中 Column，padding vertical 12dp
    // 头像区：Box 叠加层
    //   层 1: Canvas → drawCircle 金色光圈（主麦专属发光效果）
    //         - color = MenaColors.Primary.copy(alpha = 0.35f)，radius = size/2 + 12dp (px)
    //         - 若 slot.isOccupied，追加 infiniteRepeatable 脉冲动画（scale 0.9→1.1，1200ms）
    //   层 2: AvatarWithFrame(imageUrl=slot.avatarUrl, size=80.dp, showFrame=slot.isOccupied)
    //         - 空麦时 showFrame=false，显示虚线圆圈 + "+" （见 EmptyHostSlot 子组件）
    // 昵称：slot.nickname（非空时显示），MenaColors.Primary，bodyMedium，居中
    // testTag: 复用 "mic_slot_empty_0" / "mic_slot_occupied_0"（保持兼容）
}
```

**实现要点**：
- Canvas 绘制金色光圈：`drawCircle(color = goldGlow, radius = avatarRadius + glowPx, style = Stroke(width = 6.dp.toPx()))`
- 动画：`rememberInfiniteTransition` + `animateFloat(0.92f → 1.08f, 1200ms, RepeatMode.Reverse)`，用 `graphicsLayer(scaleX, scaleY)` 应用到 Canvas 层
- 空麦时不显示光圈，改为虚线边框（见下）

#### 2. EmptySlotCircle（空麦位通用子组件，MicSlotCard.kt 内私有）

```kotlin
// 虚线圆形边框 + 金色 "+" 图标
// 使用 Canvas drawCircle + PathEffect.dashPathEffect(floatArrayOf(8f, 6f))
// border color: MenaColors.Primary.copy(alpha=0.6f)
// "+" icon: Icons.Default.Add, size=24.dp, tint=MenaColors.Primary
// 整体尺寸与 size 参数对齐（主麦 80dp，副麦 60dp）
```

#### 3. MicSlotCard（副麦卡片 — 修改）

| 状态 | 改造内容 |
|------|----------|
| EMPTY | 虚线圆圈（60dp）+ 金色"+"，废弃旧麦克风 Icon + "空位" Text |
| OCCUPIED | `AvatarWithFrame(size=60.dp, showFrame=true)` + 音浪动画保留 |
| MUTED | `AvatarWithFrame(size=60.dp, showFrame=false)` + MicOff 图标保留 |

昵称文字：`MenaColors.OnBackground`，`labelSmall`，1行截断

#### 4. MicSlotsGrid（布局重组 — 修改）

```kotlin
@Composable
fun MicSlotsGrid(
    slots: List<MicSlotUi>,        // 仍接受 9 个麦位（兼容现有调用方）
    modifier: Modifier = Modifier,
    onMicSlotClick: (index: Int) -> Unit = {},
) {
    val hostSlot = slots.getOrNull(0)
    val guestSlots = slots.drop(1)   // slots[1..8]，最多 8 个

    Column(modifier = modifier.background(MenaColors.Background)) {
        // 主麦行：Box 水平居中
        if (hostSlot != null) {
            Box(Modifier.fillMaxWidth(), contentAlignment = Alignment.Center) {
                HostMicSlot(slot = hostSlot, onClick = onMicSlotClick)
            }
        }
        // 副麦区：4 列 LazyVerticalGrid，userScrollEnabled=false
        LazyVerticalGrid(
            columns = GridCells.Fixed(4),
            modifier = Modifier
                .fillMaxWidth()
                .testTag("mic_slots_grid"),   // 保留原 testTag 供测试定位
            userScrollEnabled = false,
        ) {
            items(items = guestSlots, key = { it.index }) { slot ->
                MicSlotCard(slot = slot, onClick = onMicSlotClick)
            }
        }
    }
}
```

**高度策略**：`RoomScreen` 中移除 `height(240.dp)` 硬编码，改为 `wrapContentHeight()`（主麦约 112dp + 副麦 2 行约 80dp×2 = ~272dp 自适应）

#### 5. ChatMessageList（弹幕 — 修改）

只改两处颜色：

```kotlin
// UserMessageItem 昵称（原 MaterialTheme.colorScheme.primary → MenaColors.Primary）
Text(
    text = message.senderNickname,
    style = MaterialTheme.typography.labelSmall,
    color = MenaColors.Primary,   // #D4AF37 金色
)

// SystemNoticeItem（原 onSurface.copy(0.6f) → MenaColors.SystemMessage）
Text(
    text = message.content,
    style = MaterialTheme.typography.labelSmall,
    color = MenaColors.SystemMessage,  // #F39C12 金黄色
    textAlign = TextAlign.Center,
)
```

#### 6. RoomScreen（容器改造 — 修改）

```kotlin
// 修改 MicSlotsGrid 所在 Column：添加深色背景
Column(
    modifier = Modifier
        .fillMaxSize()
        .background(MenaColors.Background)  // 新增整体深色背景
        .padding(padding),
) {
    MicPermissionHandler(onPermissionGranted = onMicPermissionGranted) { onMicSlotClick ->
        MicSlotsGrid(
            slots = uiState.micSlots,
            onMicSlotClick = onMicSlotClick,
            modifier = Modifier
                .fillMaxWidth()
                .wrapContentHeight(),   // 移除 height(240.dp) 硬编码
        )
    }
    ChatMessageList(
        messages = uiState.messages,
        modifier = Modifier.weight(1f),
    )
}
```

### 接口 / 信令定义

本 Task 为纯 UI 视觉改造，**不新增任何 WS 信令**，不修改 `protocol.md`。

现有信令保持不变：
- `TakeMic` / `MicTaken` / `LeaveMic` / `MicLeft`（上下麦）
- `SendMessage` / `MessageReceived`（聊天）
- `JoinRoom` / `LeaveRoom` / `UserJoined` / `UserLeft`（房间生命周期）

## 三、TDD 验收用例

### 主麦槽位（HostMicSlot）

- [ ] **VS-01 正向**：`slots[0]` 有人时，`mic_slot_occupied_0` 可见，宽/高均 ≥ 80dp，AvatarWithFrame 的 `avatar_frame` testTag 存在（金色边框）
- [ ] **VS-02 正向**：`slots[0]` 空位时，`mic_slot_empty_0` 可见，Canvas 光圈不渲染，显示虚线圆圈 + "+" 图标
- [ ] **VS-03 正向**：点击主麦空位，触发 `onMicSlotClick(0)` 回调

### 副麦网格（GuestMicSlotsGrid — 4 列）

- [ ] **VS-04 正向**：`slots[1..8]` 渲染时，`mic_slots_grid` 的列数为 4（通过 `GridCells.Fixed(4)` 配置验证；UI 测试可断言每行最多 4 个子项）
- [ ] **VS-05 正向**：副麦 OCCUPIED 时，`mic_slot_occupied_{index}` 可见，AvatarWithFrame 的 `avatar_frame` 存在
- [ ] **VS-06 正向**：副麦 EMPTY 时，`mic_slot_empty_{index}` 可见，不显示麦克风 Icon 文字"空位"（旧样式已废弃）
- [ ] **VS-07 正向**：副麦 MUTED 时，`mic_slot_muted_icon_{index}` 可见

### 空麦位虚线"+"

- [ ] **VS-08 正向**：空麦位使用虚线圆圈边框（无法直接通过 testTag 断言，通过截图/视觉验证；单测可校验 `onClick` 回调触发）
- [ ] **VS-09 正向**：点击空副麦位，触发 `onMicSlotClick(index)` 回调，index 与 slot.index 一致

### 弹幕消息样式

- [ ] **VS-10 正向**：`USER_TEXT` 消息的昵称 Text color 为 `MenaColors.Primary`（`#D4AF37`）；通过 `user_message_{index}` testTag 定位后断言文本可见
- [ ] **VS-11 正向**：`SYSTEM_NOTICE` 消息的 Text color 为 `MenaColors.SystemMessage`（`#F39C12`）；通过 `system_message_{index}` testTag 定位后断言文本可见
- [ ] **VS-12 正向**：系统消息居中对齐（`TextAlign.Center`）

### WS/上下麦不回归

- [ ] **VS-13 不回归**：`RoomViewModelTest` 现有 22 个单元测试（TM-01～TM-08 + SM-01～SM-06 + RM-03～RM-05 等）**全部通过**，零改动
- [ ] **VS-14 不回归**：`MicSlotCardTest.kt`（MC-01～MC-12）**全部通过**，无新增失败
- [ ] **VS-15 不回归**：`ChatMessageListTest.kt`（CL-01～CL-08）**全部通过**，无新增失败
- [ ] **VS-16 不回归**：上麦流程回归——模拟 `MicTaken` WS 消息后，`slots[0]` 渲染 HostMicSlot 有人状态（`mic_slot_occupied_0` 可见）
- [ ] **VS-17 不回归**：下麦流程回归——模拟 `MicLeft` WS 消息后，对应 slot 渲染空麦位（`mic_slot_empty_{index}` 可见）

### 边界场景

- [ ] **VS-18 边界**：`slots` 列表为空时，`MicSlotsGrid` 不崩溃
- [ ] **VS-19 边界**：`slots` 仅 1 个元素（只有主麦，无副麦），副麦 Grid 渲染 0 项不崩溃
- [ ] **VS-20 边界**：RTL 布局（`layoutDirection = LayoutDirection.Rtl`）下，MicSlotsGrid 不崩溃、主麦居中显示

## 四、实现结果（由 TDD Agent 完成后填写）

- **实际修改的文件**：
- **测试覆盖率**：
- **完成时间**：

## 五、Reviewer意见

> 状态枚举：🟢 通过 | 🔴 未通过
> 每轮 Review 追加一条记录，不要覆盖历史。

### Round 1
- **状态**：（🟢 通过 / 🔴 未通过）
- **审查意见**：
- **改进建议**：（仅未通过时填写）
