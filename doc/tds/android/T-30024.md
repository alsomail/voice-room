<!-- 
[AI 写入规约]
1. 本文件由 Plan Agent 创建。TDD Agent 仅填写第四节【实现结果】，其余章节只读。Review Agent 仅填写第五节【Reviewer意见】。
2. 创建后必须在 doc/tasks/index.md 对应 Task 的行内补充链接：[TDS](./tds/android/T-30024.md)
3. 实现完成后，在本文件底部的【实现结果】章节补充实际的代码路径。
-->

# TDS: 个人中心页 (Task ID: T-30024)

## 一、背景与目标

- **关联需求**：来自 `doc/product.md` — 用户个人中心（Profile）功能模块
- **前置依赖**：
  - T-30018（MenaTheme ✅）：AvatarWithFrame / MenaColors / GoldButton 等可直接使用
  - T-30020（MainScreen 三 Tab ✅）：`ProfilePlaceholder` 需替换为真实 `ProfileScreen`
  - T-30004（IUserRepository ✅）：`IUserRepository.getMe()` 接口 + `UserProfile` 领域模型已就绪
- **本 Task 的目标**：实现"我的" Tab 完整页面——展示头像/昵称/ID/余额，支持复制 ID、退出登录（二次确认）、网络异常时降级展示本地缓存数据。

---

## 二、方案设计

### 核心数据流

```
ProfileScreen (Stateful)
  │  viewModel = ProfileViewModel(userRepository, tokenManager)
  │
  ▼ collectAsStateWithLifecycle
ProfileUiState (StateFlow)
  │
  ├─ Loading      → 显示骨架屏 / CircularProgressIndicator
  ├─ Success(profile, fromCache) → ProfileContent (Stateless)
  └─ Error(message, cachedProfile?) → 若有缓存则降级显示 ProfileContent + Toast 提示

ProfileViewModel.init {
  loadProfile()  // viewModelScope.launch
}

loadProfile():
  userRepository.getMe()
    .onSuccess → emit Success(profile, fromCache=false)
              → 同时写入 in-memory cache
    .onFailure (IOException) → 读取 in-memory cache
                              → 若有缓存 emit Success(cachedProfile, fromCache=true) + ShowToast
                              → 若无缓存   emit Error("网络异常，请重试")
    .onFailure (其他) → emit Error(e.message)

退出登录流程:
  ProfileScreen → 点击"退出登录" → showLogoutDialog=true
               → AlertDialog 确认 → ProfileViewModel.logout()
               → tokenManager.clearToken()
               → emit ProfileEvent.NavigateToLogin
               → MainScreen 收到事件 → AppNavGraph 导航到 "login"，popUpTo("main"){ inclusive=true }

复制 ID:
  ProfileContent → 点击 ID 区域 → ClipboardManager.setText(userId)
                → ProfileViewModel.emit(ProfileEvent.ShowToast("ID 已复制"))
```

### 涉及文件清单

| 文件路径（相对 `app/android/app/src/main/java/com/voice/room/android/`） | 变更类型 | 说明 |
|-------------------------------------------------------------------------|----------|------|
| `feature/profile/ProfileScreen.kt`                                       | **新增** | Stateful 容器 Composable；持有 ViewModel，监听 events，传递回调给 ProfileContent |
| `feature/profile/ProfileContent.kt`                                      | **新增** | Stateless UI Composable；接收 `ProfileUiState.Success` 展示头像/昵称/ID/余额/设置入口/退出登录按钮 |
| `feature/profile/ProfileViewModel.kt`                                    | **新增** | ViewModel：`StateFlow<ProfileUiState>` + `SharedFlow<ProfileEvent>`；loadProfile / logout / copyId |
| `feature/profile/ProfileUiState.kt`                                      | **新增** | sealed interface：Loading / Success(profile, fromCache) / Error(message, cachedProfile?) |
| `feature/main/MainScreen.kt`                                             | **修改** | 将 `ProfilePlaceholder()` 替换为 `ProfileScreen(appContainer, onLogout)` |
| `presentation/AppNavGraph.kt`                                            | **修改** | `MainScreen` 新增 `onLogout` 参数，退出登录后执行 `navigate("login") { popUpTo("main"){ inclusive=true } }` |
| `common/AppContainer.kt`                                                 | **修改** | 新增 `userRepository: IUserRepository` 属性，使用 `RetrofitUserRepository` 创建（复用 roomRetrofit 的 OkHttp client + AuthInterceptor） |

测试文件：

| 文件路径（相对 `app/android/app/src/`） | 类型 | 说明 |
|---------------------------------------|------|------|
| `test/.../feature/profile/ProfileViewModelTest.kt` | JVM 单元测试 | 覆盖加载成功/网络异常缓存/退出登录/复制 ID 场景 |
| `androidTest/.../feature/profile/ProfileScreenTest.kt` | androidTest | 覆盖 UI 可见性/按钮交互/退出确认弹框/testTag 协议 |

---

### 接口 / 信令定义

引用 `doc/protocol.md §2.3 GET /api/v1/users/me`：

```
GET /api/v1/users/me
Authorization: Bearer <token>

200 OK:
{
  "code": 0,
  "data": {
    "id": "550e8400-...",
    "phone": "+966512345678",
    "nickname": "User_a1b2",
    "avatar": "https://cdn.example.com/avatars/xxx.jpg",
    "coin_balance": 1000,
    "vip_level": 2,
    "created_at": "2026-04-17T00:00:00Z"
  }
}
```

领域模型已在 T-30004 定义：`domain/user/UserProfile.kt`（id / phone / nickname / avatar / coinBalance / vipLevel / createdAt）。

---

### 详细设计

#### ProfileUiState

```kotlin
sealed interface ProfileUiState {
    data object Loading : ProfileUiState
    data class Success(
        val profile: UserProfile,
        val fromCache: Boolean = false,
    ) : ProfileUiState
    data class Error(
        val message: String,
        val cachedProfile: UserProfile? = null,
    ) : ProfileUiState
}
```

#### ProfileEvent

```kotlin
sealed interface ProfileEvent {
    data object NavigateToLogin : ProfileEvent
    data class ShowToast(val message: String) : ProfileEvent
}
```

#### ProfileViewModel

```kotlin
class ProfileViewModel(
    private val userRepository: IUserRepository,
    private val tokenManager: ITokenManager,
) : ViewModel() {

    private val _uiState = MutableStateFlow<ProfileUiState>(ProfileUiState.Loading)
    val uiState: StateFlow<ProfileUiState> = _uiState.asStateFlow()

    private val _events = MutableSharedFlow<ProfileEvent>()
    val events: SharedFlow<ProfileEvent> = _events.asSharedFlow()

    // in-memory 缓存（VM 生命周期内有效）
    private var cachedProfile: UserProfile? = null

    init {
        loadProfile()
    }

    fun loadProfile() {
        viewModelScope.launch {
            _uiState.value = ProfileUiState.Loading
            userRepository.getMe()
                .onSuccess { profile ->
                    cachedProfile = profile
                    _uiState.value = ProfileUiState.Success(profile, fromCache = false)
                }
                .onFailure { e ->
                    if (e is CancellationException) throw e   // 必须 re-throw
                    val cached = cachedProfile
                    if (cached != null) {
                        _uiState.value = ProfileUiState.Success(cached, fromCache = true)
                        _events.emit(ProfileEvent.ShowToast("网络异常，显示缓存数据"))
                    } else {
                        _uiState.value = ProfileUiState.Error(e.message ?: "加载失败")
                    }
                }
        }
    }

    fun copyId(userId: String) {
        viewModelScope.launch {
            _events.emit(ProfileEvent.ShowToast("ID 已复制"))
        }
    }

    fun logout() {
        viewModelScope.launch {
            tokenManager.clearToken()
            _events.emit(ProfileEvent.NavigateToLogin)
        }
    }

    companion object {
        fun factory(
            userRepository: IUserRepository,
            tokenManager: ITokenManager,
        ) = object : ViewModelProvider.Factory {
            @Suppress("UNCHECKED_CAST")
            override fun <T : ViewModel> create(modelClass: Class<T>): T =
                ProfileViewModel(userRepository, tokenManager) as T
        }
    }
}
```

#### ProfileScreen（Stateful）

```kotlin
@Composable
fun ProfileScreen(
    appContainer: AppContainer,
    onLogout: () -> Unit,
    modifier: Modifier = Modifier,
) {
    val viewModel: ProfileViewModel = viewModel(
        factory = ProfileViewModel.factory(
            appContainer.userRepository,
            appContainer.tokenManager,
        )
    )
    val uiState by viewModel.uiState.collectAsStateWithLifecycle()
    val context = LocalContext.current
    val clipboardManager = LocalClipboardManager.current
    var showLogoutDialog by remember { mutableStateOf(false) }

    // 事件监听
    LaunchedEffect(Unit) {
        viewModel.events.collect { event ->
            when (event) {
                is ProfileEvent.NavigateToLogin -> onLogout()
                is ProfileEvent.ShowToast ->
                    Toast.makeText(context, event.message, Toast.LENGTH_SHORT).show()
            }
        }
    }

    ProfileContent(
        uiState = uiState,
        modifier = modifier,
        onCopyId = { userId ->
            clipboardManager.setText(AnnotatedString(userId))
            viewModel.copyId(userId)
        },
        onLogoutClick = { showLogoutDialog = true },
        onRetry = { viewModel.loadProfile() },
    )

    if (showLogoutDialog) {
        AlertDialog(
            onDismissRequest = { showLogoutDialog = false },
            title = { Text("退出登录") },
            text = { Text("确认退出当前账号？") },
            confirmButton = {
                TextButton(
                    onClick = {
                        showLogoutDialog = false
                        viewModel.logout()
                    }
                ) { Text("确认", color = MenaColors.Error) }
            },
            dismissButton = {
                TextButton(onClick = { showLogoutDialog = false }) { Text("取消") }
            }
        )
    }
}
```

#### ProfileContent（Stateless）

关键 UI 区域与 testTag 协议：

| 区域 | testTag | 说明 |
|------|---------|------|
| 整体容器 | `"profile_screen"` | Column，fillMaxSize，Background 色 |
| 头像 | `"profile_avatar"` | AvatarWithFrame(imageUrl, size=80.dp, showFrame=true) |
| 昵称 | `"profile_nickname"` | Text，titleLarge，OnBackground 色 |
| 用户 ID 行 | `"profile_id_row"` | Row 含 ID label + 复制图标，可点击 |
| ID 文本 | `"profile_id_text"` | "ID: {id}" |
| 余额行 | `"profile_balance"` | Text，"💰 {coinBalance} 金币"，Primary 色 |
| 缓存标记 | `"profile_cache_badge"` | 仅 fromCache=true 时显示，小字 "缓存" 标签 |
| 设置入口 | `"profile_settings_item"` | ListItem，仅展示占位，后续扩展 |
| 退出登录按钮 | `"profile_logout_button"` | GoldButton 或 TextButton，Error 色文字 |
| 错误态容器 | `"profile_error"` | Column 含错误文本 + 重试按钮 |
| 重试按钮 | `"profile_retry_button"` | |
| 退出确认弹框 | `"logout_confirm_dialog"` | AlertDialog |
| 确认按钮 | `"logout_confirm_button"` | |
| 取消按钮 | `"logout_cancel_button"` | |
| 加载态 | `"profile_loading"` | CircularProgressIndicator，居中 |

#### AppContainer 新增 userRepository

```kotlin
// 在 AppContainer.fromBuildConfig() 内，复用 roomRetrofit（已注入 AuthInterceptor）:
val userApiService = roomRetrofit.create(UserApiService::class.java)
val userRepository: IUserRepository = RetrofitUserRepository(userApiService)

// AppContainer data class 新增字段:
val userRepository: IUserRepository,
```

#### MainScreen 修改

```kotlin
composable(MainTab.PROFILE.route) {
    ProfileScreen(
        appContainer = appContainer,
        onLogout = onLogout,          // 从 MainScreen 参数接收
    )
}
```

#### AppNavGraph 修改

```kotlin
composable("main") {
    MainScreen(
        appContainer = appContainer,
        onLogout = {
            navController.navigate("login") {
                popUpTo("main") { inclusive = true }
            }
        }
    )
}
```

---

### 依赖隔离与风险说明

**T-30004 API 状态**：`IUserRepository.getMe()` 接口已定义，`RetrofitUserRepository` 实现已完成（T-30004 ✅），但 Server 端 `GET /api/v1/users/me` 接口可能尚未上线。

隔离策略：
1. `ProfileViewModel` 只依赖 `IUserRepository` 接口，不依赖具体实现
2. 若 Server 未上线，测试层注入 `FakeUserRepository`（返回固定 UserProfile）
3. `AppContainer` 使用真实 `RetrofitUserRepository`；若联调时 API 未就绪，临时替换为 `FakeUserRepository` 即可

```kotlin
// 测试辅助（ProfileViewModelTest 使用）
class FakeUserRepository(
    var result: Result<UserProfile> = Result.success(
        UserProfile("u001", "+966512345678", "TestUser", null, 1000L, 0, "2026-01-01T00:00:00Z")
    )
) : IUserRepository {
    var getCallCount = 0
    override suspend fun getMe(): Result<UserProfile> {
        getCallCount++
        return result
    }
}
```

---


### 🔌 协议路径绑定表

N/A — 本 Task 无跨端协议路径，仅 Android 端内部改造

## 三、TDD 验收用例

### 正向用例

- [ ] **PC-01** `ProfileViewModel` init 后 `uiState` 从 `Loading` 变为 `Success`，profile 字段与 repository 返回一致
- [ ] **PC-02** `Success.fromCache = false`（网络正常首次加载）
- [ ] **PC-03** `ProfileContent` 在 `Success` 状态下，`profile_nickname` / `profile_id_text` / `profile_balance` 三个 testTag 对应文本可见
- [ ] **PC-04** `AvatarWithFrame` 渲染：有 avatarUrl → AsyncImage；无 avatarUrl → `avatar_placeholder` Icon 可见
- [ ] **PC-05** 点击 ID 区域后，`ClipboardManager` 中的内容等于 `profile.id`；同时 `ProfileEvent.ShowToast("ID 已复制")` 被发出
- [ ] **PC-06** 点击退出登录 → `logout_confirm_dialog` 出现
- [ ] **PC-07** `logout_confirm_dialog` 点击"取消" → 弹框消失，`tokenManager.getToken()` 仍不为 null
- [ ] **PC-08** `logout_confirm_dialog` 点击"确认" → `tokenManager.getToken() == null`，`ProfileEvent.NavigateToLogin` 被发出
- [ ] **PC-09** 余额正确显示 `coinBalance` 数值

### 异常用例

- [ ] **PC-10** `getMe()` 返回 `IOException` 且无缓存 → `uiState` 变为 `Error`，`profile_error` 可见，`profile_retry_button` 可点击
- [ ] **PC-11** `getMe()` 返回 `IOException` 且有缓存（先成功一次再失败） → `uiState` 变为 `Success(fromCache=true)`，`profile_cache_badge` 可见，Toast 含"缓存"字样
- [ ] **PC-12** `fromCache=true` 时 `profile_cache_badge` testTag 可见；`fromCache=false` 时不可见
- [ ] **PC-13** 点击重试后，`getMe()` 被调用第 2 次，成功后 `fromCache=false`
- [ ] **PC-14** `CancellationException` 被正确 re-throw，不触发 `ShowToast` 或 `Error` 状态

### 边界用例

- [ ] **PC-15** `avatar == null` 时 `profile_avatar` 容器渲染正常，无崩溃
- [ ] **PC-16** `coinBalance = 0` 时余额展示为 "0 金币"，不崩溃
- [ ] **PC-17** `logout()` 在 `Loading` 状态时也可执行，不会崩溃
- [ ] **PC-18** `ProfileScreen` 销毁再重建（Tab 切换），ViewModel 重用（Compose Nav `restoreState`），不重复发送 `NavigateToLogin`

---

## 四、实现结果（由 TDD Agent 完成后填写）

- **实际修改的文件**：
  - **新增**：`feature/profile/ProfileUiState.kt` — sealed interface (Loading / Success / Error)
  - **新增**：`feature/profile/ProfileEvent.kt` — sealed interface (NavigateToLogin / ShowToast)
  - **新增**：`feature/profile/ProfileViewModel.kt` — StateFlow<ProfileUiState> + SharedFlow<ProfileEvent>，loadProfile / logout / copyId
  - **新增**：`feature/profile/ProfileContent.kt` — Stateless Composable；Loading / Success / Error 三态渲染，全 testTag 协议
  - **新增**：`feature/profile/ProfileScreen.kt` — Stateful 容器；持有 ViewModel，LaunchedEffect 监听 events，AlertDialog 退出二次确认
  - **修改**：`common/AppContainer.kt` — 新增 `userRepository: IUserRepository` 字段；`fromBuildConfig()` 使用 `RetrofitUserRepository`
  - **修改**：`feature/main/MainScreen.kt` — 新增 `onLogout: () -> Unit = {}` 参数；将 `ProfilePlaceholder()` 替换为 `ProfileScreen(appContainer, onLogout)`
  - **修改**：`presentation/AppNavGraph.kt` — `MainScreen` 注入 `onLogout = { navigate("login") { popUpTo("main"){ inclusive=true } } }`
  - **新增（测试）**：`test/.../feature/profile/ProfileViewModelTest.kt` — 13 个 JVM 单元测试，覆盖 PC-01/02/05/08/09/10/11/13/14/15/16/17
  - **新增（测试）**：`androidTest/.../feature/profile/ProfileScreenTest.kt` — 8 个 Compose UI 集成测试，覆盖 PC-03/04/06/07/08/10/12
  - **修改（测试）**：`androidTest/.../feature/main/MainScreenTest.kt` — 新增 `FakeUserRepository`；AppContainer 补充 `userRepository` 字段；TB08 改为检测 `profile_screen` testTag

- **测试覆盖率**：
  - JVM 单元测试：ProfileViewModelTest 13/13 通过（0 failures）
  - ProfileViewModel 行覆盖率：**100%**（Instruction: 83/83, Line: 13/13, Method: 4/4）
  - androidTest：ProfileScreenTest 8 个 UI 测试（需连接设备/模拟器运行）
  - 全量 JVM 测试：279 个，9 个预存失败（UInt/ULong 类型断言问题，与本 Task 无关）

- **完成时间**：2026-04-20

---

## 五、Reviewer意见

> 状态枚举：🟢 通过 | 🔴 未通过
> 每轮 Review 追加一条记录，不要覆盖历史。

### Round 1
- **状态**：🟢 通过
- **审查意见**：

  | # | 审查项 | 结论 | 严重级别 | 说明 |
  |---|--------|------|----------|------|
  | 1 | 主题合规（AvatarWithFrame / GoldButton / MenaColors） | ✅ 通过 | — | `AvatarWithFrame(imageUrl, size=80.dp, showFrame=true)` 签名匹配；`GoldButton` 用于 Error 重试按钮；退出按钮用 `TextButton + MenaColors.Error`（符合 TDS "GoldButton 或 TextButton"）；所有颜色令牌均来自 `MenaColors` 对象，无硬编码色值 |
  | 2 | ProfileViewModel — StateFlow / SharedFlow / CancellationException | ✅ 通过 | — | `MutableStateFlow<ProfileUiState>` + `asStateFlow()` ✅；`MutableSharedFlow(replay=0)` + `asSharedFlow()` ✅；`onFailure { if (e is CancellationException) throw e }` 正确 re-throw，不吞噬协程取消信号 ✅ |
  | 3 | 退出登录流程（AlertDialog → clearToken → LoginScreen） | ✅ 通过 | — | `showLogoutDialog` 状态控制弹框显隐 ✅；确认按钮调 `viewModel.logout()` → `tokenManager.clearToken()` → emit `NavigateToLogin` ✅；取消按钮仅关闭弹框，不清 token ✅；三个 testTag (`logout_confirm_dialog` / `logout_confirm_button` / `logout_cancel_button`) 均挂载正确 ✅ |
  | 4 | 复制 ID（ClipboardManager） | ✅ 通过 | — | `clipboardManager.setText(AnnotatedString(userId))` 正确用法 ✅；随后 `viewModel.copyId(userId)` emit `ShowToast("ID 已复制")` ✅；剪贴板写入在 UI 层，ViewModel 仅负责事件通知，职责清晰 ✅ |
  | 5 | 网络异常本地缓存回退 | ✅ 通过 | — | `cachedProfile` 在 `onSuccess` 时更新 ✅；`onFailure` 时有缓存 → `Success(fromCache=true)` + ShowToast ✅；无缓存 → `Error` ✅；降级逻辑完整 |
  | 6 | 导航集成（MainScreen 替换 ProfilePlaceholder） | ✅ 通过 | — | `composable(MainTab.PROFILE.route)` 内已替换为 `ProfileScreen(appContainer, onLogout)` ✅；`onLogout: () -> Unit = {}` 默认值防止旧调用方编译失败 ✅ |
  | 7 | AppNavGraph 退出登录导航（popUpTo + inclusive） | ✅ 通过 | — | `navController.navigate("login") { popUpTo("main") { inclusive = true } }` ✅；back stack 清干净，用户退出后无法返回 main ✅ |
  | 8 | RTL 支持 | ✅ 通过 | — | 无 `padding(start/end)` 或 `absolutePadding` 硬编码方向 ✅；全部使用 `padding(horizontal=...)` 对称写法或 `Arrangement.Center` ✅；Compose 默认在 `layoutDirection=RTL` 时自动镜像 Row 排布，图标+文字顺序在 RTL 下自然翻转 ✅ |
  | 9 | 测试覆盖 | ⚠️ 部分通过 | MEDIUM | **PC-12 fromCache=true 分支未在 UI 层验证**：`ProfileScreenTest` 只测了 `fromCache=false` 时 badge 隐藏，缺少 `fromCache=true` 时 `profile_cache_badge` 可见的 UI 断言（ViewModel 层 PC-11 已覆盖状态，但缺少 UI 渲染验证）。**PC-18 未测试**：Tab 切换后 ViewModel 重用、`NavigateToLogin` 不重复发送的场景（`MutableSharedFlow(replay=0)` 设计上正确，但缺对应测试用例）。其余 13 个 VM 单测 + 8 个 UI 集成测试均通过 |

- **改进建议**：

  **[MEDIUM] 补充 PC-12 fromCache=true UI 测试**

  在 `ProfileScreenTest` 中增加：先让 Repo 返回成功（填充缓存），再切换为失败，通过 `ProfileContent` 直接渲染 `ProfileUiState.Success(fromCache=true)` 并断言 `profile_cache_badge` 存在且可见。

  ```kotlin
  @Test
  fun PC12_fromCache_true_shows_cache_badge() {
      composeTestRule.setContent {
          ProfileContent(
              uiState = ProfileUiState.Success(
                  profile = FakeUserRepository.DEFAULT_PROFILE,
                  fromCache = true,
              ),
              onCopyId = {},
              onLogoutClick = {},
              onRetry = {},
          )
      }
      composeTestRule.onNodeWithTag("profile_cache_badge").assertIsDisplayed()
  }
  ```

  **[MEDIUM] 补充 PC-18 ViewModel 重用测试**

  验证 `SharedFlow(replay=0)` 不会在 Tab 切换重组后重放 `NavigateToLogin` 事件：

  ```kotlin
  @Test
  fun PC18_no_duplicate_NavigateToLogin_after_recompose() {
      val vm = buildViewModel()
      var logoutCallCount = 0
      // 触发第一次 logout
      vm.logout()
      // 模拟 LaunchedEffect 重启（recompose），收集器重新 collect
      // 因 replay=0，已发射事件不应重放
      // 断言 logoutCallCount == 1（非 2）
  }
  ```

  **[LOW] ProfileFeature.kt 过期描述**

  `ProfileFeature.descriptor.title = "Profile Placeholder"` — T-30024 实现后应更新为 `"Profile"` 并更新 description，避免误导后续开发者。

